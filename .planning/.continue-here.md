---
context: default
phase: none (between milestones)
task: 6
total_tasks: 10
status: blocked
last_updated: 2026-06-16T03:38:59.778Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Fix is UNVERIFIED against real rendering — confirmed only via build + unit tests, never by running the app under load. User reports "still a problem." Do NOT trust the software-rendering fix until reproduced live.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Verify-by-tests-only | Claimed a rendering bug fixed after build+unit tests, without running the widget under load. The symptom (bars paint, text doesn't, window-move fixes) is ALSO consistent with UI-thread/dispatcher stall, which SoftwareOnly would NOT fix. | blocking | Reproduce under real GPU/system load and capture a NEW screenshot before claiming fixed. |
| Stale-evidence | Reasoned from the pre-fix 09:16 screenshot; no fresh capture exists. Unknown if the new build was even running when user said "still a problem". | advisory | Always get a current screenshot/repro before iterating. |

<current_state>
Software-rendering toggle (AppSettings.SoftwareRenderingEnabled, default true → HwndTarget.RenderMode=SoftwareOnly) is fully wired end-to-end and builds clean (0 errors), App tests 159/159 on rerun. But user says "still a problem" and there is NO new screenshot — latest is still pre-fix 09:16. Either (a) the new build isn't running, or (b) root cause is wrong (UI-thread stall, not GPU glyph-drop).
</current_state>

<completed_work>
- AppSettings.SoftwareRenderingEnabled added (default true) + SettingsService.Defaults + SettingsSnapshot projection — Done
- MainWindow: OnSourceInitialized + ApplyRenderMode + SetSoftwareRendering; cached in ApplySettings; projected in GetCurrentSettingsSnapshot; persisted in SaveSettings; reset-to-ON in ResetToDefaults — Done
- SettingsWindow: SoftwareRenderingChanged event + populate + handler + XAML checkbox + help text — Done
- 3 tests added (round-trip, absent-defaults-true, snapshot init) — Done
- Build Debug 0 errors; App tests 159/159 on clean rerun (first run had 2 transient phrase flakes) — Done
</completed_work>

<remaining_work>
- Task 7: Launch the NEW build, reproduce under load, capture a fresh screenshot while broken
- Task 8: If software-rendering ON still breaks → pivot to UI-thread-stall hypothesis (dispatcher starvation around stats/phrase timers), not render mode
- Task 9: Check %LOCALAPPDATA%\FuzzyClock\crash.log for handled exceptions at the moment text vanishes
- Task 10: Once verified, bump csproj <Version> 4.5.4 → 4.6.0, commit, tag
</remaining_work>

<decisions_made>
- Software rendering exposed as a toggle, default ON (user chose "Software render, but as a setting")
- Wired mirroring the UpdateChecksEnabled toggle pattern (lowest-risk, matches codebase convention)
</decisions_made>

<blockers>
- Fix unverified against real rendering behavior; no current repro/screenshot. Blocking until reproduced live.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state + full context_notes
2. Memory: `gotcha_text_vanishes_under_load.md` — the diagnosis (treat as HYPOTHESIS, not confirmed)
3. Screenshots dir: `C:\Users\altab\OneDrive - TRICENTIS\Pictures\Screenshots` — get a FRESH capture before iterating

## Infrastructure State
- Working tree also contains an UNRELATED change: `UseWin` modifier in GhostModeController.cs + GhostModeControllerTests.cs (separate work stream). NOT part of this fix — leave untouched unless asked.
- Version NOT bumped (still 4.5.4).

<context>
The symptom is real but my root cause may be wrong. GPU glyph-drop and UI-thread stall produce the same visible result. SoftwareOnly only fixes the former. Next session: get a live repro FIRST, then decide which path. Do not re-claim "fixed" without observing the running app.
</context>

<next_action>
Start with: launch the freshly-built FuzzyClock.App, stress system/GPU load, reproduce the no-text condition, and capture a NEW screenshot. Then check crash.log. If software rendering is ON and it still breaks, pivot to the UI-thread-stall investigation.
</next_action>

---
phase: 01-phrase-engine
plan: 02
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - FuzzyClock.Core/PhraseEngine.cs
  - FuzzyClock.Core.Tests/PhraseEngineTests.cs
autonomous: true
requirements:
  - DISP-01
  - DISP-02
  - DISP-03

must_haves:
  truths:
    - "Given any valid DateTime, GetPhrase returns a non-empty English phrase"
    - "All 12 five-minute bucket slots per hour return a distinct phrase"
    - "Exact noon (12:00:00) returns exactly 'noon'"
    - "Exact midnight (00:00:00) returns exactly 'midnight'"
    - "No phrase contains '0' as an hour value (12-hour conversion is correct)"
    - "Minutes 58 and 59 return a phrase (no empty string or exception)"
    - "The function accepts a DateTime parameter and does not call DateTime.Now internally"
    - "FuzzyClock.Core has no WPF/System.Windows dependency"
    - "All tests pass: dotnet test exits 0"
  artifacts:
    - path: "FuzzyClock.Core/PhraseEngine.cs"
      provides: "Static GetPhrase(DateTime) method"
      exports: ["PhraseEngine", "GetPhrase"]
      min_lines: 30
    - path: "FuzzyClock.Core.Tests/PhraseEngineTests.cs"
      provides: "Exhaustive table-driven unit tests using MSTest [DataRow]"
      min_lines: 80
  key_links:
    - from: "FuzzyClock.Core.Tests/PhraseEngineTests.cs"
      to: "FuzzyClock.Core/PhraseEngine.cs"
      via: "using FuzzyClock.Core; PhraseEngine.GetPhrase(dt)"
      pattern: "PhraseEngine\\.GetPhrase"
    - from: "FuzzyClock.Core/PhraseEngine.cs"
      to: "DateTime parameter"
      via: "dt.Hour, dt.Minute"
      pattern: "static string GetPhrase\\(DateTime"
---

<objective>
Implement the phrase engine via TDD: write exhaustive failing tests first, then implement PhraseEngine.cs to make them pass, then verify no edge case is missed.

Purpose: The TDD cycle forces the complete phrase vocabulary to be locked in the test file before any implementation exists. This catches off-by-one boundary errors and missing bucket coverage before they reach integration.

Output: FuzzyClock.Core/PhraseEngine.cs (complete implementation) + FuzzyClock.Core.Tests/PhraseEngineTests.cs (exhaustive tests). All 12 buckets, both special cases, all boundary minutes, minutes 58-59 edge case — all green.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-phrase-engine/01-CONTEXT.md
@.planning/phases/01-phrase-engine/01-RESEARCH.md
@.planning/phases/01-phrase-engine/01-01-SUMMARY.md
</context>

<feature>
  <name>PhraseEngine.GetPhrase — DateTime to fuzzy English time phrase</name>
  <files>FuzzyClock.Core/PhraseEngine.cs, FuzzyClock.Core.Tests/PhraseEngineTests.cs</files>

  <behavior>
    Complete phrase vocabulary (all decisions locked):

    SPECIAL CASES (checked before bucket dispatch):
    - DateTime(any, any, any, 12, 0, 0) → "noon"
    - DateTime(any, any, any,  0, 0, 0) → "midnight"

    BUCKET TABLE (minute ranges are inclusive on both ends):
    | Bucket | Minute range | Template                        | Example at hour 3         |
    |--------|--------------|---------------------------------|---------------------------|
    | :00    | 0–2          | "{h} o'clock"                   | "3 o'clock"               |
    | :05    | 3–7          | "just after {h}"                | "just after 3"            |
    | :10    | 8–12         | "ten past {h}"                  | "ten past 3"              |
    | :15    | 13–17        | "a quarter past {h}"            | "a quarter past 3"        |
    | :20    | 18–22        | "just after quarter past {h}"   | "just after quarter past 3" |
    | :25    | 23–27        | "almost half past {h}"          | "almost half past 3"      |
    | :30    | 28–32        | "half past {h}"                 | "half past 3"             |
    | :35    | 33–37        | "just past half past {h}"       | "just past half past 3"   |
    | :40    | 38–42        | "almost a quarter before {h1}"  | "almost a quarter before 4" |
    | :45    | 43–47        | "a quarter before {h1}"         | "a quarter before 4"      |
    | :50    | 48–52        | "nearly {h1}"                   | "nearly 4"                |
    | :55    | 53–59        | "almost {h1}"                   | "almost 4"                |

    Note: :55 bucket extends to minute 59 (covers 58 and 59 as "almost" — correct and avoids dead zone).

    HOUR ARITHMETIC:
    - hour12 = dt.Hour % 12; if (hour12 == 0) hour12 = 12;  (gives 1–12)
    - nextHour12 = (hour12 % 12) + 1;  (gives 1 for hour12=12, wraps correctly)
    - {h} substituted with hour12.ToString()
    - {h1} substituted with nextHour12.ToString()

    SPECIAL CASE NEAR NOON/MIDNIGHT:
    - 12:05 → "just after 12" (not "just after noon")
    - 11:55 → "almost 12" (not "almost noon")
    - 00:05 → "just after 12" (midnight+5 uses hour 0, which converts to 12)
    - 23:55 → "almost 12" (23:55 uses nextHour12 = (11 % 12)+1 = 12)

    Test cases (input hour, minute → expected string):

    Special cases:
    - (12, 0)  → "noon"
    - (0,  0)  → "midnight"

    :00 bucket — test boundaries:
    - (3, 0)  → "3 o'clock"
    - (3, 1)  → "3 o'clock"
    - (3, 2)  → "3 o'clock"

    :05 bucket:
    - (3, 3)  → "just after 3"
    - (3, 7)  → "just after 3"

    :10 bucket:
    - (3, 8)  → "ten past 3"
    - (3, 12) → "ten past 3"

    :15 bucket:
    - (3, 13) → "a quarter past 3"
    - (3, 17) → "a quarter past 3"

    :20 bucket:
    - (3, 18) → "just after quarter past 3"
    - (3, 22) → "just after quarter past 3"

    :25 bucket:
    - (3, 23) → "almost half past 3"
    - (3, 27) → "almost half past 3"

    :30 bucket:
    - (3, 28) → "half past 3"
    - (3, 32) → "half past 3"

    :35 bucket:
    - (3, 33) → "just past half past 3"
    - (3, 37) → "just past half past 3"

    :40 bucket:
    - (3, 38) → "almost a quarter before 4"
    - (3, 42) → "almost a quarter before 4"

    :45 bucket:
    - (3, 43) → "a quarter before 4"
    - (3, 47) → "a quarter before 4"

    :50 bucket:
    - (3, 48) → "nearly 4"
    - (3, 52) → "nearly 4"

    :55 bucket (extended to 59):
    - (3, 53) → "almost 4"
    - (3, 57) → "almost 4"
    - (3, 58) → "almost 4"
    - (3, 59) → "almost 4"

    Hour conversion edge cases:
    - (12, 5)  → "just after 12"   (noon+5 is NOT "noon"; hour12=12, not 0)
    - (0,  5)  → "just after 12"   (midnight+5; hour 0 → hour12=12)
    - (12, 45) → "a quarter before 1"  (nextHour12 = (12%12)+1 = 1, not 13)
    - (11, 55) → "almost 12"       (nextHour12 = (11%12)+1 = 12)
    - (23, 55) → "almost 12"       (hour 23 → hour12=11, nextHour12=12)

    No phrase should contain "0" as an hour value. Test (0, 5) → "just after 12" verifies this.
  </behavior>

  <implementation>
    Structure: namespace FuzzyClock.Core; public static class PhraseEngine with a single public method.

    Use a private static readonly array of (int UpperBound, string Template) tuples as the bucket table. Walk the array with foreach, return the first match where minute &lt;= upperBound.

    Apply special-case guards (noon/midnight) BEFORE computing hour12 — use totalMinutes == 720 and totalMinutes == 0.

    Compute hour12 and nextHour12 AFTER the special case guards.

    Use string.Replace("{h}", ...) and string.Replace("{h1}", ...) for substitution.

    The :55 bucket upper bound must be 59 (not 57) to cover minutes 58–59.

    Do NOT call DateTime.Now anywhere in PhraseEngine.cs. The method signature is:
    public static string GetPhrase(DateTime dt)

    File: FuzzyClock.Core/PhraseEngine.cs
    Namespace: FuzzyClock.Core
    Class: PhraseEngine (public static)
    Method: GetPhrase (public static string)

    Test file structure:
    - [TestClass] PhraseEngineTests in namespace FuzzyClock.Core.Tests
    - Group test methods by bucket (one [TestMethod] per bucket or concern)
    - Each [TestMethod] has multiple [DataRow] entries covering both boundary minutes
    - Add a dedicated test method for hour conversion edge cases
    - Add a dedicated test method for special cases (noon, midnight)
  </implementation>
</feature>

<verification>
After RED-GREEN-REFACTOR cycle:
- dotnet test FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj exits 0
- All test methods pass (look for "X passed, 0 failed")
- No test method is skipped
- PhraseEngine.cs has no using System.Windows or WPF references
- PhraseEngine.cs does not contain DateTime.Now
</verification>

<success_criteria>
All 12 buckets have at least 2 passing [DataRow] tests (first and last minute of bucket). Both special cases pass. Hour-conversion edge cases pass (no "0" in phrase, no "13" next-hour). Minutes 58-59 pass. dotnet test exits 0. PhraseEngine.GetPhrase accepts a DateTime parameter and has zero WPF dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/01-phrase-engine/01-02-SUMMARY.md`
</output>

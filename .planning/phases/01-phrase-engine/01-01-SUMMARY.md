---
phase: 01-phrase-engine
plan: 01
subsystem: testing
tags: [dotnet, csharp, mstest, classlib, solution]

# Dependency graph
requires: []
provides:
  - FuzzyClock.slnx solution file containing both projects
  - FuzzyClock.Core classlib targeting net10.0 with no WPF dependency
  - FuzzyClock.Core.Tests MSTest v4 test project wired to FuzzyClock.Core
affects:
  - 01-02-phrase-engine (TDD cycle needs this scaffold to add tests)
  - 02-window-shell (needs FuzzyClock.Core as a reference target)

# Tech tracking
tech-stack:
  added: [dotnet 10.0 SDK, MSTest 4.0.1, .slnx solution format]
  patterns: [classlib + test project separation, no WPF in logic layer]

key-files:
  created:
    - FuzzyClock.slnx
    - FuzzyClock.Core/FuzzyClock.Core.csproj
    - FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj
    - FuzzyClock.Core.Tests/MSTestSettings.cs
  modified: []

key-decisions:
  - "dotnet 10.0 SDK generates .slnx (new XML format) instead of .sln — functionally identical, both build and test commands work the same way"
  - "Placeholder files (Class1.cs, Test1.cs) deleted immediately to keep scaffold clean for TDD cycle in Plan 02"

patterns-established:
  - "FuzzyClock.Core must never reference WPF or System.Windows — verified by TargetFramework net10.0 (not net10.0-windows)"
  - "Test project uses ProjectReference to core library, enabling in-solution testing without NuGet packaging"

requirements-completed: [DISP-01, DISP-02, DISP-03]

# Metrics
duration: 2min
completed: 2026-02-25
---

# Phase 1 Plan 01: Phrase Engine Scaffold Summary

**net10.0 classlib + MSTest v4 test project in .slnx solution, zero WPF dependency, build and test runner verified**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-25T00:02:06Z
- **Completed:** 2026-02-25T00:04:13Z
- **Tasks:** 2
- **Files modified:** 4 created

## Accomplishments
- Solution file (FuzzyClock.slnx) containing both projects
- FuzzyClock.Core classlib targeting net10.0 with no WPF or System.Windows reference
- FuzzyClock.Core.Tests MSTest v4 test project referencing FuzzyClock.Core
- `dotnet build FuzzyClock.slnx` exits 0 with 0 errors, 0 warnings
- `dotnet test FuzzyClock.Core.Tests` exits 0 with test runner discovery confirmed

## Task Commits

Each task was committed atomically:

1. **Task 1: Scaffold the .NET solution and projects** - `b804f66` (feat)
2. **Task 2: Verify test runner discovery** - no additional files; verified via `dotnet test` exit 0

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified
- `FuzzyClock.slnx` - Solution file referencing both projects
- `FuzzyClock.Core/FuzzyClock.Core.csproj` - Classlib targeting net10.0, no WPF
- `FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj` - MSTest v4 test project with ProjectReference to core
- `FuzzyClock.Core.Tests/MSTestSettings.cs` - MSTest v4 settings generated by template

## Decisions Made
- dotnet 10.0 SDK defaults to `.slnx` (new XML-based solution format) rather than the legacy `.sln` text format. The plan specified `FuzzyClock.sln` but dotnet 10 generated `FuzzyClock.slnx`. All `dotnet build` and `dotnet test` commands work identically with this format. Accepted as correct output for this SDK version.
- MSTest v4 template in dotnet 10 generates `Test1.cs` (not `UnitTest1.cs` as the plan expected). Deleted the actual generated placeholder (`Test1.cs`). Result is identical: zero placeholder tests.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Solution file format mismatch: .slnx instead of .sln**
- **Found during:** Task 1 (Scaffold the .NET solution and projects)
- **Issue:** Plan specified `FuzzyClock.sln` but `dotnet new sln` in SDK 10.0.103 creates `FuzzyClock.slnx` (new XML format). Running `dotnet build FuzzyClock.sln` returned MSB1009 (file not found).
- **Fix:** Used `FuzzyClock.slnx` for all build/test commands. No file rename needed — the generated name is the correct SDK-10 output.
- **Files modified:** None (the .slnx was the correctly generated file)
- **Verification:** `dotnet build FuzzyClock.slnx` exits 0; both projects build.
- **Committed in:** b804f66 (Task 1 commit)

**2. [Rule 1 - Bug] MSTest placeholder filename changed: Test1.cs instead of UnitTest1.cs**
- **Found during:** Task 1 (deleting placeholders)
- **Issue:** Plan instructed deletion of `UnitTest1.cs` but dotnet 10 MSTest template creates `Test1.cs`.
- **Fix:** Deleted `Test1.cs` instead. Outcome identical.
- **Files modified:** FuzzyClock.Core.Tests/ (Test1.cs removed)
- **Verification:** No placeholder files remain; test runner exits 0 with 0 tests.
- **Committed in:** b804f66 (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - SDK version differences)
**Impact on plan:** Both differences are pure dotnet SDK 10 template changes; no scope creep, no architectural impact.

## Issues Encountered
- dotnet 10.0 SDK generates different artifact names than dotnet 8/9 templates assumed in the plan (`.slnx` vs `.sln`, `Test1.cs` vs `UnitTest1.cs`). Both resolved immediately by adapting to the actual generated output.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Scaffold complete: Plan 02 can immediately begin TDD cycle (RED phase: write failing tests for PhraseEngine.GetPhrase)
- FuzzyClock.Core has no WPF dependency — architectural guarantee holds
- Test runner wired: adding test methods to FuzzyClock.Core.Tests will be discovered automatically
- No blockers or concerns for Plan 02

## Self-Check: PASSED

- FOUND: FuzzyClock.slnx
- FOUND: FuzzyClock.Core/FuzzyClock.Core.csproj
- FOUND: FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj
- FOUND: .planning/phases/01-phrase-engine/01-01-SUMMARY.md
- FOUND: commit b804f66

---
*Phase: 01-phrase-engine*
*Completed: 2026-02-25*

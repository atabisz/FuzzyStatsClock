---
phase: 24-system-tray-icon
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: []
autonomous: false
requirements: [TRAY-01, TRAY-02, TRAY-03, TRAY-04, TRAY-05, TRAY-06]

must_haves:
  truths:
    - "Tray icon is visible in Windows notification area while the app runs"
    - "Tray context menu shows exactly Reset to Defaults and Quit (no other items)"
    - "Reset to Defaults visually changes accent to white, opacity to 100%, centers widget instantly"
    - "Reset to Defaults writes updated settings to settings.json"
    - "Quit removes tray icon and exits process cleanly"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all 5 success criteria for Phase 24: system tray icon is present, context menu has the right items, Reset to Defaults works instantly and persists, Quit exits cleanly with no lingering icon.

Purpose: Confirms the automated implementation from Plan 01 produces the correct user-visible behavior.
Output: Verified phase 24 complete.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-system-tray-icon/24-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human Verify: System Tray Icon — TRAY-01 through TRAY-06</name>
  <action>Run the app and verify all 6 TRAY requirements as described in how-to-verify below.</action>
  <verify>Human inspection — run app, right-click tray, test Reset to Defaults and Quit.</verify>
  <done>All 7 checks in how-to-verify pass without errors.</done>
  <what-built>
    System tray icon with Reset to Defaults and Quit context menu items.
    - Tray icon: 16x16 white filled circle, tooltip "FuzzyClock"
    - Reset to Defaults: sets White accent + 100% opacity + centers on primary screen + saves settings.json
    - Quit: Application.Current.Shutdown() — process exits, tray icon disappears
    - Tray icon disposed in Window.Closed event
  </what-built>
  <how-to-verify>
    1. Run the app: `dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj`
    2. TRAY-01: Check Windows system tray (notification area, bottom-right). Confirm a white circle icon labeled "FuzzyClock" is present while the app is running.
    3. TRAY-02: Right-click the tray icon. Confirm the context menu shows exactly two items: "Reset to Defaults" and "Quit" (no other items).
    4. TRAY-03: Change the accent color to something other than White (e.g., Amber via right-click Theme > Amber) and drag the widget to a corner. Then right-click the tray icon and click "Reset to Defaults". Confirm: accent immediately turns white, opacity is 100% (fully opaque), and the widget snaps to the center of the primary screen. No restart required.
    5. TRAY-04: After "Reset to Defaults" completes, close the app and relaunch it. Confirm the widget starts centered with White accent and 100% opacity — the reset state was written to settings.json.
    6. TRAY-05: Right-click the tray icon and click "Quit". Confirm the application process exits completely (no FuzzyClock window remains, no process in Task Manager).
    7. TRAY-06: Confirm the tray icon is gone from the system tray after clicking "Quit" (no orphaned icon). Also close the app via Alt+F4 or the WPF close mechanism and confirm the tray icon disappears immediately on close.
  </how-to-verify>
  <resume-signal>Type "approved" when all 7 checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
All 5 phase success criteria verified by human:
1. Tray icon visible while running (TRAY-01)
2. Context menu has exactly Reset to Defaults + Quit (TRAY-02)
3. Reset works instantly with no restart (TRAY-03)
4. Reset state persists to settings.json (TRAY-04)
5. Quit exits cleanly, no lingering icon (TRAY-05 + TRAY-06)
</verification>

<success_criteria>
Human confirms all 6 TRAY requirements are met. Phase 24 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/24-system-tray-icon/24-02-SUMMARY.md`
</output>

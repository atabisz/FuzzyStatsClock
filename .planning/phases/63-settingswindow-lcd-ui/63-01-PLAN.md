---
phase: 63-settingswindow-lcd-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/SettingsWindow.xaml
  - FuzzyClock.App/SettingsWindow.xaml.cs
autonomous: true
requirements:
  - LCD-01
  - LCD-02
  - LCD-03
  - LCD-04
  - LCD-05

must_haves:
  truths:
    - "Settings > Appearance shows a fourth 'LCD' button in the Clock Style rail alongside Phrase/Dial/Nixie"
    - "Clicking BtnLcd fires ClockTypeChanged with ClockType.Lcd and updates button selection state"
    - "LCD options panel (24hr checkbox, seconds checkbox, segment style combo) is visible when LCD is selected"
    - "LCD options panel is collapsed when Phrase, Dial, or Nixie is the active clock style"
    - "PopulateControls reads SettingsSnapshot LCD fields and sets each control to the persisted value"
    - "Toggling 24hr or seconds checkboxes fires the corresponding event with the new boolean value"
    - "Selecting a segment style fires LcdStyleChanged with the selected Content string"
    - "All three CS0067 warnings for LcdUse24HrChanged/LcdShowSecondsChanged/LcdStyleChanged are resolved"
  artifacts:
    - path: "FuzzyClock.App/SettingsWindow.xaml"
      provides: "BtnLcd button, Row 6 RowDefinition, LcdOptionsLabel, LcdOptionsPanel with 2 checkboxes + 1 ComboBox"
      contains: "x:Name=\"BtnLcd\""
    - path: "FuzzyClock.App/SettingsWindow.xaml.cs"
      provides: "BtnLcd_Click, ChkLcdUse24Hr_Changed, ChkLcdShowSeconds_Changed, CmbLcdStyle_SelectionChanged handlers; extended SetClockStyleButtonStates and PopulateControls"
      contains: "BtnLcd_Click"
  key_links:
    - from: "FuzzyClock.App/SettingsWindow.xaml"
      to: "FuzzyClock.App/SettingsWindow.xaml.cs"
      via: "Click/Checked/Unchecked/SelectionChanged attributes referencing handler method names"
      pattern: "Click=\"BtnLcd_Click\""
    - from: "FuzzyClock.App/SettingsWindow.xaml.cs (handlers)"
      to: "FuzzyClock.App/SettingsWindow.xaml.cs (events at lines 27-29)"
      via: "Handler methods invoke the three LCD event stubs"
      pattern: "LcdUse24HrChanged\\?\\.Invoke"
    - from: "FuzzyClock.App/SettingsWindow.xaml.cs (PopulateControls)"
      to: "FuzzyClock.App/SettingsSnapshot.cs (LCD fields)"
      via: "PopulateControls reads s.LcdUse24Hr, s.LcdShowSeconds, s.LcdStyle"
      pattern: "s\\.LcdUse24Hr"
---

<objective>
Add LCD clock style selection and options to SettingsWindow. This adds the BtnLcd button to the Clock Style rail and an LCD options panel (Row 6) with 24-hour mode checkbox, show-seconds checkbox, and segment style ComboBox. All controls wire to existing event stubs and populate from SettingsSnapshot.

Purpose: Enable users to select and configure the LCD clock face from Settings > Appearance, completing the SettingsWindow side of the LCD integration (MainWindow subscriptions already exist).

Output: Two modified files — SettingsWindow.xaml with new XAML elements, SettingsWindow.xaml.cs with extended methods and new handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-settingswindow-lcd-ui/63-CONTEXT.md
@.planning/phases/63-settingswindow-lcd-ui/63-RESEARCH.md
@.planning/phases/63-settingswindow-lcd-ui/63-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From FuzzyClock.App/SettingsWindow.xaml.cs (existing event stubs at lines 27-29):
```csharp
public event Action<bool>?   LcdUse24HrChanged;      // line 27
public event Action<bool>?   LcdShowSecondsChanged;   // line 28
public event Action<string>? LcdStyleChanged;         // line 29
```

From FuzzyClock.App/SettingsWindow.xaml.cs (existing method at lines 209-219):
```csharp
private void SetClockStyleButtonStates(ClockType ct)
{
    BtnPhrase.Tag = ct == ClockType.Phrase ? "selected" : null;
    BtnDial.Tag   = ct == ClockType.Dial   ? "selected" : null;
    BtnNixie.Tag  = ct == ClockType.Nixie  ? "selected" : null;

    // Dial face row: visible only for Dial clock style
    var dialVis = ct == ClockType.Dial ? Visibility.Visible : Visibility.Collapsed;
    DialFaceLabel.Visibility = dialVis;
    DialFacePanel.Visibility = dialVis;
}
```

From FuzzyClock.App/SettingsWindow.xaml.cs (PopulateControls dial section at lines 163-166):
```csharp
// Dial face decoration checkboxes
ChkShowHourTicks.IsChecked   = s.ShowHourTicks;
ChkShowMinuteDots.IsChecked  = s.ShowMinuteDots;
ChkShowHourNumbers.IsChecked = s.ShowHourNumbers;
```

From FuzzyClock.App/SettingsWindow.xaml.cs (BtnNixie_Click pattern at lines 413-418):
```csharp
private void BtnNixie_Click(object sender, RoutedEventArgs e)
{
    if (_suppressEvents) return;
    SetClockStyleButtonStates(ClockType.Nixie);
    ClockTypeChanged?.Invoke(ClockType.Nixie);
}
```

From FuzzyClock.App/SettingsWindow.xaml.cs (CmbPhraseStyle_SelectionChanged at lines 421-426):
```csharp
private void CmbPhraseStyle_SelectionChanged(object sender, SelectionChangedEventArgs e)
{
    if (_suppressEvents) return;
    if (CmbPhraseStyle.SelectedItem is ComboBoxItem item)
        PhraseStyleChanged?.Invoke((string)item.Content);
}
```

From FuzzyClock.App/SettingsWindow.xaml.cs (ChkShowHourTicks_Changed at lines 587-591):
```csharp
private void ChkShowHourTicks_Changed(object sender, RoutedEventArgs e)
{
    if (_suppressEvents) return;
    ShowHourTicksChanged?.Invoke(ChkShowHourTicks.IsChecked == true);
}
```

From FuzzyClock.App/SettingsWindow.xaml (Clock Style rail at lines 288-300):
```xml
<!-- Clock Style -->
<TextBlock Grid.Row="2" Grid.Column="0"
           Text="Clock Style" VerticalAlignment="Center"
           HorizontalAlignment="Right" Margin="0,8,10,0"/>
<Border Grid.Row="2" Grid.Column="1"
        Background="#FF3A3A3A" CornerRadius="4" Padding="2"
        HorizontalAlignment="Left" Margin="0,8,0,0">
    <StackPanel Orientation="Horizontal">
        <Button x:Name="BtnPhrase" Content="Phrase" Style="{StaticResource SegmentButtonStyle}" Click="BtnPhrase_Click"/>
        <Button x:Name="BtnDial"   Content="Dial"   Style="{StaticResource SegmentButtonStyle}" Click="BtnDial_Click"/>
        <Button x:Name="BtnNixie"  Content="Nixie"  Style="{StaticResource SegmentButtonStyle}" Click="BtnNixie_Click"/>
    </StackPanel>
</Border>
```

From FuzzyClock.App/SettingsWindow.xaml (DialFace row at lines 333-346):
```xml
<!-- Dial Face (per D-01, D-02, D-03) -->
<TextBlock x:Name="DialFaceLabel" Grid.Row="5" Grid.Column="0"
           Text="Dial Face" VerticalAlignment="Top"
           HorizontalAlignment="Right" Margin="0,8,10,0"/>
<StackPanel x:Name="DialFacePanel"
            Grid.Row="5" Grid.Column="1"
            Margin="0,8,0,0">
    <CheckBox x:Name="ChkShowHourTicks" Content="Hour Ticks"
              Checked="ChkShowHourTicks_Changed" Unchecked="ChkShowHourTicks_Changed"/>
    <CheckBox x:Name="ChkShowMinuteDots" Content="Minute Dots"
              Checked="ChkShowMinuteDots_Changed" Unchecked="ChkShowMinuteDots_Changed"/>
    <CheckBox x:Name="ChkShowHourNumbers" Content="Hour Numbers"
              Checked="ChkShowHourNumbers_Changed" Unchecked="ChkShowHourNumbers_Changed"/>
</StackPanel>
```

From FuzzyClock.App/SettingsWindow.xaml (RowDefinitions at lines 249-256):
```xml
<Grid.RowDefinitions>
    <RowDefinition Height="Auto"/>  <!-- Row 0: Opacity -->
    <RowDefinition Height="Auto"/>  <!-- Row 1: Font Size -->
    <RowDefinition Height="Auto"/>  <!-- Row 2: Clock Style -->
    <RowDefinition Height="Auto"/>  <!-- Row 3: Phrase Style -->
    <RowDefinition Height="Auto"/>  <!-- Row 4: Phrase Wrap -->
    <RowDefinition Height="Auto"/>  <!-- Row 5: Dial Face -->
</Grid.RowDefinitions>
```

From FuzzyClock.App/SettingsSnapshot.cs (LCD fields):
```csharp
public bool LcdUse24Hr { get; init; }       // default false
public bool LcdShowSeconds { get; init; }    // default true
public string LcdStyle { get; init; }        // default "Dark"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BtnLcd and LCD options panel to SettingsWindow XAML</name>
  <files>FuzzyClock.App/SettingsWindow.xaml</files>
  <read_first>
    - FuzzyClock.App/SettingsWindow.xaml (current state of the file being modified)
    - .planning/phases/63-settingswindow-lcd-ui/63-UI-SPEC.md (component inventory and spacing values)
  </read_first>
  <action>
Modify FuzzyClock.App/SettingsWindow.xaml with three changes in the Appearance tab's two-column Grid (the Grid starting at line 244):

**Change 1 — Add BtnLcd to Clock Style rail (per D-01):**
Inside the Clock Style rail's horizontal StackPanel (line 295-299), add a fourth Button after BtnNixie (after line 298):
```xml
<Button x:Name="BtnLcd" Content="LCD" Style="{StaticResource SegmentButtonStyle}" Click="BtnLcd_Click"/>
```

**Change 2 — Add Row 6 RowDefinition (per D-04):**
Append one `<RowDefinition Height="Auto"/>` as the LAST entry in the Grid.RowDefinitions block (after line 255, which is the sixth RowDefinition for Row 5). This becomes index 6. Do NOT insert it between existing entries — that would shift all subsequent rows.

**Change 3 — Add LcdOptionsLabel and LcdOptionsPanel in Row 6 (per D-05, D-06):**
After the DialFacePanel closing tag (after line 346), add these two elements:

```xml
<!-- LCD Options (per D-05, D-06) -->
<TextBlock x:Name="LcdOptionsLabel"
           Grid.Row="6" Grid.Column="0"
           Text="LCD"
           VerticalAlignment="Top"
           HorizontalAlignment="Right"
           Margin="0,8,10,0"/>
<StackPanel x:Name="LcdOptionsPanel"
            Grid.Row="6" Grid.Column="1"
            Margin="0,8,0,0">
    <CheckBox x:Name="ChkLcdUse24Hr" Content="24-hour mode"
              Checked="ChkLcdUse24Hr_Changed"
              Unchecked="ChkLcdUse24Hr_Changed"/>
    <CheckBox x:Name="ChkLcdShowSeconds" Content="Show seconds"
              Checked="ChkLcdShowSeconds_Changed"
              Unchecked="ChkLcdShowSeconds_Changed"/>
    <ComboBox x:Name="CmbLcdStyle"
              Width="120" HorizontalAlignment="Left"
              Margin="0,8,0,0" VerticalAlignment="Center"
              SelectionChanged="CmbLcdStyle_SelectionChanged">
        <ComboBoxItem Content="Dark"/>
        <ComboBoxItem Content="Paper"/>
        <ComboBoxItem Content="Silver"/>
    </ComboBox>
</StackPanel>
```

Do NOT set Visibility="Collapsed" as a static XAML attribute on LcdOptionsLabel or LcdOptionsPanel — the SetClockStyleButtonStates code-behind call handles initial visibility state (per Research Pitfall 1).

Verify that BtnNixie is at line 298 before inserting BtnLcd after it. Verify that Grid.RowDefinitions has exactly 6 entries (indices 0-5) before appending the 7th. Verify that DialFacePanel ends at line 346 before inserting the new elements after it.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && dotnet build FuzzyClock.App/FuzzyClock.App.csproj 2>&1 | grep -E "error|Error" | grep -v warning || echo "BUILD OK (errors only check)"</automated>
  </verify>
  <acceptance_criteria>
    - SettingsWindow.xaml contains `x:Name="BtnLcd"` with `Content="LCD"` and `Click="BtnLcd_Click"` and `Style="{StaticResource SegmentButtonStyle}"`
    - SettingsWindow.xaml contains exactly 7 `<RowDefinition Height="Auto"/>` entries in the Appearance Grid RowDefinitions block
    - SettingsWindow.xaml contains `x:Name="LcdOptionsLabel"` with `Grid.Row="6"` and `Grid.Column="0"` and `Text="LCD"`
    - SettingsWindow.xaml contains `x:Name="LcdOptionsPanel"` with `Grid.Row="6"` and `Grid.Column="1"`
    - SettingsWindow.xaml contains `x:Name="ChkLcdUse24Hr"` with `Content="24-hour mode"` and both `Checked="ChkLcdUse24Hr_Changed"` and `Unchecked="ChkLcdUse24Hr_Changed"`
    - SettingsWindow.xaml contains `x:Name="ChkLcdShowSeconds"` with `Content="Show seconds"` and both `Checked="ChkLcdShowSeconds_Changed"` and `Unchecked="ChkLcdShowSeconds_Changed"`
    - SettingsWindow.xaml contains `x:Name="CmbLcdStyle"` with `Width="120"` and `SelectionChanged="CmbLcdStyle_SelectionChanged"`
    - SettingsWindow.xaml contains three ComboBoxItem entries: `Content="Dark"`, `Content="Paper"`, `Content="Silver"` inside CmbLcdStyle
    - BtnLcd appears AFTER BtnNixie in the file (not before it)
    - LcdOptionsLabel Grid.Row="6" appears AFTER DialFacePanel in the file
    - SettingsWindow.xaml does NOT contain `Visibility="Collapsed"` on LcdOptionsLabel or LcdOptionsPanel as a static attribute
  </acceptance_criteria>
  <done>BtnLcd button is in the Clock Style rail after BtnNixie; Row 6 RowDefinition is appended; LcdOptionsLabel and LcdOptionsPanel with all three child controls exist at Grid.Row 6; the project compiles (handlers are created in Task 2)</done>
</task>

<task type="auto">
  <name>Task 2: Add LCD handlers and extend SetClockStyleButtonStates and PopulateControls in code-behind</name>
  <files>FuzzyClock.App/SettingsWindow.xaml.cs</files>
  <read_first>
    - FuzzyClock.App/SettingsWindow.xaml.cs (current state of the file being modified)
    - FuzzyClock.App/SettingsWindow.xaml (the XAML modified in Task 1, to verify handler names match)
    - FuzzyClock.App/SettingsSnapshot.cs (to confirm LCD field names: LcdUse24Hr, LcdShowSeconds, LcdStyle)
  </read_first>
  <action>
Modify FuzzyClock.App/SettingsWindow.xaml.cs with three groups of changes:

**Change 1 — Extend SetClockStyleButtonStates (per D-03, D-09):**
In the `SetClockStyleButtonStates(ClockType ct)` method (currently lines 209-219), add after the existing `BtnNixie.Tag` line (line 213):
```csharp
BtnLcd.Tag    = ct == ClockType.Lcd    ? "selected" : null;
```

And after the existing DialFacePanel visibility block (after line 218), add:
```csharp
// LCD options row: visible only for LCD clock style
var lcdVis = ct == ClockType.Lcd ? Visibility.Visible : Visibility.Collapsed;
LcdOptionsLabel.Visibility = lcdVis;
LcdOptionsPanel.Visibility = lcdVis;
```

**Change 2 — Extend PopulateControls (per D-10):**
In the `PopulateControls(SettingsSnapshot s)` method, after the dial face decoration checkboxes block (after line 166 which sets `ChkShowHourNumbers.IsChecked`), add:
```csharp
// LCD options
ChkLcdUse24Hr.IsChecked     = s.LcdUse24Hr;
ChkLcdShowSeconds.IsChecked = s.LcdShowSeconds;
CmbLcdStyle.SelectedIndex   = s.LcdStyle switch { "Paper" => 1, "Silver" => 2, _ => 0 };
```

**Change 3 — Add four handler methods (per D-02, D-11, D-12, D-08):**
After the existing BtnNixie_Click method (ends at line 418), add these four methods:

```csharp
private void BtnLcd_Click(object sender, RoutedEventArgs e)
{
    if (_suppressEvents) return;
    SetClockStyleButtonStates(ClockType.Lcd);
    ClockTypeChanged?.Invoke(ClockType.Lcd);
}

// ── LCD option controls ────────────────────────────────────────────
private void ChkLcdUse24Hr_Changed(object sender, RoutedEventArgs e)
{
    if (_suppressEvents) return;
    LcdUse24HrChanged?.Invoke(ChkLcdUse24Hr.IsChecked == true);
}

private void ChkLcdShowSeconds_Changed(object sender, RoutedEventArgs e)
{
    if (_suppressEvents) return;
    LcdShowSecondsChanged?.Invoke(ChkLcdShowSeconds.IsChecked == true);
}

private void CmbLcdStyle_SelectionChanged(object sender, SelectionChangedEventArgs e)
{
    if (_suppressEvents) return;
    if (CmbLcdStyle.SelectedItem is ComboBoxItem item)
        LcdStyleChanged?.Invoke((string)item.Content);
}
```

Place `BtnLcd_Click` in the "Clock style buttons" region (after BtnNixie_Click), and the three LCD option handlers can follow immediately or be grouped together. The exact placement order is: BtnLcd_Click right after BtnNixie_Click (same pattern group), then the three LCD handlers grouped together before the existing CmbPhraseStyle_SelectionChanged handler section.

All three CS0067 warnings (LcdUse24HrChanged, LcdShowSecondsChanged, LcdStyleChanged never used) will be resolved by these handler invocations.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && dotnet build FuzzyClock.App/FuzzyClock.App.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SettingsWindow.xaml.cs contains `private void BtnLcd_Click(object sender, RoutedEventArgs e)` method
    - SettingsWindow.xaml.cs BtnLcd_Click body contains `SetClockStyleButtonStates(ClockType.Lcd)` and `ClockTypeChanged?.Invoke(ClockType.Lcd)`
    - SettingsWindow.xaml.cs contains `private void ChkLcdUse24Hr_Changed(object sender, RoutedEventArgs e)` method
    - SettingsWindow.xaml.cs ChkLcdUse24Hr_Changed body contains `LcdUse24HrChanged?.Invoke(ChkLcdUse24Hr.IsChecked == true)`
    - SettingsWindow.xaml.cs contains `private void ChkLcdShowSeconds_Changed(object sender, RoutedEventArgs e)` method
    - SettingsWindow.xaml.cs ChkLcdShowSeconds_Changed body contains `LcdShowSecondsChanged?.Invoke(ChkLcdShowSeconds.IsChecked == true)`
    - SettingsWindow.xaml.cs contains `private void CmbLcdStyle_SelectionChanged(object sender, SelectionChangedEventArgs e)` method
    - SettingsWindow.xaml.cs CmbLcdStyle_SelectionChanged body contains `LcdStyleChanged?.Invoke((string)item.Content)`
    - SettingsWindow.xaml.cs SetClockStyleButtonStates contains `BtnLcd.Tag` assignment with `ClockType.Lcd`
    - SettingsWindow.xaml.cs SetClockStyleButtonStates contains `LcdOptionsLabel.Visibility = lcdVis` and `LcdOptionsPanel.Visibility = lcdVis`
    - SettingsWindow.xaml.cs PopulateControls contains `ChkLcdUse24Hr.IsChecked` assignment from `s.LcdUse24Hr`
    - SettingsWindow.xaml.cs PopulateControls contains `ChkLcdShowSeconds.IsChecked` assignment from `s.LcdShowSeconds`
    - SettingsWindow.xaml.cs PopulateControls contains `CmbLcdStyle.SelectedIndex` with switch expression mapping "Paper" to 1, "Silver" to 2, default to 0
    - All four handler methods contain `if (_suppressEvents) return;` as the first statement
    - `dotnet build FuzzyClock.App/FuzzyClock.App.csproj` produces 0 errors
    - Build output does NOT contain `CS0067` warning for `LcdUse24HrChanged`, `LcdShowSecondsChanged`, or `LcdStyleChanged`
  </acceptance_criteria>
  <done>All four handlers exist and invoke their corresponding events; SetClockStyleButtonStates gates BtnLcd.Tag and LCD options panel visibility; PopulateControls reads all three LCD SettingsSnapshot fields; build succeeds with zero CS0067 warnings for LCD events</done>
</task>

</tasks>

<verification>
1. `dotnet build FuzzyClock.App/FuzzyClock.App.csproj` — 0 errors, 0 CS0067 warnings for LCD events
2. `dotnet test` — all 318 existing tests pass (no regressions)
3. Grep verification:
   - `grep -c "BtnLcd" FuzzyClock.App/SettingsWindow.xaml` returns >= 1
   - `grep -c "LcdOptionsPanel" FuzzyClock.App/SettingsWindow.xaml` returns >= 1
   - `grep -c "LcdOptionsLabel" FuzzyClock.App/SettingsWindow.xaml` returns >= 1
   - `grep "BtnLcd_Click" FuzzyClock.App/SettingsWindow.xaml.cs` returns a match
   - `grep "LcdUse24HrChanged?.Invoke" FuzzyClock.App/SettingsWindow.xaml.cs` returns a match
   - `grep "LcdShowSecondsChanged?.Invoke" FuzzyClock.App/SettingsWindow.xaml.cs` returns a match
   - `grep "LcdStyleChanged?.Invoke" FuzzyClock.App/SettingsWindow.xaml.cs` returns a match
</verification>

<success_criteria>
- BtnLcd button is visible in the Clock Style rail as the fourth button (after Phrase, Dial, Nixie)
- Clicking BtnLcd selects it (Tag="selected") and deselects others
- LCD options row (Row 6) shows when LCD is active, collapses for all other clock styles
- Dial Face row (Row 5) continues to show only when Dial is active (no regression)
- PopulateControls correctly reads LcdUse24Hr, LcdShowSeconds, LcdStyle from the snapshot
- All three LCD checkbox/combo handlers fire their respective events with correct values
- Build produces zero errors and zero CS0067 warnings for the three LCD events
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/63-settingswindow-lcd-ui/63-01-SUMMARY.md`
</output>

---
phase: 45-english-phrase-style-personalities
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.Core/TersePhraseProvider.cs
  - FuzzyClock.Core/PoeticPhraseProvider.cs
  - FuzzyClock.Core/RudePhraseProvider.cs
  - FuzzyClock.Core/PhraseEngine.cs
  - FuzzyClock.Core.Tests/PhraseStyleProviderTests.cs
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03

must_haves:
  truths:
    - "SetLocale en-terse succeeds and GetPhrase returns compact British-style phrases"
    - "SetLocale en-poetic succeeds and GetPhrase returns evocative time-of-day phrases"
    - "SetLocale en-rude succeeds and GetPhrase returns blunt callout phrases"
    - "All 122 existing tests still pass after providers are registered"
    - "GetStructuredPhrase returns empty qualifier and full phrase for all three new providers"
  artifacts:
    - path: "FuzzyClock.Core/TersePhraseProvider.cs"
      provides: "IPhraseProvider implementation for en-terse"
      contains: "class TersePhraseProvider"
    - path: "FuzzyClock.Core/PoeticPhraseProvider.cs"
      provides: "IPhraseProvider implementation for en-poetic"
      contains: "class PoeticPhraseProvider"
    - path: "FuzzyClock.Core/RudePhraseProvider.cs"
      provides: "IPhraseProvider implementation for en-rude"
      contains: "class RudePhraseProvider"
    - path: "FuzzyClock.Core/PhraseEngine.cs"
      provides: "Provider registry with all four English locale keys"
      contains: "en-terse"
    - path: "FuzzyClock.Core.Tests/PhraseStyleProviderTests.cs"
      provides: "Unit tests for all three new providers"
      contains: "TersePhraseProviderTests"
  key_links:
    - from: "FuzzyClock.Core/PhraseEngine.cs"
      to: "TersePhraseProvider / PoeticPhraseProvider / RudePhraseProvider"
      via: "_providers dictionary"
      pattern: "en-terse.*TersePhraseProvider"
    - from: "FuzzyClock.Core.Tests/PhraseStyleProviderTests.cs"
      to: "FuzzyClock.Core/PhraseEngine.cs"
      via: "PhraseEngine.SetLocale()"
      pattern: "SetLocale"
---

<objective>
TDD implementation of three new IPhraseProvider classes (Terse, Poetic, Rude) and their registration in PhraseEngine. Uses RED→GREEN→REFACTOR to specify and verify provider contracts before Plan 02 wires them to the UI.

Purpose: The provider extensibility seam from Phase 41 is in place but only en-classic is registered. Adding en-terse, en-poetic, en-rude unlocks the full phrase style selector feature. TDD guards against static-state leaks between tests (a known pitfall with the static PhraseEngine) and ensures all three providers satisfy the IPhraseProvider contract including GetStructuredPhrase.
Output: Three new provider source files, PhraseEngine updated with four locale registrations, PhraseStyleProviderTests.cs covering all three styles. Suite grows from 122 to 140+ passing tests.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-english-phrase-style-personalities/45-RESEARCH.md
@FuzzyClock.Core/IPhraseProvider.cs
@FuzzyClock.Core/PhraseEngine.cs
@FuzzyClock.Core/EnglishPhraseProvider.cs
@FuzzyClock.Core.Tests/PhraseEngineCoordinatorTests.cs

<interfaces>
<!-- Established contracts — executor uses these directly. -->

From FuzzyClock.Core/IPhraseProvider.cs:
```csharp
public interface IPhraseProvider
{
    string GetPhrase(DateTime dt);
    (string Qualifier, string Emphasis) GetStructuredPhrase(DateTime dt);
}
```

From FuzzyClock.Core/PhraseEngine.cs (current — only en-classic registered):
```csharp
private static readonly Dictionary<string, IPhraseProvider> _providers = new()
{
    ["en-classic"] = new EnglishPhraseProvider()
};
public static string CurrentLocale { get; private set; } = "en-classic";
public static bool SetLocale(string locale);
public static string GetPhrase(DateTime dt);
public static (string Qualifier, string Emphasis) GetStructuredPhrase(DateTime dt);
```

From FuzzyClock.Core.Tests/PhraseEngineCoordinatorTests.cs (established TestCleanup pattern):
```csharp
[TestCleanup]
public void ResetLocale() => PhraseEngine.SetLocale("en-classic");
```
IMPORTANT: PhraseEngine is static. Every [TestClass] that calls SetLocale MUST have a
[TestCleanup] method that resets to "en-classic". Failure causes cross-test contamination
when the suite runs in parallel (MSTestSettings.cs sets MethodLevel parallelism).

EnglishPhraseProvider bucket table pattern (reference implementation):
```csharp
private static readonly (int UpperBound, string Template)[] Buckets =
[
    ( 2, "{h} o'clock"),
    ( 7, "just after {h}"),
    (12, "ten past {h}"),
    ...
    (59, "almost {h1}"),
];
// Walk: return first where minute <= upperBound
// {h} = hour12, {h1} = nextHour12
// Special cases: totalMinutes==720 => "noon", ==0 => "midnight"
```
</interfaces>
</context>

<feature>
  <name>Three new English phrase style providers: Terse, Poetic, Rude</name>
  <files>
    FuzzyClock.Core/TersePhraseProvider.cs,
    FuzzyClock.Core/PoeticPhraseProvider.cs,
    FuzzyClock.Core/RudePhraseProvider.cs,
    FuzzyClock.Core/PhraseEngine.cs,
    FuzzyClock.Core.Tests/PhraseStyleProviderTests.cs
  </files>
  <behavior>
    Test class: TersePhraseProviderTests
    - SetLocale_EnTerse_ReturnsTrue → PhraseEngine.SetLocale("en-terse") returns true
    - Terse_HalfHour_ReturnsBritishHalf → SetLocale("en-terse"); GetPhrase(3:30) == "half four" (British "half X" = half past X-1, so 3:30 → "half four")
    - Terse_OnTheHour_ReturnsJustHourWord → SetLocale("en-terse"); GetPhrase(3:00) == "three"
    - Terse_QuarterPast_ReturnsQuarterPast → SetLocale("en-terse"); GetPhrase(3:15) == "quarter past three"
    - Terse_GetStructuredPhrase_ReturnsEmptyQualifier → SetLocale("en-terse"); GetStructuredPhrase(3:30) returns ("", "half four")
    - [TestCleanup] resets to "en-classic"

    Test class: PoeticPhraseProviderTests
    - SetLocale_EnPoetic_ReturnsTrue → PhraseEngine.SetLocale("en-poetic") returns true
    - Poetic_SmallHours_Returns → SetLocale("en-poetic"); GetPhrase(3:00) returns non-empty string containing expected poetic phrase (e.g. "the small hours")
    - Poetic_Noon_ReturnsHighNoon → SetLocale("en-poetic"); GetPhrase(12:00) returns "high noon"
    - Poetic_GetStructuredPhrase_ReturnsEmptyQualifier → GetStructuredPhrase(3:00) returns ("", non-empty)
    - [TestCleanup] resets to "en-classic"

    Test class: RudePhraseProviderTests
    - SetLocale_EnRude_ReturnsTrue → PhraseEngine.SetLocale("en-rude") returns true
    - Rude_OnTheHour_ContainsHourWord → SetLocale("en-rude"); GetPhrase(4:00) contains "four"
    - Rude_NearlyHour_ContainsCallout → SetLocale("en-rude"); GetPhrase(4:55) contains "move it" or "get on with it" (phrase has callout suffix)
    - Rude_GetStructuredPhrase_ReturnsEmptyQualifier → GetStructuredPhrase(4:00) returns ("", non-empty)
    - [TestCleanup] resets to "en-classic"

    Bucket table specs:

    TERSE (12 buckets, British idiom):
    ( 2, "{h}")                  -- "three" — bare hour word
    ( 7, "just gone {h}")
    (12, "ten past {h}")
    (17, "quarter past {h}")
    (22, "twenty past {h}")
    (27, "half {h1}")            -- British: "half four" = 3:30
    (32, "half past {h}")
    (37, "just gone half {h}")
    (42, "twenty to {h1}")
    (47, "quarter to {h1}")
    (52, "ten to {h1}")
    (59, "nearly {h1}")
    Special: noon/midnight same as Classic.
    GetStructuredPhrase: return ("", GetPhrase(dt)) for all cases — no natural split.

    POETIC (time-of-day segments, not per-minute buckets):
    Hour 0, minute 0: "the witching hour"
    Hour 1–5: "the small hours"
    Hour 6–8: "the morning stirs"
    Hour 9–11: "the day grows long"
    Hour 12, minute 0: "high noon"
    Hour 12–13: "early afternoon"
    Hour 14–16: "the afternoon wanes"
    Hour 17–18: "the golden hour"
    Hour 19–20: "dusk settles"
    Hour 21–22: "the evening deepens"
    Hour 23: "the night draws in"
    GetStructuredPhrase: return ("", GetPhrase(dt)) for all cases.

    RUDE (12 buckets, callout suffixes on selected entries):
    ( 2, "exactly {h}, what do you want")
    ( 7, "just gone {h}")
    (12, "ten past {h}, wake up")
    (17, "quarter past {h}")
    (22, "gone quarter past {h}")
    (27, "nearly half past {h}")
    (32, "half past {h}, still here?")
    (37, "just gone half past {h}")
    (42, "almost quarter to {h1}")
    (47, "quarter to {h1}")
    (52, "nearly {h1}, move it")
    (59, "almost {h1}, get on with it")
    Special: noon/midnight same as Classic.
    GetStructuredPhrase: return ("", GetPhrase(dt)) for all cases.
  </behavior>
  <implementation>
    RED: Create PhraseStyleProviderTests.cs in FuzzyClock.Core.Tests/ with all three [TestClass]
    blocks and [TestCleanup] on each. Run `dotnet test` — tests MUST fail because the three
    provider files do not exist yet and en-terse/en-poetic/en-rude are not in _providers.
    Commit: `test(45-01): add failing provider tests for Terse/Poetic/Rude`

    GREEN:
    1. Create TersePhraseProvider.cs — copy HourWords array and bucket walk from EnglishPhraseProvider,
       replace Buckets array with the Terse table above, implement GetStructuredPhrase as
       return ("", GetPhrase(dt)). Handle noon/midnight special cases.
    2. Create PoeticPhraseProvider.cs — implement GetPhrase using hour-range conditionals (not
       bucket walk). Handle the witching hour (h==0 && m==0), then hour ranges in order.
       GetStructuredPhrase returns ("", GetPhrase(dt)).
    3. Create RudePhraseProvider.cs — copy HourWords array and bucket walk from EnglishPhraseProvider,
       replace Buckets array with the Rude table above. GetStructuredPhrase returns ("", GetPhrase(dt)).
       Handle noon/midnight special cases.
    4. Add the three new providers to PhraseEngine._providers dictionary:
       ["en-terse"]  = new TersePhraseProvider(),
       ["en-poetic"] = new PoeticPhraseProvider(),
       ["en-rude"]   = new RudePhraseProvider(),
    Run `dotnet test` — ALL tests must pass. Suite grows from 122 to 134+ passing.
    Commit: `feat(45-01): implement TersePhraseProvider, PoeticPhraseProvider, RudePhraseProvider`

    REFACTOR (if needed): Inspect providers for shared code that could be extracted (e.g. HourWords
    array, bucket walk helper). Do NOT extract to a shared base class — project pattern uses flat
    independent providers. If duplication is acceptable (small classes), leave as-is.
    Run `dotnet test` — must still pass.
    Commit if changed: `refactor(45-01): clean up provider implementations`
  </implementation>
</feature>

<verification>
`dotnet test C:/src/FuzzyStatsClock/FuzzyClock.sln --no-restore 2>&1 | tail -10`

Must report: 134+ passed, 0 failed.
Must exit with code 0.
All three new [TestClass] blocks present in PhraseStyleProviderTests.cs.
PhraseEngine._providers contains "en-terse", "en-poetic", "en-rude" entries.
</verification>

<success_criteria>
- TersePhraseProvider.cs, PoeticPhraseProvider.cs, RudePhraseProvider.cs all exist in FuzzyClock.Core/
- Each implements IPhraseProvider with GetPhrase(DateTime) and GetStructuredPhrase(DateTime)
- GetStructuredPhrase returns ("", fullPhrase) for all three — no natural qualifier split
- PhraseEngine._providers has four entries: en-classic, en-terse, en-poetic, en-rude
- PhraseStyleProviderTests.cs exists with three [TestClass] blocks, each with [TestCleanup] resetting to en-classic
- `dotnet test` exits 0 with 0 failures, suite size 134 or more
- No existing test file modified
</success_criteria>

<output>
After completion, create `.planning/phases/45-english-phrase-style-personalities/45-01-SUMMARY.md`
</output>

---
phase: 34-uptime-process-count-readme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "README features list includes all v2.7+ features: ghost mode, auto-contrast, tray controls, accent colors, opacity, uptime row with process count, auto-launch, per-monitor position memory"
    - "README usage section covers all three interaction modes: right-click context menu, mouse interactions (drag + scroll wheel), and system tray controls"
    - "Uptime row description in README mentions the 142p process count example"
  artifacts:
    - path: "README.md"
      provides: "Complete feature list and usage documentation for v2.8"
      contains: "auto-contrast"
  key_links:
    - from: "README.md"
      to: "Features section"
      via: "bullet list"
      pattern: "Auto-contrast|auto-contrast"
    - from: "README.md"
      to: "Usage section"
      via: "three subsections"
      pattern: "Right-click|Mouse interactions|System tray"
---

<objective>
Verify the README accurately documents all current app features and interaction modes required by DOCS-01 and DOCS-02. Compare the README against the full feature set and fix any gaps or outdated content. The README must be the single source of truth for a new user discovering the app.

Purpose: README is the user-facing documentation for the v2.8 release. All features shipped through v2.7 must be accurately described.
Output: README.md confirmed accurate (or updated) for all DOCS-01 and DOCS-02 requirements.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update README for DOCS-01 and DOCS-02 compliance</name>
  <files>README.md</files>
  <action>
Read README.md in full. Verify it satisfies both requirements:

**DOCS-01 checklist — Features section must include ALL of these:**
- [ ] Ghost mode (hover-to-hide, Opacity=0 + click-through, toggleable via tray)
- [ ] Auto-contrast (WCAG screen sampling, black/white text switch, toggleable via tray)
- [ ] Tray controls (system tray icon with right-click menu)
- [ ] Accent colors (5 presets + custom color picker)
- [ ] Window opacity (presets + scroll wheel)
- [ ] Uptime row (uptime string + load averages + process count in `{N}p` format, e.g. `142p`)
- [ ] Auto-launch (login toggle via tray)
- [ ] Per-monitor position memory (restores position per monitor)

**DOCS-02 checklist — Usage section must cover ALL three interaction modes:**
- [ ] Right-click context menu (table of menu items and their descriptions)
- [ ] Mouse interactions (left-click drag, scroll wheel opacity, hover ghost mode, Ctrl+Alt suppress)
- [ ] System tray controls (Auto-Launch, Ghost Mode, Auto-Contrast, Reset to Defaults, Quit)

For each item: if already present and accurate — leave unchanged.
If missing or inaccurate — add or correct the content to match actual behavior.

Key accuracy checks:
- Ghost mode: "hover-to-hide — fully transparent and click-through; Ctrl+Alt suppresses ghost mode"
- Auto-contrast: "samples screen color under widget every 500ms; WCAG-based; switches to black or white"
- System tray: must list Auto-Launch, Ghost Mode, Auto-Contrast, Reset to Defaults, Quit
- Uptime row: must show `142p` or equivalent `{N}p` example
- Opacity: tray submenu (25/50/75/100%) AND scroll wheel (10% steps)

Do NOT add prose sections, marketing copy, or screenshots. Keep the style consistent with the existing README (minimal markdown, bullet lists, tables). Only add what is genuinely missing — do not restructure working sections.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && grep -c "auto-contrast\|Auto-Contrast\|Auto-contrast" README.md && grep -c "Auto-Launch\|auto-launch" README.md && grep -c "per-monitor\|Per-monitor" README.md && grep -c "142p\|{N}p\|procCount\|process count" README.md</automated>
    <manual>Read README.md Usage section — confirm three subsections exist: Right-click context menu, Mouse interactions, System tray — each with correct content</manual>
  </verify>
  <done>
README.md features list contains all 8 DOCS-01 items. README.md usage section contains all 3 DOCS-02 interaction modes. Uptime row description mentions process count with example (e.g. 142p).
  </done>
</task>

</tasks>

<verification>
Read README.md and confirm:

1. DOCS-01: Features bullet list includes ghost mode, auto-contrast, tray controls, accent colors, opacity, uptime row (with 142p example), auto-launch, per-monitor position memory
2. DOCS-02: Usage has three subsections — Right-click context menu (table), Mouse interactions (table), System tray (list with Auto-Launch / Ghost Mode / Auto-Contrast / Reset to Defaults / Quit)
3. No broken markdown, consistent style, no duplicate sections
</verification>

<success_criteria>
1. README features section: all 8 features from DOCS-01 requirement are present and accurate
2. README usage section: all 3 interaction modes from DOCS-02 are documented with correct behavior descriptions
3. README is consistent — no stale references to removed features (e.g. no mention of right-click menu being "primary UI" if tray-only controls are now standard)
</success_criteria>

<output>
After completion, create `.planning/phases/34-uptime-process-count-readme/34-02-SUMMARY.md`
</output>

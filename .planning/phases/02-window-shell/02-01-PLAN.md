---
phase: 02-window-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/FuzzyClock.App.csproj
  - FuzzyClock.slnx
autonomous: true
requirements:
  - WIN-01
  - WIN-02
  - WIN-03

must_haves:
  truths:
    - "dotnet build FuzzyClock.slnx succeeds and includes FuzzyClock.App"
    - "FuzzyClock.App targets net10.0-windows with UseWPF=true and OutputType=WinExe"
    - "FuzzyClock.App has a ProjectReference to FuzzyClock.Core"
    - "FuzzyClock.App appears in the solution alongside FuzzyClock.Core and FuzzyClock.Core.Tests"
  artifacts:
    - path: "FuzzyClock.App/FuzzyClock.App.csproj"
      provides: "WPF application project targeting net10.0-windows"
      contains: "UseWPF"
    - path: "FuzzyClock.slnx"
      provides: "Solution file now including FuzzyClock.App"
      contains: "FuzzyClock.App"
  key_links:
    - from: "FuzzyClock.App/FuzzyClock.App.csproj"
      to: "FuzzyClock.Core/FuzzyClock.Core.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*FuzzyClock\\.Core\\.csproj"
    - from: "FuzzyClock.slnx"
      to: "FuzzyClock.App/FuzzyClock.App.csproj"
      via: "dotnet solution add"
      pattern: "FuzzyClock\\.App"
---

<objective>
Create the FuzzyClock.App WPF project, add it to the solution, and wire the FuzzyClock.Core project reference.

Purpose: Establishes the application project that Plan 02 will fill with the transparent overlay implementation. The scaffold must exist before any XAML or code-behind can be written. The Core reference is added now so Phase 3 wiring is a one-line change.

Output: FuzzyClock.App/FuzzyClock.App.csproj (net10.0-windows, UseWPF=true, OutputType=WinExe, ProjectReference to Core), FuzzyClock.slnx updated to include FuzzyClock.App.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-window-shell/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold FuzzyClock.App WPF project and wire to solution</name>
  <files>
    FuzzyClock.App/FuzzyClock.App.csproj
    FuzzyClock.slnx
  </files>
  <action>
    Run the following commands from the repo root (C:/src/gsd1) in sequence:

    1. Create the WPF application project (net10.0-windows, UseWPF=true, OutputType=WinExe):
       dotnet new wpf -n FuzzyClock.App -f net10.0 -o FuzzyClock.App

    2. Add the project to the existing solution (FuzzyClock.slnx — the .slnx format generated by dotnet 10 SDK):
       dotnet solution FuzzyClock.slnx add FuzzyClock.App/FuzzyClock.App.csproj

    3. Add the ProjectReference from FuzzyClock.App to FuzzyClock.Core:
       dotnet add FuzzyClock.App/FuzzyClock.App.csproj reference FuzzyClock.Core/FuzzyClock.Core.csproj

    The `dotnet new wpf` template generates placeholder files: App.xaml, App.xaml.cs, MainWindow.xaml, MainWindow.xaml.cs, and AssemblyInfo.cs. Do NOT delete these — they are valid WPF scaffolding that Plan 02 will overwrite. Do NOT delete AssemblyInfo.cs (it contains the ThemeInfo attribute required by WPF).

    The generated csproj will contain OutputType=WinExe, TargetFramework=net10.0-windows, UseWPF=true. Do not modify the csproj beyond what dotnet CLI commands produce — Plan 02 does not require any additional csproj properties.

    Do NOT add UseWindowsForms=true or any non-WPF references.
  </action>
  <verify>
    <automated>dotnet build FuzzyClock.slnx</automated>
    <manual>Confirm FuzzyClock.App/FuzzyClock.App.csproj contains TargetFramework net10.0-windows, UseWPF=true, OutputType=WinExe. Confirm a ProjectReference to FuzzyClock.Core.csproj is present in the csproj.</manual>
    <sampling_rate>run after this task</sampling_rate>
  </verify>
  <done>dotnet build FuzzyClock.slnx exits 0 with zero errors. FuzzyClock.App is in the solution. FuzzyClock.App.csproj references FuzzyClock.Core.csproj via ProjectReference. The generated template files exist (App.xaml, MainWindow.xaml, etc.).</done>
</task>

</tasks>

<verification>
After this plan:
- dotnet build FuzzyClock.slnx exits 0
- FuzzyClock.App/FuzzyClock.App.csproj exists with TargetFramework=net10.0-windows, UseWPF=true, OutputType=WinExe
- FuzzyClock.App.csproj contains a ProjectReference to FuzzyClock.Core.csproj
- FuzzyClock.slnx lists FuzzyClock.App alongside FuzzyClock.Core and FuzzyClock.Core.Tests
- Template placeholder files exist (App.xaml, MainWindow.xaml, etc.) for Plan 02 to overwrite
</verification>

<success_criteria>
The FuzzyClock.App WPF project scaffold is clean, builds successfully as part of the solution, and has the Core project reference ready. Plan 02 can immediately begin implementing the transparent overlay without any project setup steps.
</success_criteria>

<output>
After completion, create `.planning/phases/02-window-shell/02-01-SUMMARY.md`
</output>

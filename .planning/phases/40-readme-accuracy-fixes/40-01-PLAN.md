---
phase: 40-readme-accuracy-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "README Short format example matches DateFormatter.Format output: 'Sat, Mar 7' style (ddd, MMM d)"
    - "README Long format example matches DateFormatter.Format output: 'Saturday, March 7' style (dddd, MMMM d) — no year"
    - "README test count reads '122 unit tests'"
  artifacts:
    - path: "README.md"
      provides: "Accurate documentation of date formats and test count"
      contains: "Sat, Mar 7"
  key_links:
    - from: "README.md date format examples"
      to: "FuzzyClock.Core/DateFormatter.cs format strings"
      via: "ddd, MMM d and dddd, MMMM d"
      pattern: "Sat, Mar 7"
---

<objective>
Fix three accuracy errors in README.md: Short and Long date format examples that don't match what DateFormatter.Format() actually produces, and a stale test count.

Purpose: README is the product's face — examples that don't match runtime output erode trust and mislead users configuring date formats.
Output: README.md with corrected Short/Long examples (both occurrences each) and updated test count.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Source of truth for format strings:
@FuzzyClock.Core/DateFormatter.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Short and Long format examples in README features list</name>
  <files>README.md</files>
  <action>
In README.md, find the date display feature bullet in the Features section (around line 13–16). It currently reads:

  - Short: `Mon 3/7`
  - Long: `Monday, March 7, 2026`

Replace with:

  - Short: `Sat, Mar 7`
  - Long: `Saturday, March 7`

Rationale:
- DateFormatter uses format string "ddd, MMM d" for Short — produces abbreviated day + abbreviated month + day (e.g. "Sat, Mar 7"), not "Mon 3/7"
- DateFormatter uses format string "dddd, MMMM d" for Long — produces full day + full month + day with NO year (e.g. "Saturday, March 7"), not "Monday, March 7, 2026"

Do NOT change the Numeric or ISO examples — they are correct.
  </action>
  <verify>
    <automated>grep -n "Sat, Mar 7" C:/src/FuzzyStatsClock/README.md</automated>
  </verify>
  <done>"Sat, Mar 7" appears in the Features section bullet list; "Mon 3/7" is gone from the features list; "Saturday, March 7" appears without a year in the features list; "Monday, March 7, 2026" is gone from the features list.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Short and Long examples in tray menu table and update test count</name>
  <files>README.md</files>
  <action>
Two edits in one pass:

Edit A — Tray menu table (around line 76). The Date Format row currently reads:

  | **Date Format** | Short (Mon 3/7) / Long (Monday, March 7, 2026) / Numeric (3/7/2026) / ISO (2026-03-07) |

Replace with:

  | **Date Format** | Short (Sat, Mar 7) / Long (Saturday, March 7) / Numeric (3/7/2026) / ISO (2026-03-07) |

Edit B — Test count line (around line 59). Currently reads:

  114 unit tests: phrase engine (all 5-minute buckets, noon/midnight, edge cases), dial geometry, uptime formatter, date formatter (all 4 formats), settings validation and migration, and app integration tests.

Replace "114" with "122". Do NOT change any other text in that sentence.
  </action>
  <verify>
    <automated>grep -n "122 unit tests\|Sat, Mar 7\|Saturday, March 7" C:/src/FuzzyStatsClock/README.md</automated>
  </verify>
  <done>grep returns three matching lines: the test count line with "122 unit tests", the features-section Short bullet with "Sat, Mar 7", and the tray table Date Format row also with "Sat, Mar 7". No remaining instances of "114 unit tests", "Mon 3/7", or "Monday, March 7, 2026" anywhere in the file.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Confirm stale values are gone
grep -n "Mon 3/7\|Monday, March 7, 2026\|114 unit tests" C:/src/FuzzyStatsClock/README.md
# Expected: no output (zero matches)

# Confirm correct values are present
grep -n "Sat, Mar 7\|Saturday, March 7\|122 unit tests" C:/src/FuzzyStatsClock/README.md
# Expected: 3 lines — features Short bullet, tray table Date Format row, test count line
```
</verification>

<success_criteria>
- `grep "Mon 3/7" README.md` returns nothing
- `grep "Monday, March 7, 2026" README.md` returns nothing
- `grep "114 unit tests" README.md` returns nothing
- `grep "Sat, Mar 7" README.md` returns 2 lines (features list + tray table)
- `grep "Saturday, March 7" README.md` returns 2 lines (features list + tray table)
- `grep "122 unit tests" README.md` returns 1 line
</success_criteria>

<output>
After completion, create `.planning/phases/40-readme-accuracy-fixes/40-01-SUMMARY.md`
</output>

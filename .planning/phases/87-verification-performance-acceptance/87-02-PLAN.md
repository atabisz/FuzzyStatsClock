---
phase: 87-verification-performance-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/MainWindow.xaml.cs
  - .planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md
autonomous: false
requirements:
  - PERF-01
  - TEST-04
must_haves:
  truths:
    - "OnGhostEnabledChanged(false) called mid-fade clears _currentRatio and _targetRatio to 0.0 and writes this.Opacity = _windowOpacity when the settings window is not visible (D-CARRY-01 / WR-04 fix)"
    - "OnGhostEnabledChanged(false) called mid-fade does NOT overwrite this.Opacity when the settings window is visible — the settings-window pin contract is preserved (D-CARRY-01 polarity)"
    - "Disabling ghost mode mid-fade no longer leaves the widget half-transparent — the user can recover full opacity by toggling ghost mode off (TEST-04 re-asserted as a behavior gate)"
    - "PERF-01 manual run reaches 25–50% sustained CPU load via PowerShell while($true){} windows and produces a written attestation in 87-VERIFICATION.md (D-PERF-01, D-PERF-02)"
    - "87-VERIFICATION.md captures load level reached, monitor refresh rate, subjective smoothness verdict, and an explicit sign-off line per D-PERF-02"
    - "87-VERIFICATION.md absorbs the 12 carry-forward human_verification items from Phases 85 (5 items) and 86 (7 items) per D-CARRY-03 — items observable during the load run are ticked off; items not observable from the load scenario remain noted as open"
    - "Full MSTest suite (`dotnet test FuzzyClock.sln --nologo --verbosity quiet`) is green at milestone end and the result is recorded in 87-VERIFICATION.md (TEST-04 evidence)"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "OnGhostEnabledChanged(false) branch that clears _currentRatio/_targetRatio and restores this.Opacity unless settings-window-pinned"
      contains: "_currentRatio = 0.0"
    - path: ".planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md"
      provides: "Phase 87 verification report with PERF-01 attestation and TEST-04 evidence"
      contains: "Load level reached:"
  key_links:
    - from: "FuzzyClock.App/MainWindow.xaml.cs::OnGhostEnabledChanged(false)"
      to: "FuzzyClock.App/MainWindow.xaml.cs::_settingsWindow"
      via: "null-safe IsVisible check before writing this.Opacity"
      pattern: "_settingsWindow\\?\\.IsVisible\\s*!=\\s*true"
    - from: ".planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md"
      to: "PERF-01 evidence (manual smoothness verdict)"
      via: "human-recorded attestation block in Behavioral Spot-Checks section"
      pattern: "Smoothness verdict:"
    - from: ".planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md"
      to: "TEST-04 evidence (dotnet test Passed line)"
      via: "captured Passed: NNN, Failed: 0, Skipped: 0 line in Behavioral Spot-Checks section"
      pattern: "Passed:\\s*\\d+,\\s*Failed:\\s*0"
---

<objective>
Fix Phase 86 review item WR-04 — disabling ghost mode mid-fade leaves the widget half-transparent — with the six-line patch inside `MainWindow.OnGhostEnabledChanged(false)` per D-CARRY-01, then run the PERF-01 manual smoothness check under sustained 25–50% CPU load via PowerShell `while ($true) {}` windows per D-PERF-01, then write `87-VERIFICATION.md` capturing the PERF-01 attestation, the TEST-04 `dotnet test` evidence, and the absorbed 12 carry-forward human_verification items from Phases 85 and 86 per D-CARRY-03.

Purpose: PERF-01 is the milestone's user-visible acceptance gate — observation-only by design. Bundling the WR-04 fix into Phase 87 means v4.4 ships without the half-transparent toggle-off glitch. The verification report is the milestone's written evidence trail and absorbs absorbed UAT items rather than leaving them as open standalone artifacts.

Output:
- Six-line edit to `FuzzyClock.App/MainWindow.xaml.cs` lines 260-275 inside `OnGhostEnabledChanged(false)` per D-CARRY-01: zero `_currentRatio` and `_targetRatio`, write `this.Opacity = _windowOpacity` when `_settingsWindow?.IsVisible != true`.
- New verification document `.planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md` mirroring the `86-VERIFICATION.md` shape, with PERF-01 attestation block (load level, monitor refresh rate, smoothness verdict, sign-off) and TEST-04 `dotnet test` evidence.
- `dotnet build FuzzyClock.sln` is 0 Warnings / 0 Errors after the fix; `dotnet test FuzzyClock.sln --nologo --verbosity quiet` reports `Passed >= 578 + new rows from 87-01, Failed: 0, Skipped: 0`.

This plan has `autonomous: false` because the PERF-01 manual run requires a human observer to record the subjective smoothness verdict — automation cannot substitute eyeballs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-verification-performance-acceptance/87-CONTEXT.md
@.planning/phases/87-verification-performance-acceptance/87-PATTERNS.md
@.planning/phases/86-frame-driven-opacity-rendering/86-VERIFICATION.md
@.planning/phases/86-frame-driven-opacity-rendering/86-REVIEW.md
@.planning/phases/85-off-thread-sampling-refactor/85-VERIFICATION.md

<interfaces>
<!-- Key handler shape and field references the executor needs. Extracted from MainWindow.xaml.cs. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From FuzzyClock.App/MainWindow.xaml.cs (relevant declarations and current handler):
- `private bool _renderPumpAttached` — internal flag tracking the CompositionTarget.Rendering subscription
- `private double _currentRatio` (Phase 86 D-12), `private double _targetRatio` (Phase 86 D-13) — both fields exist; both are written to 0.0 by the WR-04 fix
- `private double _windowOpacity` — the user-set base opacity; written into `this.Opacity` when proximity fade is bypassed (e.g. settings window pinned)
- `private SettingsWindow? _settingsWindow` — null when not open; `_settingsWindow?.IsVisible == true` is the canonical pin guard pattern at lines 1489 and 313
- `private DateTime? _previousRenderTime` — reset to `null` in the existing `enabled` branch; not touched by the WR-04 fix
- Current handler shape at lines 260-275 (read PATTERNS.md § "MainWindow.xaml.cs:260-275" for the exact before/after diff)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply WR-04 fix to OnGhostEnabledChanged(false) — clear lerp state and restore Opacity unless settings-window-pinned (D-CARRY-01)</name>
  <files>FuzzyClock.App/MainWindow.xaml.cs</files>
  <behavior>
    - Calling `OnGhostEnabledChanged(false)` while `_currentRatio` is mid-fade (e.g. 0.5) and `_settingsWindow?.IsVisible != true` results in `_currentRatio == 0.0`, `_targetRatio == 0.0`, and `this.Opacity == _windowOpacity` after the call returns
    - Calling `OnGhostEnabledChanged(false)` while `_settingsWindow?.IsVisible == true` results in `_currentRatio == 0.0` and `_targetRatio == 0.0`, but `this.Opacity` is left untouched (settings-window pin contract preserved per D-CARRY-01 inverse polarity)
    - Calling `OnGhostEnabledChanged(false)` while `_renderPumpAttached == false` returns early without writing any field — existing early-return preserved (current line 271)
    - The existing `enabled == true` branch is byte-for-byte unchanged
  </behavior>
  <read_first>
    - FuzzyClock.App/MainWindow.xaml.cs (read lines 240-290 for the full `OnGhostEnabledChanged` body and surrounding context — the exact six-line patch lands between the existing `_renderPumpAttached = false;` at line 273 and the closing brace at line 274)
    - FuzzyClock.App/MainWindow.xaml.cs (read lines 1480-1500 for the `SetOpacity` settings-pin reference pattern at lines 1484-1494 — confirms the `_settingsWindow?.IsVisible == true` polarity convention used elsewhere in the file; the WR-04 fix uses the inverse `!= true` polarity per D-CARRY-01)
    - FuzzyClock.App/MainWindow.xaml.cs (read lines 180-200 if accessible — confirms the existing `_currentRatio = 0.0; this.Opacity = _windowOpacity;` two-line reset pattern at the initialization-time path that the WR-04 fix mirrors)
    - .planning/phases/86-frame-driven-opacity-rendering/86-REVIEW.md (§ WR-04 — the canonical fix diff and rationale; the planner has already cited the six-line shape verbatim in CONTEXT.md `<specifics>`)
    - .planning/phases/87-verification-performance-acceptance/87-CONTEXT.md (D-CARRY-01 fix shape in `<specifics>` — the six-line patch is specified verbatim there; copy the literal field writes and the `_settingsWindow?.IsVisible != true` polarity)
    - .planning/phases/87-verification-performance-acceptance/87-PATTERNS.md (§ "MainWindow.xaml.cs:260-275 (controller, request-response edit)" — exact mirror specifics including comment placement)
  </read_first>
  <action>
    Inside `OnGhostEnabledChanged(bool enabled)` at MainWindow.xaml.cs lines 260-275, modify ONLY the `else` (disable) branch. Leave the `if (enabled)` branch byte-for-byte unchanged. Inside the `else` branch, after the existing two lines `System.Windows.Media.CompositionTarget.Rendering -= OnRenderingTick;` and `_renderPumpAttached = false;`, append exactly the six-line patch from CONTEXT.md `<specifics>` D-CARRY-01: a comment line (e.g. `// Phase 87 WR-04 fix: clear residual lerp state and restore Opacity unless pinned.`), then `_currentRatio = 0.0;`, then `_targetRatio  = 0.0;`, then `if (_settingsWindow?.IsVisible != true) this.Opacity = _windowOpacity;`. Use the inverse polarity `!= true` (NOT `== true`) per D-CARRY-01 because the disable branch only writes `this.Opacity` when NOT pinned by the settings window — the settings-window pin contract requires the pin value to remain. Do NOT change the method signature, do NOT add new fields, do NOT add a defensive null check on `_settingsWindow` itself (the `?.` operator already handles null safely — that is the established convention at lines 1489 and 313). Do NOT touch the early-return guard `if (!_renderPumpAttached) return;` — it remains the first line of the disable branch.
  </action>
  <verify>
    <automated>grep -n "_currentRatio = 0.0" FuzzyClock.App/MainWindow.xaml.cs && grep -n "_targetRatio" FuzzyClock.App/MainWindow.xaml.cs && grep -n "_settingsWindow?.IsVisible != true" FuzzyClock.App/MainWindow.xaml.cs && dotnet test FuzzyClock.sln --nologo --verbosity quiet</automated>
  </verify>
  <acceptance_criteria>
    - Source: `MainWindow.xaml.cs` `OnGhostEnabledChanged` body contains the literal substring `_currentRatio = 0.0;` inside the disable branch
    - Source: `MainWindow.xaml.cs` `OnGhostEnabledChanged` body contains the literal substring `_targetRatio` followed by `= 0.0;` inside the disable branch
    - Source: `MainWindow.xaml.cs` `OnGhostEnabledChanged` body contains the literal substring `_settingsWindow?.IsVisible != true` (inverse-polarity pin guard)
    - Source: `MainWindow.xaml.cs` `OnGhostEnabledChanged` body contains the literal substring `this.Opacity = _windowOpacity` inside the disable branch (gated by the inverse pin guard)
    - Source: the existing `if (enabled)` branch of `OnGhostEnabledChanged` is byte-for-byte unchanged (still contains `_previousRenderTime = null;` and `_renderPumpAttached = true;` and the `+= OnRenderingTick` subscription, all in the same order as before)
    - Source: the existing `if (!_renderPumpAttached) return;` early-return at the top of the disable branch is preserved
    - Build: `dotnet build FuzzyClock.sln --nologo --verbosity quiet` exits 0 with 0 Warnings, 0 Errors
    - Test command: `dotnet test FuzzyClock.sln --nologo --verbosity quiet` exits 0 with `Passed: NNN, Failed: 0, Skipped: 0` where `NNN >= 578 + new rows from 87-01` — TEST-04 baseline preserved after the production-code change
  </acceptance_criteria>
  <done>WR-04 fix lands as a six-line patch inside the `OnGhostEnabledChanged(false)` branch; build green; full MSTest suite green; settings-window pin contract preserved by the inverse-polarity guard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PERF-01 manual smoothness run under 25–50% sustained CPU load (D-PERF-01) — observer required</name>
  <files>(observation only — no file written in this task)</files>
  <read_first>
    - .planning/phases/87-verification-performance-acceptance/87-CONTEXT.md (D-PERF-01 PowerShell load methodology — `powershell -NoProfile -Command "while ($true) {}"` in 1–2 windows targeting ~25–50% sustained CPU on the test box, ~12.5% load per window on a typical 8-core dev box)
    - .planning/phases/87-verification-performance-acceptance/87-CONTEXT.md (D-PERF-02 attestation requirements — load level, monitor refresh rate, smoothness verdict from {"smooth", "barely-stepping", "clearly-stepping"}, sign-off)
    - .planning/phases/85-off-thread-sampling-refactor/85-VERIFICATION.md (5 absorbed `human_verification:` items per D-CARRY-03)
    - .planning/phases/86-frame-driven-opacity-rendering/86-VERIFICATION.md (7 absorbed `human_verification:` items per D-CARRY-03 including the PERF-01 sanity precursor)
  </read_first>
  <what-built>Phase 85 off-thread sampling refactor + Phase 86 frame-driven opacity rendering + Phase 87 Task 1 WR-04 mid-fade toggle-off fix. The visible fade is meant to glide at display refresh rate, decoupled from the 33 ms sampling cadence, even when the system is under sustained 25–50% CPU contention.</what-built>
  <action>
    Pause here for the human observer. The observer must run the production widget under sustained 25–50% CPU load (PowerShell `while ($true) {}` windows, 1 window per ~12.5% target on an 8-core box), grade the fade traversal smoothness as one of `smooth` / `barely-stepping` / `clearly-stepping`, and tick off which absorbed UAT items (5 from Phase 85 + 7 from Phase 86 per D-CARRY-03) were observable during the load run. Detailed step-by-step instructions are in `<how-to-verify>` below; observer reports verdict + load level + monitor refresh rate + window count + UAT-items-observed list via the `<resume-signal>` format. Task 3 cannot start until this human verdict is recorded.
  </action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint by design. PERF-01 acceptance is observation-only per REQUIREMENTS.md and CONTEXT.md D-PERF-02; no automated frame-time instrumentation exists (deferred per CONTEXT.md "Deferred Ideas"). Verification is the human verdict captured in the resume signal.</automated>
    <human-check>
      Observer confirms via the resume signal:
      1. Load level reached (e.g. "32% sustained over 30 s") matches the 25–50% target band
      2. Monitor refresh rate noted (e.g. "144 Hz primary")
      3. Smoothness verdict explicitly grades the fade traversal: `smooth` (acceptance) | `barely-stepping` (proceeds with caveats) | `clearly-stepping` (PERF-01 fails — STOP)
      4. UAT-items-observed list enumerates which of the 12 absorbed items were visible during the run, with each item explicitly tagged "observed" or "open"
      5. WR-04 fix specifically observed: toggling ghost mode OFF mid-fade now recovers full opacity (proves Task 1's six-line patch works end-to-end)
    </human-check>
  </verify>
  <how-to-verify>
    1. Run the production build of `FuzzyClock.App` (e.g. `dotnet run --project FuzzyClock.App`) so the widget is visible on the desktop.
    2. Open Task Manager → Performance tab so you can observe the CPU load reading live.
    3. Open 1 PowerShell window per ~12.5% target load and run the load one-liner: `powershell -NoProfile -Command "while ($true) {}"`. Open enough windows to reach the target band — 2 windows for ~25%, 4 windows for ~50% on an 8-core box. Wait for the CPU reading to stabilize (≥10 s sustained). Record the actual sustained percentage you observe.
    4. Note the primary monitor refresh rate from Display settings (e.g. 60 Hz, 144 Hz).
    5. Hover the cursor toward the widget several times across the full fade traversal (cursor far → cursor inside → cursor far), under load. Subjectively grade smoothness as one of: `smooth` / `barely-stepping` / `clearly-stepping`. PERF-01 acceptance is `smooth` at 30+ fps subjective for the full fade traversal.
    6. While the load is running, observe whether each of the absorbed UAT items behaves as expected:
       - Drag widget while ghost mode is mid-fade — drag freeze holds (SEM-04)
       - Open settings window while ghost mode is mid-fade — fade pin holds, opacity does not change (SEM-04)
       - Right-click menu pin (RMB-04) while ghost mode is mid-fade — menu open holds opacity (SEM-04)
       - Mouse-wheel opacity adjust while ghost mode is mid-fade — direct-write Opacity wins
       - Toggle ghost mode OFF mid-fade — widget recovers full opacity (THIS IS THE WR-04 FIX from Task 1)
       - Hover-in cursor → confirm `WS_EX_TRANSPARENT` activates at proximity 1.0 (SEM-01)
       - Hover-out cursor → confirm `Restored` event fires only when ratio fully reaches 0.0 (SEM-02)
       - Modifier-held override (Ctrl/Alt/Shift per current config) forces ratio to 0.0 (SEM-03)
       - Ghost-mode tray toggle off → no sampling, no events (SEM-05)
       - Tray Exit → clean shutdown
    7. Close all PowerShell load windows.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `verdict: smooth — load X% sustained @ NN Hz; UAT items observed: [list]; UAT items deferred: [list]` — proceeds to Task 3
    - `verdict: barely-stepping — [details]` — proceeds to Task 3 with caveats noted
    - `verdict: clearly-stepping — [details]` — STOP, file a regression and re-plan; PERF-01 fails
    Or describe specific issues observed.
  </resume-signal>
  <acceptance_criteria>
    - Human observer has run the production build, generated 25–50% sustained CPU load via PowerShell `while ($true) {}` windows, observed the fade traversal, and recorded a verdict from {"smooth", "barely-stepping", "clearly-stepping"}
    - Human observer has noted the actual load percentage reached (Task Manager reading), the monitor refresh rate, and the window count used
    - Human observer has ticked off which absorbed UAT items (from Phase 85 and Phase 86 carry-forward) were observable during the load run vs. which remain open as standalone observation artifacts
    - PERF-01 verdict is `smooth` (the milestone's acceptance gate) — `barely-stepping` may proceed with caveats; `clearly-stepping` blocks the milestone and triggers regression investigation
  </acceptance_criteria>
  <done>Human observer has produced a structured PERF-01 verdict and the absorbed-UAT-items checklist needed to populate `87-VERIFICATION.md` in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Write 87-VERIFICATION.md capturing PERF-01 attestation, TEST-04 evidence, and absorbed UAT items (D-PERF-02, D-CARRY-03)</name>
  <files>.planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/86-frame-driven-opacity-rendering/86-VERIFICATION.md (analog file — read end-to-end for the YAML frontmatter shape, the nine body sections in order, the `human_verification:` block format, and the footer convention)
    - .planning/phases/85-off-thread-sampling-refactor/85-VERIFICATION.md (read the 5 `human_verification:` items absorbed into the Phase 87 UAT checklist per D-CARRY-03)
    - .planning/phases/87-verification-performance-acceptance/87-CONTEXT.md (D-PERF-02 attestation requirements — load level, monitor refresh rate, smoothness verdict, sign-off; D-CARRY-03 UAT-checklist requirement; `<specifics>` "TEST-04 evidence one-liner" for the `dotnet test` invocation)
    - .planning/phases/87-verification-performance-acceptance/87-PATTERNS.md (§ "87-VERIFICATION.md (documentation, attestation)" — exact mirror specifics for frontmatter and body sections)
    - .planning/REQUIREMENTS.md (TEST-01..04 + PERF-01 — the requirements coverage table in the body must list each with its evidence)
  </read_first>
  <action>
    Create `.planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md` mirroring `86-VERIFICATION.md` exactly. YAML frontmatter at the top with keys `phase: 87-verification-performance-acceptance`, `verified: <iso timestamp>`, `status: human_needed` (until the PERF-01 sign-off lands; flip to `verified` once Task 2 verdict is `smooth` or `barely-stepping`-with-acceptance), `score: N/N must-haves verified`, `overrides_applied: 0`, `human_verification:` list. Under `human_verification:` include the 12 absorbed items from Phases 85 (5 items, lifted from 85-VERIFICATION.md) and 86 (7 items, lifted from 86-VERIFICATION.md) per D-CARRY-03 — each item retains the analog's three-key shape (`test:`, `expected:`, `why_human:`). Body sections in fixed order matching the analog: (1) `# Phase 87: Verification & performance acceptance — Verification Report` heading + meta lines (Phase Goal, Verified, Status, Re-verification); (2) `## Goal Achievement` narrative summary; (3) `## Observable Truths` table (#, Truth, Status, Evidence); (4) `## Required Artifacts` table; (5) `## Key Link Verification` table; (6) `## Behavioral Spot-Checks` table — MUST contain a row capturing the PERF-01 attestation per D-PERF-02 with these literal sub-fields recorded: `Load level reached: <percent>% sustained over <duration>`, `Monitor refresh rate: <NN> Hz <primary>`, `Smoothness verdict: <smooth | barely-stepping | clearly-stepping>`, `Sign-off: <signature line>`; MUST also contain a row capturing the TEST-04 evidence: run `dotnet test FuzzyClock.sln --nologo --verbosity quiet` and record the exact `Passed: NNN, Failed: 0, Skipped: 0` line; (7) `## Requirements Coverage` table with one row per requirement (TEST-01, TEST-02, TEST-03, TEST-04, PERF-01) listing source plan (87-01 or 87-02) + description + status + evidence pointer; (8) `## Anti-Patterns Found` (scan results — expected: none, since no new production patterns introduced beyond the WR-04 fix and the visibility relaxation); (9) `## Human Verification Required` narrative pointing back to the frontmatter list and recording which UAT items were observed during the PERF-01 load run (Task 2 ticked-off list) vs. which remain open as standalone observation artifacts; (10) `## Gaps Summary` narrative — call out the explicitly deferred Phase 86 WR-01 / WR-02 / WR-03 items per D-CARRY-02. Footer pattern matching `86-VERIFICATION.md` lines 188-191: `_Verified: <timestamp>_`, `_Verifier: Claude (gsd-verifier)_`, `_Build: dotnet build FuzzyClock.slnx → 0 Warnings, 0 Errors_`, `_Tests: NNN / NNN App + NNN / NNN Core = NNN / NNN baseline preserved_`. The PERF-01 verdict, load-level percentage, window count, monitor refresh rate, sign-off, and absorbed-UAT-items checklist all come from the Task 2 resume signal — populate them verbatim from what the human observer reported. Do NOT fabricate any of these fields; if Task 2 returned a `barely-stepping` or `clearly-stepping` verdict, record it honestly and call out the consequence in the Gaps Summary.
  </action>
  <verify>
    <automated>grep -E "phase:\s*87-verification-performance-acceptance" .planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md && grep -E "Load level reached:" .planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md && grep -E "Monitor refresh rate:" .planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md && grep -E "Smoothness verdict:" .planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md && grep -E "Passed:\s*[0-9]+,\s*Failed:\s*0" .planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md && grep -E "TEST-01|TEST-02|TEST-03|TEST-04|PERF-01" .planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md`
    - Source: file starts with YAML frontmatter containing `phase: 87-verification-performance-acceptance`, `verified:` (ISO timestamp), `status:` (one of `human_needed` or `verified`), `score:` (`N/N must-haves verified` form), `overrides_applied: 0`, and a `human_verification:` list with at least 12 items absorbed from Phases 85 and 86 per D-CARRY-03
    - Source: body contains the nine sections in order — `## Goal Achievement`, `## Observable Truths`, `## Required Artifacts`, `## Key Link Verification`, `## Behavioral Spot-Checks`, `## Requirements Coverage`, `## Anti-Patterns Found`, `## Human Verification Required`, `## Gaps Summary`
    - Source: `## Behavioral Spot-Checks` section contains the literal strings `Load level reached:`, `Monitor refresh rate:`, `Smoothness verdict:`, and a captured `dotnet test` line of the form `Passed: NNN, Failed: 0, Skipped: 0`
    - Source: `## Behavioral Spot-Checks` section contains an explicit sign-off line attributable to the human observer (free-form but distinguishable as a sign-off, e.g. `Sign-off: Alex T. — 2026-05-20`)
    - Source: `## Requirements Coverage` table lists all five Phase 87 requirements (`TEST-01`, `TEST-02`, `TEST-03`, `TEST-04`, `PERF-01`) with status and evidence pointers
    - Source: `## Gaps Summary` calls out the explicitly deferred Phase 86 WR-01 / WR-02 / WR-03 items per D-CARRY-02
    - Source: footer contains the four `_..._` italic lines mirroring `86-VERIFICATION.md` (Verified / Verifier / Build / Tests)
    - PERF-01 verdict, load percentage, window count, monitor refresh rate, sign-off, and absorbed-UAT-items checklist match what the human observer reported in the Task 2 resume signal — none are fabricated
  </acceptance_criteria>
  <done>`87-VERIFICATION.md` exists with PERF-01 attestation captured per D-PERF-02, TEST-04 `dotnet test` evidence recorded, all 12 absorbed UAT items present, Phase 86 WR-01..WR-03 deferrals called out in Gaps Summary, and the footer reflects the build/test status accurately.</done>
</task>

</tasks>

<verification>
After all three tasks land:
1. `dotnet build FuzzyClock.sln --nologo --verbosity quiet` exits 0 with 0 Warnings, 0 Errors.
2. `dotnet test FuzzyClock.sln --nologo --verbosity quiet` exits 0 with `Passed: NNN, Failed: 0, Skipped: 0` where `NNN >= 578 + new rows from 87-01`.
3. `grep -n "_settingsWindow?.IsVisible != true" FuzzyClock.App/MainWindow.xaml.cs` returns at least one match (proves the WR-04 inverse-polarity guard is in place).
4. `grep -n "_currentRatio = 0.0" FuzzyClock.App/MainWindow.xaml.cs` returns matches in both the existing initialization-time path AND the new disable-branch path.
5. `87-VERIFICATION.md` exists with the YAML frontmatter and nine body sections in fixed order; PERF-01 attestation block contains load-level / refresh-rate / verdict / sign-off lines verbatim from the Task 2 resume signal; TEST-04 `dotnet test` evidence line is recorded.
</verification>

<success_criteria>
- D-CARRY-01: `OnGhostEnabledChanged(false)` clears `_currentRatio` / `_targetRatio` and restores `this.Opacity = _windowOpacity` unless settings-window-pinned; toggle-off mid-fade no longer strands the widget half-transparent.
- D-CARRY-02: Phase 86 WR-01 / WR-02 / WR-03 explicitly called out as deferred in `87-VERIFICATION.md` § Gaps Summary — not fixed in Phase 87, captured for v4.5 backlog.
- D-CARRY-03: 12 absorbed UAT items from Phases 85 (5) and 86 (7) appear in `87-VERIFICATION.md` `human_verification:` block; observable items ticked off, non-observable items noted as open.
- D-PERF-01: PowerShell `while ($true) {}` load methodology used; window count documented.
- D-PERF-02: attestation captures load level reached, monitor refresh rate, smoothness verdict, sign-off line — matches the analog `86-VERIFICATION.md` shape.
- PERF-01: human observer has graded the fade traversal under sustained 25–50% CPU load.
- TEST-04 (re-asserted): `dotnet test FuzzyClock.sln --nologo --verbosity quiet` reports `Passed >= 578 + new rows from 87-01, Failed: 0, Skipped: 0` after the WR-04 fix.
</success_criteria>

<output>
Create `.planning/phases/87-verification-performance-acceptance/87-02-SUMMARY.md` when done — capture: the WR-04 fix diff (six lines, location), the PERF-01 verdict + load level + window count + monitor refresh rate, the final `dotnet test` Passed/Failed/Skipped numbers, the absorbed-UAT-items checklist outcome (observed vs. open), and any deviations from the D-PERF-02 attestation format.
</output>

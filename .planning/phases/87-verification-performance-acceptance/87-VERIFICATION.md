---
phase: 87-verification-performance-acceptance
verified: 2026-05-21T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
requirements_verified: [TEST-01, TEST-02, TEST-03, TEST-04, PERF-01]
tests_run: 589
tests_passed: 589
tests_failed: 0
human_verification:
  # 5 items absorbed from Phase 85 per D-CARRY-03 (lifted verbatim from 85-VERIFICATION.md frontmatter)
  - test: "Hover cursor over widget for >33 ms then move away — confirm widget does not become click-through after cursor leaves"
    expected: "Widget remains interactive (grabbable) when cursor leaves; no observable lag where window enters WS_EX_TRANSPARENT after cursor has already retreated"
    why_human: "CR-01 identifies a timing-dependent stale-_isGhostMode window between sampler-tick decision and async Activate(). Behavior depends on real dispatcher backpressure under load — cannot be verified by grep or single-thread tests. This is the question the prompt explicitly asks about: does the threading model deliver SAMP/SEM in practice."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward"
  - test: "Toggle ghost mode off via tray, then perform cursor movement over the widget for 30+ seconds, then re-enable"
    expected: "Zero visible activity while disabled (no opacity flicker, no click-through). On re-enable, normal ghost behavior resumes."
    why_human: "SEM-05 / PROX-09 disable-gate verification needs runtime observation that no events are raised, no opacity manipulation occurs. Code shows OnSampleTick / OnSampleThreadTick early-bail on !IsEnabled, but full UI silence requires human confirmation."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward"
  - test: "Slow cursor retreat from inside widget to outside (multiple ticks at 0 < ratio < 1.0)"
    expected: "BackdropBorder.Background visibly resets when cursor fully exits proximity zone (ratio reaches 0.0)"
    why_human: "WR-05 (advisory) flags a pre-existing latent bug where Restored may not fire on slow retreat. This is a known carryover from v4.0; Phase 85 preserved byte-for-byte semantics. Human run confirms whether the production-realistic case still works as users expect."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward"
  - test: "Close application via tray Exit while cursor is over widget"
    expected: "Clean process termination within 1 second; no exception dialogs; no late event raises against torn-down window"
    why_human: "D-03 + D-09 close the teardown race in theory; WR-02 (advisory) flags that already-queued BeginInvoke lambdas can survive Dispose. Real shutdown sequencing under WPF requires runtime observation."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward"
  - test: "Run application under sustained 25-50% CPU load (background CPU stressor) and confirm cursor proximity ghost activation/restore still feels responsive"
    expected: "Ghost activation triggers within ~33 ms of cursor reaching widget edge; restore on retreat feels prompt; widget UI does not freeze"
    why_human: "SAMP-01's whole purpose is to keep sampling responsive under UI-thread contention. Whether the off-thread sampler delivers measurable improvement vs the old DispatcherTimer is observation-only. Phase 87 owns the full perf acceptance test (PERF-01); this is a sanity check that the architectural change does what it was meant to."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward — the PERF-01 run focused on the fade-traversal smoothness verdict, not on per-tick activation/restore latency feel"
  # 7 items absorbed from Phase 86 per D-CARRY-03 (lifted verbatim from 86-VERIFICATION.md frontmatter)
  - test: "Hover cursor toward widget — confirm visible fade is smooth at display refresh rate (no stepping/jank tied to 33 ms sampling cadence)"
    expected: "Fade traversal looks smoothly continuous at 60 Hz / 144 Hz; visibly different from pre-Phase-86 stepwise behavior; activation crisp at proximity = 1.0; restore crisp at proximity = 0.0"
    why_human: "FADE-01 visible smoothness is observation-only — grep can confirm CompositionTarget.Rendering subscription and per-frame LerpRatio call, but only a human eye on the running widget can confirm the user-perceptible smoothness improvement. Phase 87 owns formal PERF-01 acceptance under sustained 25-50% CPU load."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward — see PERF-01 caveat below: occasional freeze-during-fade observed but not characterised against the 60/144 Hz comparison this item asks for"
  - test: "Drag the widget by the title region during an active fade — confirm Opacity freezes mid-fade until drag releases, then resumes smoothly"
    expected: "Visible Opacity does NOT advance while _isDragging guard is active; on drag release, opacity catches up to the lerped current state on the next frame with no perceived jump"
    why_human: "SEM-04 guard chain preservation — code shows the three guards (_isDragging, _settingsWindow?.IsVisible, _menuOpen) inside OnRenderingTick at lines 312-314 in the original byte-for-byte order, but real-time interaction behavior under WPF dispatcher load needs human observation to confirm parity with v4.3 behavior."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward"
  - test: "Open Settings window during an active fade — confirm widget Opacity freezes while Settings is open, resumes after Settings closes"
    expected: "Opacity write short-circuits while _settingsWindow?.IsVisible == true; lerp continues underneath so visible state catches up the moment Settings closes"
    why_human: "SEM-04 guard chain — runtime observation of settings-window-open freeze. Visual confirmation that the widget pin behavior matches pre-Phase-86 expectations."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward"
  - test: "Right-click the widget to open the tray ContextMenuStrip during an active fade — confirm Opacity pins until the menu closes (RMB-04)"
    expected: "Visible Opacity stays at the value at menu-open; resumes smoothly after menu closes"
    why_human: "SEM-04 RMB-04 guard preservation — _menuOpen guard now lives in OnRenderingTick rather than the ProximityChanged lambda; user-observable behavior must match exactly."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward"
  - test: "Scroll the mouse wheel over the widget during an active fade — confirm wheel-driven Opacity step takes effect immediately and the next frame multiplies through the new _windowOpacity"
    expected: "10% step lands instantly; subsequent fade traversal correctly multiplies through the updated _windowOpacity (no contention or fight between SetOpacity / Window_PreviewMouseWheel direct writes and the per-frame pump)"
    why_human: "SEM-04 D-15 mouse-wheel direct-write contract — code shows Window_PreviewMouseWheel at line 1636 still writes `this.Opacity = _windowOpacity;` directly, and SetOpacity at line 1492 multiplies through `_currentRatio`, but the natural convergence between direct writes and the next render frame is observation-only."
    status: open
    rationale: "not observed during 87 PERF-01 load run; carry forward"
  - test: "Toggle ghost mode OFF via tray while cursor is in proximity halo (mid-fade with _currentRatio in 0..1) — confirm widget recovers to full _windowOpacity opacity"
    expected: "Widget becomes fully opaque on disable, no residual fade trapped at e.g. 0.5"
    why_human: "WR-04 (advisory) flags an interaction-state-stranding edge case where toggling ghost OFF mid-fade leaves this.Opacity at the lerped value with no recovery driver. The OnGhostEnabledChanged(false) branch detaches the pump but does not reset _currentRatio or write Opacity = _windowOpacity. Pre-Phase-86 had the same shape (sampler stopped emitting; last-written Opacity was sticky), so this may not be a Phase 86 regression — but the new design adds long-lived _currentRatio state that carries the residual through re-enable. Human confirmation needed to assess severity vs. fix priority."
    status: open
    rationale: "pending manual verification — the WR-04 patch landed at commit 3dec4bf and is structurally identical to the D-CARRY-01 spec, but the human observer did not exercise this scenario during the PERF-01 load run (\"can't test this situation, move on\"). Carry forward through future UAT."
  - test: "Sustained CPU load (Phase 87 PERF-01 sanity precursor) — run application with a 25-50% CPU stressor and confirm fade is still visibly smooth across the full traversal"
    expected: "Fade subjectively ≥30 fps under load; no stepping/jank visible to the eye"
    why_human: "Phase 87 owns the formal PERF-01 acceptance, but a sanity check that the new architecture delivers its design intent (visible smoothness under load) is appropriate at Phase 86 verification."
    status: open
    rationale: "PARTIALLY observed during 87 PERF-01 load run — verdict was `barely-stepping` (mostly smooth, occasional freeze during fade then resumes smoothly). Item remains open because the underlying freeze cause was not investigated this phase; carry forward as a smoothness-blemish observation gate for the next pass."
---

# Phase 87: Verification & performance acceptance — Verification Report

**Phase Goal:** Lock in the new threading + rendering model from Phases 85 and 86 by (1) extending automated test coverage of the pure-static seams (`LerpRatio`, `OnSampleTick`), (2) fixing Phase 86 review item WR-04 (mid-fade toggle-off stranding) inside scope, and (3) producing PERF-01 evidence via a manual smoothness check under sustained 25–50% CPU load. The 12 carry-forward `human_verification:` items absorbed from Phases 85 and 86 are tracked in the frontmatter above.

**Verified:** 2026-05-21T00:00:00Z
**Status:** passed (PERF-01 verdict `barely-stepping` — phase passes with the freeze-during-fade caveat documented in Behavioral Spot-Checks; no PERF-01 fail, no regression of the 578-test baseline)
**Re-verification:** No — initial verification

---

## Goal Achievement

The phase goal decomposes into five Phase 87 requirements (TEST-01..04 + PERF-01) plus the in-scope WR-04 fix from Phase 86 review. All are observably present:

1. **TEST-01** (existing pure-static tests pass unchanged) — full Core suite reports 449 / 449 passed; full App suite reports 140 / 140 passed; no regression of the 578-test baseline carried forward from Phase 86.
2. **TEST-02** (`LerpRatio` unit-test coverage) — `FuzzyClock.App.Tests/LerpRatioTests.cs` lands with 7 rows: 6 `[DataRow]` rows on `LerpRatio_TerminalStateSnap` covering the D-LERP-01 terminal-state-snap invariant (target=1.0 across currents 1.0/0.5/0.0; target=0.0 across currents 0.0/0.5/1.0); plus 1 `MidRangeTarget_DoesNotSnap` row asserting target=0.5 does NOT snap.
3. **TEST-03** (`OnSampleTick` tickable seam coverage) — `FuzzyClock.App.Tests/OnSampleTickTests.cs` lands with 4 `[DataRow]` rows on `OnSampleTick_TransitionClasses_ReturnsExpected` covering the four-class invariant (None / Activate / RestoreNoEvent / RestoreWithEvent), driven from direct test-side `controller._isGhostMode = isGhostModePre;` setup writes per D-SEAM-02.
4. **TEST-04** (full MSTest suite green) — `dotnet test FuzzyClock.sln --nologo --verbosity quiet` reports `Passed: 589, Failed: 0, Skipped: 0` (449 Core + 140 App), exceeding the ≥578 baseline by +11 (7 new LerpRatio rows + 4 new OnSampleTick rows).
5. **PERF-01** (manual smoothness check under sustained 25–50% CPU load) — human-observed run produced `barely-stepping` verdict at sustained 25–50% load on an 85 Hz primary monitor. Mostly smooth fade traversal with an occasional freeze mid-fade that resumes smoothly afterwards. Per the acceptance criterion in 87-02-PLAN.md `<resume-signal>`, `barely-stepping` proceeds with caveats noted; PERF-01 does NOT fail.

In addition, the **WR-04 fix** from Phase 86 review (D-CARRY-01) lands at commit `3dec4bf`: a six-line patch inside `MainWindow.OnGhostEnabledChanged(false)` clears `_currentRatio` and `_targetRatio` to 0.0 and writes `this.Opacity = _windowOpacity` when not pinned by the settings window. Unit-level structural verification matches the D-CARRY-01 spec verbatim; end-to-end UAT (toggle ghost mode OFF mid-fade and visually confirm full-opacity recovery) was deferred by the human observer during the PERF-01 run.

---

## Observable Truths

| #   | Truth                                                                                                                                                                                                                                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **WR-04 fix lands inside `OnGhostEnabledChanged(false)`:** clears `_currentRatio` and `_targetRatio` to 0.0 and writes `this.Opacity = _windowOpacity` when `_settingsWindow?.IsVisible != true` (D-CARRY-01)                                                                                                                                                          | ✓ VERIFIED | `git show 3dec4bf` — 5 lines added inside the existing `else` branch of `OnGhostEnabledChanged` in `FuzzyClock.App/MainWindow.xaml.cs`. Inverse-polarity guard `_settingsWindow?.IsVisible != true` preserves the settings-window pin contract. Existing `if (enabled)` attach branch byte-for-byte unchanged.                                |
| 2   | **WR-04 fix preserves the settings-window pin contract:** when `_settingsWindow?.IsVisible == true`, `OnGhostEnabledChanged(false)` clears the lerp state but does NOT overwrite `this.Opacity` (D-CARRY-01 inverse polarity)                                                                                                                                          | ✓ VERIFIED | Inverse-polarity check `if (_settingsWindow?.IsVisible != true) this.Opacity = _windowOpacity;` at the new patch site. Settings-window pin guard convention matches the established pattern at `MainWindow.xaml.cs:1489` (`SetOpacity` settings-window check) and `:313` (`OnRenderingTick` settings-window short-circuit guard).         |
| 3   | **Disabling ghost mode mid-fade no longer leaves the widget half-transparent** (TEST-04 re-asserted as a behavior gate under WR-04 — full unit suite stays green after the production change)                                                                                                                                                                          | ⚠ PARTIAL  | Unit-level: `dotnet test FuzzyClock.sln --nologo --verbosity quiet` → `Passed: 589, Failed: 0, Skipped: 0`. End-to-end UAT (toggle-off mid-fade and visually confirm recovery): pending manual verification — the human observer did not exercise this scenario during the PERF-01 run. Carry forward through future UAT.                       |
| 4   | **PERF-01 manual run reaches 25–50% sustained CPU load** via PowerShell `while ($true) {}` windows and produces a written attestation (D-PERF-01, D-PERF-02)                                                                                                                                                                                                          | ✓ VERIFIED | Human attestation captured in `## Behavioral Spot-Checks` below: load level reached 25–50% sustained band via multiple PowerShell `while($true){}` loops; primary monitor 85 Hz; verdict `barely-stepping`; Sign-off: Alex Tabisz — 2026-05-21.                                                                                                |
| 5   | **87-VERIFICATION.md captures load level reached, monitor refresh rate, smoothness verdict, and an explicit sign-off line** per D-PERF-02                                                                                                                                                                                                                              | ✓ VERIFIED | See `## Behavioral Spot-Checks` PERF-01 attestation block — contains the literal lines `Load level reached:`, `Monitor refresh rate:`, `Smoothness verdict:`, and `Sign-off:`. Matches the `86-VERIFICATION.md` analog `human_verification:` shape adapted to the D-PERF-02 attestation requirements.                                          |
| 6   | **87-VERIFICATION.md absorbs the 12 carry-forward `human_verification:` items** from Phase 85 (5 items) and Phase 86 (7 items) per D-CARRY-03                                                                                                                                                                                                                          | ✓ VERIFIED | Frontmatter `human_verification:` block contains 12 items — 5 lifted verbatim from `85-VERIFICATION.md` frontmatter and 7 lifted verbatim from `86-VERIFICATION.md` frontmatter. Each item carries `status: open` with rationale "not observed during 87 PERF-01 load run; carry forward" — the human observer did not enumerate which items were ticked off during the load run, so the conservative default holds. |
| 7   | **Full MSTest suite (`dotnet test FuzzyClock.sln --nologo --verbosity quiet`) is green at milestone end** and the result is recorded (TEST-04 evidence)                                                                                                                                                                                                                | ✓ VERIFIED | See `## Behavioral Spot-Checks` TEST-04 row: `Passed: 589, Failed: 0, Skipped: 0` (449 Core + 140 App). Baseline of 578 preserved; +11 new (7 LerpRatio + 4 OnSampleTick) added by Plan 87-01. WR-04 fix in Plan 87-02 did not regress any prior row.                                                                                          |

**Score:** 7 / 7 must-haves verified (with one ⚠ PARTIAL on Truth 3 — unit-level passes; end-to-end UAT pending per the Gaps Summary).

---

## Required Artifacts

| Artifact                                                                                              | Expected                                                                                                                            | Status     | Details                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `FuzzyClock.App/MainWindow.xaml.cs` — `OnGhostEnabledChanged(false)` WR-04 patch                       | Disable branch clears `_currentRatio` / `_targetRatio` to 0.0 and writes `this.Opacity = _windowOpacity` unless settings-pinned     | ✓ VERIFIED | `git show 3dec4bf -- FuzzyClock.App/MainWindow.xaml.cs` shows the five-line addition. Patch contains literal substrings `_currentRatio = 0.0`, `_targetRatio`, `_settingsWindow?.IsVisible != true`, and `this.Opacity = _windowOpacity` inside the disable branch. |
| `FuzzyClock.App.Tests/LerpRatioTests.cs` (Phase 87 Plan 01)                                           | 7 rows covering D-LERP-01 terminal-state-snap invariant                                                                              | ✓ VERIFIED | Plan 87-01 commit `358bb8c`. 6 `[DataRow]` rows on `LerpRatio_TerminalStateSnap` + 1 `MidRangeTarget_DoesNotSnap` row. All passing in the 589-test suite.                                                                                                                  |
| `FuzzyClock.App.Tests/OnSampleTickTests.cs` (Phase 87 Plan 01)                                        | 4 rows covering D-SEAM-01 four-class transition invariant                                                                            | ✓ VERIFIED | Plan 87-01 commit `d90d303`. 4 `[DataRow]` rows on `OnSampleTick_TransitionClasses_ReturnsExpected` (None / Activate / RestoreNoEvent / RestoreWithEvent). All passing.                                                                                                  |
| `FuzzyClock.App/GhostModeController.cs:70` visibility relaxation (Phase 87 Plan 01)                   | `private volatile bool _isGhostMode` → `internal volatile bool _isGhostMode` (D-SEAM-02b)                                            | ✓ VERIFIED | Plan 87-01 commit `c8d5cea`. One-keyword change; `volatile` modifier preserved; `public bool IsActive => _isGhostMode;` getter byte-for-byte unchanged. Phase 85 D-06 single-owner production-side write rule unaffected.                                                |
| `FuzzyClock.App.Tests/MSTestSettings.cs` `[assembly: DiscoverInternals]` (Phase 87 Plan 01)           | Enables MSTest discovery of internal test class with internal-typed parametric parameter                                            | ✓ VERIFIED | Plan 87-01 commit `d90d303`. One-line attribute add. Required because `OnSampleTickTests` consumes `GhostModeController.GhostTransition` (nested enum inside `internal sealed class`) as a parameter type.                                                                |
| `.planning/phases/87-verification-performance-acceptance/87-VERIFICATION.md`                          | Phase 87 verification report with PERF-01 attestation and TEST-04 evidence (this file)                                              | ✓ VERIFIED | This file. Contains literal substrings `Load level reached:`, `Monitor refresh rate:`, `Smoothness verdict:`, and `Passed: 589, Failed: 0, Skipped: 0` per the 87-02-PLAN `must_haves.artifacts.contains` and `key_links.pattern` requirements.                       |

---

## Key Link Verification

| From                                                                       | To                                                                              | Via                                                                       | Status      | Details                                                                                                                                                                                                                                                                                  |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `FuzzyClock.App/MainWindow.xaml.cs::OnGhostEnabledChanged(false)`           | `FuzzyClock.App/MainWindow.xaml.cs::_settingsWindow`                            | null-safe `IsVisible` check before writing `this.Opacity`                  | ✓ WIRED     | Patch line `if (_settingsWindow?.IsVisible != true) this.Opacity = _windowOpacity;` — null-safe `?.` operator handles the not-yet-opened settings-window case; `!= true` polarity is the inverse of the settings-window-pin convention used at `:1489` / `:313`.                                  |
| `87-VERIFICATION.md`                                                       | PERF-01 evidence (manual smoothness verdict)                                    | human-recorded attestation block in `## Behavioral Spot-Checks` section    | ✓ WIRED     | Pattern `Smoothness verdict:` present in `## Behavioral Spot-Checks` PERF-01 attestation block — verdict value is `barely-stepping` per the human observer's resume signal.                                                                                                                       |
| `87-VERIFICATION.md`                                                       | TEST-04 evidence (`dotnet test` Passed line)                                    | captured `Passed: 589, Failed: 0, Skipped: 0` line in Behavioral Spot-Checks | ✓ WIRED     | Pattern `Passed:\s*\d+,\s*Failed:\s*0` matches the captured line `Passed: 589, Failed: 0, Skipped: 0` in `## Behavioral Spot-Checks` below.                                                                                                                                                       |
| `FuzzyClock.App.Tests`                                                     | `GhostModeController._isGhostMode` (internal field)                             | D-SEAM-02b visibility relaxation + Phase 83 InternalsVisibleTo plumbing      | ✓ WIRED     | `OnSampleTickTests` performs direct-write `controller._isGhostMode = isGhostModePre;` for RestoreNoEvent / RestoreWithEvent setup rows. `InternalsVisibleTo("FuzzyClock.App.Tests")` at `FuzzyClock.App.csproj:7-11` exposes the now-`internal` field; production-side single-owner rule preserved. |
| `FuzzyClock.App.Tests::OnSampleTickTests`                                  | MSTest 4.x runner discovery                                                     | `[assembly: DiscoverInternals]`                                            | ✓ WIRED     | Internal test class with parametric method consuming nested-internal-enum parameter type is discovered correctly; all 4 rows execute and pass.                                                                                                                                                    |

---

## Behavioral Spot-Checks

| Behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Command / Evidence                                                                                              | Result                                                                | Status                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ----------------------- |
| Solution compiles cleanly after WR-04 fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `dotnet build FuzzyClock.sln --nologo --verbosity quiet`                                                        | Build succeeded; 0 Errors (32 pre-existing MSTEST0037 warnings unrelated, out of scope per Plan 87-01 SUMMARY) | ✓ PASS                  |
| Full MSTest suite passes (TEST-04 evidence)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `dotnet test FuzzyClock.sln --nologo --verbosity quiet`                                                          | **Passed: 589, Failed: 0, Skipped: 0** (449 Core + 140 App)            | ✓ PASS                  |
| 578-test baseline preserved across the WR-04 production-code change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | (449 + 140) − 11 new Plan 87-01 rows = 578                                                                       | Baseline preserved; +11 new (7 LerpRatio + 4 OnSampleTick) per Plan 87-01 | ✓ PASS                  |
| WR-04 fix structural shape matches D-CARRY-01 spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `git show 3dec4bf -- FuzzyClock.App/MainWindow.xaml.cs`                                                          | Five lines added in the existing `else` branch: clear `_currentRatio` to 0.0; clear `_targetRatio` to 0.0; inverse-polarity guard `if (_settingsWindow?.IsVisible != true) this.Opacity = _windowOpacity;` | ✓ PASS                  |
| WR-04 (toggle-off mid-fade recovers full opacity) — end-to-end UAT                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | manual run during PERF-01                                                                                        | **WR-04 (toggle-off mid-fade recovers full opacity): pending manual verification — patch landed at commit 3dec4bf, end-to-end UAT deferred to future pass** | ⚠ PENDING               |
| **PERF-01 attestation (D-PERF-02)**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | manual run via PowerShell `while ($true) {}` load + Task Manager observation + visual smoothness grading        | See PERF-01 block below                                                | ✓ PASS (with caveat)    |

### PERF-01 attestation (D-PERF-02)

```
Load level reached: 25–50% sustained band (multiple PowerShell `while($true){}` loops running per D-PERF-01; precise percentage not measured against Task Manager — observer reported "somewhere in the 25–50% sustained band")
Monitor refresh rate: 85 Hz (primary monitor)
Window count: not specified (multiple PowerShell `while($true){}` loops; window count not enumerated by the observer)
Smoothness verdict: barely-stepping — mostly smooth, occasional freeze during fade then resumes (noted caveat)
Sign-off: Alex Tabisz — 2026-05-21
```

**Caveat (recorded inline, not blocking — 87-02-PLAN.md `<resume-signal>` permits `barely-stepping` to proceed with caveats noted):** the human observer reports the fade traversal is mostly smooth at 85 Hz under sustained 25–50% CPU load, but occasionally freezes briefly during a fade transition then resumes smoothly. The freeze cause was not investigated this phase. This is a smoothness blemish on top of the PERF-01 acceptance gate, not a failure of it. Carry forward as a smoothness-observation gate for the next pass — the underlying cause may be Phase 86 WR-01 (stale `_previousRenderTime` baseline first-post-convergence — first lerp step ~78% of remaining gap due to clamp at 0.1 s), which is explicitly deferred per D-CARRY-02 and tracked in `## Gaps Summary` below. PERF-01 itself is graded `barely-stepping` and the milestone proceeds.

---

## Requirements Coverage

| Requirement | Source Plan | Description (from REQUIREMENTS.md)                                                                                                                                                                | Status         | Evidence                                                                                                                                                                                                                                                                                                              |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TEST-01     | 87-01       | Existing pure-static unit tests on `ComputeProximityRatio` (12 cases) and `IsModifierHeld` (configurable-override coverage) keep passing without modification                                        | ✓ SATISFIED    | `dotnet test` reports 449 Core + 140 App = 589 passing; the 578-test baseline (449 + 129) carried forward from Phase 86 is preserved byte-for-byte; no prior row regressed. See `## Behavioral Spot-Checks`.                                                                                                              |
| TEST-02     | 87-01       | New per-frame lerp logic is extracted as a pure static method (`LerpRatio`) with unit tests covering convergence toward target, terminal-state snap (`1.0` and `0.0`), and step-size bounds          | ✓ SATISFIED    | `FuzzyClock.App.Tests/LerpRatioTests.cs` — 6 `[DataRow]` rows on `LerpRatio_TerminalStateSnap` (D-LERP-01 terminal-snap invariant) + 1 `MidRangeTarget_DoesNotSnap` row. Convergence shape and step-size bounds intentionally skipped per D-LERP-01 over-spec exclusion (see Plan 87-01 SUMMARY).                            |
| TEST-03     | 87-01       | The sampling loop's pure-logic core is reachable from tests without spinning up real timers or threads — via a tickable seam (internal `OnSampleTick` method that takes injected cursor/rect/key-state) | ✓ SATISFIED    | `FuzzyClock.App.Tests/OnSampleTickTests.cs` — 4 `[DataRow]` rows on `OnSampleTick_TransitionClasses_ReturnsExpected` covering the four-class invariant (None / Activate / RestoreNoEvent / RestoreWithEvent). Direct `controller._isGhostMode = isGhostModePre;` setup writes via D-SEAM-02b visibility relaxation.       |
| TEST-04     | 87-01 + 87-02 | Full MSTest suite (`FuzzyClock.Core.Tests` + `FuzzyClock.App.Tests`) is green at milestone end with no regressions in the existing 574 tests                                                          | ✓ SATISFIED    | `dotnet test FuzzyClock.sln --nologo --verbosity quiet` → `Passed: 589, Failed: 0, Skipped: 0`. ≥574-row baseline exceeded by 15 (574 → 578 at Phase 86 end → 589 at Phase 87 end). WR-04 fix in 87-02 did not regress any prior row.                                                                                       |
| PERF-01     | 87-02       | Under sustained 25–50% CPU load, ghost-fade is visibly smooth (no stepping/jank) at 30+ fps for the full fade traversal — verified by manual run with a CPU-load generator and human visual confirmation | ⚠ PASSED (CAVEAT) | Human-observed run at sustained 25–50% CPU load on an 85 Hz primary monitor; verdict `barely-stepping` (mostly smooth, occasional freeze during fade then resumes smoothly); Sign-off Alex Tabisz — 2026-05-21. Per 87-02-PLAN `<resume-signal>`, `barely-stepping` proceeds with caveats — PERF-01 does NOT fail. See PERF-01 caveat in `## Behavioral Spot-Checks`. |

All five Phase 87 requirements covered. PERF-01 carries an inline caveat (occasional freeze-during-fade) that does not block the milestone but is logged in `## Gaps Summary` for follow-up.

---

## Anti-Patterns Found

Scanned `FuzzyClock.App/MainWindow.xaml.cs` (the only production-code file modified this phase, via the WR-04 fix at commit `3dec4bf`) and the Plan 87-01 changes (`GhostModeController.cs` line 70 visibility relaxation; new test files; `MSTestSettings.cs` attribute add) for stub/debt/empty-implementation patterns.

| File   | Line | Pattern | Severity | Impact                                                                                                                                                                                                                                |
| ------ | ---- | ------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none) | —    | —       | —        | No `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` markers introduced by Phase 87. The WR-04 fix is a five-line patch composed entirely of field assignments and a guarded write — no stubs, no empty bodies, no placeholder values. The Plan 87-01 visibility relaxation is a one-keyword change. |

The Phase 86 advisory WARNINGs (WR-01 stale `_previousRenderTime` baseline; WR-02 stale RMB-04 comment; WR-03 asymmetric event-unsubscribe in `Closed`) are NOT anti-patterns introduced this phase — they are explicitly deferred per D-CARRY-02 and tracked in `## Gaps Summary` below.

---

## Human Verification Required

See the `human_verification:` block in the frontmatter for the 12 carry-forward items absorbed from Phases 85 and 86 per D-CARRY-03. The PERF-01 manual run focused on the smoothness verdict under sustained 25–50% CPU load and did not enumerate which absorbed items were ticked off in passing. Per the `state_at_handoff` directive in this executor's prompt, all 12 items remain tagged `status: open` with rationale "not observed during 87 PERF-01 load run; carry forward" — the conservative default. Items split:

- **5 from Phase 85** (lifted verbatim from `85-VERIFICATION.md` frontmatter): click-through timing on cursor reversal (CR-01); disable-gate full silence (SEM-05); slow-retreat `Restored` event (WR-05 advisory); clean shutdown via tray Exit; smoothness under load (PERF-01 sanity precursor — partially observed during the 87 PERF-01 run as `barely-stepping`).
- **7 from Phase 86** (lifted verbatim from `86-VERIFICATION.md` frontmatter): visible smoothness at display refresh rate (FADE-01 design intent); drag-during-fade freeze (SEM-04 guard 1); settings-window-during-fade pin (SEM-04 guard 2); RMB-04 menu pin during fade (SEM-04 guard 3); mouse-wheel direct-write during fade (SEM-04 D-15); toggle-off mid-fade recovery (WR-04 — patched at commit `3dec4bf` but end-to-end UAT pending); PERF-01 sanity precursor (partially observed during the 87 PERF-01 run).

These items remain open because the human observer did not enumerate which were observed during the load run. They carry forward to the next UAT pass, alongside the WR-04 end-to-end check that the observer explicitly deferred.

---

## Gaps Summary

Phase 87 goal achievement holds: all 7 must-haves verified (one ⚠ PARTIAL on the WR-04 end-to-end UAT), all 5 phase requirements satisfied (one ⚠ CAVEAT on PERF-01), full 589-test suite green, build clean, WR-04 fix lands as a five-line patch matching the D-CARRY-01 spec, PERF-01 attestation captured per D-PERF-02. The following gaps surface explicitly for downstream tooling (`/gsd-progress`, `/gsd-audit-uat`, the next milestone's planning):

1. **12 absorbed UAT items still open (D-CARRY-03 carry-forward).** All 12 carry-forward `human_verification:` items from Phases 85 (5 items) and 86 (7 items) remain tagged `status: open` in the frontmatter `human_verification:` block. The PERF-01 load run produced a smoothness verdict but did not enumerate which absorbed items were ticked off in passing. Carry forward to the next UAT pass.

2. **WR-04 end-to-end manual verification pending.** The WR-04 fix landed at commit `3dec4bf` and is structurally identical to the D-CARRY-01 spec verbatim. Unit-level: full 589-test suite stays green after the production change. End-to-end (toggle ghost mode OFF mid-fade and visually confirm full-opacity recovery): the human observer explicitly deferred this scenario during the PERF-01 run ("can't test this situation, move on"). Carry forward as a single-shot UAT for the next pass — five-line patch correctness depends on the inverse-polarity guard, which is reviewed at the structural level only.

3. **PERF-01 caveat: occasional freeze during fade observed; root cause not investigated this phase** (`status: noted`). Verdict `barely-stepping` permits the milestone to proceed per 87-02-PLAN `<resume-signal>`. The freeze cause is most likely Phase 86 WR-01 (stale `_previousRenderTime` baseline first-post-convergence — first lerp step ~78% of remaining gap due to clamp at 0.1 s). WR-01 is explicitly deferred per D-CARRY-02; this PERF-01 caveat is the first user-observed signal that the WR-01 deferral may be visible in practice. Promote WR-01 to a v4.5 fix candidate.

4. **Phase 86 WR-01 / WR-02 / WR-03 explicitly deferred per D-CARRY-02.** Not fixed in Phase 87. Captured for the v4.5 backlog or a future bugfix phase:
   - **WR-01** (`MainWindow.xaml.cs:288-300`): `_previousRenderTime` not updated on convergence early-return path → first post-convergence frame measures multi-second delta clamped to 100 ms (~78% lerp jump on first re-engagement after long idle). May be the underlying cause of the PERF-01 caveat above.
   - **WR-02** (`MainWindow.xaml.cs:225-230`): stale RMB-04 comment still references the (removed) `ProximityChanged` lambda's `_menuOpen` guard; the guard now lives in `OnRenderingTick`. One-line doc update.
   - **WR-03** (`MainWindow.xaml.cs:183-199, 238-249`): asymmetric `Closed` cleanup — explicit `CompositionTarget.Rendering -=` but no `_ghostMode.EnabledChanged -=` / `Restored -=`. Pre-existing pattern but breaks symmetry now that the render pump is involved. Not load-bearing because `_ghostMode.Dispose()` makes the controller unreachable.

The phase has no automated-detectable gaps. The four items above are observation/deferred-decision artifacts, not verification failures.

---

_Verified: 2026-05-21T00:00:00Z_
_Verifier: Claude (gsd-executor) — Phase 87 Plan 02 continuation agent_
_Build: dotnet build FuzzyClock.sln → 0 Errors (32 pre-existing MSTEST0037 warnings unrelated, out of scope per Plan 87-01)_
_Tests: 140 / 140 App + 449 / 449 Core = 589 / 589 (578 baseline + 11 new from Plan 87-01)_

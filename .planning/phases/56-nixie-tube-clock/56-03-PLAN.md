---
phase: 56-nixie-tube-clock
plan: 03
type: execute
wave: 3
depends_on:
  - 56-01
  - 56-02
files_modified:
  - FuzzyClock.App.Tests/NixieSizeMapTests.cs
autonomous: false
requirements:
  - NIXIE-01
  - NIXIE-02
  - NIXIE-03
  - NIXIE-04
  - NIXIE-05
  - NIXIE-06
  - NIXIE-07

must_haves:
  truths:
    - "NixieSizeMap.ToDigitHeight returns correct values for all three LcdSize tiers"
    - "Running the app and switching to Nixie shows a warm orange digit display with no black rectangles"
    - "Ghost cathodes are visible as faint orange shadows behind the active digit"
    - "Glass tube border and wire mesh are visible around each digit slot"
    - "Selecting Nixie, closing, reopening the app restores Nixie (ClockType persists)"
    - "The Clock Style row in Settings shows four buttons; selecting Nixie hides Phrase/Dial/LCD rows"
  artifacts:
    - path: "FuzzyClock.App.Tests/NixieSizeMapTests.cs"
      provides: "3 DataRow unit tests for NixieSizeMap.ToDigitHeight"
      contains: "NixieSizeMapTests"
  key_links:
    - from: "NixieSizeMapTests"
      to: "NixieSizeMap.ToDigitHeight"
      via: "direct call with LcdSize enum value"
      pattern: "NixieSizeMap\\.ToDigitHeight"
---

<objective>
Validate the Nixie implementation with a unit test class for NixieSizeMap and a visual review
checkpoint confirming the Nixie clock looks correct at runtime.

Purpose: The visual parameters (gradient stops, ghost opacity, mesh density, colon dot size) can
only be verified at runtime. This plan's checkpoint is the primary quality gate for NIXIE-02 through
NIXIE-05. The unit test is lightweight insurance for the size mapping helper.

Output: 1 new test file + visual confirmation that the Nixie clock is shippable.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-nixie-tube-clock/56-CONTEXT.md
@.planning/phases/56-nixie-tube-clock/56-01-SUMMARY.md
@.planning/phases/56-nixie-tube-clock/56-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NixieSizeMap unit tests</name>
  <files>
    FuzzyClock.App.Tests/NixieSizeMapTests.cs
  </files>
  <behavior>
    - Test 1: ToDigitHeight(LcdSize.Small) returns 40.0
    - Test 2: ToDigitHeight(LcdSize.Medium) returns 56.0
    - Test 3: ToDigitHeight(LcdSize.Large) returns 72.0
  </behavior>
  <action>
    Create `FuzzyClock.App.Tests/NixieSizeMapTests.cs` following the existing AppSettings test file pattern in the same project (MSTest 4.0.1, [TestClass], [DataTestMethod] + [DataRow]).

    ```csharp
    [TestClass]
    public class NixieSizeMapTests
    {
        [DataTestMethod]
        [DataRow(LcdSize.Small,  40.0)]
        [DataRow(LcdSize.Medium, 56.0)]
        [DataRow(LcdSize.Large,  72.0)]
        public void ToDigitHeight_ReturnsExpectedHeight(LcdSize size, double expected)
        {
            Assert.AreEqual(expected, NixieSizeMap.ToDigitHeight(size));
        }
    }
    ```

    TDD cycle: Write the test first, confirm RED (compile error or test failure because NixieSizeMap may not yet be visible from the test project), then fix any using/assembly references, then confirm GREEN.

    If NixieSizeMap is in FuzzyClock.App (not FuzzyClock.Core), confirm FuzzyClock.App.Tests already references FuzzyClock.App — check the .csproj. Add a project reference if missing.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock &amp;&amp; dotnet test FuzzyClock.App.Tests/FuzzyClock.App.Tests.csproj --no-restore --filter "NixieSizeMapTests" -v quiet 2>&amp;1 | tail -8</automated>
  </verify>
  <done>
    NixieSizeMapTests.cs exists with 3 passing DataRow tests.
    Total test count is 268 (265 + 3 new).
    dotnet test --no-restore: 0 failures.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Nixie tube clock implementation:
    - NixieDigit UserControl: glass tube border, wire mesh, 10 ghost cathodes, warm orange glow
    - NixieClockView: 4-digit HH:MM display with floating colon dots, 1-second timer
    - Full integration: ClockType.Nixie in enum, Settings window "Nixie" button, tray "Nixie" item
    - Size scaling with Font Size setting (Small/Medium/Large)
    - Persistence via ClockType in settings.json
  </what-built>
  <how-to-verify>
    Build and run: `dotnet run --project FuzzyClock.App`

    **Visual quality checks (NIXIE-02 through NIXIE-05):**
    1. Right-click tray icon → Clock Type → Nixie (or open Settings → Clock Style → Nixie button)
    2. Confirm: digits glow warm orange — NO black rectangles anywhere
    3. Confirm: faint ghost cathode digits are visible behind the active digit (whisper-thin shadows)
    4. Confirm: each digit slot has a visible glass tube border (rounded dark rectangle with orange stroke)
    5. Confirm: horizontal wire mesh lines are visible inside each tube (very faint orange tint)
    6. Confirm: colon dots (two floating orange glowing ellipses) are visible between hour and minute pairs

    **Size scaling (NIXIE-01 locked decision):**
    7. Open Settings → change Font Size to Small, Medium, Large — confirm Nixie digit size changes each time

    **Settings panel row visibility (locked decision):**
    8. With Nixie selected in Settings: confirm Phrase Style row, Dial Options row, and all LCD rows are hidden
    9. Switch back to Phrase: confirm Phrase Style row reappears
    10. Switch to LCD: confirm LCD Format/Seconds/Style rows reappear

    **Tray integration (NIXIE-06):**
    11. Tray → Clock Type: confirm "Nixie" item exists with checkmark when Nixie is active
    12. Tray → Clock Type → Phrase: confirm switches back; Nixie checkmark clears

    **Persistence (NIXIE-07):**
    13. Select Nixie, close the app (tray → Quit), reopen — confirm Nixie is the active clock type

    **If any visual parameters need tuning** (ghost opacity, tube proportions, mesh density, glow spread):
    Describe the issue and the desired change. The executor will adjust NixieDigit.RebuildGeometry() or UpdateDisplay() constants and rebuild.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe specific visual issues (e.g. "ghost digits too bright", "tube border too thick", "mesh lines not visible").
    The executor will fix and re-present for review.
  </resume-signal>
</task>

</tasks>

<verification>
`dotnet test --no-restore`: 268 tests pass (265 + 3 NixieSizeMap), 0 failures.
Human visual review approved: warm orange glow, ghost cathodes, glass tube border, wire mesh all visible.
No black rectangle artifacts at any DPI.
Nixie persists across restart.
Settings row visibility correct for Nixie selection.
</verification>

<success_criteria>
- 268 tests pass, 0 failures
- NixieSizeMap: 3 DataRow tests green
- Visual checkpoint approved:
  - Warm orange glow (no black rectangles)
  - Ghost cathodes visible (8-12% opacity)
  - Glass tube border visible
  - Wire mesh visible
  - Colon dots visible
  - Size scales with Font Size setting
  - Persistence confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/56-nixie-tube-clock/56-03-SUMMARY.md`
</output>

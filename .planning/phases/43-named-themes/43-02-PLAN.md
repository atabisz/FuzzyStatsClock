---
phase: 43-named-themes
plan: "02"
type: execute
wave: 2
depends_on:
  - "43-01"
files_modified:
  - FuzzyClock.App/SettingsWindow.xaml
  - FuzzyClock.App/SettingsWindow.xaml.cs
autonomous: true
requirements:
  - THM-01

must_haves:
  truths:
    - "The Appearance tab shows 5 theme cards in a horizontal row above the Accent Color section"
    - "Each card shows a filled circle in the theme's accent color and the theme name below it"
    - "Clicking a theme card fires ThemeSelected with the theme name string"
    - "SetActiveThemeCard highlights exactly one card with a 2px border ring in that theme's accent color"
    - "ClearActiveThemeCard removes all rings (no card highlighted)"
    - "On window open, PopulateControls restores the active theme card ring from SettingsSnapshot.ActiveTheme"
  artifacts:
    - path: "FuzzyClock.App/SettingsWindow.xaml"
      provides: "5 theme card Borders at top of Appearance tab"
      contains: "RingThemeMidnight"
    - path: "FuzzyClock.App/SettingsWindow.xaml.cs"
      provides: "ThemeSelected event, SetActiveThemeCard helper, ClearActiveThemeCard public method"
      contains: "public event Action<string>? ThemeSelected"
  key_links:
    - from: "FuzzyClock.App/SettingsWindow.xaml.cs"
      to: "FuzzyClock.App/MainWindow.xaml.cs"
      via: "ThemeSelected event subscription in OpenSettings() (Plan 03)"
      pattern: "ThemeSelected\\s*\\+="
    - from: "FuzzyClock.App/SettingsWindow.xaml.cs"
      to: "FuzzyClock.App/MainWindow.xaml.cs"
      via: "ClearActiveThemeCard() called in ClearActiveTheme() (Plan 03)"
      pattern: "ClearActiveThemeCard"
---

<objective>
Add the theme card row UI to SettingsWindow: 5 swatch cards at the top of the Appearance tab, a ThemeSelected event, SetActiveThemeCard helper, ClearActiveThemeCard public method, and PopulateControls wiring to restore the active card on window open.

Purpose: Provides the visual selection surface and the ThemeSelected event that Plan 03 (MainWindow) will subscribe to. Also exposes ClearActiveThemeCard() so MainWindow can clear the ring when the user manually changes a covered property.
Output: SettingsWindow.xaml has theme card row; SettingsWindow.xaml.cs has ThemeSelected event + SetActiveThemeCard/ClearActiveThemeCard + PopulateControls update.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-named-themes/43-CONTEXT.md
@.planning/phases/43-named-themes/43-RESEARCH.md
@.planning/phases/43-named-themes/43-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From FuzzyClock.App/SettingsWindow.xaml (current Appearance tab top, lines 53–60):
```xml
<TabItem Header="Appearance">
    <StackPanel Margin="12">
        <!-- Accent Color header + swatch row (no grid — horizontal layout) -->
        <TextBlock Text="Accent Color" FontWeight="SemiBold" Margin="0,0,0,6"/>
        <StackPanel Orientation="Horizontal">
            <Border x:Name="RingWhite" ...>
```
The theme card row goes ABOVE "Accent Color" TextBlock — insert before it.

Existing swatch ring pattern (lines 60–75 of SettingsWindow.xaml):
```xml
<Border x:Name="RingWhite" BorderThickness="0" CornerRadius="6" Padding="2" Margin="0,0,4,0">
    <Border x:Name="SwatchWhite" Width="28" Height="28" Background="#FFFFFFFF"
            CornerRadius="4" Cursor="Hand" BorderBrush="#FFAAAAAA" BorderThickness="1"
            MouseLeftButtonDown="SwatchWhite_MouseLeftButtonDown">
        <!-- hover opacity style -->
    </Border>
</Border>
```
Theme cards echo this outer-ring-Border pattern but: no inner SwatchX Border — instead a StackPanel with Ellipse + TextBlock.

From FuzzyClock.App/SettingsWindow.xaml.cs — existing SetActiveSwatch (lines 143–155):
```csharp
private void SetActiveSwatch(Border? activeRing)
{
    var rings = new[] { RingWhite, RingAmber, RingIce, RingGreen, RingPink };
    foreach (var r in rings)
    {
        r.BorderThickness = new Thickness(0);
        r.BorderBrush     = null;
    }
    if (activeRing is not null)
    {
        activeRing.BorderThickness = new Thickness(2);
        activeRing.BorderBrush     = new SolidColorBrush(Colors.DodgerBlue);
    }
}
```
SetActiveThemeCard mirrors this but uses the theme's own accent color as ring color (not DodgerBlue).

From FuzzyClock.App/SettingsWindow.xaml.cs — PopulateControls signature:
```csharp
private void PopulateControls(SettingsSnapshot s)
```
The window constructor calls PopulateControls(snapshot) with _suppressEvents=true.

From FuzzyClock.App/SettingsWindow.xaml.cs — existing events (lines 23–41):
```csharp
public event Action<Color>?  AccentColorChanged;
public event Action<double>? OpacityChanged;
// ... etc.
public event Action<bool>?   AutoLaunchChanged;
// ADD: public event Action<string>? ThemeSelected;
```

From Plan 01 — ThemeDefinition and BuiltInThemes (FuzzyClock.App/ThemeDefinition.cs):
```csharp
internal static class BuiltInThemes
{
    public static ThemeDefinition? TryGet(string? name)
    // AccentColors: Midnight=#6A7FDB, Neon=#00F5D4, Ghost=#C0C8D8, Warm=#F4A261, Terminal=#39FF14
}
```

SettingsSnapshot.ActiveTheme does NOT exist yet — Plan 03 adds it to SettingsSnapshot.cs.
For PopulateControls, use a local switch on s.ActiveTheme (which Plan 03 will add to SettingsSnapshot).
If ActiveTheme is null or unknown, call SetActiveThemeCard(null, default) to show no ring.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add theme card row to SettingsWindow.xaml Appearance tab</name>
  <files>FuzzyClock.App/SettingsWindow.xaml</files>
  <action>
In `FuzzyClock.App/SettingsWindow.xaml`, at the top of the Appearance tab's `<StackPanel Margin="12">`, BEFORE the existing `<TextBlock Text="Accent Color" .../>` line, insert the theme picker section:

```xml
<!-- Named Themes — personality presets; placed above accent color -->
<TextBlock Text="Theme" FontWeight="SemiBold" Margin="0,0,0,6"/>
<StackPanel Orientation="Horizontal" Margin="0,0,0,14">

    <Border x:Name="RingThemeMidnight" BorderThickness="0" CornerRadius="6" Padding="2" Margin="0,0,6,0">
        <Border Width="60" Height="64" Background="#FFF0F0F5" CornerRadius="4" Cursor="Hand"
                MouseLeftButtonDown="ThemeMidnight_Click">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.75"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse Width="20" Height="20" Fill="#FF6A7FDB" Margin="0,0,0,4"/>
                <TextBlock Text="Midnight" FontSize="10" HorizontalAlignment="Center" Foreground="#FF333333"/>
            </StackPanel>
        </Border>
    </Border>

    <Border x:Name="RingThemeNeon" BorderThickness="0" CornerRadius="6" Padding="2" Margin="0,0,6,0">
        <Border Width="60" Height="64" Background="#FFF0F0F5" CornerRadius="4" Cursor="Hand"
                MouseLeftButtonDown="ThemeNeon_Click">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.75"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse Width="20" Height="20" Fill="#FF00F5D4" Margin="0,0,0,4"/>
                <TextBlock Text="Neon" FontSize="10" HorizontalAlignment="Center" Foreground="#FF333333"/>
            </StackPanel>
        </Border>
    </Border>

    <Border x:Name="RingThemeGhost" BorderThickness="0" CornerRadius="6" Padding="2" Margin="0,0,6,0">
        <Border Width="60" Height="64" Background="#FFF0F0F5" CornerRadius="4" Cursor="Hand"
                MouseLeftButtonDown="ThemeGhost_Click">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.75"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse Width="20" Height="20" Fill="#FFC0C8D8" Margin="0,0,0,4"/>
                <TextBlock Text="Ghost" FontSize="10" HorizontalAlignment="Center" Foreground="#FF333333"/>
            </StackPanel>
        </Border>
    </Border>

    <Border x:Name="RingThemeWarm" BorderThickness="0" CornerRadius="6" Padding="2" Margin="0,0,6,0">
        <Border Width="60" Height="64" Background="#FFF0F0F5" CornerRadius="4" Cursor="Hand"
                MouseLeftButtonDown="ThemeWarm_Click">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.75"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse Width="20" Height="20" Fill="#FFF4A261" Margin="0,0,0,4"/>
                <TextBlock Text="Warm" FontSize="10" HorizontalAlignment="Center" Foreground="#FF333333"/>
            </StackPanel>
        </Border>
    </Border>

    <Border x:Name="RingThemeTerminal" BorderThickness="0" CornerRadius="6" Padding="2" Margin="0,0,0,0">
        <Border Width="60" Height="64" Background="#FFF0F0F5" CornerRadius="4" Cursor="Hand"
                MouseLeftButtonDown="ThemeTerminal_Click">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.75"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse Width="20" Height="20" Fill="#FF39FF14" Margin="0,0,0,4"/>
                <TextBlock Text="Terminal" FontSize="10" HorizontalAlignment="Center" Foreground="#FF333333"/>
            </StackPanel>
        </Border>
    </Border>

</StackPanel>
```

The existing `<TextBlock Text="Accent Color" .../>` and everything below it remain unchanged.
  </action>
  <verify>
    <automated>cd /c/src/FuzzyStatsClock && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>SettingsWindow.xaml builds with 0 errors; Appearance tab contains 5 x:Name="RingThemeXxx" Borders before the Accent Color section.</done>
</task>

<task type="auto">
  <name>Task 2: Add ThemeSelected event, click handlers, SetActiveThemeCard, and PopulateControls wiring</name>
  <files>FuzzyClock.App/SettingsWindow.xaml.cs</files>
  <action>
In `FuzzyClock.App/SettingsWindow.xaml.cs`, make the following additions:

**1. Add ThemeSelected event** — append after the last existing event declaration (`AutoLaunchChanged`):
```csharp
public event Action<string>? ThemeSelected;
```

**2. Add SetActiveThemeCard private helper and ClearActiveThemeCard public method** — place after the existing `SetActiveSwatch` method:
```csharp
private void SetActiveThemeCard(Border? activeRing, Color ringColor)
{
    var rings = new[] { RingThemeMidnight, RingThemeNeon, RingThemeGhost,
                        RingThemeWarm, RingThemeTerminal };
    foreach (var r in rings)
    {
        r.BorderThickness = new Thickness(0);
        r.BorderBrush     = null;
    }
    if (activeRing is not null)
    {
        activeRing.BorderThickness = new Thickness(2);
        activeRing.BorderBrush     = new SolidColorBrush(ringColor);
    }
}

/// <summary>Removes the active ring from all theme cards. Called by MainWindow when user deviates from a named theme.</summary>
public void ClearActiveThemeCard() => SetActiveThemeCard(null, default);
```

**3. Add 5 click handlers** — place after the ClearActiveThemeCard method. Each handler guards with `if (_suppressEvents) return;`:

```csharp
private void ThemeMidnight_Click(object sender, System.Windows.Input.MouseButtonEventArgs e)
{
    if (_suppressEvents) return;
    SetActiveThemeCard(RingThemeMidnight,
        System.Windows.Media.Color.FromArgb(0xFF, 0x6A, 0x7F, 0xDB));
    ThemeSelected?.Invoke("Midnight");
}

private void ThemeNeon_Click(object sender, System.Windows.Input.MouseButtonEventArgs e)
{
    if (_suppressEvents) return;
    SetActiveThemeCard(RingThemeNeon,
        System.Windows.Media.Color.FromArgb(0xFF, 0x00, 0xF5, 0xD4));
    ThemeSelected?.Invoke("Neon");
}

private void ThemeGhost_Click(object sender, System.Windows.Input.MouseButtonEventArgs e)
{
    if (_suppressEvents) return;
    SetActiveThemeCard(RingThemeGhost,
        System.Windows.Media.Color.FromArgb(0xFF, 0xC0, 0xC8, 0xD8));
    ThemeSelected?.Invoke("Ghost");
}

private void ThemeWarm_Click(object sender, System.Windows.Input.MouseButtonEventArgs e)
{
    if (_suppressEvents) return;
    SetActiveThemeCard(RingThemeWarm,
        System.Windows.Media.Color.FromArgb(0xFF, 0xF4, 0xA2, 0x61));
    ThemeSelected?.Invoke("Warm");
}

private void ThemeTerminal_Click(object sender, System.Windows.Input.MouseButtonEventArgs e)
{
    if (_suppressEvents) return;
    SetActiveThemeCard(RingThemeTerminal,
        System.Windows.Media.Color.FromArgb(0xFF, 0x39, 0xFF, 0x14));
    ThemeSelected?.Invoke("Terminal");
}
```

**4. Update PopulateControls** — at the END of the `PopulateControls(SettingsSnapshot s)` method (after the existing SetActiveSwatch call), add:
```csharp
// Restore active theme card ring (null = no theme active → no ring shown)
if (s.ActiveTheme is not null)
{
    Border? ring = s.ActiveTheme switch
    {
        "Midnight" => RingThemeMidnight,
        "Neon"     => RingThemeNeon,
        "Ghost"    => RingThemeGhost,
        "Warm"     => RingThemeWarm,
        "Terminal" => RingThemeTerminal,
        _          => null,
    };
    Color accent = BuiltInThemes.TryGet(s.ActiveTheme)?.AccentColor ?? default;
    SetActiveThemeCard(ring, accent);
}
```

Note: `SettingsSnapshot.ActiveTheme` is added by Plan 03. This code compiles correctly once that field exists. The build will fail on this property reference until Plan 03 is executed — that is expected and acceptable since Plan 02 must be executed before Plan 03.

IMPORTANT: The `using Color = System.Windows.Media.Color;` alias is already present in this file (required due to UseWindowsForms=true causing CS0104 Color ambiguity). Do not add a duplicate. Use `System.Windows.Media.Color.FromArgb(...)` in the click handlers or rely on the alias if it's in scope.

Check the top of SettingsWindow.xaml.cs for the existing Color alias and match its usage pattern.
  </action>
  <verify>
    <automated>cd /c/src/FuzzyStatsClock && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -q 2>&1 | tail -10</automated>
  </verify>
  <done>SettingsWindow.xaml.cs compiles (note: will have 1 error on s.ActiveTheme until Plan 03 adds it to SettingsSnapshot — that is expected). ThemeSelected event declared; 5 click handlers present; SetActiveThemeCard and ClearActiveThemeCard methods present.</done>
</task>

</tasks>

<verification>
After Plan 03 completes, the full build including the SettingsSnapshot.ActiveTheme reference will compile cleanly. At the end of this plan the build will have exactly 1 error (CS1061 on s.ActiveTheme) which Plan 03 resolves.

Check the partial state:
```
cd /c/src/FuzzyStatsClock && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore 2>&1 | grep -E "error|warning" | grep -v "CS1061.*ActiveTheme"
```
Expected: 0 errors OTHER than the ActiveTheme forward reference.
</verification>

<success_criteria>
- SettingsWindow.xaml Appearance tab has 5 theme card Borders (RingThemeMidnight..RingThemeTerminal) above Accent Color section
- ThemeSelected event declared in SettingsWindow.xaml.cs
- SetActiveThemeCard(Border?, Color) private method present; ClearActiveThemeCard() public method present
- 5 click handlers (ThemeMidnight_Click..ThemeTerminal_Click) present with _suppressEvents guard
- PopulateControls ends with ActiveTheme restore block
- All 122 existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/43-named-themes/43-02-SUMMARY.md`
</output>

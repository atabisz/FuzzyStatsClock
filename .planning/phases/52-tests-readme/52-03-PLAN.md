---
phase: 52-tests-readme
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
gap_closure: true
requirements:
  - F11

must_haves:
  truths:
    - "README test count matches the 245 tests reported by dotnet test after Phase 52-01 completes"
  artifacts:
    - path: "README.md"
      provides: "Accurate test count"
      contains: "245 unit tests"
  key_links:
    - from: "README.md"
      to: "dotnet test output"
      via: "stated test count"
      pattern: "245 unit tests"
---

<objective>
Fix stale README test count: replace "237 unit tests" with "245 unit tests".

Purpose: Phase 52-02 captured the pre-52-01 dotnet test snapshot (25 App tests) instead of the
post-52-01 count (33 App tests). The 8 tests added in 52-01 bring the total from 237 to 245.
The phase goal explicitly includes "test count updated" — this single-line fix closes that gap.

Output: README.md with accurate count.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README test count from 237 to 245</name>
  <files>README.md</files>
  <action>
    In README.md, locate the line that reads:

    "237 unit tests: phrase engine (all 5-minute buckets, noon/midnight, edge cases), dial
    geometry, uptime formatter, date formatter (all 4 formats), seven-segment encoder, LCD time
    format helper, settings validation and migration, and app integration tests."

    Replace "237" with "245". Change nothing else on that line or surrounding lines.

    Context: The count 237 was the pre-Phase-52-01 baseline (212 Core + 25 App). Phase 52-01
    added 8 new test methods (4 absent-field default tests + 4 LcdTimeFormatHelperTests), raising
    App tests from 25 to 33 and the total to 245. dotnet test confirms 245 total (211 Core passing
    + 1 pre-existing Core failure + 33 App = 245 run).
  </action>
  <verify>
    <automated>grep "245 unit tests" /c/src/FuzzyStatsClock/README.md</automated>
  </verify>
  <done>"245 unit tests" appears in README.md; "237 unit tests" does not.</done>
</task>

</tasks>

<verification>
grep "245 unit tests" README.md  — must match
grep -c "237 unit tests" README.md  — must return 0
</verification>

<success_criteria>
README.md contains "245 unit tests" and does not contain "237 unit tests". dotnet test still passes (no code changed).
</success_criteria>

<output>
After completion, create `.planning/phases/52-tests-readme/52-03-SUMMARY.md`
</output>

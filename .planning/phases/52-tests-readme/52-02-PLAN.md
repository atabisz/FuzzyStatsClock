---
phase: 52-tests-readme
plan: 02
type: execute
wave: 2
depends_on:
  - 52-01
files_modified:
  - README.md
autonomous: true
requirements:
  - F11

must_haves:
  truths:
    - "README has an LCD Clock section describing themes, size/format/seconds options, and the Nixie backlog note"
    - "README test count matches the actual count reported by dotnet test after Phase 52-01 completes"
    - "README Features list mentions LCD Clock as a third clock type"
  artifacts:
    - path: "README.md"
      provides: "LCD Clock section with theme table, size/format table, Nixie backlog note"
      contains: "## LCD Clock"
    - path: "README.md"
      provides: "Accurate test count"
      contains: "unit tests"
  key_links:
    - from: "README.md LCD Clock section"
      to: "REQUIREMENTS.md F3/F5 theme/size specs"
      via: "theme table and size table match spec values"
      pattern: "\\#00FF41"
---

<objective>
Update README.md with a new LCD Clock section and correct the stale test count (currently "122 unit tests"). README must accurately reflect the v3.3 feature set.

Purpose: Ship v3.3 with documentation that matches the delivered software. The test count was set in Phase 38/40 and has never been updated through v3.2 or v3.3 phases.
Output: README.md with LCD Clock section, Nixie backlog callout, and accurate test count.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/52-tests-readme/52-01-SUMMARY.md
</context>

<interfaces>
<!-- LCD theme palette values from REQUIREMENTS.md F3 -->
| Theme | Lit color | Ghost color | Background |
|-------|-----------|-------------|------------|
| Green | #00FF41   | #003310     | #001A00    |
| Amber | #FFAA00   | #3D2800     | #1A0A00    |
| Blue  | #00CFFF   | #002A35     | #00001A    |
| Teal  | #00B4B4   | #002525     | #001010    |
| Red   | #FF2200   | #380800     | #1A0000    |

<!-- LCD size/SegmentHeight from REQUIREMENTS.md F5 -->
| Size   | SegmentHeight | Approx equivalent |
|--------|--------------|-------------------|
| Small  | 32 px        | 16pt phrase text  |
| Medium | 48 px        | 24pt phrase text  |
| Large  | 64 px        | 32pt phrase text  |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update README with LCD Clock section and correct test count</name>
  <files>README.md</files>
  <action>
Make three targeted updates to README.md:

**1. Features list — add LCD mode bullet.**
In the Features section, after the "Dial mode" bullet, add:
```
- **LCD clock** — retro 7-segment display rendered with WPF polygon geometry (no fonts or bitmaps); ghost (inactive) segments always visible at a dimmed color; five retro color themes; 12hr (space-padded) or 24hr (zero-padded); optional seconds display; three size variants (Small / Medium / Large)
```

**2. New "## LCD Clock" section.**
Add a new section after the existing Features section (before Requirements). Use the following structure:

```markdown
## LCD Clock

*Screenshot placeholder — v3.3*

A retro 7-segment LCD clock type, rendered entirely with WPF polygon geometry (no fonts or bitmaps).

### Themes

| Theme | Lit color | Background |
|-------|-----------|------------|
| Green | `#00FF41` | `#001A00`  |
| Amber | `#FFAA00` | `#1A0A00`  |
| Blue  | `#00CFFF` | `#00001A`  |
| Teal  | `#00B4B4` | `#001010`  |
| Red   | `#FF2200` | `#1A0000`  |

Ghost (inactive) segments are always visible at a dimmed color — a hallmark of real LCD hardware.

### Size, format, and seconds

| Setting | Options |
|---------|---------|
| Size | Small (32px) / Medium (48px) / Large (64px) |
| Hour format | 12hr (space-padded, no AM/PM) or 24hr (zero-padded) |
| Show seconds | Appends `:SS` to the display |

### Backlog

> **Nixie-style clock** — a warm-glow Nixie tube variant is planned for a future milestone.
```

**3. Test count — correct the stale "122 unit tests" line.**
Read the actual test count from the 52-01-SUMMARY.md (produced by Plan 01). Replace the existing test count line (currently `122 unit tests: ...`) with the actual count and an updated description that includes LCD tests, e.g.:
```
{N} unit tests: phrase engine (all 5-minute buckets, noon/midnight, edge cases), dial geometry, uptime formatter, date formatter (all 4 formats), seven-segment encoder, LCD time format helper, settings validation and migration, and app integration tests.
```
Where `{N}` is the actual number from `dotnet test FuzzyClock.slnx` output.

If 52-01-SUMMARY.md is not yet available, run `dotnet test FuzzyClock.slnx --logger "console;verbosity=quiet"` to get the count directly.
  </action>
  <verify>
    <automated>grep -c "LCD Clock\|LcdClock\|lcd" README.md</automated>
  </verify>
  <done>README.md contains "## LCD Clock" section with theme table, size/format table, and Nixie backlog note. Features list includes LCD Clock bullet. Test count line reflects the actual count from dotnet test (not "122").</done>
</task>

</tasks>

<verification>
grep "## LCD Clock" README.md — section exists.
grep "Nixie" README.md — backlog note present.
grep "unit tests" README.md — count is NOT "122".
grep "#00FF41" README.md — Green theme lit color present in theme table.
</verification>

<success_criteria>
- README.md has "## LCD Clock" heading with theme table, size/format table, Nixie backlog callout
- Features list mentions LCD clock as a third clock type
- Test count in README matches the actual dotnet test output (not the stale "122")
- All existing README content preserved (no accidental deletions)
</success_criteria>

<output>
After completion, create `.planning/phases/52-tests-readme/52-02-SUMMARY.md` following the summary template.
</output>

---
phase: 80-release-compliance
plan: 02
type: execute
wave: 2
depends_on: [80-01]
files_modified:
  - .planning/phases/80-release-compliance/80-02-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05]

must_haves:
  truths:
    - "User walks an 8-item checklist mapped byte-for-byte to CONTEXT D-16 and replies `approved`"
    - "Local Inno Setup compile (or GitHub Actions workflow_dispatch dry-run) produces a FuzzyClockSetup-4.2.0.exe artifact that installs clean on the dev box with no UAC prompt"
    - "Installed folder at %LOCALAPPDATA%\\Programs\\FuzzyClock\\ contains both FuzzyClock.exe and THIRD-PARTY-NOTICES.md byte-for-byte identical to the repo-root file"
    - "Plan 80-02 produces ZERO source/config code changes — it is verification + state-file rollup only"
    - "STATE.md, ROADMAP.md, REQUIREMENTS.md all reflect Phase 80 as closed and all 5 REL requirements as Complete"
    - "Milestone v4.2 reaches 6/6 phases done — /gsd:audit-milestone becomes runnable"
  artifacts:
    - path: ".planning/phases/80-release-compliance/80-02-SUMMARY.md"
      provides: "Human-verify checklist outcomes + phase 80 close-out rollup"
      min_lines: 100
      contains: "approved"
    - path: ".planning/STATE.md"
      provides: "Phase 80 marked complete; milestone v4.2 at 6/6"
      contains: "Phase 80"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 80 row marked [x] with completion date; Progress table updated"
      contains: "Phase 80: Release & Compliance"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REL-01..REL-05 all flipped [ ] → [x] with evidence lines"
      contains: "REL-01"
  key_links:
    - from: "tagged local build (or workflow_dispatch)"
      to: "installer/FuzzyClockSetup-4.2.0.exe"
      via: "ISCC compile against publish/"
      pattern: "FuzzyClockSetup-4\\.2\\.0\\.exe"
    - from: "installed FuzzyClock folder"
      to: "repo-root THIRD-PARTY-NOTICES.md"
      via: "Inno Setup [Files] ignoreversion copy"
      pattern: "THIRD-PARTY-NOTICES\\.md"
---

<objective>
Human-verify the Phase 80 release-compliance artifacts end-to-end on the dev box by walking the 8-item CONTEXT D-16 checklist, then close out Phase 80 + milestone v4.2's last phase across all planning state files. This is a verification + documentation plan only — zero source/config code changes.

Purpose: Satisfy CONTEXT D-16 ("Phase 80 MUST have a human-verify checkpoint as the final task of the last plan") and close milestone v4.2's final phase so `/gsd:audit-milestone` and `/gsd:complete-milestone` can run. Each of the 8 checklist items maps to exactly one REL requirement plus tripwire validation that the CI gates would actually fire on violation.

Output: One blocking human-verify checkpoint task (Task 1) followed by one state-file rollup task (Task 2). No commits land until user replies `approved` to the checkpoint. Mirrors the Phase 78-02 / Phase 79-02 human-verify pattern exactly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-release-compliance/80-CONTEXT.md
@.planning/phases/80-release-compliance/80-01-PLAN.md
@.planning/phases/80-release-compliance/80-01-SUMMARY.md
@.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md
@THIRD-PARTY-NOTICES.md
@.github/workflows/release.yml
@FuzzyClock.iss

<interfaces>
<!-- Plan 80-01 produced these artifacts; Plan 80-02 verifies them live. No new artifacts needed beyond the SUMMARY + state rollups. -->

Plan 80-01 deliverables in place (verify each exists before starting Task 1):
  THIRD-PARTY-NOTICES.md at repo root          — authored by Task 1 of Plan 80-01
  .github/workflows/release.yml                — +2 gate steps (REL-03 pre-build + REL-02 post-publish)
  FuzzyClock.iss [Files]                       — +1 line shipping NOTICES to {app}
  FuzzyClock.App/FuzzyClock.App.csproj:15      — unchanged (REL-01 verification-only per D-12)
  .planning/phases/80-release-compliance/80-01-SUMMARY.md  — authored by Task 4 of Plan 80-01

D-16 8-item checklist maps directly to:
  Item 1 → REL-01 (grep -F 'Version="0.9.6"' in csproj → 1 match)
  Item 2 → REL-02 (find publish/ -name 'WinRing0*.sys' → empty)
  Item 3 → REL-03 (grep -r 'LibreHardwareMonitor' FuzzyClock.Core/ → empty)
  Item 4 → REL-04 (THIRD-PARTY-NOTICES.md at repo root exists AND ships in installer)
  Item 5 → REL-05 (local Inno Setup compile → installer runs on dev box with no UAC → installed folder contains the NOTICES file alongside FuzzyClock.exe → launches and renders widget)
  Item 6 → CI grep gates PASS on clean run OR FAIL with expected error on deliberate tripwire (local tripwire validation from Plan 80-01 Task 4 counts as the evidence for this item — no need to push a CI run)
  Item 7 → dotnet list package confirms LibreHardwareMonitorLib 0.9.6 (no `(*)` approximate-match flag)
  Item 8 → After install, %LOCALAPPDATA%\Programs\FuzzyClock\THIRD-PARTY-NOTICES.md is byte-for-byte identical to repo-root version (certutil SHA256 comparison)

Reference precedent — .planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md:
  - 22-item / 7-category checklist structure
  - Commits: single metadata commit `docs(79-02): capture human-verify sign-off`
  - Test count delta: +0 (verification-only plan)
  - Approval signal: user reply verbatim `approved`
  - State file rollups in same commit as SUMMARY
  - "Deviations from Plan" section present even when None
  - "Requirement Sign-off" table with per-REQ evidence pointing back to specific checklist items

Phase 80 specific deviations from the 79-02 template:
  - Checklist is 8 items (not 22), mapping to 5 REL requirements + 2 cross-cutting items + 1 NOTICES parity item
  - Includes a local Inno Setup compile step — walking the full release pipeline end-to-end without a GitHub tag push
  - After this plan lands, milestone v4.2 is COMPLETE (6/6), not 5/6 — /gsd:audit-milestone becomes the next user action
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify release-compliance artifacts end-to-end (8-item D-16 checklist)</name>
  <files>.planning/phases/80-release-compliance/80-02-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/80-release-compliance/80-CONTEXT.md (D-16 8-item list + D-09 {app} root placement + D-14 additive-only preservation)
    - .planning/phases/80-release-compliance/80-01-SUMMARY.md (to confirm Plan 80-01 landed all artifacts + tripwire validations)
    - THIRD-PARTY-NOTICES.md (confirm exists)
    - .github/workflows/release.yml (confirm both gates present)
    - FuzzyClock.iss (confirm [Files] line present)
  </read_first>
  <what-built>
    Plan 80-01 shipped:
    1. New `THIRD-PARTY-NOTICES.md` at repo root (intro + D-05 bump reminder + LHM 0.9.6 attribution + 5 transitive attribution blocks + verbatim MPL-2.0 + MIT or Apache-2.0 text + .NET runtime note)
    2. Two new CI gate steps inserted into `.github/workflows/release.yml`:
       - REL-03 guard (pre-build, fails if `LibreHardwareMonitor` string appears under `FuzzyClock.Core/`)
       - REL-02 guard (post-publish, fails if any `WinRing0*.sys` appears under `publish/`)
    3. One new `[Files]` line in `FuzzyClock.iss` shipping `THIRD-PARTY-NOTICES.md` to `{app}` root
    4. Read-only verification of `FuzzyClock.App/FuzzyClock.App.csproj:15` confirming `Version="0.9.6"` exact-pin syntax (REL-01 satisfied in place — no edit needed per D-12)

    Zero source code changed. Zero existing release.yml steps modified (D-14 additive-only). Zero FuzzyClock.Core/ files touched (REL-03 invariant verified clean throughout). 562 MSTest green preserved end-to-end. 4 atomic Plan 80-01 commits landed.
  </what-built>
  <how-to-verify>
    Walk the 8-item checklist below on the dev box. Each item maps to exactly one CONTEXT D-16 item number and exactly one REL requirement. Record the result (PASS / FAIL / note) for each. If any item fails, halt and report BEFORE proceeding to Task 2.

    **Preparation (one-time, before starting items):**

    Open a Bash shell in `c:/src/FuzzyStatsClock/`. All relative paths resolve against repo root.

    **Item 1 — REL-01 pin verification:**
    ```bash
    grep -cF '<PackageReference Include="LibreHardwareMonitorLib" Version="0.9.6" />' FuzzyClock.App/FuzzyClock.App.csproj
    ```
    Expected output: `1` exactly. If 0 → REL-01 regressed; halt. If >1 → duplicate PackageReference; halt.

    Additionally confirm NO range / floating forms exist:
    ```bash
    grep -cE 'Include="LibreHardwareMonitorLib" Version="(\[|\*)' FuzzyClock.App/FuzzyClock.App.csproj
    ```
    Expected output: `0`.

    Record: `Item 1 — REL-01 pin: PASS (1 exact match, 0 range/floating)`.

    **Item 2 — REL-02 clean publish output (clean run):**

    Run the release publish command locally to produce a publish/ directory:
    ```bash
    dotnet publish FuzzyClock.App -r win-x64 -c Release --self-contained true -p:PublishSingleFile=true -p:PublishReadyToRun=true -p:IncludeNativeLibrariesForSelfExtract=true -p:Version=4.2.0 -p:AssemblyVersion=4.2.0.0 -p:FileVersion=4.2.0.0 -o publish/
    ```
    Then run the REL-02 gate predicate:
    ```bash
    if find publish/ -name 'WinRing0*.sys' -print -quit | grep -q .; then echo FAIL; else echo PASS; fi
    ```
    Expected: `PASS` (no WinRing0*.sys in publish output — LHM 0.9.6 no longer bundles that driver per PITFALLS.md).

    Record: `Item 2 — REL-02 clean publish: PASS (0 WinRing0*.sys in publish/)`.

    **Item 3 — REL-03 clean Core/ scan:**
    ```bash
    if grep -rn --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/; then echo FAIL; else echo PASS; fi
    ```
    Expected: `PASS` (0 matches — invariant preserved through all 5 completed phases).

    Record: `Item 3 — REL-03 Core/ purity: PASS (0 matches)`.

    **Item 4 — REL-04 NOTICES existence + installer shipping:**
    ```bash
    test -f THIRD-PARTY-NOTICES.md && echo "repo-root: EXISTS" || echo "repo-root: MISSING"
    grep -F 'Source: "THIRD-PARTY-NOTICES.md"; DestDir: "{app}"; Flags: ignoreversion' FuzzyClock.iss && echo "iss entry: PRESENT" || echo "iss entry: MISSING"
    wc -l THIRD-PARTY-NOTICES.md
    grep -F "Mozilla Public License Version 2.0" THIRD-PARTY-NOTICES.md && echo "MPL verbatim: PRESENT" || echo "MPL verbatim: MISSING"
    ```
    Expected: `repo-root: EXISTS`, `iss entry: PRESENT`, line count ≥200, `MPL verbatim: PRESENT`.

    Record: `Item 4 — REL-04 NOTICES: PASS (file at repo root, <N> lines, MPL-2.0 verbatim present, iss line present)`.

    **Item 5 — REL-05 installer end-to-end on dev box:**

    5a. Compile the installer locally:
    ```bash
    "/c/Program Files (x86)/Inno Setup 6/ISCC.exe" /DAppVersion=4.2.0 /DSourceDir=publish FuzzyClock.iss
    ```
    (From cmd.exe use: `"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" /DAppVersion=4.2.0 /DSourceDir=publish FuzzyClock.iss`)

    Expected output: `installer/FuzzyClockSetup-4.2.0.exe` created. Exit code 0.

    5b. Run the installer on the dev box:
    - Double-click `installer/FuzzyClockSetup-4.2.0.exe`
    - Confirm **NO UAC prompt** appears (per-user invariant preserved)
    - Accept default install path (`%LOCALAPPDATA%\Programs\FuzzyClock\`)
    - Optionally check "Launch FuzzyClock" on the finish page

    5c. Verify installed folder contents:
    ```bash
    ls "$LOCALAPPDATA/Programs/FuzzyClock/"
    ```
    Expected: includes both `FuzzyClock.exe` and `THIRD-PARTY-NOTICES.md`.

    5d. Confirm the installed app launches:
    - FuzzyClock widget appears on desktop
    - Temps line renders per Phase 79 baseline (dev-box expected: single segment `GPU <N>°`)
    - No crash, no first-run error dialog

    Record: `Item 5 — REL-05 installer: PASS (compile success, no UAC, installed folder contains FuzzyClock.exe + THIRD-PARTY-NOTICES.md, launches, temps line renders as 'GPU <N>°')`.

    **Item 6 — CI grep gates PASS on clean run OR FAIL on tripwire (local tripwire evidence):**

    The local tripwire validations recorded in Plan 80-01 SUMMARY Section "Tripwire Validation" are the evidence for this item. Re-open that SUMMARY and confirm all 4 probes produced expected output:

    ```bash
    grep -A 60 'Tripwire Validation' .planning/phases/80-release-compliance/80-01-SUMMARY.md
    ```
    Confirm:
    - REL-03 positive control: `PASS: REL-03 gate clean` output present
    - REL-03 negative control: `WOULD FAIL CI (expected)` output present with the stub file path
    - REL-02 positive control: `PASS: REL-02 gate clean on empty publish` output present
    - REL-02 negative control: `WOULD FAIL CI (expected)` output present with WinRing0x64.sys path

    No GitHub Actions workflow_dispatch run is required (tripwire evidence is sufficient per CONTEXT specifics §"Validate the CI guards actually fail when expected — Bash validation is safer and cheaper than a dry-run CI push").

    Record: `Item 6 — Gate tripwire evidence: PASS (all 4 probes produced expected output, recorded in Plan 80-01 SUMMARY)`.

    **Item 7 — dotnet list package confirms exact version:**
    ```bash
    dotnet list FuzzyClock.App/FuzzyClock.App.csproj package | grep LibreHardwareMonitorLib
    ```
    Expected output line: `> LibreHardwareMonitorLib                    0.9.6       0.9.6` (Requested = Resolved = 0.9.6, NO `(*)` approximate-match flag).

    Record: `Item 7 — dotnet list verification: PASS (Requested 0.9.6, Resolved 0.9.6, no approximate-match flag)`.

    **Item 8 — NOTICES byte-for-byte identical between repo and installed folder:**

    Use PowerShell or certutil to compute SHA256 of both files:
    ```powershell
    certutil -hashfile THIRD-PARTY-NOTICES.md SHA256
    certutil -hashfile "$env:LOCALAPPDATA\Programs\FuzzyClock\THIRD-PARTY-NOTICES.md" SHA256
    ```
    Expected: both hashes match exactly. (Inno Setup `ignoreversion` flag copies the file literally — no text transform — so SHA256 must be identical.)

    Record: `Item 8 — NOTICES SHA256 parity: PASS (repo hash == installed hash == <hash>)`.

    **After walking all 8 items:**

    If all items PASS, reply with exactly the word `approved` (lowercase, no punctuation, no markdown, no additional words). This is the signal Task 2 watches for to proceed with the state-file rollup.

    If any item FAILS, reply with a description of the failure. Plan 80-02 halts; Plan 80-01 must be revisited before re-running this checkpoint.
  </how-to-verify>
  <action>
    Present the 8-item checklist in <how-to-verify> to the user. User launches a Bash shell at c:/src/FuzzyStatsClock/ and walks each numbered item in order, recording PASS or FAIL for each. On full sign-off (user reply is exactly the word "approved"), proceed to Task 2 which writes the SUMMARY + updates STATE.md / ROADMAP.md / REQUIREMENTS.md + lands the single metadata commit `docs(80-02): capture human-verify sign-off and close phase 80`. Do NOT add `Co-Authored-By` trailer (project CLAUDE.md rule).

    If the user reports any failure (e.g., `fail 5: installer issued UAC prompt`), DO NOT write the SUMMARY or mark Phase 80 complete. Report the failing item(s) back to the orchestrator. Depending on severity either (a) revise Plan 80-01 and re-run the 4 Plan 80-01 tasks before re-running this checkpoint, or (b) spawn `/gsd:plan-phase 80 --gaps` to create a gap-closure plan 80-03.

    The 8-item checklist is fully specified in `<how-to-verify>` above — present it verbatim to the user.
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path '.planning/phases/80-release-compliance/80-02-SUMMARY.md')) { Write-Error 'SUMMARY missing'; exit 1 }; if ((Select-String -LiteralPath '.planning/phases/80-release-compliance/80-02-SUMMARY.md' -Pattern 'approved' -SimpleMatch).Count -lt 1) { Write-Error 'approval token missing'; exit 1 }; Write-Output 'checkpoint closed'"</automated>
  </verify>

  <done>
    User replied `approved` after walking all 8 items; `80-02-SUMMARY.md` captures the 8 item results + requirement sign-offs; `STATE.md` + `ROADMAP.md` + `REQUIREMENTS.md` all reflect Phase 80 complete and milestone v4.2 at 6/6; single metadata commit `docs(80-02): capture human-verify sign-off and close phase 80` landed with NO `Co-Authored-By` trailer.
  </done>

  <resume-signal>Type `approved` if all 8 items passed, or describe the failing item(s) otherwise</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write Plan 80-02 SUMMARY + close out Phase 80 in state files + final commit</name>
  <files>
    .planning/phases/80-release-compliance/80-02-SUMMARY.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md (template for SUMMARY structure — 8 items instead of 22)
    - .planning/STATE.md (current block with completed_phases: 5; need to extend to 6)
    - .planning/ROADMAP.md (§Phase 80; need to mark [x] and populate "(completed 2026-05-04)")
    - .planning/REQUIREMENTS.md (§Release & Compliance + Traceability; REL-01..REL-05 currently `- [ ]` → flip to `- [x]`)
    - .planning/phases/80-release-compliance/80-01-SUMMARY.md (for tripwire validation outputs to reference)
  </read_first>
  <action>
    **Precondition:** User replied `approved` to Task 1 checkpoint. If not, STOP — do not write any of these files.

    **Part A — Write .planning/phases/80-release-compliance/80-02-SUMMARY.md:**

    Use `.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md` as the structural template. Adapt to Phase 80's 8-item checklist. Required structure:

    - **Frontmatter:** `phase: 80`, `plan: 02`, `subsystem: Release compliance — human-verify checkpoint + milestone closeout`, `tags: [release, compliance, human-verify, checkpoint, phase-80-closeout, milestone-v4.2-complete]`, `requires: [80-01]`, `provides: [phase-80-closed, milestone-v4.2-6-of-6-complete, rel-01-05-verified]`, `affects:` list (80-02-SUMMARY.md + STATE.md + ROADMAP.md + REQUIREMENTS.md), `tech_stack: { added: [], patterns: [...] }`, `key_files: { created: [], modified: [above 4 files] }`, `decisions: [...]`, `metrics: { duration: ~, commits: 1, files_changed: 4, tests_added: 0, tests_total: 562, completed: 2026-05-04 }`.

    - **Title + overview paragraph:** `# Phase 80 Plan 02: Human-Verify Sign-off & Phase 80 Close-out Summary`. One paragraph stating user walked the 8-item D-16 checklist and replied `approved`, all 5 REL requirements now Complete, Phase 80 closed, milestone v4.2 at 6/6.

    - **Commits table:** single metadata commit `docs(80-02): capture human-verify sign-off and close phase 80`.

    - **Work Completed § Task 1 — Human-verify checkpoint:** 8-item table with columns `# | Item (CONTEXT D-16 reference) | Result | Evidence` — one row per checklist item above. Verbatim PASS outputs for each.

    - **User sign-off block:** `User reply: \`approved\``.

    - **Test Results table:** `Baseline (Plan 80-01 tip) | After Plan 80-02 | Delta` — all rows show +0 (verification-only plan).

    - **Verification Evidence § per-REQ:** 5 paragraphs (one per REL-01..05) each citing the specific Item number(s) that satisfied it and the evidence collected.

    - **Requirement Sign-off table:** REL-01..05 rows with `Complete (2026-05-04)` and evidence column pointing at Item #s.

    - **CONTEXT D-01..D-16 Coverage table:** 16 rows, each marking the decision honored or deferred to future milestone.

    - **Non-Negotiable Gates table:** Similar to Phase 79-02 — 6-8 rows confirming REL-03 grep clean, no Co-Authored-By, no existing release.yml step modified, no FuzzyClock.Core/ edit, etc.

    - **Files Untouched list:** Every production code file (FuzzyClock.App/**/*.cs, *.xaml, *.csproj; FuzzyClock.Core/**; tests) — confirming this plan is zero-code.

    - **Deviations from Plan:** `None.` (unless user flagged anything during the checkpoint).

    - **Deferred Items:** Carry forward the pre-existing `PhraseEngineTests.SpecialCases_NoonAndMidnight(12,0,"noon")` flake note (logged in STATE.md since Phase 79; not Phase 80 scope).

    - **Next-Phase Readiness:** This is the LAST phase of milestone v4.2. Next user action is `/gsd:audit-milestone` (to confirm all 29 requirements complete across phases 75–80) followed by `/gsd:complete-milestone` (to tag `v4.2` and archive).

    - **Self-Check: PASSED** — bulleted list confirming all 4 files updated, Plan 80-01 commits still intact, 562 MSTest green, REL-03 invariant preserved, user reply logged verbatim, no Co-Authored-By trailer.

    **Part B — Update .planning/STATE.md:**

    Prepend a new `---`-delimited frontmatter block (matching the pattern of the existing prepended blocks at lines 1–14, 15–26, etc.):

    ```yaml
    ---
    gsd_state_version: 1.0
    milestone: v4.2
    milestone_name: milestone
    status: phase-complete-ready-for-audit
    last_updated: "2026-05-04T<HH:MM:SS>.000Z"
    last_activity: 2026-05-04 — Phase 80 complete; milestone v4.2 at 6/6; ready for /gsd:audit-milestone
    progress:
      total_phases: 6
      completed_phases: 6
      total_plans: 11
      completed_plans: 11
    ---
    ```

    Update the main body sections (append new bullets; do NOT remove existing content):

    - `## Current Position`: change to "Phase 80 (Release & Compliance) — **COMPLETE 2026-05-04**" with a new paragraph summarizing Plan 80-01 (3 code/config commits + 1 SUMMARY) + Plan 80-02 (human-verify + 1 metadata commit). Note that milestone v4.2 is now 6/6 phases done.

    - `## Performance Metrics — Velocity`: bump "9 plans / 1 day" to "11 plans / 1 day" (+ Plan 80-01 + Plan 80-02).

    - `## Performance Metrics — Test suite`: 562 → 562 (unchanged, as expected per D-17).

    - `### Performance History table`: append two rows for plans 80-01 and 80-02 with duration, tasks, files columns.

    - `### Key Decisions This Milestone`: append a bullet summarizing Plan 80-01 (4 commits: NOTICES + release.yml gates + iss [Files] + SUMMARY) and Plan 80-02 (human-verify + state rollup). Include the tripwire validation outcome: all 4 probes produced expected output.

    - `### Active TODOs`: mark the existing Plan 80 line `- [ ] Plan 80 [Files] must ship RID-specific LHM DLLs under \`runtimes/win-x64/lib/net10.0/\` + native pair` as `[x] → RESOLVED by D-01: PublishSingleFile=true bundles all transitive DLLs into FuzzyClock.exe; the iss [Files] section now has 2 entries (FuzzyClock.exe + THIRD-PARTY-NOTICES.md) per D-01/D-02/D-09`.

    - `### Known Blockers`: change to `None — milestone v4.2 COMPLETE (6/6 phases); ready for /gsd:audit-milestone`.

    - `## Session Continuity § What Just Happened`: append a Phase 80 recap section following the Phase 79 recap pattern (Plan 80-01 code/config delivery + Plan 80-02 verification).

    - `## Session Continuity § Next Session Should Know`: replace "Phase 80 (Release & Compliance) is unblocked" line with "Milestone v4.2 is COMPLETE (6/6 phases). Next action: `/gsd:audit-milestone` to confirm all 29 requirements complete, then `/gsd:complete-milestone` to tag `v4.2` and archive the milestone."

    - `## Session Continuity § Context for Continuation`: append `Milestone v4.2 shipped 2026-05-04 across phases 75–80 (11 plans, 562 MSTest green, 0 REL-03 violations through all phases)`.

    Final line at EOF: update the `*State snapshot: ...*` italic footer to reflect the Phase 80 close + 6/6 milestone state.

    **Part C — Update .planning/ROADMAP.md:**

    - In the top `## Phases` list, change `- [ ] **Phase 80: Release & Compliance** — Pin LHM 0.9.6, ship THIRD-PARTY-NOTICES, CI grep gates, installer DLL capture.` to `- [x] **Phase 80: Release & Compliance** — Pin LHM 0.9.6, ship THIRD-PARTY-NOTICES, CI grep gates, installer DLL capture. (completed 2026-05-04)`.

    - In the `### Phase 80: Release & Compliance` subsection, replace the `**Plans:** TBD` line with:
      ```
      **Plans:** 2/2 plans complete

      Plans:
      - [x] 80-01-PLAN.md — THIRD-PARTY-NOTICES.md + release.yml gates + iss [Files] + tripwire validation (wave 1) — **DONE 2026-05-04**; 4 commits; 562 MSTest green preserved
      - [x] 80-02-PLAN.md — Human-verify 8-item D-16 checklist + phase 80 close-out state rollup (wave 2) — **DONE 2026-05-04**; single metadata commit; 8/8 checklist items approved
      ```

    - In the `## Progress` table, update the Phase 80 row: `| 80. Release & Compliance | 2/2 | Complete | 2026-05-04 |`.

    **Part D — Update .planning/REQUIREMENTS.md:**

    - In `### Release & Compliance` section (lines ~53-58), flip all 5 requirements from `- [ ]` to `- [x]` and append evidence clauses per the 79-02 pattern:

    ```
    - [x] **REL-01:** `LibreHardwareMonitorLib` is pinned at exactly version `0.9.6` in `FuzzyClock.App.csproj`; auto-upgrade is disabled (no version range, no floating). _(Satisfied 2026-05-04 by Plan 80-01 read-only verification of csproj line 15; grep -F confirms 1 exact match, 0 range/floating forms; Plan 80-02 Checklist Item 1.)_
    - [x] **REL-02:** CI grep gate fails the build if any `WinRing0*.sys` file appears in the `dotnet publish` output directory. _(Satisfied 2026-05-04 by Plan 80-01 commit `<hash>` inserting the post-publish gate into release.yml; tripwire positive+negative controls in Plan 80-01 SUMMARY confirm gate fires on violation; Plan 80-02 Checklist Items 2+6.)_
    - [x] **REL-03:** CI grep gate fails the build if the string `LibreHardwareMonitor` appears anywhere in `FuzzyClock.Core/` (preserves Core's pure `net10.0` posture). _(Satisfied 2026-05-04 by Plan 80-01 commit `<hash>` inserting the pre-build gate into release.yml; invariant verified clean end-to-end through Phases 75–80; tripwire positive+negative controls in Plan 80-01 SUMMARY confirm gate fires on violation; Plan 80-02 Checklist Items 3+6.)_
    - [x] **REL-04:** `THIRD-PARTY-NOTICES.md` exists at repo root and is shipped in the installer; includes verbatim MPL-2.0 text and attribution for `LibreHardwareMonitorLib` plus any transitive deps requiring notice. _(Satisfied 2026-05-04 by Plan 80-01 commit `<hash>` creating the file at repo root with 1 primary + 5 transitive attribution blocks + verbatim MPL-2.0 + D-05 bump reminder; shipping confirmed via Plan 80-01 commit `<hash>` adding the iss [Files] line; Plan 80-02 Checklist Items 4+8.)_
    - [x] **REL-05:** Inno Setup `[Files]` section captures the LHM DLL and all transitive DLLs introduced by the new reference; installer continues to install per-user with no UAC prompt. _(Satisfied 2026-05-04 by D-01 reinterpretation: `PublishSingleFile=true` + `IncludeNativeLibrariesForSelfExtract=true` bundles every transitive DLL into FuzzyClock.exe; Plan 80-01 commit `<hash>` adds the NOTICES [Files] line; Plan 80-02 Checklist Item 5 confirmed clean install on dev box with no UAC + installed folder contains FuzzyClock.exe + THIRD-PARTY-NOTICES.md + app launches.)_
    ```

    - In the `## Traceability` table (lines ~110-114), update each of the 5 REL rows:
      - `REL-01 | Phase 80 | Complete (2026-05-04)`
      - `REL-02 | Phase 80 | Complete (2026-05-04)`
      - `REL-03 | Phase 80 | Complete (2026-05-04)`
      - `REL-04 | Phase 80 | Complete (2026-05-04)`
      - `REL-05 | Phase 80 | Complete (2026-05-04)`

    - The hash placeholders `<hash>` in the evidence clauses should be filled in from `git log --oneline` showing the 4 Plan 80-01 commits (the specific NOTICES commit for REL-04, the release.yml commit for REL-02+REL-03, the iss commit for REL-04+REL-05, and the SUMMARY commit for audit trail).

    **Part E — Final single commit:**

    Stage all 4 modified files (SUMMARY + STATE + ROADMAP + REQUIREMENTS) and commit together:

    ```bash
    git add .planning/phases/80-release-compliance/80-02-SUMMARY.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md
    git commit -m "docs(80-02): capture human-verify sign-off and close phase 80"
    ```

    **NO `Co-Authored-By` trailer** per project CLAUDE.md rule. Single-line commit subject; multi-line body acceptable if needed but keep it factual (what + why, not how).

    **Post-commit validation:**

    ```bash
    git status --short
    ```
    Expected: empty output (tree clean).

    ```bash
    git log --oneline -6
    ```
    Expected top 6 commits (newest first):
    1. `docs(80-02): capture human-verify sign-off and close phase 80` (this commit)
    2. `docs(80-01): capture plan summary and tripwire validation` (Plan 80-01 Task 4)
    3. `feat(80): ship THIRD-PARTY-NOTICES.md in installer and verify REL-01 pin` (Plan 80-01 Task 3)
    4. `ci(80): add REL-02 and REL-03 grep gates to release.yml` (Plan 80-01 Task 2)
    5. `docs(80): add THIRD-PARTY-NOTICES.md at repo root` (Plan 80-01 Task 1)
    6. `docs(80): capture phase context` (from the /gsd:discuss-phase run — already landed per git log at session start: `5ffd047`)

    ```bash
    git log --format='%B' HEAD~5..HEAD | grep -c 'Co-Authored-By'
    ```
    Expected: `0`.
  </action>
  <verify>
    <automated>test -f .planning/phases/80-release-compliance/80-02-SUMMARY.md &amp;&amp; grep -F "approved" .planning/phases/80-release-compliance/80-02-SUMMARY.md &amp;&amp; grep -cE '^- \[x\] \*\*REL-0[1-5]:' .planning/REQUIREMENTS.md &amp;&amp; grep -F "Phase 80: Release &amp; Compliance" .planning/ROADMAP.md &amp;&amp; grep -F "completed_phases: 6" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/80-release-compliance/80-02-SUMMARY.md` exists with ≥100 lines.
    - SUMMARY contains the user's `approved` reply verbatim (grep -F "approved").
    - SUMMARY contains all 8 checklist item numbers ("Item 1" through "Item 8") at least once each.
    - SUMMARY contains all 5 REL requirement IDs in the Requirement Sign-off table.
    - `.planning/REQUIREMENTS.md` has exactly 5 `- [x] **REL-0N:**` entries (all 5 flipped from `- [ ]` to `- [x]`): `grep -cE '^- \[x\] \*\*REL-0[1-5]:' .planning/REQUIREMENTS.md` returns 5.
    - `.planning/REQUIREMENTS.md` Traceability table: all 5 REL rows show `Complete (2026-05-04)`.
    - `.planning/ROADMAP.md` Phase 80 row in top list shows `- [x]` with `(completed 2026-05-04)`.
    - `.planning/ROADMAP.md` Progress table Phase 80 row reads `| 80. Release & Compliance | 2/2 | Complete | 2026-05-04 |`.
    - `.planning/STATE.md` top frontmatter block shows `completed_phases: 6` and `total_plans: 11`, `completed_plans: 11`.
    - `.planning/STATE.md` Known Blockers section says `None` or equivalent with milestone-complete language.
    - Single commit landed: `git log -1 --format='%s'` returns `docs(80-02): capture human-verify sign-off and close phase 80`.
    - No `Co-Authored-By` trailer in ANY Phase 80 commit: `git log --format='%B' HEAD~5..HEAD | grep -c 'Co-Authored-By'` returns 0.
    - Tree clean post-commit: `git status --short` returns empty.
    - REL-03 invariant preserved: `grep -r --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/` returns 0 matches.
    - 562 MSTest green preserved: `dotnet test --no-build --no-restore` reports `Total: 562, Failed: 0`.
  </acceptance_criteria>
  <done>
    Plan 80-02 closed with a single metadata commit encompassing SUMMARY + all 3 state file rollups. All 5 REL requirements marked Complete in REQUIREMENTS.md Traceability table. ROADMAP.md shows Phase 80 as `[x]` completed 2026-05-04 (2/2 plans, Complete status). STATE.md top frontmatter block shows milestone v4.2 at 6/6 phases done, ready for `/gsd:audit-milestone`. Milestone v4.2 Temps & Menu is COMPLETE — next user action is `/gsd:audit-milestone` followed by `/gsd:complete-milestone` to tag v4.2.
  </done>
</task>

</tasks>

<verification>
Plan 80-02 complete when:

1. User replied `approved` to Task 1's 8-item D-16 checklist on the dev box.
2. `.planning/phases/80-release-compliance/80-02-SUMMARY.md` exists and documents all 8 checklist outcomes + requirement sign-offs + CONTEXT D-01..D-16 coverage + non-negotiable gates.
3. `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` all updated — Phase 80 marked complete, all 5 REL requirements flipped to `[x]`, milestone v4.2 at 6/6.
4. Single Plan 80-02 metadata commit landed with no `Co-Authored-By` trailer.
5. `dotnet test` still reports 562/562 green (this plan changed zero production code).
6. REL-03 invariant preserved: `grep -r --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/` returns 0 matches.
7. Tree clean: `git status --short` returns empty.
</verification>

<success_criteria>
Plan 80-02 complete when:
- [ ] User walked the 8-item D-16 checklist on the dev box and replied exactly `approved`
- [ ] Local Inno Setup compile produced `installer/FuzzyClockSetup-4.2.0.exe`
- [ ] Installer ran on dev box with no UAC prompt
- [ ] Installed folder contains both `FuzzyClock.exe` and `THIRD-PARTY-NOTICES.md` (SHA256 parity with repo-root version)
- [ ] `.planning/phases/80-release-compliance/80-02-SUMMARY.md` written following the 79-02 template
- [ ] STATE.md, ROADMAP.md, REQUIREMENTS.md all updated to reflect Phase 80 closure + milestone v4.2 6/6 complete
- [ ] Single metadata commit `docs(80-02): capture human-verify sign-off and close phase 80` landed
- [ ] Zero `Co-Authored-By` trailers in any Phase 80 commit
- [ ] 562 MSTest green preserved
- [ ] User knows next action is `/gsd:audit-milestone`
</success_criteria>

<output>
After Plan 80-02 completes, milestone v4.2 Temps & Menu is SHIP-READY. User runs `/gsd:audit-milestone` to confirm all 29 requirements across phases 75–80 are complete, then `/gsd:complete-milestone` to tag `v4.2` and archive the milestone into `.planning/archive/`.
</output>

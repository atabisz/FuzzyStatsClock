---
phase: 80-release-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - THIRD-PARTY-NOTICES.md
  - .github/workflows/release.yml
  - FuzzyClock.iss
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05]

must_haves:
  truths:
    - "THIRD-PARTY-NOTICES.md exists at repo root and contains verbatim MPL-2.0 plus attribution blocks for LibreHardwareMonitorLib 0.9.6 and transitive MPL-2.0 deps"
    - "release.yml has a REL-03 pre-build gate that fails the workflow if the literal string 'LibreHardwareMonitor' appears anywhere under FuzzyClock.Core/"
    - "release.yml has a REL-02 post-publish gate that fails the workflow if any WinRing0*.sys file appears under publish/"
    - "FuzzyClock.iss ships THIRD-PARTY-NOTICES.md to {app} root alongside FuzzyClock.exe"
    - "FuzzyClock.App.csproj line 15 still contains the literal string 'Version=\"0.9.6\"' (REL-01 invariant preserved — verification-only, no edit)"
    - "No existing release.yml steps (Setup .NET / Extract version from tag / Restore / Test / Publish / Compile installer / Rename EXE artifact / Generate checksums / Create GitHub Release) are modified (D-14 additive-only posture)"
    - "No file under FuzzyClock.Core/ is modified (REL-03 invariant is verified, not reshaped)"
  artifacts:
    - path: "THIRD-PARTY-NOTICES.md"
      provides: "MPL-2.0 verbatim + LHM 0.9.6 attribution + transitive dep attribution blocks"
      min_lines: 200
      contains: "Mozilla Public License Version 2.0"
    - path: ".github/workflows/release.yml"
      provides: "REL-03 pre-build gate + REL-02 post-publish gate inserted as additive steps"
      contains: "REL-03 guard"
    - path: "FuzzyClock.iss"
      provides: "[Files] entry shipping THIRD-PARTY-NOTICES.md to {app}"
      contains: "THIRD-PARTY-NOTICES.md"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "FuzzyClock.Core/"
      via: "pre-build grep gate"
      pattern: "grep -r .* 'LibreHardwareMonitor' FuzzyClock.Core/"
    - from: ".github/workflows/release.yml"
      to: "publish/"
      via: "post-publish find gate"
      pattern: "find publish/ -name 'WinRing0\\*.sys'"
    - from: "FuzzyClock.iss"
      to: "THIRD-PARTY-NOTICES.md"
      via: "[Files] Source directive"
      pattern: "Source: \"THIRD-PARTY-NOTICES.md\""
---

<objective>
Ship the license-clean, per-user, no-UAC release compliance artifacts for milestone v4.2 — one new repo-root documentation file plus three additive edits to existing CI + installer config. Zero runtime code changes; zero modifications to existing release pipeline steps or installer [Setup] directives.

Purpose: Close the final five milestone requirements (REL-01..REL-05) so that (a) `LibreHardwareMonitorLib` stays pinned at exactly 0.9.6, (b) CI fails loudly if a WinRing0 driver leaks into publish output, (c) CI fails loudly if the `LibreHardwareMonitor` string creeps into the Core assembly, (d) recipients of the binary can find the MPL-2.0 attribution in the installed folder, and (e) the installer continues to install per-user with no UAC prompt. These gates permanently prevent the two failure modes identified in `.planning/research/PITFALLS.md` (WinRing0 driver leakage + Core layering erosion) and satisfy the MPL-2.0 file-level copyleft redistribution obligation.

Output: One new file (`THIRD-PARTY-NOTICES.md`), two inserted YAML steps in `.github/workflows/release.yml`, one inserted `[Files]` line in `FuzzyClock.iss`, and a verification-only read of `FuzzyClock.App/FuzzyClock.App.csproj` confirming the REL-01 pin is already present. `FuzzyClock.Core/` and `FuzzyClock.App/FuzzyClock.App.csproj` are NOT edited.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-release-compliance/80-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md

<interfaces>
<!-- Authoritative transitive-dep set captured via `dotnet list FuzzyClock.App/FuzzyClock.App.csproj package --include-transitive` 2026-05-04. -->
<!-- Executor should use this list directly for THIRD-PARTY-NOTICES.md attribution blocks — no re-enumeration needed. -->

Top-level NuGet references in FuzzyClock.App/FuzzyClock.App.csproj (line 15):
  LibreHardwareMonitorLib 0.9.6          (MPL-2.0 — PRIMARY ATTRIBUTION)
  System.Diagnostics.PerformanceCounter 10.0.0   (MIT — Microsoft BCL, no separate notice required)

Transitive packages requiring individual attribution blocks:
  BlackSharp.Core 1.0.7            (MPL-2.0)
  DiskInfoToolkit 1.1.2            (MPL-2.0)
  HidSharp 2.6.4                   (Apache-2.0 or MIT — confirm via github.com/IntergatedCircuits/HidSharp LICENSE)
  RAMSPDToolkit-NDD 1.4.2          (MPL-2.0)
  Mono.Posix.NETStandard 1.0.0     (MIT — github.com/mono/mono)

Transitive packages NOT requiring separate attribution (Microsoft BCL / .NET runtime components shipped under the .NET runtime license):
  System.IO.Ports 10.0.3
  System.Management 10.0.2
  System.IO.FileSystem.AccessControl 5.0.0
  runtime.* native shims (21 packages)

Existing release.yml 9-step sequence (byte-for-byte locked per D-14 — DO NOT edit these):
  1. actions/checkout@v4
  2. Setup .NET (actions/setup-dotnet@v4)
  3. Extract version from tag  ← REL-03 gate inserts BEFORE this step (after Setup .NET)
  4. Restore
  5. Test
  6. Publish  ← REL-02 gate inserts AFTER this step (before Compile installer)
  7. Compile installer
  8. Rename EXE artifact
  9. Generate checksums
 10. Create GitHub Release

Existing FuzzyClock.iss [Files] section (1 entry, line 32):
  Source: "{#SourceDir}\FuzzyClock.exe"; DestDir: "{app}"; Flags: ignoreversion

REL-01 verification target — FuzzyClock.App/FuzzyClock.App.csproj:15 currently reads:
  <PackageReference Include="LibreHardwareMonitorLib" Version="0.9.6" />

  This is the EXACT-VERSION pin required by REL-01. No range brackets, no star, no Update= suffix. D-12 explicit: REL-01 is verification-only; no csproj edit needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author THIRD-PARTY-NOTICES.md at repo root (REL-04 content)</name>
  <files>THIRD-PARTY-NOTICES.md</files>
  <read_first>
    - .planning/phases/80-release-compliance/80-CONTEXT.md (D-03 structure + D-04 no auto-gen + D-05 bump-reminder)
    - .planning/research/STACK.md (§"Transitive Dependencies" table + §"MPL-2.0 Compliance for Closed-Source Redistribution")
    - FuzzyClock.App/FuzzyClock.App.csproj (line 15 confirms `LibreHardwareMonitorLib 0.9.6` is the top-level reference)
    - CLAUDE.md (no Co-Authored-By trailers)
  </read_first>
  <action>
    Create a NEW file at `THIRD-PARTY-NOTICES.md` in the repo root (absolute path: `c:/src/FuzzyStatsClock/THIRD-PARTY-NOTICES.md`). Use the Write tool (not Bash heredoc).

    Required structure (in order, top to bottom):

    **Section 1 — Title + intro paragraph (D-03 bullet 1):**
    ```
    # Third-Party Notices

    This product uses third-party components; their licenses and attributions follow.
    ```

    Then a sentence immediately below the intro noting: "This file is version-bound to `LibreHardwareMonitorLib 0.9.6` and its current transitive dependency set. Future milestones that bump `LibreHardwareMonitorLib` MUST refresh this file as part of the bump PR." (D-05 maintenance reminder baked into the file itself.)

    **Section 2 — LibreHardwareMonitorLib 0.9.6 attribution block (D-03 bullet 3):**

    Heading `## LibreHardwareMonitorLib 0.9.6`. Body includes:
    - Version: `0.9.6`
    - License: `MPL-2.0`
    - Upstream: `https://github.com/LibreHardwareMonitor/LibreHardwareMonitor`
    - Copyright line verbatim from the upstream project's LICENSE header: `Copyright (c) LibreHardwareMonitor and Contributors. All rights reserved.`
    - Note: `Unmodified binary redistribution. Source available at the upstream repository above.`

    **Section 3 — Transitive MPL-2.0 / MIT / Apache-2.0 attribution blocks (D-03 bullet 4 + STACK.md transitive table):**

    One `##` heading per transitive package below. Use the exact version numbers captured by `dotnet list FuzzyClock.App/FuzzyClock.App.csproj package --include-transitive` on 2026-05-04 and recorded in the `<interfaces>` block of this PLAN:

    - `## BlackSharp.Core 1.0.7` — License: `MPL-2.0`; Upstream: best-available NuGet page URL (executor looks up `https://www.nuget.org/packages/BlackSharp.Core` — the package page lists Project URL / Repository URL). Copyright line: per upstream LICENSE header. Note: unmodified binary redistribution.
    - `## DiskInfoToolkit 1.1.2` — License: `MPL-2.0`; Upstream: per NuGet page. Copyright / note analogous.
    - `## HidSharp 2.6.4` — License: look up the exact license on NuGet (`https://www.nuget.org/packages/HidSharp`) and the upstream LICENSE file (`github.com/IntergatedCircuits/HidSharp/blob/master/LICENSE.md` — Apache-2.0 per STACK.md line 210 note; Confirm and use whichever is authoritative). Record the copyright line verbatim.
    - `## RAMSPDToolkit-NDD 1.4.2` — License: `MPL-2.0`; Upstream: per NuGet page; copyright line verbatim from upstream.
    - `## Mono.Posix.NETStandard 1.0.0` — License: `MIT`; Upstream: `https://github.com/mono/mono`; Copyright: `Copyright (c) Xamarin Inc and Contributors.` Note: bundled via LHM transitive closure; inert on Windows.

    For each block, format consistently:
    ```
    ## <package name> <version>

    - **License:** <SPDX identifier>
    - **Upstream:** <primary URL>
    - **Copyright:** <verbatim copyright line from upstream LICENSE>

    <optional one-line note about redistribution status>
    ```

    **Section 4 — Verbatim MPL-2.0 license text (D-03 bullet 2):**

    Heading `## Mozilla Public License Version 2.0`. Body: paste the full MPL-2.0 license text (all three Exhibits A, B, and the definitions, terms, and conditions). The canonical plaintext source is `https://www.mozilla.org/media/MPL/2.0/index.815ca599c9df.txt`. The license text is ~170 lines. Executor uses WebFetch against `https://www.mozilla.org/en-US/MPL/2.0/` (or the plaintext mirror above) to retrieve the verbatim body, then pastes it under this heading exactly as delivered — preserving whitespace and section numbering. No paraphrasing. No trimming.

    **Section 5 — Optional MIT license text (D-03 bullet 5, optional):**

    If HidSharp turns out to be MIT-licensed per verification above, append a `## MIT License (HidSharp, Mono.Posix.NETStandard)` section containing the verbatim MIT license text (the ~21-line OSI canonical form). If HidSharp is Apache-2.0, substitute an `## Apache License 2.0 (HidSharp)` section with the verbatim Apache-2.0 text instead. Mono.Posix.NETStandard's MIT text is already covered in whichever section applies.

    **Section 6 — .NET runtime components note (clarifies why BCL packages get no separate block):**

    Append a short `## Microsoft .NET Runtime Components` paragraph noting: "`System.Management`, `System.IO.Ports`, `System.IO.FileSystem.AccessControl`, `System.Diagnostics.PerformanceCounter`, and the associated `runtime.*` native shim packages ship as part of the .NET runtime and are covered by the .NET runtime license at `https://github.com/dotnet/runtime/blob/main/LICENSE.TXT` (MIT). They are not enumerated individually above."

    **Final constraint — no Co-Authored-By anywhere in the file or commits.** Project CLAUDE.md rule.

    **Executor workflow:**
    1. Use WebFetch to retrieve the MPL-2.0 verbatim text from `https://www.mozilla.org/en-US/MPL/2.0/` (prompt: "Return the full plaintext of the Mozilla Public License Version 2.0 exactly as published, preserving section numbering and whitespace; do not summarize.").
    2. Use WebFetch against each transitive package's NuGet page URL (if needed) to confirm license SPDX + copyright line (prompt: "Return the license SPDX identifier and the project URL / repository URL shown on this NuGet package page."). For HidSharp and Mono.Posix, retrieve the LICENSE file content from the upstream repo to get the exact MIT or Apache-2.0 text + copyright holder name.
    3. Use the Write tool to create `THIRD-PARTY-NOTICES.md` with the full content assembled from the fetched license text + authored attribution blocks + authored intro + D-05 reminder sentence.
    4. Confirm the file ends at a clean trailing newline (one `\n` at EOF; no trailing blank lines).

    **If a WebFetch call fails or returns redacted/summarized text:** fall back to the plaintext mirror `https://www.mozilla.org/media/MPL/2.0/index.815ca599c9df.txt` for MPL-2.0, or to the package's GitHub LICENSE file URL for transitive deps. If all fetches fail for a given package, record the license SPDX + upstream URL + a short "see upstream" note (do NOT fabricate copyright lines). Never summarize MPL-2.0 — it must be verbatim.
  </action>
  <verify>
    <automated>grep -F "Mozilla Public License Version 2.0" THIRD-PARTY-NOTICES.md &amp;&amp; grep -F "LibreHardwareMonitorLib 0.9.6" THIRD-PARTY-NOTICES.md &amp;&amp; grep -F "BlackSharp.Core 1.0.7" THIRD-PARTY-NOTICES.md &amp;&amp; grep -F "DiskInfoToolkit 1.1.2" THIRD-PARTY-NOTICES.md &amp;&amp; grep -F "HidSharp 2.6.4" THIRD-PARTY-NOTICES.md &amp;&amp; grep -F "RAMSPDToolkit-NDD 1.4.2" THIRD-PARTY-NOTICES.md &amp;&amp; grep -F "Mono.Posix.NETStandard 1.0.0" THIRD-PARTY-NOTICES.md</automated>
  </verify>
  <acceptance_criteria>
    - File `THIRD-PARTY-NOTICES.md` exists at repo root (`c:/src/FuzzyStatsClock/THIRD-PARTY-NOTICES.md`).
    - File has ≥200 lines (MPL-2.0 verbatim alone is ~170 lines).
    - `grep -F "Mozilla Public License Version 2.0" THIRD-PARTY-NOTICES.md` returns ≥1 match.
    - `grep -F "LibreHardwareMonitorLib 0.9.6" THIRD-PARTY-NOTICES.md` returns ≥1 match.
    - `grep -F "BlackSharp.Core 1.0.7" THIRD-PARTY-NOTICES.md` returns ≥1 match.
    - `grep -F "DiskInfoToolkit 1.1.2" THIRD-PARTY-NOTICES.md` returns ≥1 match.
    - `grep -F "HidSharp 2.6.4" THIRD-PARTY-NOTICES.md` returns ≥1 match.
    - `grep -F "RAMSPDToolkit-NDD 1.4.2" THIRD-PARTY-NOTICES.md` returns ≥1 match.
    - `grep -F "Mono.Posix.NETStandard 1.0.0" THIRD-PARTY-NOTICES.md` returns ≥1 match.
    - `grep -F "MPL-2.0" THIRD-PARTY-NOTICES.md` returns ≥4 matches (primary + 3 MPL transitive deps: BlackSharp.Core, DiskInfoToolkit, RAMSPDToolkit-NDD).
    - `grep -F "future milestones that bump" THIRD-PARTY-NOTICES.md` (case-insensitive `-i`) returns ≥1 match (D-05 bump reminder present).
    - File ends with exactly one trailing newline.
  </acceptance_criteria>
  <done>
    A handwritten, human-readable `THIRD-PARTY-NOTICES.md` ships at repo root. Contains intro + D-05 bump reminder + LHM 0.9.6 attribution + 5 transitive attribution blocks (BlackSharp.Core 1.0.7, DiskInfoToolkit 1.1.2, HidSharp 2.6.4, RAMSPDToolkit-NDD 1.4.2, Mono.Posix.NETStandard 1.0.0) + verbatim MPL-2.0 body + verbatim MIT (or Apache-2.0) text for HidSharp/Mono.Posix + .NET runtime note. No auto-generation tool was used. Commit message: `docs(80): add THIRD-PARTY-NOTICES.md at repo root`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert REL-03 pre-build gate + REL-02 post-publish gate into release.yml</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml (all 86 lines — the 9 existing steps are byte-for-byte locked per D-14)
    - .planning/phases/80-release-compliance/80-CONTEXT.md (D-06 REL-03 pre-build placement + D-07 REL-02 post-publish placement + D-08 no redundant pre-build WinRing0 check + D-14 additive-only)
    - CLAUDE.md (no Co-Authored-By)
  </read_first>
  <action>
    Edit `.github/workflows/release.yml` to INSERT exactly TWO new steps. Do NOT touch any existing step (D-14 additive-only).

    **Insertion 1 — REL-03 gate (pre-build fail-fast):**

    Insert AFTER the existing `Setup .NET` step (ends at line 20 — `dotnet-version: '10.0.x'`) and BEFORE the existing `Extract version from tag` step (starts at line 22 — `- name: Extract version from tag`). The new step becomes step 3 in the workflow; the original step 3 (`Extract version from tag`) shifts to step 4.

    Exact YAML to insert (match existing 6-space indentation for `- name:` items, `shell: bash`, and `run: |` block style used by the existing `Extract version from tag` step at line 22):

    ```yaml
      - name: REL-03 guard — LibreHardwareMonitor absent from FuzzyClock.Core/
        shell: bash
        run: |
          if grep -rn --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/; then
            echo "::error::REL-03 violation: 'LibreHardwareMonitor' found in FuzzyClock.Core/"
            exit 1
          fi
    ```

    Rationale for this exact grep form: restricts to .cs + .csproj to prevent false hits on .obj / .bin (none are checked in under Core/, but the guard is future-proof). Uses `-n` so any future violation's line number appears in the CI log alongside the file path. `::error::` prefix surfaces in GitHub Actions workflow summary per D-06. Exit 1 aborts the workflow before `Restore` / `Test` / `Publish` consume any runner minutes (fail-fast per D-06).

    **Insertion 2 — REL-02 gate (post-publish, fails if WinRing0 leaked):**

    Insert AFTER the existing `Publish` step (the multi-line `run: >` block that ends at line 52 — `-o publish/`) and BEFORE the existing `Compile installer` step (starts at line 54 — `- name: Compile installer`).

    Exact YAML to insert:

    ```yaml
      - name: REL-02 guard — no WinRing0 driver in publish output
        shell: bash
        run: |
          if find publish/ -name 'WinRing0*.sys' -print -quit | grep -q .; then
            echo "::error::REL-02 violation: WinRing0*.sys leaked into publish/"
            find publish/ -name 'WinRing0*.sys'
            exit 1
          fi
    ```

    Rationale: `find -print -quit | grep -q .` is shorter than a full enumeration loop and exits with 0 only when at least one match exists. On match, the diagnostic `find` call enumerates every matching path so the CI log shows exactly which file leaked. Must run AFTER Publish (publish/ directory doesn't exist otherwise — D-07, D-08).

    **Use the Edit tool (not Write) since this is an existing file.** Two separate Edit calls are preferable to one monolithic Edit to minimize accidental rewrite of untouched steps.

    **Preserve line endings:** If `.github/workflows/release.yml` uses LF (Unix) line endings, inserted content MUST also use LF. Do not mix CRLF into an LF file.

    **No decision about the D-13 optional REL-01 sentry in this task.** Claude's discretion per CONTEXT: skip it. REL-01 enforcement is the PackageReference syntax itself + the verification step in Task 3 of this plan + the human-verify checklist item in Plan 80-02. Adding a third CI tripwire for the same invariant is noise.

    **After editing, verify YAML shape:** read back the modified file and confirm all 11 steps (9 original + 2 new) appear in order: checkout → Setup .NET → **REL-03 guard** → Extract version from tag → Restore → Test → Publish → **REL-02 guard** → Compile installer → Rename EXE artifact → Generate checksums → Create GitHub Release.
  </action>
  <verify>
    <automated>grep -F "REL-03 guard" .github/workflows/release.yml &amp;&amp; grep -F "REL-02 guard" .github/workflows/release.yml &amp;&amp; grep -F "grep -rn --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/" .github/workflows/release.yml &amp;&amp; grep -F "find publish/ -name 'WinRing0*.sys'" .github/workflows/release.yml &amp;&amp; grep -c "^  - name:" .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "REL-03 guard" .github/workflows/release.yml` returns ≥1 match.
    - `grep -F "REL-02 guard" .github/workflows/release.yml` returns ≥1 match.
    - `grep -F "grep -rn --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/" .github/workflows/release.yml` returns ≥1 match.
    - `grep -F "find publish/ -name 'WinRing0*.sys'" .github/workflows/release.yml` returns ≥1 match (either the predicate form or the diagnostic echo — both strings include the literal quoted glob).
    - `grep -c "^  - name:" .github/workflows/release.yml` returns exactly 11 (9 original steps + 2 new gate steps).
    - All 9 original step names still present (grep each one): `actions/checkout@v4`, `Setup .NET`, `Extract version from tag`, `- name: Restore` (with leading dash form), `- name: Test`, `- name: Publish`, `Compile installer`, `Rename EXE artifact`, `Generate checksums`, `Create GitHub Release`.
    - `git diff .github/workflows/release.yml` shows ADDITIONS only (no `-` deletion lines except possibly whitespace at the insertion boundary); the diff touches only two insertion blocks.
    - File is still valid YAML: `python -c "import yaml, sys; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0 (or equivalent PowerShell/.NET validator if Python unavailable — alternative: visual inspection of indentation depth).
  </acceptance_criteria>
  <done>
    `.github/workflows/release.yml` contains exactly 11 `- name:` steps in the documented order. REL-03 gate runs after Setup .NET and before Extract version from tag (fail-fast per D-06). REL-02 gate runs after Publish and before Compile installer (first opportunity per D-07). No existing step was modified (D-14). Commit message: `ci(80): add REL-02 and REL-03 grep gates to release.yml`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert THIRD-PARTY-NOTICES.md into FuzzyClock.iss [Files] + verify REL-01 pin in csproj</name>
  <files>FuzzyClock.iss</files>
  <read_first>
    - FuzzyClock.iss (all 86 lines — [Files] currently has exactly one entry at line 32)
    - FuzzyClock.App/FuzzyClock.App.csproj (line 15 — REL-01 verification target)
    - .planning/phases/80-release-compliance/80-CONTEXT.md (D-09 {app} root placement + D-10 no isreadme + D-11 no Docs subfolder + D-12 REL-01 verification-only)
  </read_first>
  <action>
    **Part A — Edit FuzzyClock.iss (REL-04 + REL-05 installer shipping):**

    Use the Edit tool to insert exactly ONE new line in the `[Files]` section. The existing line 32 reads:

    ```inno
    Source: "{#SourceDir}\FuzzyClock.exe"; DestDir: "{app}"; Flags: ignoreversion
    ```

    Insert the following line IMMEDIATELY after it (line 33 becomes the new entry; line 34 becomes the original blank/`[Icons]` separator):

    ```inno
    Source: "THIRD-PARTY-NOTICES.md"; DestDir: "{app}"; Flags: ignoreversion
    ```

    Exact syntax requirements:
    - `Source: "THIRD-PARTY-NOTICES.md"` — relative path from the `.iss` file's own directory (repo root) to the notices file. This is relative (not `{#SourceDir}\` prefixed) because NOTICES.md is a repo-root file, not a build output. ISCC resolves relative Source paths against the `.iss` location (the repo root for our case).
    - `DestDir: "{app}"` — installs to `%LOCALAPPDATA%\Programs\FuzzyClock\THIRD-PARTY-NOTICES.md` (D-09 {app} root placement).
    - `Flags: ignoreversion` — matches the existing FuzzyClock.exe entry; allows overwrite on upgrade without checking version metadata (NOTICES has no file version anyway).
    - NO `isreadme` flag (D-10 explicit rejection).
    - NO DestDir subfolder (D-11 explicit rejection — no `{app}\licenses\` or `{app}\Docs\`).

    No other changes to `FuzzyClock.iss`:
    - `[Setup]` section untouched — `PrivilegesRequired=lowest` invariant preserved (per-user, no UAC).
    - `[Icons]`, `[Run]`, `[Code]` sections untouched.
    - `AppId` GUID unchanged (stable across upgrades is critical for per-user installers).

    **Part B — REL-01 verification (read-only):**

    Read `FuzzyClock.App/FuzzyClock.App.csproj` and confirm line 15 contains EXACTLY the literal string `<PackageReference Include="LibreHardwareMonitorLib" Version="0.9.6" />` (whitespace-exact match within the tag; surrounding indentation may differ by leading tabs/spaces). Do NOT edit the file.

    If the exact literal is present: REL-01 is satisfied by construction (NuGet resolves `Version="X.Y.Z"` as an exact-version pin; it is neither a range `[0.9.6,)` nor floating `*`). Record the confirmed line number + full literal in the Plan 80-01 SUMMARY.

    If for any reason the literal is absent or has been modified to a range/floating form: DO NOT auto-fix silently. Stop, re-read the CONTEXT D-12 confirmation, and escalate to the user. CONTEXT D-12 states this file is ALREADY correct at 2026-05-04; Phase 80 must not discover a regression.

    **D-15 version bump decision (Claude's discretion):**

    `FuzzyClock.App.csproj` has stale `<Version>3.6.0</Version>` at line 28 (+ matching AssemblyVersion/FileVersion/InformationalVersion). CI injects the real version from git tag via `/p:Version=…`, so local-build inaccuracy is cosmetic. **Decision for this plan: SKIP the bump.** Rationale: (a) zero functional consequence; (b) risks coupling a csproj edit to the release-compliance commit, muddying `git blame` for REL-04/REL-05 attribution; (c) trivial to do in a post-milestone polish commit. Do NOT edit `<Version>` in this plan.

    **D-13 optional REL-01 CI sentry (Claude's discretion):**

    **Decision: SKIP.** A grep-for-literal-version in CI adds a third enforcement path for an invariant already enforced by (i) NuGet PackageReference semantics, (ii) the Task 3 Part B read-only verification, and (iii) the Plan 80-02 human-verify Item 1. Adding a fourth point of maintenance surfaces a coupling: any future deliberate version bump now requires editing 4 places instead of 3. Not worth it per CONTEXT D-13 "planner's call whether this is worth the 100ms CI cost" guidance.
  </action>
  <verify>
    <automated>grep -F 'Source: "THIRD-PARTY-NOTICES.md"; DestDir: "{app}"; Flags: ignoreversion' FuzzyClock.iss &amp;&amp; grep -F 'PrivilegesRequired=lowest' FuzzyClock.iss &amp;&amp; grep -F '<PackageReference Include="LibreHardwareMonitorLib" Version="0.9.6" />' FuzzyClock.App/FuzzyClock.App.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'Source: "THIRD-PARTY-NOTICES.md"; DestDir: "{app}"; Flags: ignoreversion' FuzzyClock.iss` returns exactly 1 match.
    - `grep -c '^Source: ' FuzzyClock.iss` returns exactly 2 (original FuzzyClock.exe entry + new NOTICES entry).
    - `grep -F 'PrivilegesRequired=lowest' FuzzyClock.iss` still returns ≥1 match (per-user invariant preserved — D-09/REL-05 no-UAC gate).
    - `grep -F 'AppId={{B8F2E3A1-7C4D-4E5F-9A6B-1D2E3F4A5B6C}' FuzzyClock.iss` still returns ≥1 match (AppId GUID unchanged — upgrade continuity preserved).
    - `grep -cE 'isreadme' FuzzyClock.iss` returns 0 (D-10 honored).
    - `grep -cE '\{app\}\\\\(licenses|Docs)' FuzzyClock.iss` returns 0 (D-11 honored — no `{app}\licenses` or `{app}\Docs` subfolder).
    - `grep -F '<PackageReference Include="LibreHardwareMonitorLib" Version="0.9.6" />' FuzzyClock.App/FuzzyClock.App.csproj` returns exactly 1 match (REL-01 verified).
    - `grep -F 'Version="*"' FuzzyClock.App/FuzzyClock.App.csproj` returns 0 matches (no floating version).
    - `grep -F 'Version="[0.9.6,)"' FuzzyClock.App/FuzzyClock.App.csproj` returns 0 matches (no range form).
    - `git diff FuzzyClock.App/FuzzyClock.App.csproj` shows NO changes (REL-01 is verification-only per D-12).
    - `git diff FuzzyClock.iss` shows exactly one additive line under `[Files]`.
  </acceptance_criteria>
  <done>
    `FuzzyClock.iss` ships `THIRD-PARTY-NOTICES.md` to `{app}` root via a single additive `[Files]` line. No other installer settings touched; per-user no-UAC invariant preserved. `FuzzyClock.App/FuzzyClock.App.csproj` is verified (not edited) to confirm `LibreHardwareMonitorLib Version="0.9.6"` exact-pin syntax at line 15. Commit message: `feat(80): ship THIRD-PARTY-NOTICES.md in installer and verify REL-01 pin`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Local tripwire validation of grep-gate logic + Plan 80-01 close-out</name>
  <files>.planning/phases/80-release-compliance/80-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/80-release-compliance/80-CONTEXT.md (specifics §"Validate the CI guards actually fail when expected")
    - .github/workflows/release.yml (the two gate blocks inserted by Task 2)
    - THIRD-PARTY-NOTICES.md (authored by Task 1)
    - FuzzyClock.iss (edited by Task 3)
  </read_first>
  <action>
    **Part A — Tripwire validation (prove the gates would actually fire on violation):**

    Run these POSITIVE control checks locally to confirm the grep logic in Task 2's inserted YAML is correct. These are dry-run shell validations only — NO commits, NO file creations that persist.

    1. **REL-03 positive control (clean case must pass):**
       Run from repo root in a Bash shell:
       ```bash
       if grep -rn --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/; then
         echo "WOULD FAIL CI"; exit 1
       else
         echo "PASS: REL-03 gate clean"
       fi
       ```
       Expected: `PASS: REL-03 gate clean` and exit 0. This proves the gate is correctly scoped (matches only FuzzyClock.Core/, not the App project which legitimately contains the string).

    2. **REL-03 negative control (simulated violation must fail):**
       Do NOT create a file in FuzzyClock.Core/ — that would dirty the tree. Instead validate the grep syntax in isolation:
       ```bash
       mkdir -p /tmp/rel03-probe/FuzzyClock.Core
       echo '// uses LibreHardwareMonitor' > /tmp/rel03-probe/FuzzyClock.Core/Stub.cs
       (cd /tmp/rel03-probe && grep -rn --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/) && echo "WOULD FAIL CI (expected)"
       rm -rf /tmp/rel03-probe
       ```
       Expected: output `FuzzyClock.Core/Stub.cs:1://uses LibreHardwareMonitor` followed by `WOULD FAIL CI (expected)`. This proves the grep syntax successfully detects violations.

       If `/tmp` is unavailable on Windows, use `$env:TEMP` equivalent via PowerShell or `%TEMP%` via cmd — the logic is identical.

    3. **REL-02 positive control (clean publish dir must pass):**
       Do NOT run `dotnet publish` (that consumes ~60s and is not the concern). Instead validate the find syntax:
       ```bash
       mkdir -p /tmp/rel02-probe/publish
       if find /tmp/rel02-probe/publish/ -name 'WinRing0*.sys' -print -quit | grep -q .; then
         echo "WOULD FAIL CI"
       else
         echo "PASS: REL-02 gate clean on empty publish"
       fi
       rm -rf /tmp/rel02-probe
       ```
       Expected: `PASS: REL-02 gate clean on empty publish`.

    4. **REL-02 negative control (simulated violation must fail):**
       ```bash
       mkdir -p /tmp/rel02-probe/publish
       touch /tmp/rel02-probe/publish/WinRing0x64.sys
       if find /tmp/rel02-probe/publish/ -name 'WinRing0*.sys' -print -quit | grep -q .; then
         echo "WOULD FAIL CI (expected)"; find /tmp/rel02-probe/publish/ -name 'WinRing0*.sys'
       else
         echo "FAIL: gate did not fire"
       fi
       rm -rf /tmp/rel02-probe
       ```
       Expected: `WOULD FAIL CI (expected)` followed by the .sys file path. This proves the WinRing0 detection works.

    Record the 4 tripwire outputs in Plan 80-01 SUMMARY Section "Tripwire Validation". If any of the 4 produces unexpected output, do NOT proceed to Plan 80-02 — fix the gate syntax in Task 2 first.

    **Part B — Write Plan 80-01 SUMMARY:**

    Write `.planning/phases/80-release-compliance/80-01-SUMMARY.md` following the pattern from `.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md`. Required sections (minimum):

    - YAML frontmatter: `phase: 80`, `plan: 01`, `subsystem: Release compliance — CI gates + NOTICES + installer`, `tags: [release, compliance, ci, installer, mpl-2.0, rel-01, rel-02, rel-03, rel-04, rel-05]`, `requires: [79-02]`, `provides: [rel-gates-wired, notices-shipped, phase-80-01-complete]`, `affects:` list (the 3 modified files + 4 state files for Plan 80-02), `tech_stack: { added: [], patterns: [...] }`, `key_files: { created: [THIRD-PARTY-NOTICES.md], modified: [.github/workflows/release.yml, FuzzyClock.iss] }`, `decisions:` list summarizing the D-13 + D-15 Claude's-discretion calls, `metrics: { duration: ~, commits: 3, files_changed: 3, tests_added: 0, tests_total: 562, completed: 2026-05-04 }`.
    - `# Phase 80 Plan 01: Release Compliance Summary` title + one-paragraph overview
    - `## Commits` table listing the 3 atomic commits created by Tasks 1–3.
    - `## Work Completed` with a subsection per task referencing commit hashes.
    - `## Tripwire Validation` reproducing the 4 probe outputs from Part A verbatim, confirming both gate syntaxes fire on violation and pass on clean state.
    - `## Test Results` table (before / after / delta — should be 562 → 562, +0, matching D-17 zero-test-addition expectation).
    - `## Verification Evidence` section with the grep hit counts for all the acceptance_criteria assertions.
    - `## Requirement Sign-off` table: REL-01 / REL-02 / REL-03 / REL-04 / REL-05 each mapped to the Task + commit that satisfies it.
    - `## CONTEXT D-01..D-16 Coverage` table showing each decision honored or deferred (with rationale for any defer).
    - `## Non-Negotiable Gates` table (REL-03 grep clean throughout, no Co-Authored-By, no existing release.yml step touched, no FuzzyClock.Core/ file touched, no FuzzyClock.App.csproj edit).
    - `## Files Untouched` list — every file NOT modified in this plan (FuzzyClock.Core/**, FuzzyClock.App/**/*.cs, FuzzyClock.App/**/*.xaml, FuzzyClock.App.Tests/**, FuzzyClock.Core.Tests/**, FuzzyClock.App.csproj).
    - `## Deviations from Plan` — record any, or "None".
    - `## Next-Phase Readiness` noting Plan 80-02 (human-verify checkpoint) is unblocked.
    - `## Self-Check: PASSED` — confirm all artifacts exist and all commits landed.

    **Part C — Update planning state files (deferred to Plan 80-02 close-out, NOT this plan):**

    STATE.md, ROADMAP.md, REQUIREMENTS.md updates happen at the end of Plan 80-02 (after human-verify approval) — matching the Phase 78/79 precedent where state rollups occur AFTER the blocking human-verify. Do NOT edit state files in this Task 4. Plan 80-01 SUMMARY is the only documentation output of Task 4.
  </action>
  <verify>
    <automated>test -f .planning/phases/80-release-compliance/80-01-SUMMARY.md &amp;&amp; grep -F "Tripwire Validation" .planning/phases/80-release-compliance/80-01-SUMMARY.md &amp;&amp; grep -F "REL-01" .planning/phases/80-release-compliance/80-01-SUMMARY.md &amp;&amp; grep -F "REL-02" .planning/phases/80-release-compliance/80-01-SUMMARY.md &amp;&amp; grep -F "REL-03" .planning/phases/80-release-compliance/80-01-SUMMARY.md &amp;&amp; grep -F "REL-04" .planning/phases/80-release-compliance/80-01-SUMMARY.md &amp;&amp; grep -F "REL-05" .planning/phases/80-release-compliance/80-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/80-release-compliance/80-01-SUMMARY.md` exists.
    - SUMMARY contains the literal string `## Tripwire Validation` (Section heading present).
    - SUMMARY contains all 5 requirement IDs (REL-01, REL-02, REL-03, REL-04, REL-05) at least once each.
    - All 4 tripwire probe outputs from Part A appear in the SUMMARY verbatim (positive + negative control for each gate).
    - SUMMARY records `tests_total: 562` and `tests_added: 0` in frontmatter (matches phase-level D-17 zero-test-addition expectation).
    - SUMMARY acknowledges D-13 skip + D-15 skip decisions with rationale.
    - `git log --oneline -5 HEAD | grep -cE '^[a-f0-9]+ (docs|ci|feat)\(80\)'` returns ≥3 (one commit per Tasks 1/2/3, all prefixed with phase id `(80)`; this Task 4 adds the SUMMARY commit to bring the count toward 4).
    - No `Co-Authored-By` trailer in any Phase 80 commit message: `git log --format='%B' HEAD~4..HEAD | grep -c 'Co-Authored-By' ` returns 0.
    - REL-03 invariant still clean after all edits: `grep -r --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/` returns 0 matches.
  </acceptance_criteria>
  <done>
    The four tripwire probes have been executed and their outputs recorded in the SUMMARY, demonstrating both gates fire on violation and pass on clean state. `.planning/phases/80-release-compliance/80-01-SUMMARY.md` is complete and follows the Phase 79-02 SUMMARY pattern. Plan 80-01 is closed with 4 commits (Tasks 1–3 code/config + this SUMMARY metadata commit). State files (STATE.md / ROADMAP.md / REQUIREMENTS.md) are deliberately NOT updated in this task — that close-out happens after the Plan 80-02 human-verify. Commit message for this task: `docs(80-01): capture plan summary and tripwire validation`.
  </done>
</task>

</tasks>

<verification>
Phase-level verification after Plan 80-01 completes:

1. **REL-01:** `grep -F '<PackageReference Include="LibreHardwareMonitorLib" Version="0.9.6" />' FuzzyClock.App/FuzzyClock.App.csproj` → exactly 1 match. `grep -Ec 'Version="\*"|Version="\[' FuzzyClock.App/FuzzyClock.App.csproj` → 0 matches (no floating / range).
2. **REL-02:** `grep -F "REL-02 guard" .github/workflows/release.yml` → ≥1. `grep -F "find publish/ -name 'WinRing0*.sys'" .github/workflows/release.yml` → ≥1.
3. **REL-03:** `grep -F "REL-03 guard" .github/workflows/release.yml` → ≥1. `grep -r --include='*.cs' --include='*.csproj' 'LibreHardwareMonitor' FuzzyClock.Core/` → 0 matches (invariant preserved).
4. **REL-04:** `test -f THIRD-PARTY-NOTICES.md` → exists. `grep -F "Mozilla Public License Version 2.0" THIRD-PARTY-NOTICES.md` → ≥1. `grep -F 'THIRD-PARTY-NOTICES.md' FuzzyClock.iss` → ≥1 (installer ships it).
5. **REL-05:** `grep -c '^Source: ' FuzzyClock.iss` → exactly 2. `grep -F 'PrivilegesRequired=lowest' FuzzyClock.iss` → ≥1 (per-user invariant preserved; `PublishSingleFile=true` in release.yml unchanged per D-01 → transitive DLLs continue to bundle into FuzzyClock.exe).
6. **D-14 additive-only:** `git diff HEAD~4 .github/workflows/release.yml` shows only `+` lines in two insertion blocks; no `-` lines except possibly whitespace-adjacent.
7. **Test suite unchanged:** `dotnet test` reports 562/562 green (0 tests added, 0 lost — D-17 expectation).
8. **No Core/ mutation:** `git diff HEAD~4 FuzzyClock.Core/` shows empty diff.
9. **No App code mutation:** `git diff HEAD~4 FuzzyClock.App/` excluding `.planning/` shows empty diff (REL-01 verify-only per D-12; the FuzzyClock.iss and release.yml and NOTICES are all outside FuzzyClock.App/ subtree).
</verification>

<success_criteria>
Plan 80-01 complete when:
- [ ] `THIRD-PARTY-NOTICES.md` exists at repo root with MPL-2.0 verbatim + 1 primary + 5 transitive attribution blocks + D-05 bump reminder
- [ ] `.github/workflows/release.yml` has REL-03 pre-build gate after `Setup .NET` and REL-02 post-publish gate after `Publish`; all 9 original steps unchanged (D-14)
- [ ] `FuzzyClock.iss` has 2 `Source:` entries in `[Files]` (FuzzyClock.exe + THIRD-PARTY-NOTICES.md, both → `{app}`); `PrivilegesRequired=lowest` preserved
- [ ] `FuzzyClock.App.csproj:15` verified (not edited) at `Version="0.9.6"` exact pin
- [ ] 4 tripwire probes executed and recorded in Plan 80-01 SUMMARY (2 positive + 2 negative controls); all produced expected output
- [ ] `dotnet test` → 562/562 green (REL-03 preserved end-to-end)
- [ ] 4 atomic commits landed: (1) `docs(80): add THIRD-PARTY-NOTICES.md at repo root`, (2) `ci(80): add REL-02 and REL-03 grep gates to release.yml`, (3) `feat(80): ship THIRD-PARTY-NOTICES.md in installer and verify REL-01 pin`, (4) `docs(80-01): capture plan summary and tripwire validation`
- [ ] Zero `Co-Authored-By` trailers in any Phase 80 commit
- [ ] Plan 80-02 (human-verify checkpoint) is unblocked
</success_criteria>

<output>
After Plan 80-01 completes, create `.planning/phases/80-release-compliance/80-01-SUMMARY.md` (Task 4 above already encodes this requirement). STATE.md / ROADMAP.md / REQUIREMENTS.md updates are DEFERRED to Plan 80-02 close-out, matching the Phase 78-02 / Phase 79-02 precedent where state rollups occur after blocking human-verify approval.
</output>

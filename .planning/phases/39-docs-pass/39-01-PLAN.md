---
phase: 39-docs-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "README Features list mentions date display with Show Date toggle and all 4 format options, each with an example output string"
    - "README Features list mentions battery row and explicitly states 'N/A on desktops or VMs with no battery'"
    - "README tray menu table includes Show Date, Date Format, and Show BATT rows"
    - "README test count reflects the current suite size (114 tests, not the stale 88)"
    - "README Project Structure mentions DateFormatter in the FuzzyClock.Core description"
  artifacts:
    - path: "README.md"
      provides: "Accurate v3.1 feature documentation"
      contains: "Date Display"
  key_links:
    - from: "README Features list"
      to: "README tray menu table"
      via: "Feature names match tray item names exactly"
      pattern: "Show Date.*toggle|Date Format"
---

<objective>
Update README.md to accurately describe the v3.0 date display feature and the v3.1 battery row so any user reading the README can discover and use both features.

Purpose: Phase 36 shipped date display and Phase 37 shipped battery row, but README.md still reflects v2.8 content. DOCS-03 requires these features be discoverable from the README.

Output: Updated README.md with date display and battery row in Features, tray menu table updated, test count corrected, Project Structure updated.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add date display and battery row to Features list</name>
  <files>README.md</files>
  <action>
In the Features section, insert two new bullet points.

Date display bullet — insert after the "Uptime row" bullet, before "Ghost mode":
- **Date display** — shows the current date below the clock phrase or dial in a muted accent color; toggleable via tray (Show Date); four format options selectable from the tray Date Format submenu:
  - Short: `Mon 3/7`
  - Long: `Monday, March 7, 2026`
  - Numeric: `3/7/2026`
  - ISO: `2026-03-07`

Battery — update the existing "Stats panel" bullet to mention Battery. Change the panel description to: "live CPU / GPU / MEM / PAG / Battery usage as horizontal bars below the phrase or dial; Battery row shows charge percentage (and ⚡ when AC-connected) or 'N/A' on desktops/VMs with no battery; per-row visibility toggles; 1s / 3s / 10s update interval". If the line becomes unwieldy, split battery into its own bullet. The requirement is that the README clearly states: battery charge % is shown, ⚡ appears when plugged in, and "N/A" appears on desktops or VMs with no battery.
  </action>
  <verify>
    <automated>grep -i "date display\|show date\|date format" C:/src/FuzzyStatsClock/README.md && grep -i "battery\|N/A\|batt" C:/src/FuzzyStatsClock/README.md</automated>
  </verify>
  <done>README Features section mentions date display with all 4 format examples, and mentions battery row with the N/A behavior on desktops/VMs</done>
</task>

<task type="auto">
  <name>Task 2: Update tray menu table, test count, and project structure</name>
  <files>README.md</files>
  <action>
Three targeted edits to README.md:

1. Tray menu table — add rows for the new tray items. Insert after the existing "Dial Face" row:
   - `| **Show Date** | Show or hide the date line below the clock phrase or dial; persisted |`
   - `| **Date Format** | Short (Mon 3/7) / Long (Monday, March 7, 2026) / Numeric (3/7/2026) / ISO (2026-03-07) |`
   Also update the Stats row description: change "toggle individual rows (CPU/GPU/MEM/PAG/Uptime)" to "toggle individual rows (CPU/GPU/MEM/PAG/BATT/Uptime)".

2. Test count — the README currently says "88 unit tests". Update to "114 unit tests" and add date formatter to the description: "114 unit tests: phrase engine (all 5-minute buckets, noon/midnight, edge cases), dial geometry, uptime formatter, date formatter (all 4 formats), settings validation and migration, and app integration tests."

3. Project Structure — in the FuzzyClock.Core comment line, add DateFormatter. Change `# Pure logic (PhraseEngine, DialGeometry, UptimeFormatter, ContrastService)` to `# Pure logic (PhraseEngine, DialGeometry, UptimeFormatter, DateFormatter, ContrastService)`.
  </action>
  <verify>
    <automated>grep -n "114\|Show Date\|Date Format\|BATT\|DateFormatter" C:/src/FuzzyStatsClock/README.md</automated>
  </verify>
  <done>Tray menu table has Show Date and Date Format rows; Stats row description includes BATT; test count reads 114; Project Structure comment includes DateFormatter</done>
</task>

</tasks>

<verification>
After both tasks complete, run:
grep -i "show date\|date format\|battery\|N/A\|114\|DateFormatter\|BATT" C:/src/FuzzyStatsClock/README.md
All six terms must appear. Confirm the tray menu table has the new rows and the Stats row mentions BATT.
</verification>

<success_criteria>
1. README Features section describes date display with Show Date toggle and all 4 format examples (Mon 3/7 / Monday March 7 2026 / 3/7/2026 / 2026-03-07)
2. README Features section describes battery row including N/A on desktops/VMs and the AC indicator
3. Tray menu table has Show Date and Date Format rows; existing Stats row description includes BATT
4. Test count updated from 88 to 114 with date formatter mentioned
5. Project Structure FuzzyClock.Core line includes DateFormatter
</success_criteria>

<output>
After completion, create `.planning/phases/39-docs-pass/39-01-SUMMARY.md`
</output>

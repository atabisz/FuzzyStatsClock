---
phase: 47-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/ThemeDefinition.cs
  - FuzzyClock.App/AppSettings.cs
  - FuzzyClock.App/SettingsWindow.xaml.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Ghost theme FontSize is 24; the 24pt button in Settings Appearance tab is highlighted when Ghost theme is active"
    - "AppSettings.cs PhraseStyle property has no trailing comment"
    - "SettingsWindow constructor contains exactly one _suppressEvents = true assignment (line 49 only)"
    - "All 224 existing tests pass"
  artifacts:
    - path: "FuzzyClock.App/ThemeDefinition.cs"
      provides: "Ghost theme entry with FontSize=24"
      contains: "FontSize     = 24,"
    - path: "FuzzyClock.App/AppSettings.cs"
      provides: "PhraseStyle property without stale comment"
    - path: "FuzzyClock.App/SettingsWindow.xaml.cs"
      provides: "SettingsWindow constructor with single _suppressEvents = true"
  key_links:
    - from: "FuzzyClock.App/ThemeDefinition.cs"
      to: "FuzzyClock.App/SettingsWindow.xaml.cs"
      via: "SetFontSizeButtonStates maps 16/24/32/40; Ghost FontSize=24 matches 24 button"
      pattern: "FontSize\s*=\s*24"
---

<objective>
Close three non-blocking tech debt items surfaced by the v3.2 milestone audit: change Ghost theme FontSize from 28 to 24, remove a stale comment from AppSettings.cs, and remove a redundant _suppressEvents assignment from the SettingsWindow constructor.

Purpose: The Ghost theme's FontSize=28 is not representable by the four font-size buttons in Settings (16/24/32/40), so the active-button highlight is wrong when Ghost is applied. The other two items are dead code and stale documentation with no behavioral impact. All three are surgical single-line edits with zero risk of side effects.
Output: Three modified files; all 224 tests still green.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-tech-debt-cleanup/47-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply three surgical code edits</name>
  <files>FuzzyClock.App/ThemeDefinition.cs, FuzzyClock.App/AppSettings.cs, FuzzyClock.App/SettingsWindow.xaml.cs</files>
  <action>
Make exactly three single-line edits. Read each file before editing to confirm the exact current content, then apply the change.

Edit A — FuzzyClock.App/ThemeDefinition.cs, line 46:
Change `FontSize     = 28,` to `FontSize     = 24,`
The Ghost entry is at lines 41-49. Only the FontSize value changes; leave all other fields untouched (Name, AccentColor, Opacity, DialMode, StatsVisible).

Edit B — FuzzyClock.App/AppSettings.cs, line 35:
Remove the trailing comment from the PhraseStyle property line.
Before: public string PhraseStyle  { get; init; } = "Classic";  // "Classic" is the only option in v3.2; Phase 45 adds Terse/Poetic/Rude
After:  public string PhraseStyle  { get; init; } = "Classic";
Do NOT touch line 34 (TextStyle property with its own comment // "Classic"|"Split"|"Literary"|"Mono"). Only line 35 changes.

Edit C — FuzzyClock.App/SettingsWindow.xaml.cs, line 60:
Remove the redundant second `_suppressEvents = true;` from the constructor. The first assignment at line 49 remains. The `_suppressEvents = false;` immediately after PopulateControls(snapshot) must NOT be touched — only the second `= true` (line 60, immediately before the PopulateControls call) is removed.

After edits confirm: ThemeDefinition.cs Ghost entry has FontSize = 24; AppSettings.cs PhraseStyle line ends with = "Classic"; with no trailing comment; SettingsWindow.xaml.cs constructor has _suppressEvents = true at line 49 only and _suppressEvents = false still follows PopulateControls.
  </action>
  <verify>
    <automated>grep -n "FontSize" FuzzyClock.App/ThemeDefinition.cs && grep -n "PhraseStyle" FuzzyClock.App/AppSettings.cs && grep -n "_suppressEvents = true" FuzzyClock.App/SettingsWindow.xaml.cs</automated>
  </verify>
  <done>ThemeDefinition.cs Ghost entry shows FontSize = 24; AppSettings.cs PhraseStyle line has no trailing comment; SettingsWindow.xaml.cs shows exactly two occurrences of _suppressEvents = true (line 49 in constructor, line ~228 in event-handler guard) — the redundant line 60 is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm no regressions</name>
  <files></files>
  <action>
Run the complete solution test suite. All 224 tests must pass. No new tests are needed — the edits are cosmetic/structural and the existing tests already cover the affected code paths.

Run: dotnet test FuzzyClock.sln

If any test fails, investigate before proceeding. A failure here indicates a pre-existing issue or an incorrect edit (e.g., removed the wrong _suppressEvents line rather than the redundant second = true).
  </action>
  <verify>
    <automated>cd /c/src/FuzzyStatsClock && dotnet test FuzzyClock.sln</automated>
  </verify>
  <done>Test run reports 224 passed, 0 failed, 0 skipped.</done>
</task>

</tasks>

<verification>
1. grep -n "FontSize" FuzzyClock.App/ThemeDefinition.cs — Ghost entry shows FontSize     = 24,
2. grep -n "PhraseStyle" FuzzyClock.App/AppSettings.cs — line 35 ends with = "Classic"; with no trailing comment
3. grep -n "_suppressEvents = true" FuzzyClock.App/SettingsWindow.xaml.cs — exactly two occurrences (constructor ~line 49, event-handler guard ~line 228)
4. dotnet test FuzzyClock.sln — 224 passed, 0 failed
</verification>

<success_criteria>
- Ghost theme FontSize is 24 in ThemeDefinition.cs
- PhraseStyle property in AppSettings.cs has no trailing comment
- SettingsWindow constructor has one (not two) _suppressEvents = true assignment
- dotnet test reports 224 passed, 0 failed
</success_criteria>

<output>
After completion, create `.planning/phases/47-tech-debt-cleanup/47-01-SUMMARY.md`
</output>

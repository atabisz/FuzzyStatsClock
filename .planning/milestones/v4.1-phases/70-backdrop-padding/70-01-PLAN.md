---
phase: 70-backdrop-padding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/MainWindow.xaml
autonomous: false
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "Widget has visibly larger breathing room around clock text, date, stats, and uptime"
    - "Edge snapping still triggers within 8px of screen edges after dragging widget near edge"
    - "Ghost mode proximity fade works correctly with new larger window dimensions"
    - "Contrast sampling covers the full backdrop footprint including new padding areas"
    - "Widget that was previously near a screen edge is clamped correctly on launch after padding increase"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml"
      provides: "Updated padding and margin values for backdrop breathing room"
      contains: "Padding=\"12\""
  key_links:
    - from: "FuzzyClock.App/MainWindow.xaml"
      to: "WPF SizeToContent layout engine"
      via: "SizeToContent=\"WidthAndHeight\" auto-sizes window to include new margins"
      pattern: "SizeToContent=\"WidthAndHeight\""
    - from: "FuzzyClock.App/MainWindow.xaml"
      to: "FuzzyClock.App/MainWindow.xaml.cs SnapToEdge()"
      via: "ActualWidth/ActualHeight auto-reflect new dimensions"
      pattern: "ActualWidth"
    - from: "FuzzyClock.App/MainWindow.xaml"
      to: "GhostModeController.cs GetWindowRect"
      via: "HWND bounds match WPF layout dimensions"
      pattern: "GetWindowRect"
---

<objective>
Add 12px backdrop padding around all widget content (clock, date, stats) by increasing inner margins on XAML elements.

Purpose: Give the widget generous visual breathing room so content does not feel crammed against backdrop edges. The current 6px padding looks tight; doubling to 12px creates a balanced, polished appearance.

Output: Updated MainWindow.xaml with three margin/padding changes. No C# code changes required -- all five interacting systems (edge snapping, ghost mode, contrast sampling, position clamping, per-monitor memory) automatically reflect the new window dimensions through WPF's SizeToContent layout propagation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-backdrop-padding/70-RESEARCH.md
@.planning/phases/70-backdrop-padding/70-UI-SPEC.md

<interfaces>
<!-- MainWindow.xaml current state — the three elements being modified -->

From FuzzyClock.App/MainWindow.xaml (line 42-46):
```xml
<Border x:Name="ContentBorder"
        Grid.Row="0"
        Background="Transparent"
        CornerRadius="5"
        Padding="6">
```

From FuzzyClock.App/MainWindow.xaml (line 130-132):
```xml
<TextBlock x:Name="DateText"
           Grid.Row="1"
           Margin="0,2,0,0"
```

From FuzzyClock.App/MainWindow.xaml (line 149-152):
```xml
<StackPanel x:Name="StatsPanel"
            Grid.Row="2"
            Width="184"
            Margin="0,4,0,0"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase backdrop padding to 12px on all content elements</name>
  <files>FuzzyClock.App/MainWindow.xaml</files>
  <read_first>
    - FuzzyClock.App/MainWindow.xaml (current XAML — verify exact line numbers and current values before editing)
    - .planning/phases/70-backdrop-padding/70-UI-SPEC.md (spacing scale table with exact target values)
  </read_first>
  <action>
Edit three elements in FuzzyClock.App/MainWindow.xaml to increase backdrop padding from the current minimal values to 12px:

1. **ContentBorder (line 46):** Change `Padding="6"` to `Padding="12"`
   - This is the clock content inner padding wrapping PhraseText, DialCanvas, LcdView, NixieView
   - Doubles the current 6px to 12px on all four sides of the clock content area

2. **DateText (line 132):** Change `Margin="0,2,0,0"` to `Margin="12,8,12,0"`
   - Left margin: 0 -> 12px (matches horizontal padding of ContentBorder)
   - Top margin: 2 -> 8px (more generous vertical gap between clock and date)
   - Right margin: 0 -> 12px (matches horizontal padding of ContentBorder)
   - Bottom margin: stays 0 (stats panel below provides its own top spacing)

3. **StatsPanel (line 152):** Change `Margin="0,4,0,0"` to `Margin="12,8,12,12"`
   - Left margin: 0 -> 12px (matches horizontal padding)
   - Top margin: 4 -> 8px (more generous vertical gap between date and stats)
   - Right margin: 0 -> 12px (matches horizontal padding)
   - Bottom margin: 0 -> 12px (provides breathing room at bottom edge of backdrop)

Do NOT change:
- `BackdropBorder` -- it must NOT have Padding (would shrink backdrop visual relative to window bounds)
- Outer `Grid Background="#01000000"` -- it must NOT have Padding (would shrink hit-test surface)
- `StatsPanel Width="184"` -- keep unchanged (prevents horizontal jitter from percentage text changes)
- Any stat row margins (`CpuRow`, `GpuRow`, etc. with `Margin="0,2,0,0"`) -- these are internal stat spacing, not backdrop padding
- Any C# code files -- WPF SizeToContent propagates dimension changes automatically

Expected visual impact:
- Window width increases ~24px (12px left + 12px right margins)
- Window height increases ~22px (8px-2px date top + 8px-4px stats top + 12px stats bottom)
- Backdrop expands to match new window dimensions via SizeToContent="WidthAndHeight"
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && dotnet build FuzzyClock.App --verbosity quiet 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - MainWindow.xaml contains `Padding="12"` on ContentBorder (not `Padding="6"`)
    - MainWindow.xaml contains `Margin="12,8,12,0"` on DateText (not `Margin="0,2,0,0"`)
    - MainWindow.xaml contains `Margin="12,8,12,12"` on StatsPanel (not `Margin="0,4,0,0"`)
    - MainWindow.xaml still contains `Width="184"` on StatsPanel (unchanged)
    - MainWindow.xaml still contains `Background="#01000000"` on outer Grid (no padding added)
    - MainWindow.xaml still contains `IsHitTestVisible="False"` on BackdropBorder (no padding added)
    - `dotnet build FuzzyClock.App` succeeds with 0 errors
    - `dotnet test` runs with 0 failures (existing 414 tests still pass)
  </acceptance_criteria>
  <done>MainWindow.xaml has 12px backdrop padding on all three content rows (ContentBorder, DateText, StatsPanel). Build succeeds. All 414 existing tests pass unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of backdrop padding</name>
  <files>FuzzyClock.App/MainWindow.xaml</files>
  <action>
Launch the application and visually verify that 12px backdrop padding is correctly applied around all widget content. This is a human verification checkpoint -- no code changes are made in this task.

Run: `dotnet run --project FuzzyClock.App`

Verification checklist for the user:
1. Clock text (phrase mode) has visibly larger breathing room on all sides
2. Date text has 12px left/right margins and 8px vertical gap above it
3. Stats panel has 12px left/right/bottom margins and 8px vertical gap above it
4. Edge snapping still works when dragging to screen edges (8px threshold)
5. Ghost mode proximity fade works correctly with new larger window
6. All four clock types (Phrase/Dial/LCD/Nixie) show consistent padding
7. Backdrop color visible around content when "Backdrop Always Visible" is ON
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && dotnet build FuzzyClock.App --verbosity quiet 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - User confirms padding is visibly larger and balanced on all sides
    - User confirms edge snapping still works (drag to screen edge, widget snaps)
    - User confirms ghost mode still works (cursor moves away, widget fades)
    - User confirms all four clock types display correctly with new padding
  </acceptance_criteria>
  <done>User has approved the visual appearance of 12px backdrop padding across all clock types and confirmed no regressions in edge snapping, ghost mode, or other interactive features.</done>
</task>

</tasks>

<verification>
1. `dotnet build FuzzyClock.App` succeeds with 0 errors, 0 warnings
2. `dotnet test` runs all 414 tests with 0 failures
3. Visual inspection confirms 12px breathing room on all sides of content
4. Edge snapping triggers within 8px of screen edges (unchanged behavior)
5. Ghost mode proximity fade works correctly (GetWindowRect returns new larger bounds)
</verification>

<success_criteria>
- Widget displays with visibly larger padding (12px) around clock text, date, and stats (VIS-01)
- Edge snapping, ghost mode, contrast sampling, and position clamping all work correctly with new dimensions (VIS-02)
- All 414 existing MSTest tests pass without modification
- User approves visual appearance at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/70-backdrop-padding/70-01-SUMMARY.md`
</output>

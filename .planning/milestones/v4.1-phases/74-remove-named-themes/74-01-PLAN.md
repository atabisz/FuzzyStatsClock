---
phase: 74-remove-named-themes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/ThemeDefinition.cs
  - FuzzyClock.App/AppSettings.cs
  - FuzzyClock.App/SettingsSnapshot.cs
  - FuzzyClock.App/SettingsWindow.xaml
  - FuzzyClock.App/SettingsWindow.xaml.cs
  - FuzzyClock.App/MainWindow.xaml.cs
  - .planning/PROJECT.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04

must_haves:
  truths:
    - "Settings > Appearance tab no longer shows Theme header or 5 theme cards"
    - "Build succeeds with zero compilation errors after all theme code is deleted"
    - "All existing tests pass unchanged (no test references theme infrastructure)"
    - "Users upgrading from v4.0 with a saved Theme field see their AccentColor preserved (JSON ignores unknown keys)"
    - "PROJECT.md no longer mentions named themes"
  artifacts:
    - path: "FuzzyClock.App/ThemeDefinition.cs"
      provides: "File must NOT exist (deleted)"
    - path: "FuzzyClock.App/AppSettings.cs"
      provides: "No Theme field present"
      contains: "AccentColor"
    - path: "FuzzyClock.App/SettingsSnapshot.cs"
      provides: "No ActiveTheme field present"
    - path: "FuzzyClock.App/SettingsWindow.xaml"
      provides: "No theme card XAML; Accent Color is first section in Appearance tab"
    - path: "FuzzyClock.App/SettingsWindow.xaml.cs"
      provides: "No ThemeSelected event, no theme click handlers, no SetActiveThemeCard, no ClearActiveThemeCard"
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "No _currentTheme, no ApplyNamedTheme, no ClearActiveTheme, no theme restore block"
  key_links:
    - from: "FuzzyClock.App/MainWindow.xaml.cs"
      to: "FuzzyClock.App/SettingsWindow.xaml.cs"
      via: "Event subscriptions in OpenSettingsWindow()"
      pattern: "AccentColorChanged.*SetAccentColor"
    - from: "FuzzyClock.App/MainWindow.xaml.cs"
      to: "FuzzyClock.App/AppSettings.cs"
      via: "SaveSettings() with-expression"
      pattern: "AccentColor.*=.*_accentColor"
---

<objective>
Delete the entire named theme system (Midnight/Neon/Ghost/Warm/Terminal) from the codebase — XAML, code-behind, data model, and documentation. No migration code needed because SaveSettings() has always persisted AccentColor as an individual field alongside Theme; System.Text.Json silently ignores unknown keys during deserialization.

Purpose: Simplify the Settings window and remove dead infrastructure. Theme was always a convenience shortcut over individually persisted properties.
Output: Clean build with no theme references; Settings Appearance tab starts at "Accent Color" section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-remove-named-themes/74-CONTEXT.md
@.planning/phases/74-remove-named-themes/74-UI-SPEC.md

<interfaces>
<!-- Key files to delete from or modify. Executor reads these first. -->

From FuzzyClock.App/AppSettings.cs (line 43, to be removed):
```csharp
public string? Theme { get; init; } = null;
```

From FuzzyClock.App/SettingsSnapshot.cs (line 38, to be removed):
```csharp
public string? ActiveTheme { get; init; } = null;
```

From FuzzyClock.App/ThemeDefinition.cs (entire file, to be deleted):
```csharp
internal record ThemeDefinition { ... }
internal static class BuiltInThemes { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all theme infrastructure from codebase</name>
  <files>
    FuzzyClock.App/ThemeDefinition.cs,
    FuzzyClock.App/AppSettings.cs,
    FuzzyClock.App/SettingsSnapshot.cs,
    FuzzyClock.App/SettingsWindow.xaml,
    FuzzyClock.App/SettingsWindow.xaml.cs,
    FuzzyClock.App/MainWindow.xaml.cs
  </files>
  <read_first>
    FuzzyClock.App/ThemeDefinition.cs,
    FuzzyClock.App/AppSettings.cs,
    FuzzyClock.App/SettingsSnapshot.cs,
    FuzzyClock.App/SettingsWindow.xaml,
    FuzzyClock.App/SettingsWindow.xaml.cs,
    FuzzyClock.App/MainWindow.xaml.cs
  </read_first>
  <action>
Delete the named theme system across 6 files in this order (dependency-safe):

**1. Delete ThemeDefinition.cs entirely**
- Delete the file `FuzzyClock.App/ThemeDefinition.cs` (contains `ThemeDefinition` record and `BuiltInThemes` static registry, 73 lines)

**2. Remove Theme field from AppSettings.cs**
- Delete line 43: `public string? Theme { get; init; } = null;`
- Keep all other fields unchanged

**3. Remove ActiveTheme field from SettingsSnapshot.cs**
- Delete line 38: `public string? ActiveTheme { get; init; } = null;`
- Keep all other fields unchanged

**4. Delete theme card XAML from SettingsWindow.xaml**
- Delete lines 58-157 (the comment `<!-- Named Themes -->`, the "Theme" TextBlock header, the horizontal StackPanel containing 5 theme card Borders `RingThemeMidnight` through `RingThemeTerminal`, and the closing `</StackPanel>`)
- Keep line 56 (`<StackPanel Margin="12">`) and line 159+ (`<!-- Accent Color -->` section) — "Accent Color" becomes the first child of the Appearance tab's StackPanel
- The blank line at 158 can also be removed for cleanliness

**5. Delete theme code from SettingsWindow.xaml.cs**
- Delete the `ThemeSelected` event declaration at line 48: `public event Action<string>? ThemeSelected;`
- Delete the ActiveTheme restore block in `PopulateControls()` (lines 201-215): the `if (s.ActiveTheme is not null)` block and its contents
- Delete the `SetActiveThemeCard` method (lines 261-275)
- Delete the `ClearActiveThemeCard` method (line 278)
- Delete all 5 theme click handlers (lines 288-327): `ThemeMidnight_Click`, `ThemeNeon_Click`, `ThemeGhost_Click`, `ThemeWarm_Click`, `ThemeTerminal_Click`
- Keep `RefreshControls()` method (lines 280-286) — it is still used for non-theme purposes (currently called from ThemeSelected handler only, but RefreshControls is a generic method; however since the ONLY caller is the ThemeSelected handler at line 483 of MainWindow, AND that handler is being deleted, RefreshControls can also be deleted). Actually, check if RefreshControls has any other callers first. If the only caller is the ThemeSelected handler, delete RefreshControls too.

**6. Delete theme code from MainWindow.xaml.cs**
- Delete the `_currentTheme` field at line 58: `private string? _currentTheme = null;`
- Delete the startup theme restore block at lines 360-372:
  ```csharp
  // Startup theme restore: set fields only...
  if (s.Theme is not null && BuiltInThemes.TryGet(s.Theme) is { } savedTheme)
  {
      _currentTheme    = s.Theme;
      _accentColor     = savedTheme.AccentColor;
      _windowOpacity   = savedTheme.Opacity;
      _currentFontSize = savedTheme.FontSize;
      _clockType       = savedTheme.ClockType;
  }
  ```
  Also delete the closing brace on line 372 and the comment block above the if statement (lines 360-362).
- In `GetCurrentSettingsSnapshot()`, delete line 403: `ActiveTheme = _currentTheme,`
- In `OpenSettingsWindow()`, simplify 5 event subscriptions that wrap calls in `ClearActiveTheme()`:
  - Line 421: `_settingsWindow.AccentColorChanged += c => { ClearActiveTheme(); SetAccentColor(c); };`
    becomes: `_settingsWindow.AccentColorChanged += c => SetAccentColor(c);`
  - Line 422: `_settingsWindow.OpacityChanged += o => { ClearActiveTheme(); SetOpacity(o); };`
    becomes: `_settingsWindow.OpacityChanged += o => SetOpacity(o);`
  - Line 423: `_settingsWindow.FontSizeChanged += sz => { ClearActiveTheme(); ApplyFontSize(sz); SaveSettings(); };`
    becomes: `_settingsWindow.FontSizeChanged += sz => { ApplyFontSize(sz); SaveSettings(); };`
  - Line 424: `_settingsWindow.ClockTypeChanged += ct => { ClearActiveTheme(); SetClockType(ct); };`
    becomes: `_settingsWindow.ClockTypeChanged += ct => SetClockType(ct);`
  - Line 450: `_settingsWindow.StatsVisibleChanged += v => { ClearActiveTheme(); SetStatsVisible(v); };`
    becomes: `_settingsWindow.StatsVisibleChanged += v => SetStatsVisible(v);`
- Delete the `ThemeSelected` subscription block at lines 478-485:
  ```csharp
  _settingsWindow.ThemeSelected += name =>
  {
      if (BuiltInThemes.TryGet(name) is { } theme)
      {
          ApplyNamedTheme(theme);
          _settingsWindow?.RefreshControls(GetCurrentSettingsSnapshot());
      }
  };
  ```
- In `SaveSettings()`, delete line 547: `Theme = _currentTheme,`
- In `ResetToDefaults()`, delete lines 1196-1198:
  ```csharp
  // Reset named theme: clear active theme so no card is highlighted after reset
  _currentTheme = null;
  _settingsWindow?.ClearActiveThemeCard();
  ```
- Delete `ApplyNamedTheme()` method entirely (lines 1218-1235)
- Delete `ClearActiveTheme()` method entirely (lines 1237-1244)
- In `Window_PreviewMouseWheel()`, delete line 1467: `ClearActiveTheme();`

**Important:** After all deletions, remove any orphaned `using` directives that referenced `ThemeDefinition` or `BuiltInThemes` (check top of MainWindow.xaml.cs and SettingsWindow.xaml.cs for any explicit using statements — though these types are in the same namespace `FuzzyClock.App` so there likely are none).

Build the solution after all changes:
```
dotnet build FuzzyClock.sln
```
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock &amp;&amp; dotnet build FuzzyClock.sln --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeDefinition.cs` file does not exist (deleted)
    - `AppSettings.cs` does NOT contain `Theme` as a property (grep for `public string? Theme` returns no match)
    - `SettingsSnapshot.cs` does NOT contain `ActiveTheme` (grep for `ActiveTheme` returns no match)
    - `SettingsWindow.xaml` does NOT contain `RingThemeMidnight` or `RingThemeNeon` or `ThemeMidnight_Click`
    - `SettingsWindow.xaml` line after `<StackPanel Margin="12">` is the "Accent Color" TextBlock
    - `SettingsWindow.xaml.cs` does NOT contain `ThemeSelected`, `SetActiveThemeCard`, `ClearActiveThemeCard`, `ThemeMidnight_Click`
    - `MainWindow.xaml.cs` does NOT contain `_currentTheme`, `ApplyNamedTheme`, `ClearActiveTheme`, `BuiltInThemes`, `ThemeSelected`
    - `MainWindow.xaml.cs` AccentColorChanged subscription is `+= c => SetAccentColor(c);` (no ClearActiveTheme wrapper)
    - `MainWindow.xaml.cs` SaveSettings with-expression does NOT contain `Theme =`
    - `dotnet build FuzzyClock.sln` succeeds with 0 errors
  </acceptance_criteria>
  <done>All theme infrastructure deleted from 6 files. ThemeDefinition.cs file deleted. Build succeeds with zero errors. No references to ThemeDefinition, BuiltInThemes, _currentTheme, ApplyNamedTheme, ClearActiveTheme, ThemeSelected, or ActiveTheme remain in the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md and run full test suite</name>
  <files>
    .planning/PROJECT.md
  </files>
  <read_first>
    .planning/PROJECT.md
  </read_first>
  <action>
**1. Update PROJECT.md line 5** (the "What This Is" paragraph):
- Find the sentence: `Five built-in named themes (Minimal, Neon, Ghost, Warm, Ocean) apply accent color, opacity, font size, clock style, and stats visibility atomically.`
- Delete that entire sentence from the paragraph. Do not replace it with anything.

**2. Update PROJECT.md decision log entries** (lines 93-96 and 188-196):
- At lines 93-96, the THEME-01 through THEME-04 decisions describe the old accent color preset system (renamed to accent swatches in v3.2). These describe accent color presets and custom color picker, NOT named themes. Leave them as-is — they document the accent color feature which still exists.
- At lines 194-196, the THM-01 through THM-03 decisions describe named themes. Add a note after each: ` — REMOVED in v4.1 (CLEAN-01 through CLEAN-04)`:
  - `- ✓ THM-01: Settings window Appearance tab offers 5 named built-in themes — v3.2 — REMOVED in v4.1 (CLEAN-01 through CLEAN-04)`
  - `- ✓ THM-02: Applying a theme atomically sets accent color, opacity, font size, clock style, and stats panel visibility — v3.2 — REMOVED in v4.1 (CLEAN-01 through CLEAN-04)`
  - `- ✓ THM-03: Active theme name persists to settings.json and restores on launch — v3.2 — REMOVED in v4.1 (CLEAN-01 through CLEAN-04)`
- At line 31, the v3.2 shipped line mentions "5 named themes". Update to: `**v3.2 shipped: 2026-03-09** — Settings window (3-tab), named themes (removed in v4.1), battery low alert, English phrase personalities (Terse/Poetic/Rude), multilingual phrases (fr/es/de/ja/pl)`
- At line 313, update "Remove the 5 named themes" to past tense: `- Removed the 5 named themes (Midnight/Neon/Ghost/Warm/Terminal) from Settings`

**3. Run full test suite:**
```
dotnet test FuzzyClock.sln
```
All existing tests must pass. No test modifications needed (per CONTEXT.md D-11 — no tests reference theme infrastructure).
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock &amp;&amp; dotnet test FuzzyClock.sln --no-restore 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `PROJECT.md` line 5 does NOT contain `Five built-in named themes`
    - `PROJECT.md` THM-01, THM-02, THM-03 lines contain `REMOVED in v4.1`
    - `PROJECT.md` line 31 mentions `named themes (removed in v4.1)` instead of `5 named themes`
    - `dotnet test FuzzyClock.sln` reports 0 failed tests
    - Total test count matches or exceeds 467 (current baseline from STATE.md)
  </acceptance_criteria>
  <done>PROJECT.md updated to reflect theme removal. Full test suite passes with 0 failures. All CLEAN-01 through CLEAN-04 requirements satisfied.</done>
</task>

</tasks>

<verification>
1. Build verification: `dotnet build FuzzyClock.sln` — 0 errors, 0 warnings related to themes
2. Test verification: `dotnet test FuzzyClock.sln` — all tests pass, count >= 467
3. Grep verification: `grep -r "ThemeDefinition\|BuiltInThemes\|ApplyNamedTheme\|ClearActiveTheme\|_currentTheme" FuzzyClock.App/ FuzzyClock.Core/` — returns no matches
4. File verification: `test ! -f FuzzyClock.App/ThemeDefinition.cs` — file does not exist
5. Settings migration: The absence of `AppSettings.Theme` field means `System.Text.Json` ignores any `"Theme"` key in existing settings.json files; `AccentColor` field (always persisted individually) loads directly
</verification>

<success_criteria>
- Settings > Appearance no longer shows Theme header or 5 theme cards (CLEAN-01)
- ThemeDefinition.cs deleted; ThemeDefinition record, BuiltInThemes registry, ApplyNamedTheme() removed (CLEAN-02)
- Users with saved Theme field in settings.json see their AccentColor preserved via JSON ignore-unknown-keys (CLEAN-03)
- AppSettings.Theme field removed; no Validate() changes needed since Theme was nullable with null default (CLEAN-04)
- Full test suite passes unchanged
- PROJECT.md updated to reflect removal
</success_criteria>

<output>
After completion, create `.planning/phases/74-remove-named-themes/74-01-SUMMARY.md`
</output>

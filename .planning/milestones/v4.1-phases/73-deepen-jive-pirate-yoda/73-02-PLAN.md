---
phase: 73-deepen-jive-pirate-yoda
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.Core/PiratePhraseProvider.cs
  - FuzzyClock.Core.Tests/PiratePhraseProviderExpandedTests.cs
autonomous: true
requirements: [PERS-02]

must_haves:
  truths:
    - "Pirate provider uses authentic nautical metaphors, not just movie catchphrases"
    - "Every bucket has exactly 5 candidates mixing nautical terms with pirate vocabulary"
    - "Noon and midnight have multi-candidate arrays (5 each)"
    - "Phrases reference authentic maritime concepts: bells, watch, glass, mark, course, bearing, log, strike"
    - "No phrase uses 'shiver me timbers' (movie cliche removed)"
  artifacts:
    - path: "FuzzyClock.Core/PiratePhraseProvider.cs"
      provides: "Expanded Pirate provider with 5 candidates per bucket"
      contains: "12 buckets + noon + midnight = 14 slots, 5 candidates each = 70 total"
    - path: "FuzzyClock.Core.Tests/PiratePhraseProviderExpandedTests.cs"
      provides: "Authenticity and coverage tests for expanded Pirate provider"
      min_lines: 80
  key_links:
    - from: "FuzzyClock.Core.Tests/PiratePhraseProviderExpandedTests.cs"
      to: "FuzzyClock.Core/PiratePhraseProvider.cs"
      via: "new PiratePhraseProvider() direct instantiation"
      pattern: "new PiratePhraseProvider"
---

<objective>
Deepen the PiratePhraseProvider from movie-cliche pirate vocabulary to authentic nautical language with maritime metaphors, ship's bells references, watch terminology, and seafaring idioms. Expand all 14 slots to 5 candidates each. Add authenticity tests.

Purpose: PERS-02 requires Pirate provider phrases use nautical metaphors and seafaring language naturally in time expressions. Current provider uses "shiver me timbers" and "it's {h} o'clock" — landlubber phrasing with pirate vocabulary appended.

Output: Expanded PiratePhraseProvider.cs (70 phrase candidates) + PiratePhraseProviderExpandedTests.cs
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-deepen-jive-pirate-yoda/73-RESEARCH.md

@FuzzyClock.Core/PiratePhraseProvider.cs
@FuzzyClock.Core.Tests/PhraseStyleProviderTests.cs
@FuzzyClock.Core.Tests/TersePhraseProviderExpandedTests.cs

<interfaces>
<!-- PiratePhraseProvider implements IPhraseProvider -->
From FuzzyClock.Core/IPhraseProvider.cs:
```csharp
public interface IPhraseProvider
{
    string GetPhrase(DateTime dt);
    (string Qualifier, string Emphasis) GetStructuredPhrase(DateTime dt);
    string GetSegmentKey(DateTime dt);
}
```

From FuzzyClock.Core/PiratePhraseProvider.cs:
```csharp
// Bucket structure: (int UpperBound, string[] Candidates)[]
// Template tokens: {h} = current hour word, {h1} = next hour word
// Noon: totalMinutes == 720, single return (to become multi-candidate)
// Midnight: totalMinutes == 0, single return (to become multi-candidate)
// Selection: candidates[Random.Shared.Next(candidates.Length)]
// GetSegmentKey: "en-pirate:noon", "en-pirate:midnight", or "en-pirate:{bucketIndex}"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand PiratePhraseProvider to 5 candidates per slot with authentic nautical language</name>
  <files>FuzzyClock.Core/PiratePhraseProvider.cs</files>
  <read_first>
    - FuzzyClock.Core/PiratePhraseProvider.cs (current 4-candidate implementation)
    - .planning/phases/73-deepen-jive-pirate-yoda/73-RESEARCH.md (nautical patterns and examples)
    - FuzzyClock.Core/TersePhraseProvider.cs (reference for noon/midnight multi-candidate pattern from Phase 72)
  </read_first>
  <action>
Rewrite all 14 slots in PiratePhraseProvider.cs to have exactly 5 candidates each, following these nautical authenticity rules:

**Authenticity rules (document in class summary comment):**
1. Nautical terms: bells (watch marking), watch (duty period), glass (hourglass/timepiece), mark (note time officially), course/bearing (navigation), trim (ship balance), log (official record), strike (bell sound)
2. Balance: Mix pirate vocabulary (arr, yarr, avast, ahoy, blimey) with authentic maritime concepts
3. Remove "shiver me timbers" and "it's {h} o'clock" (movie cliches / landlubber phrasing)
4. Vary intensity: some subtle nautical ("mark {h} by the watch"), some more expressive ("arr, {h} bells strike!")
5. Readability: user must be able to glance and know the time

**Noon — convert single return to 5-candidate array:**
Replace `return "high noon at sea, arr";` with:
```csharp
string[] noonCandidates = [
    "high noon at sea, arr",
    "the sun's at zenith, yarr — noon watch",
    "noon on the meridian, steady on",
    "eight bells — noon, by the log",
    "high noon, all hands — avast",
];
return noonCandidates[Random.Shared.Next(noonCandidates.Length)];
```

**Midnight — convert single return to 5-candidate array:**
Replace `return "the dead of night, yarr";` with:
```csharp
string[] midnightCandidates = [
    "the dead of night, yarr",
    "midnight watch begins, arr",
    "middle watch — the dark hours, avast",
    "eight bells — midnight, steady on",
    "the graveyard watch, blimey",
];
return midnightCandidates[Random.Shared.Next(midnightCandidates.Length)];
```

**Bucket rewrites — each bucket gets 5 candidates with authentic nautical metaphors:**

Bucket 0 (0-2 min, on the hour):
```csharp
( 2, [
    "eight bells strike {h}, arr",
    "mark {h} by the watch, yarr",
    "the glass shows {h}, steady on",
    "on the stroke of {h}, avast",
    "{h} bells true, by the log",
]),
```

Bucket 1 (3-7 min, just past):
```csharp
( 7, [
    "just past {h} bells, yarr",
    "barely past {h}, arr — steady as she goes",
    "a tick past {h} on the glass",
    "five past {h}, by the watch",
    "past the bell of {h}, blimey",
]),
```

Bucket 2 (8-12 min, ten past):
```csharp
(12, [
    "ten past {h}, arr — hold course",
    "ten past {h}, yarr — steady on",
    "ten past {h} by the log",
    "ten past {h}, blimey — mark it",
    "ten past {h} on the glass, avast",
]),
```

Bucket 3 (13-17 min, quarter past):
```csharp
(17, [
    "a quarter past {h}, yarr",
    "quarter past {h}, arr — trim the sails",
    "quarter past {h}, by the crow's nest",
    "a quarter past {h} on the watch",
    "quarter past {h}, steady on course",
]),
```

Bucket 4 (18-22 min, twenty past):
```csharp
(22, [
    "past the quarter bell of {h}, arr",
    "twenty past {h}, yarr — hold bearing",
    "twenty past {h}, arr — heave ho",
    "twenty past {h} by the glass",
    "twenty past {h}, aye — mark it",
]),
```

Bucket 5 (23-27 min, near half):
```csharp
(27, [
    "nigh on half past {h}, arr",
    "near half past {h}, yarr — steady",
    "comin' up on half past {h}, avast",
    "almost half past {h} by the watch",
    "near the half-glass of {h}, blimey",
]),
```

Bucket 6 (28-32 min, half past):
```csharp
(32, [
    "half past {h}, arr — steady as she goes",
    "half the glass of {h}, yarr",
    "gone the half-watch of {h}, avast",
    "half past {h}, trim yer course",
    "mid-watch past {h}, by the log",
]),
```

Bucket 7 (33-37 min, past half):
```csharp
(37, [
    "just past the half bell, yarr",
    "gone half past {h}, arr — hold course",
    "half past {h} and a tick, blimey",
    "past the half-glass, steady on",
    "beyond half past {h}, mark it",
]),
```

Bucket 8 (38-42 min, near quarter to):
```csharp
(42, [
    "nigh on a quarter to {h1}, arr",
    "almost quarter to {h1}, yarr — steady",
    "near a quarter to {h1} by the watch",
    "comin' up on quarter to {h1}, avast",
    "twenty to {h1}, arr — trim course",
]),
```

Bucket 9 (43-47 min, quarter to):
```csharp
(47, [
    "a quarter to {h1}, arr",
    "a quarter to {h1}, yarr — all hands",
    "a quarter to {h1}, man the watch",
    "fifteen minutes to {h1} by the glass",
    "quarter to {h1}, arr — hold bearing",
]),
```

Bucket 10 (48-52 min, ten to):
```csharp
(52, [
    "nearly {h1}, yarr — stand ready",
    "ten to {h1}, arr — all hands",
    "ten to {h1}, the watch nears end",
    "ten minutes to {h1}, by the log",
    "nigh on {h1}, blimey — steady on",
]),
```

Bucket 11 (53-59 min, almost next hour):
```csharp
(59, [
    "almost {h1}, arr — bells soon",
    "nearly {h1}, by the watch",
    "five to {h1}, yarr — make ready",
    "the glass nears {h1}, avast",
    "{h1} on the horizon, arr",
]),
```

**Update class summary comment** to add nautical authenticity rules:
```csharp
/// Nautical authenticity rules:
/// - Authentic maritime terms: bells, watch, glass, mark, course, bearing, trim, log, strike
/// - No "shiver me timbers" or landlubber phrasing ("it's X o'clock")
/// - Balance pirate vocabulary (arr, yarr, avast, ahoy, blimey) with real seafaring idioms
/// - Vary intensity: some subtle nautical, some more expressive
```

Keep existing GetPhrase logic, GetStructuredPhrase, GetSegmentKey, and HourWords unchanged.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && dotnet build FuzzyClock.Core/FuzzyClock.Core.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PiratePhraseProvider.cs Buckets array has exactly 12 entries, each with 5 strings in the Candidates array
    - Noon branch contains `noonCandidates` array with 5 elements and `Random.Shared.Next`
    - Midnight branch contains `midnightCandidates` array with 5 elements and `Random.Shared.Next`
    - No phrase contains "shiver me timbers"
    - No phrase contains "it's {h} o'clock" pattern
    - Class summary contains "Nautical authenticity rules" section
    - File compiles without errors
  </acceptance_criteria>
  <done>PiratePhraseProvider has 70 phrase candidates (14 slots x 5) with authentic nautical metaphors, no movie cliches, proper maritime terminology throughout</done>
</task>

<task type="auto">
  <name>Task 2: Add expanded Pirate authenticity and coverage tests</name>
  <files>FuzzyClock.Core.Tests/PiratePhraseProviderExpandedTests.cs</files>
  <read_first>
    - FuzzyClock.Core/PiratePhraseProvider.cs (just-modified provider with 5 candidates)
    - FuzzyClock.Core.Tests/TersePhraseProviderExpandedTests.cs (reference test pattern from Phase 72)
    - FuzzyClock.Core.Tests/PhraseStyleProviderTests.cs (existing Pirate tests — check noon/midnight assertions)
    - .planning/phases/73-deepen-jive-pirate-yoda/73-RESEARCH.md (authenticity test examples)
  </read_first>
  <action>
Create new file `FuzzyClock.Core.Tests/PiratePhraseProviderExpandedTests.cs` following the TersePhraseProviderExpandedTests.cs pattern but adding nautical authenticity checks.

**IMPORTANT:** The existing PiratePhraseProviderTests in PhraseStyleProviderTests.cs has `Pirate_Noon_ReturnsNoonPhrase` asserting `Assert.AreEqual("high noon at sea, arr", phrase)`. Since noon is now multi-candidate, this test WILL FAIL. Update the existing test in PhraseStyleProviderTests.cs to use pattern-based assertion instead:
```csharp
// Change from:
Assert.AreEqual("high noon at sea, arr", phrase);
// To:
Assert.IsTrue(phrase.Contains("noon") || phrase.Contains("zenith"),
    $"Noon phrase '{phrase}' should contain 'noon' or 'zenith'");
```

Similarly update `Pirate_Midnight_ReturnsMidnightPhrase`:
```csharp
// Change from:
Assert.AreEqual("the dead of night, yarr", phrase);
// To:
Assert.IsTrue(phrase.Contains("midnight") || phrase.Contains("night") || phrase.Contains("watch"),
    $"Midnight phrase '{phrase}' should contain midnight-related vocabulary");
```

Then create the new expanded test file:

```csharp
using FuzzyClock.Core;

namespace FuzzyClock.Core.Tests;

/// <summary>
/// Comprehensive tests for the expanded PiratePhraseProvider with multi-candidate buckets.
/// Tests bucket coverage, noon/midnight variants, nautical authenticity patterns,
/// segment key stability, and randomization variety.
/// </summary>
[TestClass]
public class PiratePhraseProviderExpandedTests
{
    private static readonly IPhraseProvider _provider = new PiratePhraseProvider();

    [TestMethod]
    public void Pirate_AllBuckets_PhraseContainsHourWord()
    {
        int[] sampleMinutes = [1, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55];

        foreach (int minute in sampleMinutes)
        {
            string phrase = _provider.GetPhrase(new DateTime(2024, 1, 1, 3, minute, 0));
            bool containsHourWord = phrase.Contains("three") || phrase.Contains("four");
            Assert.IsTrue(containsHourWord,
                $"Phrase '{phrase}' at minute {minute} should contain 'three' or 'four'");
        }
    }

    [TestMethod]
    public void Pirate_Noon_ContainsNoonVariant()
    {
        var distinctPhrases = new HashSet<string>();
        for (int i = 0; i < 30; i++)
        {
            string phrase = _provider.GetPhrase(new DateTime(2024, 1, 1, 12, 0, 0));
            distinctPhrases.Add(phrase);
            bool isNoonVariant = phrase.Contains("noon") || phrase.Contains("zenith");
            Assert.IsTrue(isNoonVariant,
                $"Noon phrase '{phrase}' should contain 'noon' or 'zenith'");
        }
        Assert.IsTrue(distinctPhrases.Count >= 2,
            $"Expected at least 2 distinct noon phrases from 30 calls, got {distinctPhrases.Count}");
    }

    [TestMethod]
    public void Pirate_Midnight_ContainsMidnightVariant()
    {
        var distinctPhrases = new HashSet<string>();
        for (int i = 0; i < 30; i++)
        {
            string phrase = _provider.GetPhrase(new DateTime(2024, 1, 1, 0, 0, 0));
            distinctPhrases.Add(phrase);
            bool isMidnightVariant = phrase.Contains("midnight") || phrase.Contains("night")
                                  || phrase.Contains("watch") || phrase.Contains("graveyard");
            Assert.IsTrue(isMidnightVariant,
                $"Midnight phrase '{phrase}' should contain midnight-related vocabulary");
        }
        Assert.IsTrue(distinctPhrases.Count >= 2,
            $"Expected at least 2 distinct midnight phrases from 30 calls, got {distinctPhrases.Count}");
    }

    [TestMethod]
    public void Pirate_AllPhrases_UseNauticalOrPirateTerminology()
    {
        string[] nauticalTerms = ["bells", "watch", "mark", "glass", "course",
            "bearing", "log", "strike", "trim", "steady", "horizon"];
        string[] pirateVocab = ["arr", "yarr", "avast", "ahoy", "blimey",
            "aye", "heave"];

        int[] sampleMinutes = [1, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55];

        foreach (int minute in sampleMinutes)
        {
            for (int iteration = 0; iteration < 10; iteration++)
            {
                string phrase = _provider.GetPhrase(new DateTime(2024, 1, 1, 3, minute, 0));
                bool hasNautical = nauticalTerms.Any(t =>
                    phrase.Contains(t, StringComparison.OrdinalIgnoreCase));
                bool hasPirate = pirateVocab.Any(t =>
                    phrase.Contains(t, StringComparison.OrdinalIgnoreCase));

                Assert.IsTrue(hasNautical || hasPirate,
                    $"Pirate phrase '{phrase}' at minute {minute} lacks nautical and pirate vocabulary");
            }
        }
    }

    [TestMethod]
    public void Pirate_NoPhrases_ContainShiverMeTimbers()
    {
        int[] sampleMinutes = [1, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55];

        foreach (int minute in sampleMinutes)
        {
            for (int iteration = 0; iteration < 10; iteration++)
            {
                string phrase = _provider.GetPhrase(new DateTime(2024, 1, 1, 3, minute, 0));
                Assert.IsFalse(phrase.Contains("shiver me timbers"),
                    $"Pirate phrase '{phrase}' contains movie cliche 'shiver me timbers'");
            }
        }
    }

    [TestMethod]
    public void Pirate_GetSegmentKey_Noon_ReturnsNoonKey()
    {
        string key = _provider.GetSegmentKey(new DateTime(2024, 1, 1, 12, 0, 0));
        Assert.AreEqual("en-pirate:noon", key);
    }

    [TestMethod]
    public void Pirate_GetSegmentKey_Midnight_ReturnsMidnightKey()
    {
        string key = _provider.GetSegmentKey(new DateTime(2024, 1, 1, 0, 0, 0));
        Assert.AreEqual("en-pirate:midnight", key);
    }

    [TestMethod]
    public void Pirate_GetSegmentKey_SameBucket_ReturnsSameKey()
    {
        string key1 = _provider.GetSegmentKey(new DateTime(2024, 1, 1, 4, 0, 0));
        string key2 = _provider.GetSegmentKey(new DateTime(2024, 1, 1, 4, 2, 0));
        Assert.AreEqual(key1, key2);
    }

    [TestMethod]
    public void Pirate_GetSegmentKey_AdjacentBuckets_ReturnDifferentKeys()
    {
        string key1 = _provider.GetSegmentKey(new DateTime(2024, 1, 1, 4, 2, 0));
        string key2 = _provider.GetSegmentKey(new DateTime(2024, 1, 1, 4, 3, 0));
        Assert.AreNotEqual(key1, key2);
    }

    [TestMethod]
    public void Pirate_Randomization_ProducesVariety()
    {
        var distinctPhrases = new HashSet<string>();
        var testTime = new DateTime(2024, 1, 1, 3, 15, 0);

        for (int i = 0; i < 50; i++)
        {
            string phrase = _provider.GetPhrase(testTime);
            distinctPhrases.Add(phrase);
        }

        Assert.IsTrue(distinctPhrases.Count >= 2,
            $"Expected at least 2 distinct phrases from 50 calls, got {distinctPhrases.Count}");
    }

    [TestMethod]
    public void Pirate_GetStructuredPhrase_AlwaysEmptyQualifier()
    {
        DateTime[] testTimes = [
            new DateTime(2024, 1, 1, 3, 0, 0),
            new DateTime(2024, 1, 1, 3, 30, 0),
            new DateTime(2024, 1, 1, 3, 45, 0),
        ];

        foreach (var time in testTimes)
        {
            var (qualifier, emphasis) = _provider.GetStructuredPhrase(time);
            Assert.AreEqual("", qualifier,
                $"Pirate style should always return empty qualifier at {time:HH:mm}");
            Assert.IsFalse(string.IsNullOrEmpty(emphasis),
                $"Emphasis should not be empty at {time:HH:mm}");
        }
    }
}
```

Two files modified: PhraseStyleProviderTests.cs (fix 2 exact-match assertions for Pirate noon/midnight) and new PiratePhraseProviderExpandedTests.cs.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && dotnet test FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj --filter "FullyQualifiedName~PiratePhraseProvider" --no-restore 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File FuzzyClock.Core.Tests/PiratePhraseProviderExpandedTests.cs exists
    - Contains `[TestClass] public class PiratePhraseProviderExpandedTests`
    - Contains test methods: Pirate_AllBuckets_PhraseContainsHourWord, Pirate_Noon_ContainsNoonVariant, Pirate_Midnight_ContainsMidnightVariant, Pirate_AllPhrases_UseNauticalOrPirateTerminology, Pirate_NoPhrases_ContainShiverMeTimbers, Pirate_Randomization_ProducesVariety
    - PhraseStyleProviderTests.cs `Pirate_Noon_ReturnsNoonPhrase` updated to pattern-based assertion (no longer `Assert.AreEqual("high noon at sea, arr"`)
    - PhraseStyleProviderTests.cs `Pirate_Midnight_ReturnsMidnightPhrase` updated to pattern-based assertion
    - All Pirate tests pass (both existing and new): `dotnet test --filter "FullyQualifiedName~Pirate"`
  </acceptance_criteria>
  <done>12+ authenticity and coverage tests for Pirate provider all pass, verifying nautical terminology, no movie cliches, multi-candidate noon/midnight, vocabulary presence, and randomization</done>
</task>

</tasks>

<verification>
1. `dotnet build FuzzyClock.Core/FuzzyClock.Core.csproj` succeeds with 0 errors
2. `dotnet test FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj --filter "FullyQualifiedName~Pirate"` — all tests pass
3. `dotnet test` (full suite) — all 467+ tests pass, zero regressions
</verification>

<success_criteria>
- PiratePhraseProvider has exactly 5 candidates per slot (12 buckets + noon + midnight = 70 total phrases)
- No phrase contains "shiver me timbers" (movie cliche removed)
- No phrase uses "it's {h} o'clock" pattern (landlubber phrasing removed)
- Authentic nautical terms present throughout (bells, watch, glass, mark, course, etc.)
- Existing Pirate tests updated to handle multi-candidate noon/midnight
- All existing and new tests pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/73-deepen-jive-pirate-yoda/73-02-SUMMARY.md`
</output>

---
phase: 03-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - FuzzyClock.App/App.xaml.cs
autonomous: false

requirements:
  - DISP-04

must_haves:
  truths:
    - "The widget shows the correct current phrase from the very first rendered frame — no placeholder flash"
    - "The phrase updates within 10 seconds of crossing a 5-minute clock boundary"
    - "The phrase text is readable over both light and dark desktop wallpapers"
    - "The widget does not drift — after extended running the displayed phrase matches the current time bucket"
  artifacts:
    - path: "FuzzyClock.App/App.xaml.cs"
      provides: "Initial phrase set before Show() via SetInitialPhrase"
      contains: "SetInitialPhrase"
  key_links:
    - from: "FuzzyClock.App/App.xaml.cs OnStartup"
      to: "FuzzyClock.App/MainWindow.xaml.cs SetInitialPhrase"
      via: "mainWindow.SetInitialPhrase(PhraseEngine.GetPhrase(DateTime.Now)) before mainWindow.Show()"
      pattern: "SetInitialPhrase.*PhraseEngine\\.GetPhrase"
---

<objective>
Wire the initial phrase into App.xaml.cs so the widget shows the correct live phrase before the window becomes visible, then run human visual verification of the complete integrated widget.

Purpose: Completes Phase 3 — the widget now shows correct time from launch and updates automatically. Human verification confirms legibility, correct phrase, and proper update behavior.
Output: App.xaml.cs with SetInitialPhrase call before Show(); human confirmation of live widget behavior.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@FuzzyClock.App/App.xaml.cs
@.planning/phases/03-integration/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update App.xaml.cs — call SetInitialPhrase before Show()</name>
  <files>FuzzyClock.App/App.xaml.cs</files>
  <action>
In OnStartup in App.xaml.cs, after constructing MainWindow and setting its Owner but before calling mainWindow.Show(), add the initial phrase call:

```csharp
mainWindow.SetInitialPhrase(PhraseEngine.GetPhrase(DateTime.Now));
```

The exact sequence must be:
1. `var mainWindow = new MainWindow();`
2. `mainWindow.Owner = hiddenOwner;`
3. `mainWindow.SetInitialPhrase(PhraseEngine.GetPhrase(DateTime.Now));`  ← INSERT HERE
4. `mainWindow.Show();`

This ensures the window is constructed and both TextBlocks exist (InitializeComponent has run) before SetInitialPhrase is called, but the window is not yet visible so the first rendered frame shows the live phrase.

Add `using FuzzyClock.Core;` to App.xaml.cs if not already present.

Do NOT move the timer start or PositionTopRight call here — ContentRendered handles those after Show() triggers the layout pass.

After making the change, do a full build and run a quick sanity check:
```
dotnet build FuzzyClock.slnx --no-restore -v q
```
  </action>
  <verify>
    <automated>cd C:/src/gsd1 && dotnet build FuzzyClock.slnx --no-restore -v q 2>&1 | tail -5</automated>
    <manual>Confirm App.xaml.cs has SetInitialPhrase(PhraseEngine.GetPhrase(DateTime.Now)) placed between mainWindow.Owner assignment and mainWindow.Show().</manual>
  </verify>
  <done>App.xaml.cs builds with 0 errors; SetInitialPhrase is called with the live phrase before Show(); sequence is: construct → set owner → set phrase → show.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — live phrase, update behavior, legibility</name>
  <files>FuzzyClock.App/MainWindow.xaml</files>
  <action>
Run `dotnet run --project FuzzyClock.App` from the solution root and verify the integrated widget visually.
  </action>
  <verify>
    <automated>cd C:/src/gsd1 && dotnet build FuzzyClock.slnx --no-restore -v q 2>&1 | tail -3</automated>
    <manual>
Run the widget:
  cd C:/src/gsd1 && dotnet run --project FuzzyClock.App

Check each item:

1. PHRASE CORRECTNESS — The displayed phrase matches the current time. Look at your clock and verify (e.g., if it is 2:47, the phrase should be "almost quarter to 3" or "ten to 3" depending on exact bucket). No placeholder "half past 3" appears.

2. BACKDROP LEGIBILITY — The phrase text has a very subtle dark hinted background (nearly invisible — hints of dark rectangle behind the text, not a heavy widget box). Text remains readable over your desktop wallpaper.

3. POSITION — Widget is anchored top-right, approximately 20px from the right and top screen edges.

4. UPDATE BEHAVIOR (optional, takes up to 10 minutes) — If near a 5-minute boundary, wait and confirm the phrase updates within 10 seconds. The update should be an instant snap with no animation.

5. LONG PHRASE WIDTH (important) — Note if the window appears clipped or oddly sized. The longest phrase at FontSize=32 should fit comfortably on a 1920px-wide screen without clipping.

Close with right-click → Close. Confirm the process exits cleanly.
    </manual>
  </verify>
  <done>Widget shows correct live phrase on launch; backdrop is nearly invisible; widget closes cleanly via right-click → Close; human approves.</done>
  <what-built>
Complete Phase 3 integration: FuzzyClock widget now shows the correct live fuzzy time phrase from the first frame, updates automatically every 10 seconds (catching each 5-minute clock boundary), and has a semi-transparent dark backdrop (15% black, rounded corners) for legibility on any wallpaper.
  </what-built>
  <resume-signal>Type "approved" if all checks pass, or describe any issues seen (wrong phrase, no backdrop, clipping, etc.)</resume-signal>
</task>

</tasks>

<verification>
After Task 1 (automated):
1. `dotnet build FuzzyClock.slnx` — 0 errors, 0 warnings
2. App.xaml.cs contains `SetInitialPhrase` before `mainWindow.Show()`

After Task 2 (human):
- Widget shows correct live phrase on launch
- Backdrop is visible but nearly invisible (subtle dark hint behind text)
- Widget closes cleanly via right-click → Close
</verification>

<success_criteria>
- App.xaml.cs: SetInitialPhrase(PhraseEngine.GetPhrase(DateTime.Now)) called before Show()
- Widget launched displays correct current time phrase — no "half past 3" placeholder visible
- Backdrop provides legibility without visual heaviness (per "barely there" design intent)
- Human verification: approved
- Phase 3 complete: DISP-04 satisfied — phrase updates at 5-minute clock boundaries via 10-second poll
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration/03-02-SUMMARY.md`
</output>

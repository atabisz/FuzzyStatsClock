---
phase: 01-phrase-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.sln
  - FuzzyClock.Core/FuzzyClock.Core.csproj
  - FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj
autonomous: true
requirements:
  - DISP-01
  - DISP-02
  - DISP-03

must_haves:
  truths:
    - "dotnet build succeeds for the solution with zero errors"
    - "dotnet test runs (no tests yet, but the test runner finds the test project)"
    - "FuzzyClock.Core project has no reference to WPF or System.Windows"
    - "FuzzyClock.Core.Tests references FuzzyClock.Core"
  artifacts:
    - path: "FuzzyClock.sln"
      provides: "Solution file containing both projects"
      contains: "FuzzyClock.Core"
    - path: "FuzzyClock.Core/FuzzyClock.Core.csproj"
      provides: "Classlib project targeting net10.0"
      contains: "net10.0"
    - path: "FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj"
      provides: "MSTest v4 test project targeting net10.0"
      contains: "MSTest"
  key_links:
    - from: "FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj"
      to: "FuzzyClock.Core/FuzzyClock.Core.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*FuzzyClock\\.Core\\.csproj"
---

<objective>
Create the .NET solution scaffold: a classlib project for the phrase engine and an MSTest v4 test project that references it.

Purpose: Establish the project structure so Plan 02 can immediately begin the TDD cycle without setup friction. The classlib must have zero WPF dependency — this is the architectural guarantee that the phrase engine remains independently testable.

Output: FuzzyClock.sln, FuzzyClock.Core classlib, FuzzyClock.Core.Tests mstest project, project reference wired.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-phrase-engine/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the .NET solution and projects</name>
  <files>
    FuzzyClock.sln
    FuzzyClock.Core/FuzzyClock.Core.csproj
    FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj
  </files>
  <action>
    Run the following commands from the repo root (C:/src/gsd1) in sequence:

    1. Create the solution file:
       dotnet new sln -n FuzzyClock

    2. Create the core logic library (classlib, no WPF, net10.0):
       dotnet new classlib -n FuzzyClock.Core -o FuzzyClock.Core --framework net10.0

    3. Create the MSTest v4 test project (net10.0):
       dotnet new mstest -n FuzzyClock.Core.Tests -o FuzzyClock.Core.Tests --framework net10.0

    4. Add both projects to the solution:
       dotnet sln add FuzzyClock.Core/FuzzyClock.Core.csproj
       dotnet sln add FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj

    5. Add the project reference from the test project to the core library:
       dotnet add FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj reference FuzzyClock.Core/FuzzyClock.Core.csproj

    The `dotnet new classlib` scaffold creates a Class1.cs placeholder — delete it (FuzzyClock.Core/Class1.cs). The `dotnet new mstest` scaffold creates a UnitTest1.cs placeholder — delete it (FuzzyClock.Core.Tests/UnitTest1.cs). Both files will be replaced in Plan 02.

    Do NOT add any WPF or System.Windows references to FuzzyClock.Core. The TargetFramework must remain `net10.0` (not `net10.0-windows`).
  </action>
  <verify>
    <automated>dotnet build FuzzyClock.sln</automated>
    <manual>Confirm FuzzyClock.Core.csproj contains TargetFramework net10.0 (not net10.0-windows). Confirm FuzzyClock.Core.Tests.csproj contains a ProjectReference to FuzzyClock.Core.csproj.</manual>
    <sampling_rate>run after this task, before next task</sampling_rate>
  </verify>
  <done>dotnet build FuzzyClock.sln exits 0 with no errors. Both projects are in the solution. The test project references the core library. No WPF dependency in FuzzyClock.Core.</done>
</task>

<task type="auto">
  <name>Task 2: Verify test runner discovery</name>
  <files>
    FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj
  </files>
  <action>
    Run the test suite to confirm the test runner can find and execute the test project (even with zero test methods after deleting UnitTest1.cs):

    dotnet test FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj

    Expected output: "Test run for FuzzyClock.Core.Tests ... passed!" with 0 tests passed, 0 failed. This confirms the MSTest v4 runner is wired correctly and Plan 02 can add test methods without any runner configuration changes.

    If the scaffold still has UnitTest1.cs (not yet deleted from Task 1), the run may show 1 test. That is also acceptable — the important thing is zero build errors and zero test failures.

    Do NOT add any test logic in this task. This is purely a runner smoke-test.
  </action>
  <verify>
    <automated>dotnet test FuzzyClock.Core.Tests/FuzzyClock.Core.Tests.csproj --no-build</automated>
    <manual>Confirm output contains "passed" with no "Error" or "Failed" lines.</manual>
    <sampling_rate>run after this task</sampling_rate>
  </verify>
  <done>dotnet test exits 0. Test runner discovers the test project. Zero failures reported.</done>
</task>

</tasks>

<verification>
After both tasks:
- dotnet build FuzzyClock.sln exits 0
- dotnet test FuzzyClock.Core.Tests exits 0
- FuzzyClock.Core.csproj has TargetFramework net10.0 (no -windows suffix)
- FuzzyClock.Core.Tests.csproj has a ProjectReference to FuzzyClock.Core.csproj
- Placeholder files (Class1.cs, UnitTest1.cs) are deleted
</verification>

<success_criteria>
The solution scaffold is clean and ready for TDD: build passes, test runner is wired, no placeholder files, no WPF dependency in the core library.
</success_criteria>

<output>
After completion, create `.planning/phases/01-phrase-engine/01-01-SUMMARY.md`
</output>

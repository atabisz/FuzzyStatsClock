---
phase: 56-settings-window-layout-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/SettingsWindow.xaml
autonomous: false
requirements:
  - SETT-01
  - SETT-02
  - SETT-03
  - SETT-04

must_haves:
  truths:
    - "All controls on the Appearance tab are visible within the 480x600 window without clipping"
    - "Theme preset cards are visibly more compact than v3.5 (40px tall vs 64px)"
    - "Inter-section spacing is tighter with no large blank gaps between sections"
    - "Stats tab looks identical to v3.5"
    - "Behavior tab looks identical to v3.5"
  artifacts:
    - path: "FuzzyClock.App/SettingsWindow.xaml"
      provides: "Compacted Appearance tab layout"
      contains: "Height=\"40\""
  key_links:
    - from: "FuzzyClock.App/SettingsWindow.xaml"
      to: "FuzzyClock.App/SettingsWindow.xaml.cs"
      via: "x:Name references on RingTheme* Borders"
      pattern: "x:Name=\"RingTheme"
---

<objective>
Compact the Appearance tab layout in SettingsWindow.xaml so all controls fit within the 480x600 window without clipping.

Purpose: The current Appearance tab has oversized theme cards (Height=64) and generous inter-section margins that push the Backdrop controls below the visible area, especially at 125% DPI. Compacting the cards and tightening spacing reclaims ~48px.

Output: Modified SettingsWindow.xaml with compact theme cards and tighter margins; Stats and Behavior tabs untouched.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-settings-window-layout-redesign/56-RESEARCH.md
@.planning/phases/56-settings-window-layout-redesign/56-UI-SPEC.md
@FuzzyClock.App/SettingsWindow.xaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact theme cards and tighten Appearance tab spacing</name>
  <files>FuzzyClock.App/SettingsWindow.xaml</files>
  <read_first>
    - FuzzyClock.App/SettingsWindow.xaml (current layout — lines 54-358 are the Appearance tab)
    - .planning/phases/56-settings-window-layout-redesign/56-UI-SPEC.md (exact before/after values)
    - .planning/phases/56-settings-window-layout-redesign/56-RESEARCH.md (spacing reduction table)
  </read_first>
  <action>
Edit ONLY within the Appearance TabItem (lines 54-359 of SettingsWindow.xaml). Do NOT touch anything in the Stats TabItem (lines 362-456) or Behavior TabItem (lines 459-504).

Make these exact changes to the five theme card outer Borders (RingThemeMidnight, RingThemeNeon, RingThemeGhost, RingThemeWarm, RingThemeTerminal):
- Change outer Border `CornerRadius="6"` to `CornerRadius="4"`
- Change outer Border `Padding="2"` to `Padding="1"`
- Change inner Border `Height="64"` to `Height="40"`
- Keep inner Border `Width="60"` unchanged
- Keep all `x:Name` attributes exactly as-is
- Keep all inner content (Ellipse, TextBlock, StackPanel) exactly as-is

Change the theme cards horizontal StackPanel (line 59):
- `Margin="0,0,0,14"` to `Margin="0,0,0,8"`

Change the control Grid (line 243):
- `Margin="0,14,0,0"` to `Margin="0,8,0,0"`

Change the Backdrop section header TextBlock (line 333):
- `Margin="0,14,0,6"` to `Margin="0,8,0,4"`

Change the Backdrop CheckBox (line 334-338, specifically the Margin attribute):
- `Margin="0,0,0,8"` to `Margin="0,0,0,6"`

After editing, verify the Appearance TabItem closing tag `</TabItem>` is at the correct nesting level (should close before the Stats TabItem opens). Confirm the Stats and Behavior TabItem blocks are completely unchanged.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SettingsWindow.xaml contains `Height="40"` on all five inner theme card Borders (grep for `Height="40"` returns 5 matches within the Appearance tab)
    - SettingsWindow.xaml contains `Padding="1"` on all five outer ring Borders (RingThemeMidnight through RingThemeTerminal)
    - SettingsWindow.xaml contains `CornerRadius="4"` on all five outer ring Borders
    - Theme cards StackPanel has `Margin="0,0,0,8"` (not `0,0,0,14`)
    - Control Grid has `Margin="0,8,0,0"` (not `0,14,0,0`)
    - Backdrop TextBlock has `Margin="0,8,0,4"` (not `0,14,0,6`)
    - Backdrop CheckBox has `Margin="0,0,0,6"` (not `0,0,0,8`)
    - All five x:Name attributes unchanged: RingThemeMidnight, RingThemeNeon, RingThemeGhost, RingThemeWarm, RingThemeTerminal
    - Width="480" Height="600" on the Window element is unchanged
    - Stats TabItem content (lines 362-456) is byte-identical to before
    - Behavior TabItem content (lines 459-504) is byte-identical to before
    - `dotnet build FuzzyClock.App/FuzzyClock.App.csproj` succeeds with 0 errors
  </acceptance_criteria>
  <done>All spacing and size changes applied; build succeeds; Stats and Behavior tabs untouched</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Appearance tab layout</name>
  <what-built>Compacted Appearance tab: theme cards reduced from 64px to 40px tall, inter-section margins tightened by 6-8px each. Total vertical space reclaimed: ~48px. Stats and Behavior tabs were not modified.</what-built>
  <how-to-verify>
    1. Run the app: `dotnet run --project FuzzyClock.App`
    2. Right-click system tray icon, select "Settings..."
    3. On the Appearance tab, confirm:
       a. All five theme cards are visible and noticeably shorter than before
       b. The Backdrop section (heading + checkbox + opacity slider) is fully visible — nothing is clipped at the bottom
       c. The color swatches, opacity slider, font size, clock style, phrase style, and phrase wrap controls are all visible
       d. No content is cut off or hidden below the window edge
    4. Click the Stats tab — confirm it looks exactly the same as before (all checkboxes, interval, threshold, date controls visible)
    5. Click the Behavior tab — confirm it looks exactly the same as before (language, ghost mode, auto-contrast, auto-launch, battery alert all visible)
    6. Click a theme card to confirm the selection ring still works (blue/green/etc. border appears around the selected card)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build FuzzyClock.App/FuzzyClock.App.csproj` succeeds
- All five theme cards have Height="40" (grep count = 5)
- No Height="64" remains in the Appearance tab
- Stats and Behavior TabItem blocks are unmodified
- Visual check confirms no clipping on Appearance tab
</verification>

<success_criteria>
- All Appearance tab controls visible in the 480x600 window without clipping (SETT-01)
- Theme cards are 40px tall, down from 64px (SETT-02)
- Inter-section margins reduced: theme cards bottom 14->8, control Grid top 14->8, Backdrop header 14->8/6->4, Backdrop checkbox 8->6 (SETT-03)
- Stats and Behavior tabs are byte-identical to v3.5 (SETT-04)
</success_criteria>

<output>
After completion, create `.planning/phases/56-settings-window-layout-redesign/56-01-SUMMARY.md`
</output>

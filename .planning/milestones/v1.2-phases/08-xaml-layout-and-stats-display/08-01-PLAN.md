---
phase: 08-xaml-layout-and-stats-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/MainWindow.xaml
  - FuzzyClock.App/MainWindow.xaml.cs
autonomous: true
requirements:
  - STAT-01
  - STAT-02

must_haves:
  truths:
    - "Stats panel StackPanel (x:Name=StatsPanel) exists in Row 1 of the inner Grid with Visibility=Collapsed and Width=180"
    - "Three stat rows (CPU, GPU, MEM) each contain a label TextBlock, a track Border (x:Name=CpuBarTrack/GpuBarTrack/MemBarTrack), a fill Border (x:Name=CpuBar/GpuBar/MemBar), and a percentage TextBlock (x:Name=CpuText/GpuText/MemText)"
    - "Stats ContextMenu submenu with x:Name items MenuShowStats, MenuInterval1, MenuInterval3, MenuInterval10 is present on the outer hit-test Grid's ContextMenu"
    - "UpdateStatsDisplay() updates all six named elements (three bars + three texts) and handles GpuPercent==-1f with N/A + zero width"
    - "_statsTimer is wired in ContentRendered and left stopped (StatsPanel is Collapsed by default)"
    - "_statsService.Dispose() is called in OnClosing before base.OnClosing()"
    - "Build succeeds with 0 errors after both files are modified"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml"
      provides: "Grid RowDefinitions + StatsPanel StackPanel with named bar elements + Stats ContextMenu entries"
      contains: "x:Name=\"StatsPanel\""
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "UpdateStatsDisplay() + _statsTimer + _statsService fields + ApplySettings extension + OnClosing disposal"
      contains: "UpdateStatsDisplay"
  key_links:
    - from: "ContentRendered handler"
      to: "_statsTimer.Tick"
      via: "_statsTimer.Tick += (_, _) => UpdateStatsDisplay()"
      pattern: "_statsTimer\\.Tick"
    - from: "UpdateStatsDisplay()"
      to: "_statsService.CpuPercent / GpuPercent / MemPercent"
      via: "_statsService.Refresh() then CpuBarTrack.ActualWidth * (percent / 100.0)"
      pattern: "CpuBarTrack\\.ActualWidth"
    - from: "ApplySettings()"
      to: "_statsIntervalSeconds field"
      via: "_statsIntervalSeconds = s.StatsIntervalSeconds"
      pattern: "_statsIntervalSeconds = s\\.StatsIntervalSeconds"
---

<objective>
Restructure MainWindow.xaml to add a Grid-based two-row layout with a StatsPanel showing CPU, GPU, and MEM bars, then wire the stats DispatcherTimer and UpdateStatsDisplay() method in MainWindow.xaml.cs.

Purpose: Phase 7 delivered the data layer (StatsService). Phase 8 adds the visual display layer — converting the single-phrase layout into a phrase-plus-stats layout, wiring the stats timer, and leaving the stats panel in its default Collapsed state ready for Phase 9 controls.

Output: Modified MainWindow.xaml (two-row Grid with named StatsPanel + Stats ContextMenu entries) and modified MainWindow.xaml.cs (UpdateStatsDisplay method + _statsTimer + _statsService + _statsIntervalSeconds + disposal in OnClosing).
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-statsservice/07-01-SUMMARY.md
@FuzzyClock.App/MainWindow.xaml
@FuzzyClock.App/MainWindow.xaml.cs
@FuzzyClock.App/StatsService.cs
@FuzzyClock.App/AppSettings.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure MainWindow.xaml — inner Grid row split, StatsPanel, Stats ContextMenu</name>
  <files>FuzzyClock.App/MainWindow.xaml</files>
  <action>
Modify MainWindow.xaml to implement the two-row layout and Stats ContextMenu entries. All changes are inside the existing outer Grid (which keeps Background="#01000000" and MouseLeftButtonDown handler and ContextMenu — these are NOT moved).

1. ADD Stats submenu to the existing ContextMenu after the Font Size MenuItem and before Close:
```xml
<MenuItem Header="Stats">
    <MenuItem x:Name="MenuShowStats"
              Header="Show Stats"
              IsCheckable="True" />
    <MenuItem Header="Update Interval">
        <MenuItem x:Name="MenuInterval1"  Header="1 second"   IsCheckable="True" />
        <MenuItem x:Name="MenuInterval3"  Header="3 seconds"  IsCheckable="True" />
        <MenuItem x:Name="MenuInterval10" Header="10 seconds" IsCheckable="True" />
    </MenuItem>
</MenuItem>
```
Do NOT add Click handlers — those are wired in Phase 9.
Do NOT add IsChecked — that is also Phase 9. The x:Name values are required now so Phase 9 code-behind can reference them.

2. WRAP the existing Border (phrase display) in a new two-row inner Grid. The current structure is:
   outer Grid > Border > inner Grid > (ShadowText, PhraseText)

The new structure is:
   outer Grid > new inner Grid (2 rows) > Row 0: existing Border unchanged > Row 1: StatsPanel StackPanel

Add the new inner Grid as the direct child of the outer Grid (replacing the Border as the direct child):
```xml
<Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Row 0: phrase display — UNCHANGED from current state -->
    <Border Grid.Row="0" Background="#26000000" CornerRadius="5" Padding="6">
        <Grid>
            <TextBlock x:Name="ShadowText" ... (unchanged) />
            <TextBlock x:Name="PhraseText" ... (unchanged) />
        </Grid>
    </Border>

    <!-- Row 1: stats panel — default Collapsed -->
    <StackPanel x:Name="StatsPanel"
                Grid.Row="1"
                Width="180"
                Margin="0,4,0,0"
                Visibility="Collapsed">

        <!-- CPU row -->
        <Grid Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="36" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="CPU"
                       Foreground="White" FontFamily="Segoe UI Light" FontSize="12"
                       VerticalAlignment="Center" />
            <Border Grid.Column="1" x:Name="CpuBarTrack"
                    Background="#40FFFFFF" CornerRadius="2" Height="8" VerticalAlignment="Center">
                <Border x:Name="CpuBar"
                        HorizontalAlignment="Left" Background="White"
                        CornerRadius="2" Height="8" Width="0" />
            </Border>
            <TextBlock Grid.Column="2" x:Name="CpuText"
                       Text="0%" Foreground="White" FontFamily="Segoe UI Light" FontSize="12"
                       TextAlignment="Right" VerticalAlignment="Center" />
        </Grid>

        <!-- GPU row -->
        <Grid Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="36" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="GPU"
                       Foreground="White" FontFamily="Segoe UI Light" FontSize="12"
                       VerticalAlignment="Center" />
            <Border Grid.Column="1" x:Name="GpuBarTrack"
                    Background="#40FFFFFF" CornerRadius="2" Height="8" VerticalAlignment="Center">
                <Border x:Name="GpuBar"
                        HorizontalAlignment="Left" Background="White"
                        CornerRadius="2" Height="8" Width="0" />
            </Border>
            <TextBlock Grid.Column="2" x:Name="GpuText"
                       Text="0%" Foreground="White" FontFamily="Segoe UI Light" FontSize="12"
                       TextAlignment="Right" VerticalAlignment="Center" />
        </Grid>

        <!-- MEM row -->
        <Grid Margin="0,2,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="36" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="MEM"
                       Foreground="White" FontFamily="Segoe UI Light" FontSize="12"
                       VerticalAlignment="Center" />
            <Border Grid.Column="1" x:Name="MemBarTrack"
                    Background="#40FFFFFF" CornerRadius="2" Height="8" VerticalAlignment="Center">
                <Border x:Name="MemBar"
                        HorizontalAlignment="Left" Background="White"
                        CornerRadius="2" Height="8" Width="0" />
            </Border>
            <TextBlock Grid.Column="2" x:Name="MemText"
                       Text="0%" Foreground="White" FontFamily="Segoe UI Light" FontSize="12"
                       TextAlignment="Right" VerticalAlignment="Center" />
        </Grid>

    </StackPanel>
</Grid>
```

CRITICAL constraints:
- The outer Grid (Background="#01000000", MouseLeftButtonDown, ContextMenu) is NOT changed — it remains the direct child of Window. Only its single child element changes from `Border` to the new two-row `Grid`.
- Do NOT add DropShadowEffect or BlurEffect — AllowsTransparency window requires flat SolidColorBrush only.
- Do NOT use ProgressBar — use nested Border pattern as shown above.
- StatsPanel Visibility="Collapsed" (not Hidden) — Collapsed removes all layout space including margin.
- Width="180" on StatsPanel is required to prevent SizeToContent=WidthAndHeight window-width jitter as % text changes from "9%" to "100%".
- The ContextMenu stays on the outer Grid (not moved to the inner Grid) — keeps right-click working across the full window width, not just the 180px stats area.
  </action>
  <verify>
    <automated>cd C:/src/gsd1 && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -v q 2>&amp;1 | tail -5</automated>
    <manual>Verify in MainWindow.xaml: outer Grid still has ContextMenu + Background="#01000000"; Stats submenu has 4 x:Name items; StatsPanel has Visibility="Collapsed" + Width="180"; six x:Name bar elements present (CpuBar/GpuBar/MemBar/CpuBarTrack/GpuBarTrack/MemBarTrack/CpuText/GpuText/MemText)</manual>
  </verify>
  <done>dotnet build succeeds with 0 errors; all required x:Name elements are present; Stats submenu is in ContextMenu; StatsPanel is Collapsed with Width=180</done>
</task>

<task type="auto">
  <name>Task 2: Wire StatsService + UpdateStatsDisplay + _statsTimer in MainWindow.xaml.cs</name>
  <files>FuzzyClock.App/MainWindow.xaml.cs</files>
  <action>
Modify MainWindow.xaml.cs to add three new fields, extend ApplySettings(), add UpdateStatsDisplay(), wire the stats timer in ContentRendered, and add disposal in OnClosing.

1. ADD three new private fields after the existing `_timer` field declaration (line 10):
```csharp
private DispatcherTimer _statsTimer = null!;
private StatsService _statsService = null!;
private int _statsIntervalSeconds = 3;   // default matches AppSettings.StatsIntervalSeconds default
```

2. EXTEND ApplySettings() — add one line at the end of the method body, after the existing `if (s.Left != -1)` block:
```csharp
_statsIntervalSeconds = s.StatsIntervalSeconds;
// Note: StatsVisible wiring is deferred to Phase 9. StatsPanel Visibility is hardcoded in XAML.
```

3. ADD UpdateStatsDisplay() method — add after the existing UpdatePhraseIfChanged() method:
```csharp
private void UpdateStatsDisplay()
{
    _statsService.Refresh();

    CpuText.Text = $"{_statsService.CpuPercent:F0}%";
    CpuBar.Width = CpuBarTrack.ActualWidth * (_statsService.CpuPercent / 100.0);

    if (_statsService.GpuPercent < 0f)
    {
        GpuText.Text = "N/A";
        GpuBar.Width = 0;
    }
    else
    {
        GpuText.Text = $"{_statsService.GpuPercent:F0}%";
        GpuBar.Width = GpuBarTrack.ActualWidth * (_statsService.GpuPercent / 100.0);
    }

    MemText.Text = $"{_statsService.MemPercent:F0}%";
    MemBar.Width = MemBarTrack.ActualWidth * (_statsService.MemPercent / 100.0);
}
```

4. EXTEND ContentRendered handler — add stats timer initialization AFTER the existing `_timer.Start()` line (the existing phrase timer code is NOT changed):
```csharp
// Stats timer — independent from phrase timer (different interval, user-configurable)
// StatsService constructor starts Task.Run(Initialize) immediately; Refresh() is a safe
// no-op until initialization completes (~6s PDH cold start).
_statsService = new StatsService();
_statsTimer = new DispatcherTimer
{
    Interval = TimeSpan.FromSeconds(_statsIntervalSeconds)
};
_statsTimer.Tick += (_, _) => UpdateStatsDisplay();
// Do NOT start _statsTimer here — StatsPanel is Collapsed by default (StatsVisible=false).
// Phase 9 starts it via SetStatsVisible(true) when the user enables stats.
```

5. EXTEND OnClosing — add disposal of StatsService BEFORE the existing SaveSettings() call:
```csharp
protected override void OnClosing(System.ComponentModel.CancelEventArgs e)
{
    _statsTimer?.Stop();
    _statsService?.Dispose();
    SaveSettings();
    base.OnClosing(e);
}
```

Do NOT add a using statement — StatsService is in the same namespace (FuzzyClock.App).
Do NOT start _statsTimer in ContentRendered — it starts stopped. Phase 9 owns the start/stop lifecycle.
Do NOT call UpdateStatsDisplay() in ContentRendered — ActualWidth of bar tracks is 0 while StatsPanel is Collapsed.
  </action>
  <verify>
    <automated>cd C:/src/gsd1 && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -v q 2>&amp;1 | tail -5</automated>
    <manual>Verify in MainWindow.xaml.cs: three new fields present (_statsTimer, _statsService, _statsIntervalSeconds); UpdateStatsDisplay() handles GpuPercent &lt; 0f branch; _statsService created in ContentRendered after _timer.Start(); _statsTimer.Tick wired but NOT started; OnClosing calls _statsTimer?.Stop() and _statsService?.Dispose() before SaveSettings()</manual>
  </verify>
  <done>dotnet build succeeds with 0 errors; all 51 PhraseEngine tests still pass (dotnet test FuzzyClock.Tests/FuzzyClock.Tests.csproj --no-restore -v q); UpdateStatsDisplay() is present; _statsService and _statsTimer are initialized in ContentRendered; OnClosing disposes both</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Build check: `dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -v q` — must show "Build succeeded" with 0 errors
2. Test check: `dotnet test FuzzyClock.Tests/FuzzyClock.Tests.csproj --no-restore -v q` — must show 51 passed
3. XAML structure check: confirm StatsPanel has Visibility="Collapsed" and Width="180"
4. Code-behind check: confirm _statsTimer is NOT started in ContentRendered (timer starts stopped)
5. Named elements: confirm all 9 x:Name elements exist in XAML (CpuBarTrack, GpuBarTrack, MemBarTrack, CpuBar, GpuBar, MemBar, CpuText, GpuText, MemText) + 4 Stats menu items (MenuShowStats, MenuInterval1, MenuInterval3, MenuInterval10)
</verification>

<success_criteria>
- dotnet build succeeds with 0 errors and 0 warnings (or same warnings as before)
- All 51 PhraseEngine unit tests pass
- MainWindow.xaml has inner Grid with two RowDefinitions (Auto + Auto)
- StatsPanel StackPanel has Width="180" and Visibility="Collapsed"
- All 9 bar/text x:Name elements present (3 track borders + 3 fill borders + 3 text blocks)
- Stats ContextMenu submenu has 4 named checkable items
- UpdateStatsDisplay() handles GPU N/A sentinel (GpuPercent == -1f)
- _statsTimer is created but NOT started (Phase 9 starts it)
- _statsService?.Dispose() called in OnClosing before SaveSettings()
</success_criteria>

<output>
After completion, create `.planning/phases/08-xaml-layout-and-stats-display/08-01-SUMMARY.md`
</output>

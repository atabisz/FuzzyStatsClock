---
phase: 08-xaml-layout-and-stats-display
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified: []
autonomous: false
requirements:
  - STAT-01
  - STAT-02

must_haves:
  truths:
    - "Widget launches and shows the time phrase exactly as in v1.1 (no layout shift, no width change, no extra height)"
    - "Stats panel is not visible by default — widget appears identical to v1.1 when stats are hidden"
    - "Right-click shows Stats submenu with Show Stats (checkable) and Update Interval (submenu with 1/3/10 second options)"
    - "When StatsPanel Visibility is temporarily forced to Visible in ContentRendered, all three rows render with bars and live percentages updating every 3 seconds"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml"
      provides: "Visual layout verified correct"
      contains: "x:Name=\"StatsPanel\""
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "Timer and display logic verified functional"
      contains: "UpdateStatsDisplay"
  key_links:
    - from: "StatsPanel Visibility=Collapsed"
      to: "Widget visual identity"
      via: "Collapsed removes all layout space — widget must look identical to v1.1"
      pattern: "Visibility=\"Collapsed\""
---

<objective>
Human verifies that Phase 8 XAML layout and stats display implementation is correct: widget looks identical to v1.1 with stats hidden, Stats submenu structure is present in right-click menu, and live stats display works correctly when temporarily forced visible.

Purpose: This phase changes the MainWindow layout and wires a new DispatcherTimer. Visual and functional verification requires running the application — automated build checks cannot confirm correct rendering, correct Collapsed behavior, or live data display.

Output: Human approval that Phase 8 implementation is correct, or description of issues for gap closure.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-xaml-layout-and-stats-display/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Temporarily force StatsPanel visible for live verification</name>
  <files>FuzzyClock.App/MainWindow.xaml.cs</files>
  <action>
Temporarily modify ContentRendered in MainWindow.xaml.cs to force stats panel visible and start the stats timer so the human verifier can see live data. This is a TEST-ONLY change that will be reverted after verification.

In the ContentRendered handler, AFTER the `_statsTimer.Tick += ...` line, add:
```csharp
// TEMPORARY: force visible for Phase 8 verification — revert after checkpoint approval
StatsPanel.Visibility = System.Windows.Visibility.Visible;
_statsTimer.Start();
```

Then run the application:
```
cd C:/src/gsd1 && dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj
```

Leave the application running for the human checkpoint in Task 2. After the checkpoint is approved, Task 3 reverts these temporary lines.
  </action>
  <verify>
    <automated>cd C:/src/gsd1 && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -v q 2>&amp;1 | tail -3</automated>
    <manual>Application launches with stats panel visible and updating — proceed to Task 2 checkpoint</manual>
  </verify>
  <done>Application runs and displays both the time phrase and the stats panel with live updating values</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live stats display, layout, and context menu</name>
  <files>FuzzyClock.App/MainWindow.xaml</files>
  <action>
Human verifies Phase 8 visual and functional correctness. The application is running with StatsPanel temporarily forced Visible (from Task 1). Verify all five checks below, then type "approved" or describe issues.
  </action>
  <verify>
    <automated>MISSING — visual rendering and live data display require human verification; Task 1 runs the app</automated>
    <manual>
With the app running:

1. LAYOUT CHECK — Widget shows time phrase in top row and stats panel below it:
   - Three labeled rows visible: CPU, GPU, MEM
   - Each row has: label on left, horizontal bar in center, percentage text on right
   - Bars are white against a semi-transparent white track
   - Percentage values are updating approximately every 3 seconds

2. BAR WIDTH CHECK — Bars reflect actual utilization:
   - CPU bar width is proportional to current CPU load (not always zero, not always full)
   - MEM bar likely shows substantial width (system memory typically 50-90% used)
   - GPU bar shows a value OR "N/A" if GPU Engine counter is unavailable on this machine

3. COLLAPSED STATE CHECK — Close the app. Remove the two temporary lines from ContentRendered. Rebuild and relaunch. Verify:
   - Widget shows ONLY the time phrase, no stats rows visible
   - Widget height is identical to v1.1 (no extra space below phrase)
   - Widget width is normal (not pinned to 180px)

4. CONTEXT MENU CHECK — Right-click the widget:
   - "Font Size" submenu present (unchanged from v1.1)
   - "Stats" submenu present with "Show Stats" (checkable) and "Update Interval" submenu (1/3/10 second items)
   - Clicking Stats items does nothing (wiring deferred to Phase 9 — expected)

5. NO LAYOUT SHIFT — With stats Collapsed:
   - Widget width matches v1.1 (sized to phrase text, not 180px)
   - No extra height below phrase
   - Left-click drag still works
    </manual>
  </verify>
  <done>Human has approved visual and functional verification of Phase 8 stats display, layout, and context menu structure</done>
</task>

<task type="auto">
  <name>Task 3: Revert temporary verification code to final Collapsed state</name>
  <files>FuzzyClock.App/MainWindow.xaml.cs</files>
  <action>
Remove the two temporary lines added in Task 1 from the ContentRendered handler:
- Remove: `StatsPanel.Visibility = System.Windows.Visibility.Visible;`
- Remove: `_statsTimer.Start();`

After removal, the ContentRendered handler must end with:
```csharp
_statsTimer.Tick += (_, _) => UpdateStatsDisplay();
// Do NOT start _statsTimer here — StatsPanel is Collapsed by default.
// Phase 9 starts it via SetStatsVisible(true) when the user enables stats.
```

If the human verifier flagged issues in Task 2, address them before running the final build check.
  </action>
  <verify>
    <automated>cd C:/src/gsd1 && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -v q 2>&amp;1 | tail -3 &amp;&amp; dotnet test FuzzyClock.Tests/FuzzyClock.Tests.csproj --no-restore -v q 2>&amp;1 | tail -3</automated>
    <manual>Confirm neither "StatsPanel.Visibility = Visible" nor "_statsTimer.Start()" appear in ContentRendered — the temp lines must be gone</manual>
  </verify>
  <done>Build succeeds with 0 errors, 51 tests pass, ContentRendered does NOT start _statsTimer or set StatsPanel visible — stats timer starts stopped, StatsPanel stays Collapsed by default</done>
</task>

</tasks>

<verification>
Final state after all tasks:

1. `dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -v q` — Build succeeded, 0 errors
2. `dotnet test FuzzyClock.Tests/FuzzyClock.Tests.csproj --no-restore -v q` — 51 passed
3. No temporary verification code remains in MainWindow.xaml.cs
4. Human verified: live bars show correct proportional widths, percentage values update every ~3s, Collapsed state is identical to v1.1, Stats submenu present in right-click menu
</verification>

<success_criteria>
- Human approved visual and functional verification of stats display
- Temporary verification lines reverted from MainWindow.xaml.cs
- Build passes with 0 errors
- 51 PhraseEngine tests pass
- Widget in final state: StatsPanel Collapsed (default), stats timer stopped, app indistinguishable from v1.1 at startup
</success_criteria>

<output>
After completion, create `.planning/phases/08-xaml-layout-and-stats-display/08-02-SUMMARY.md`
</output>

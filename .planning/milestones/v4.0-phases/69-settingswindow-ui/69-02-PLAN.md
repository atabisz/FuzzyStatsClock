---
phase: 69-settingswindow-ui
plan: 02
type: execute
wave: 1
depends_on: ["69-01"]
files_modified: ["FuzzyClock.App/MainWindow.xaml.cs"]
autonomous: true
requirements: ["PROX-06", "PROX-07"]
gap_closure: true

must_haves:
  truths:
    - "Reset to Defaults restores GhostFadeRadiusPx to 80px on the controller"
    - "SaveSettings() after ResetToDefaults() persists 80 for GhostFadeRadiusPx"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "GhostFadeRadiusPx reset in ResetToDefaults()"
      contains: "_ghostMode.GhostFadeRadiusPx = 80"
  key_links:
    - from: "MainWindow.ResetToDefaults()"
      to: "GhostModeController.GhostFadeRadiusPx"
      via: "direct assignment _ghostMode.GhostFadeRadiusPx = 80"
      pattern: "_ghostMode\\.GhostFadeRadiusPx = 80"
---

<objective>
Fix the single verification gap from 69-VERIFICATION.md: ResetToDefaults() does not
reset `_ghostMode.GhostFadeRadiusPx` to 80, so "Reset to Defaults" persists whatever
stale user-set value the controller holds instead of the documented default.

Purpose: Satisfy PROX-07 ("Reset to Defaults restores to 80px") — the only failing truth
in the phase 69 verification report.

Output: One-line fix in MainWindow.xaml.cs ResetToDefaults() method.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-settingswindow-ui/69-VERIFICATION.md
@.planning/phases/69-settingswindow-ui/69-01-SUMMARY.md

<interfaces>
<!-- From FuzzyClock.App/GhostModeController.cs -->
public int GhostFadeRadiusPx { get; set; }

<!-- From FuzzyClock.App/AppSettings.cs line 49 -->
public int GhostFadeRadiusPx { get; init; } = 80;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GhostFadeRadiusPx reset in ResetToDefaults</name>
  <files>FuzzyClock.App/MainWindow.xaml.cs</files>
  <read_first>
    - FuzzyClock.App/MainWindow.xaml.cs (lines 1165-1215, the ResetToDefaults method)
    - FuzzyClock.App/AppSettings.cs (line 49, confirm default = 80)
  </read_first>
  <action>
In `ResetToDefaults()` in MainWindow.xaml.cs, add this line immediately after
`_ghostMode.IsEnabled = true;` (line 1171):

```csharp
_ghostMode.GhostFadeRadiusPx = 80;
```

This mirrors the pattern already used for `_ghostMode.IsEnabled = true;` directly above
it. The value 80 matches `AppSettings.GhostFadeRadiusPx { get; init; } = 80` (the
documented default per PROX-06/PROX-07).

Do NOT change any other code. This is a single-line insertion.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock && dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -c Release -v q && dotnet test --no-restore -v q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GhostFadeRadiusPx = 80" FuzzyClock.App/MainWindow.xaml.cs` returns a line inside ResetToDefaults() (between lines ~1142-1212)
    - `dotnet build` succeeds with 0 errors
    - All 414 tests pass
  </acceptance_criteria>
  <done>ResetToDefaults() resets _ghostMode.GhostFadeRadiusPx to 80 before SaveSettings() is called, so "Reset to Defaults" correctly persists the default fade radius value.</done>
</task>

</tasks>

<verification>
1. `grep -n "_ghostMode.GhostFadeRadiusPx = 80" FuzzyClock.App/MainWindow.xaml.cs` — confirms the line exists in ResetToDefaults()
2. `dotnet build` — 0 errors
3. `dotnet test` — all tests pass (414 expected)
4. Re-read ResetToDefaults() to confirm the line appears after `_ghostMode.IsEnabled = true;` and before `SaveSettings()`
</verification>

<success_criteria>
- PROX-07 gap closed: ResetToDefaults() resets GhostFadeRadiusPx to 80
- Build passes, all tests pass
- No other code changed
</success_criteria>

<output>
After completion, create `.planning/phases/69-settingswindow-ui/69-02-SUMMARY.md`
</output>

---
phase: 59-ui-wiring-and-build-clean
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/MainWindow.xaml.cs
autonomous: true
requirements:
  - NIX-02
  - NIX-03
  - NIX-04
  - BACK-05

must_haves:
  truths:
    - "Hovering the widget shows a single backdrop (BackdropBorder only) — no duplicate ContentBorder backdrop appears over the phrase/clock area"
    - "Nixie is selectable in Settings Clock Style rail and activates the tube clock face on the widget (pre-existing, verified)"
    - "dotnet build exits 0 with 0 errors"
    - "dotnet test passes with 0 failures (274+ tests)"
    - "ClockType.Nixie persists to settings.json across app restart (pre-existing, verified)"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "MainWindow with ContentBorder.Background never set in code"
      contains: "BackdropBorder.Background"
  key_links:
    - from: "FuzzyClock.App/MainWindow.xaml.cs"
      to: "BackdropBorder"
      via: "sole hover backdrop element"
      pattern: "BackdropBorder\\.Background"
---

<objective>
Remove the duplicate ContentBorder hover backdrop from MainWindow and verify the full solution builds and tests clean for v3.7 Nixie Clock ship.

Purpose: BACK-05 eliminates a visual artifact where hovering the widget showed two overlapping backdrop layers (BackdropBorder + ContentBorder) on the phrase/clock area. After this change, BackdropBorder is the sole hover backdrop for the entire widget. NIX-02, NIX-03, and NIX-04 were completed in prior phases — this plan verifies they remain intact and the solution is shippable.

Output: Clean MainWindow.xaml.cs with 0 ContentBorder.Background assignments; green build; green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-ui-wiring-and-build-clean/59-CONTEXT.md
@.planning/phases/59-ui-wiring-and-build-clean/59-UI-SPEC.md

<interfaces>
<!-- BackdropBorder pattern in MainWindow.xaml.cs — these lines STAY, only ContentBorder lines are removed -->

Line ~162: BackdropBorder.Background = System.Windows.Media.Brushes.Transparent;
Line ~1025-1026: BackdropBorder.Background = new System.Windows.Media.SolidColorBrush(Color.FromArgb(alpha, 0, 0, 0));
Line ~1049-1050: BackdropBorder.Background = new System.Windows.Media.SolidColorBrush(Color.FromArgb(BackdropAlpha(), 0, 0, 0));
Line ~1070: BackdropBorder.Background = System.Windows.Media.Brushes.Transparent;
Line ~1093: BackdropBorder.Background = System.Windows.Media.Brushes.Transparent;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ContentBorder.Background assignments (BACK-05)</name>
  <files>FuzzyClock.App/MainWindow.xaml.cs</files>
  <read_first>
    - FuzzyClock.App/MainWindow.xaml.cs (full file — must see all 5 ContentBorder.Background lines in context before removing)
    - .planning/phases/59-ui-wiring-and-build-clean/59-CONTEXT.md (lines 25-37 list all 5 removal targets)
  </read_first>
  <action>
Remove exactly 5 lines from FuzzyClock.App/MainWindow.xaml.cs where ContentBorder.Background is assigned. Each removal is a single line deletion — no surrounding lines change.

**Removal 1 — _ghostMode.Restored callback (~line 160):**
Delete: `ContentBorder.Background = System.Windows.Media.Brushes.Transparent;`
The two lines after it (`if (!_backdropAlwaysVisible)` and `BackdropBorder.Background = ...`) remain untouched.

**Removal 2 — SetBackdropOpacityPercent (~line 1027-1028):**
Delete the two-line statement:
```
ContentBorder.Background = new System.Windows.Media.SolidColorBrush(
    System.Windows.Media.Color.FromArgb(alpha, 0, 0, 0));
```
The preceding BackdropBorder.Background assignment (lines 1025-1026) and the closing `}` and `SaveSettings();` remain.

**Removal 3 — Window_MouseEnter normal hover path (~line 1047-1048):**
Delete the two-line statement:
```
ContentBorder.Background = new System.Windows.Media.SolidColorBrush(
    System.Windows.Media.Color.FromArgb(BackdropAlpha(), 0, 0, 0));
```
The BackdropBorder.Background assignment on lines 1049-1050 stays.

**Removal 4 — Window_MouseEnter ghost cleanup (~line 1068):**
Delete: `ContentBorder.Background = System.Windows.Media.Brushes.Transparent;`
The `if (!_backdropAlwaysVisible)` and `BackdropBorder.Background = ...` on the next lines stay.

**Removal 5 — Window_MouseLeave (~line 1091):**
Delete: `ContentBorder.Background = System.Windows.Media.Brushes.Transparent;`
The `if (!_backdropAlwaysVisible)` and `BackdropBorder.Background = ...` on the next lines stay.

DO NOT touch any BackdropBorder.Background lines. DO NOT touch the comment on line ~1651 that mentions ContentBorder.Background (it is a documentation comment in ApplyDisplayColor explaining what is deliberately excluded from accent application).
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock &amp;&amp; grep -c "ContentBorder\.Background" FuzzyClock.App/MainWindow.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ContentBorder\.Background" FuzzyClock.App/MainWindow.xaml.cs` returns exactly 1 (the comment in ApplyDisplayColor, not an assignment)
    - The single remaining match is inside a comment line containing "// ContentBorder.Background"
    - `grep -n "ContentBorder\.Background\s*=" FuzzyClock.App/MainWindow.xaml.cs` returns 0 matches (no assignments remain)
    - All 5 BackdropBorder.Background assignment lines are still present (grep count unchanged from before edit)
  </acceptance_criteria>
  <done>All 5 ContentBorder.Background assignments removed; the sole remaining reference is the documentation comment in ApplyDisplayColor; BackdropBorder logic is untouched</done>
</task>

<task type="auto">
  <name>Task 2: Verify build and tests pass (NIX-02, NIX-03, NIX-04 confirmation)</name>
  <files>FuzzyClock.App/MainWindow.xaml.cs</files>
  <read_first>
    - FuzzyClock.App/MainWindow.xaml.cs (verify the edit from Task 1 is correct before building)
  </read_first>
  <action>
Run the full solution build and test suite to confirm:
1. `dotnet build` in the repo root exits 0 with 0 errors (CS0067 warnings for stub events are expected and acceptable)
2. `dotnet test` passes with 0 failures (expect 274+ tests — current count is 299 per CONTEXT.md)

These commands also implicitly verify:
- NIX-04: No stale `_dialMode` reference (would cause build error if present)
- NIX-02: SettingsWindow compiles with all 7 event declarations
- NIX-03: MainWindow compiles with SetClockType/NixieView wiring

If build fails, examine the error output. The only file changed is MainWindow.xaml.cs — any error is likely from an incorrect line deletion in Task 1. Fix by restoring the accidentally deleted line and re-removing only the ContentBorder.Background line.
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock &amp;&amp; dotnet build --nologo -v q 2>&amp;1 | tail -5 &amp;&amp; dotnet test --nologo -v q 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` output contains "0 Error(s)" and exits with code 0
    - `dotnet test` output shows "Passed!" with 0 failed tests
    - Test count is 274 or higher (current baseline is 299)
  </acceptance_criteria>
  <done>Full solution builds with 0 errors; all tests pass with 0 failures; v3.7 Nixie Clock is build-clean and shippable</done>
</task>

</tasks>

<verification>
Phase 59 success criteria from ROADMAP:
1. `dotnet build` exits 0 with 0 errors -- verified by Task 2 build step
2. Settings Clock Style rail shows Phrase/Dial/Nixie; Nixie activates tube face -- pre-existing from Phase 57, verified by build success (compilation proves wiring intact)
3. ClockType.Nixie persists across restart -- pre-existing from Phase 58, verified by test suite (absent-field test)
4. ContentBorder background never set in hover handlers -- verified by Task 1 grep (0 assignment matches)
5. `dotnet test` passes with 0 failures -- verified by Task 2 test step
</verification>

<success_criteria>
- `grep -n "ContentBorder\.Background\s*=" FuzzyClock.App/MainWindow.xaml.cs` returns 0 matches
- `grep -c "ContentBorder\.Background" FuzzyClock.App/MainWindow.xaml.cs` returns exactly 1 (comment only)
- `dotnet build` exits 0 with "0 Error(s)"
- `dotnet test` exits 0 with "Passed!" and 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/59-ui-wiring-and-build-clean/59-01-SUMMARY.md`
</output>

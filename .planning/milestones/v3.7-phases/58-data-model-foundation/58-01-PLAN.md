---
phase: 58-data-model-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App.Tests/AppSettingsTests.cs
autonomous: true
requirements:
  - NIX-01
  - NIX-04
must_haves:
  truths:
    - "Deserializing JSON without a ClockType field produces ClockType.Phrase"
    - "dotnet build FuzzyClock.Core exits 0 with no errors"
    - "dotnet test passes with 0 failures (299+ tests)"
  artifacts:
    - path: "FuzzyClock.App.Tests/AppSettingsTests.cs"
      provides: "Absent-field test for ClockType"
      contains: "Deserialize_MissingClockType_DefaultsToPhrase"
  key_links:
    - from: "FuzzyClock.App.Tests/AppSettingsTests.cs"
      to: "FuzzyClock.App/AppSettings.cs"
      via: "JsonSerializer.Deserialize<AppSettings>"
      pattern: "Assert\\.AreEqual\\(ClockType\\.Phrase.*result\\.ClockType"
---

<objective>
Add the missing absent-field unit test for ClockType to close Phase 58's final success criterion.

Purpose: Phase 57 already completed all data model migration work (ClockType enum in AppSettings/SettingsSnapshot, GetSegmentKey in all providers, JSON migration). The only unmet Phase 58 success criterion is "absent-field test confirms ClockType defaults to Phrase". This plan adds that test and verifies all 4 success criteria.

Output: One new test method in AppSettingsTests.cs; all success criteria verified green.
</objective>

<execution_context>
@C:/Users/AlexTabisz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AlexTabisz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-data-model-foundation/58-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ClockType absent-field test and verify all Phase 58 success criteria</name>
  <files>FuzzyClock.App.Tests/AppSettingsTests.cs</files>
  <read_first>
    - FuzzyClock.App.Tests/AppSettingsTests.cs (current test file — see existing absent-field test pattern at lines 89-100, 129-135, 162-196)
    - FuzzyClock.App/AppSettings.cs (source of truth for ClockType init default — line 27: `public ClockType ClockType { get; init; } = ClockType.Phrase;`)
    - FuzzyClock.App/ClockType.cs (enum definition: `public enum ClockType { Phrase, Dial, Lcd, Nixie }`)
  </read_first>
  <behavior>
    - Test: Deserializing `{"FontSize":32}` (JSON with no ClockType field) into AppSettings produces result.ClockType == ClockType.Phrase
  </behavior>
  <action>
Add one new test method to AppSettingsTests.cs, placed after the existing `Deserialize_MissingLcdSize_DefaultsToMedium` test (after line 197, before the closing brace of the class). The test follows the exact same pattern as the existing absent-field tests (e.g., `Deserialize_MissingShowDate_DefaultsToTrue` at line 129).

New method to add:

```csharp
[TestMethod]
public void Deserialize_MissingClockType_DefaultsToPhrase()
{
    const string json = """{"FontSize":32}""";
    var result = JsonSerializer.Deserialize<AppSettings>(json)!;
    Assert.AreEqual(ClockType.Phrase, result.ClockType,
        "ClockType should default to Phrase when absent from JSON (init default)");
}
```

No STEST ID comment needed — this follows the pattern of the TextStyle absent-field test (line 119) which also has no STEST ID.

After adding the test, run:
1. `dotnet test FuzzyClock.App.Tests` — new test must pass (299+ total, 0 failures)
2. `dotnet build FuzzyClock.Core` — must exit 0 with 0 errors (confirms NIX-04 GetSegmentKey)
3. `dotnet build` — full solution must exit 0 with 0 errors

Do NOT modify AppSettings.cs, SettingsSnapshot.cs, SettingsService.cs, or any PhraseProvider file — all migration work is already complete from Phase 57.
  </action>
  <verify>
    <automated>dotnet test FuzzyClock.App.Tests --filter "FullyQualifiedName~Deserialize_MissingClockType_DefaultsToPhrase" --no-restore &amp;&amp; dotnet test FuzzyClock.slnx --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Deserialize_MissingClockType_DefaultsToPhrase" FuzzyClock.App.Tests/AppSettingsTests.cs returns 1
    - grep "Assert.AreEqual(ClockType.Phrase, result.ClockType" FuzzyClock.App.Tests/AppSettingsTests.cs returns a match
    - `dotnet test FuzzyClock.App.Tests --filter "FullyQualifiedName~Deserialize_MissingClockType_DefaultsToPhrase"` exits 0 with "Passed: 1"
    - `dotnet build FuzzyClock.Core` exits 0 with "0 Error(s)"
    - `dotnet test FuzzyClock.slnx` exits 0 with 0 failures (299+ tests)
  </acceptance_criteria>
  <done>
    - New test method Deserialize_MissingClockType_DefaultsToPhrase exists in AppSettingsTests.cs
    - Test passes: deserializing JSON without ClockType yields ClockType.Phrase
    - Full test suite passes with 0 failures
    - Full solution builds with 0 errors
  </done>
</task>

</tasks>

<verification>
All four Phase 58 ROADMAP success criteria verified:
1. `dotnet build FuzzyClock.Core` exits 0 (already true from Phase 57)
2. AppSettings has ClockType field, not DialMode; dialMode:true upgrades to ClockType.Dial (already true from Phase 57)
3. SettingsSnapshot has all 7 required fields (already true from Phase 57)
4. Absent-field test confirms ClockType defaults to Phrase (NEW — added by this plan)
</verification>

<success_criteria>
- Deserialize_MissingClockType_DefaultsToPhrase test exists and passes
- Full solution builds with 0 errors
- Full test suite passes with 0 failures (299+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/58-data-model-foundation/58-01-SUMMARY.md`
</output>

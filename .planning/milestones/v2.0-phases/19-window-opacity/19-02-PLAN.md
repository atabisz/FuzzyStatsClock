---
phase: 19-window-opacity
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified: []
autonomous: false
requirements:
  - OPAC-01
  - OPAC-02
  - OPAC-03

must_haves:
  truths:
    - "Opacity preset menu (25/50/75/100%) is visible in the right-click menu and each preset produces visible transparency change"
    - "Checkmark in the Opacity submenu correctly reflects the active preset on every menu open"
    - "Scroll wheel adjusts opacity in 10% steps; floor of 10% is enforced (widget remains interactable)"
    - "Opacity persists across widget restart — scroll-adjusted value is restored, not just last preset value"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml"
      provides: "Opacity submenu visible in running application"
      contains: "MenuOpacity25"
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "Opacity runtime logic verified at runtime"
      contains: "_windowOpacity"
  key_links:
    - from: "Right-click Opacity submenu"
      to: "Window.Opacity change visible to user"
      via: "SetOpacity() -> this.Opacity — uniform fade of all widget content"
      pattern: "visual transparency change confirmed by user"
---

<objective>
Human verification that all three opacity requirements (OPAC-01 preset menu, OPAC-02 scroll wheel, OPAC-03 window-level fade) are working correctly at runtime.

Purpose: Confirms the implementation from Plan 01 works end-to-end — right-click menu shows opacity presets, scroll wheel adjusts opacity, transparency applies uniformly to all widget content, and the setting persists across restarts.

Output: Human approval confirming Phase 19 complete.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-window-opacity/19-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify all four Phase 19 success criteria</name>
  <files>none — observation only</files>
  <action>Human verification of all four Phase 19 behavioral requirements implemented in Plan 19-01.</action>
  <what-built>
    Opacity submenu in right-click context menu with four IsCheckable presets (25%, 50%, 75%, 100%) and checkmark sync in ContextMenu_Opened(). Scroll wheel handler (PreviewMouseWheel, tunneling) adjusts opacity in 10% increments with a 10% floor enforced via Math.Clamp. Window.Opacity (UIElement.Opacity) applied at the Window level — fades phrase text, dial hands, stats bars, hover backdrop uniformly. Opacity persisted to settings.json via SaveSettings() and restored via ApplySettings() on next launch.
  </what-built>
  <how-to-verify>
    Build and launch the widget:
    ```
    cd C:/src/FuzzyStatsClock
    dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj
    ```

    **SC-1 (OPAC-01): Opacity preset menu structure**
    1. Right-click the widget — confirm "Opacity" submenu is present between "Dial Face" and "Close"
    2. Hover over "Opacity" — confirm four entries: 25%, 50%, 75%, 100%
    3. Confirm 100% has a checkmark (default, widget launched at full opacity)

    **SC-2 (OPAC-01 + OPAC-03): Preset changes apply immediately and uniformly**
    4. Click "50%" — the entire widget (phrase text or dial hands, stats bars if visible, hover backdrop on hover) should visibly fade to 50% opacity
    5. Right-click again — confirm 50% now has the checkmark; 100% does not
    6. Click "25%" — widget should be visibly more transparent (nearly ghost-like but still present)
    7. Click "100%" — widget should return to fully opaque
    8. Confirm the fade is uniform — no element appears at different opacity than others

    **SC-3 (OPAC-02): Scroll wheel adjustment**
    9. Position mouse over the widget and scroll down — opacity should decrease by ~10% per notch
    10. Scroll down many times — verify the widget does NOT become fully invisible (floor at 10%)
    11. Verify the widget is still right-clickable and left-click-draggable at minimum opacity
    12. Scroll up — opacity should increase by ~10% per notch back toward 100%
    13. Right-click at an intermediate value (e.g. ~60%) — confirm NO preset checkmark is shown

    **SC-4 (OPAC-02 + OPAC-03 persistence): Scroll-adjusted opacity persists**
    14. Scroll to approximately 60% opacity (visibly translucent but content readable)
    15. Close the widget (right-click → Close)
    16. Relaunch: `dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj`
    17. Confirm widget launches at ~60% opacity (not at 100%)
    18. Right-click — confirm no preset checkmark (60% is not a preset value)

    **Note on backdrop behavior at low opacity:** At 25% opacity, the hover backdrop (#59000000) appears very faint (~8.75% effective alpha due to Window.Opacity multiplying over AllowsTransparency per-pixel alpha). This is expected and acceptable behavior — the widget remains interactable.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual opacity behavior requires human observation</automated>
    <manual>Run app, test all 18 steps as described in how-to-verify above</manual>
  </verify>
  <done>All four Phase 19 success criteria confirmed by user: Opacity submenu present with correct checkmarks (SC-1), preset changes apply uniformly (SC-2), scroll wheel adjusts with 10% floor (SC-3), scroll-adjusted opacity persists across restart (SC-4).</done>
  <resume-signal>Type "approved" if all checks pass, or describe which step failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all four Phase 19 success criteria:
1. SC-1: Opacity submenu visible with 25/50/75/100% presets; checkmark reflects active level (OPAC-01)
2. SC-2: Preset click changes apply uniformly to entire widget (OPAC-01, OPAC-03)
3. SC-3: Scroll wheel adjusts in 10% increments; floor at 10% enforced; no checkmark at intermediate values (OPAC-02)
4. SC-4: Scroll-adjusted opacity persists across restart (OPAC-02, OPAC-03)
</verification>

<success_criteria>
Human confirms "approved" after testing all 18 verification steps. All OPAC-01, OPAC-02, OPAC-03 requirements verified from the user's perspective.
</success_criteria>

<output>
After human approval, create `.planning/phases/19-window-opacity/19-02-SUMMARY.md`

Then update:
- `.planning/ROADMAP.md` — mark Phase 19 complete: 0/TBD -> 2/2, Status: Complete, date: today
- `.planning/REQUIREMENTS.md` — mark OPAC-01, OPAC-02, OPAC-03 as [x] complete
- `.planning/STATE.md` — Phase 19 complete; current focus: Phase 20 (Accent Color Presets)
</output>

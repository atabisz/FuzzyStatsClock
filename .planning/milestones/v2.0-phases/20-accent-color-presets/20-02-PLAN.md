---
phase: 20-accent-color-presets
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified: []
autonomous: false
requirements:
  - THEME-01
  - THEME-03

must_haves:
  truths:
    - "Theme submenu is visible in the right-click context menu with 5 named presets"
    - "The active preset has a checkmark every time the menu is opened"
    - "Clicking a non-white preset immediately recolors all 14 accent elements uniformly"
    - "Bar track backgrounds, shadow text, and row labels are visually unchanged after preset selection"
    - "Relaunching the widget restores the last-selected accent color"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml"
      provides: "Theme submenu visible in running widget"
      contains: "MenuThemeWhite"
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "ApplyTheme() runtime behavior verified"
      contains: "ApplyTheme"
  key_links:
    - from: "context menu Theme submenu"
      to: "widget visual accent color"
      via: "preset click triggers SetAccentColor -> ApplyTheme -> element brush assignments"
      pattern: "visual color change confirmed by user"
---

<objective>
Human verification that all Phase 20 accent color preset requirements (THEME-01 and THEME-03) are working correctly at runtime.

Purpose: Confirms the implementation from Plan 01 works end-to-end — Theme submenu visible with checkmarks, preset selection recolors all 14 accent elements, excluded elements are unchanged, and the color persists across restarts.

Output: Human approval confirming Phase 20 complete.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-accent-color-presets/20-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify all four Phase 20 success criteria</name>
  <files>none — observation only</files>
  <action>Human verification of all four Phase 20 behavioral requirements implemented in Plan 20-01.</action>
  <what-built>
    Theme submenu in right-click context menu with five IsCheckable presets (White, Amber, Ice Blue, Green, Hello Kitty Pink) and checkmark sync in ContextMenu_Opened() via hex comparison against _accentColor. ApplyTheme() covering 14 accent elements: PhraseText.Foreground, HourHand.Stroke, MinuteHand.Stroke, _hourTickElements[].Stroke, _minuteDotElements[].Fill, _hourNumberElements[].Foreground, CpuBar/GpuBar/MemBar/PagBar.Background, CpuText/GpuText/MemText/PagText.Foreground. Excluded elements: ShadowText, bar track borders (#40FFFFFF), row label TextBlocks (no x:Name). AccentColor persisted to settings.json and restored via ApplySettings() + ContentRendered ordering constraint (ApplyTheme called after InitDialDecorations).
  </what-built>
  <how-to-verify>
    Build and launch the widget:
    ```
    cd C:/src/FuzzyStatsClock
    dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj
    ```

    **SC-1 (THEME-01): Theme submenu structure and checkmarks**
    1. Right-click the widget — confirm a "Theme" submenu is present (should appear above "Opacity")
    2. Hover over "Theme" — confirm five entries: White, Amber, Ice Blue, Green, Hello Kitty Pink
    3. Confirm "White" has a checkmark (default color on fresh launch)
    4. Click "Amber"
    5. Right-click again — confirm "Amber" now has the checkmark and "White" does not

    **SC-2 (THEME-03): All 14 accent elements recolor on preset selection**
    With Amber selected (warm yellow-orange color):
    - In phrase mode: phrase text should be amber, NOT white
    - Switch to Dial Mode (right-click → Dial Mode): both hour and minute hands should be amber
    - Enable Show Hour Ticks (right-click → Dial Face → Show Hour Ticks): tick marks should be amber
    - Enable Show Minute Marks: minute dots should be amber
    - Enable Show Hour Numbers: hour number labels should be amber
    - Show Stats (right-click → Stats → Show Stats): fill bars (CPU/GPU/MEM/PAG) should be amber, percentage text values should be amber

    **SC-3 (THEME-03): Excluded elements unchanged**
    With Amber selected:
    - Shadow text (the dark offset copy behind the phrase in phrase mode) remains dark, NOT amber
    - Bar track backgrounds (the container border behind fill bars) remain semi-transparent white, NOT amber
    - Row label text (the "CPU" / "GPU" / "MEM" / "PAG" labels in column 0 of the stats panel) remains white, NOT amber

    **SC-4 (THEME-03 persistence): Accent color restores on relaunch**
    1. Select "Hello Kitty Pink" from the Theme submenu
    2. Close the widget (right-click → Close)
    3. Relaunch: `dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj`
    4. Phrase text should be pink on launch (not white) — confirms ContentRendered ordering is correct
    5. Right-click → Theme submenu → confirm "Hello Kitty Pink" has the checkmark

    **Additional: verify White restores all elements**
    6. Select "White" — phrase text, hands, bars, and percentage text should all return to white
    7. Right-click → confirm "White" has the checkmark
  </how-to-verify>
  <verify>
    <automated>MISSING — visual color behavior requires human observation</automated>
    <manual>Run app, test all steps as described in how-to-verify above</manual>
  </verify>
  <done>All four Phase 20 success criteria confirmed by user: Theme submenu with 5 presets and correct checkmarks (SC-1), all 14 accent elements recolor on preset selection (SC-2), excluded elements unchanged (SC-3), accent color persists across restart (SC-4).</done>
  <resume-signal>Type "approved" if all checks pass, or describe which step failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all four Phase 20 success criteria:
1. SC-1: Theme submenu with 5 named presets; checkmark reflects active preset on every menu open (THEME-01)
2. SC-2: Preset selection immediately recolors all 14 accent elements (THEME-03)
3. SC-3: Bar tracks, shadow text, and row labels are NOT affected (THEME-03 exclusions)
4. SC-4: Accent color persists across widget restart; dial decorations colored correctly on launch (THEME-03)
</verification>

<success_criteria>
Human confirms "approved" after testing all verification steps. All THEME-01 and THEME-03 requirements verified from the user's perspective.
</success_criteria>

<output>
After human approval, create `.planning/phases/20-accent-color-presets/20-02-SUMMARY.md`

Then update:
- `.planning/ROADMAP.md` — mark Phase 20 complete: 0/TBD -> 2/2, Status: Complete, date: today
- `.planning/REQUIREMENTS.md` — mark THEME-01 and THEME-03 as [x] complete
- `.planning/STATE.md` — Phase 20 complete; current focus: Phase 21 (Custom Color Picker)
</output>

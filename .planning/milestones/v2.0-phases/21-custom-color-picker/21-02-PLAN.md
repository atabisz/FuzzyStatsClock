---
phase: 21-custom-color-picker
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified: []
autonomous: false
requirements:
  - THEME-02

must_haves:
  truths:
    - "Theme submenu contains a Separator and 'Custom...' entry below the five presets"
    - "Clicking 'Custom...' opens the native Windows ColorDialog in front of the always-on-top widget"
    - "Confirming a color applies it immediately to all 14 accent elements"
    - "Canceling the dialog leaves the current accent color unchanged"
    - "No preset checkmark shown when a custom color is active"
    - "Custom color survives app restart"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml"
      provides: "Custom... entry visible in running Theme submenu"
      contains: "MenuThemeCustom"
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "MenuThemeCustom_Click runtime behavior verified"
      contains: "MenuThemeCustom_Click"
  key_links:
    - from: "Theme submenu Custom... entry"
      to: "native Windows ColorDialog in front of widget"
      via: "WindowInteropHelper HWND owner passed to ShowDialog"
      pattern: "dialog visible above widget"
---

<objective>
Human verification that all five Phase 21 success criteria (THEME-02) are working correctly at runtime.

Purpose: Confirms the ColorDialog integration works end-to-end — dialog appears in front of the widget, color applies immediately across all accent elements, cancel leaves color unchanged, no preset checkmark on custom, and color persists across restarts.
Output: Human approval confirming Phase 21 and v2.0 complete.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-custom-color-picker/21-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify all five Phase 21 success criteria</name>
  <files>none — observation only</files>
  <action>Human verification of all five Phase 21 behavioral requirements (THEME-02) implemented in Plan 21-01.</action>
  <what-built>
    Plan 21-01 added UseWindowsForms=true to the csproj, a Separator + "Custom..." MenuItem (non-checkable) to the Theme submenu in MainWindow.xaml, and MenuThemeCustom_Click in MainWindow.xaml.cs with a Win32Window HWND adapter (WindowInteropHelper), ColorDialog pre-seeded with the current accent color, AllowFullOpen+FullOpen=true for the expanded custom color panel, and SetAccentColor() called on OK. Cancel falls through without side effects. ContextMenu_Opened hex-comparison already handles the "no checkmark for custom color" case — no changes to that logic were needed.
  </what-built>
  <how-to-verify>
    Build and launch the widget:
    ```
    cd C:/src/FuzzyStatsClock
    dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj
    ```

    **SC-1: Custom... entry exists in Theme submenu**
    1. Right-click the widget → hover over "Theme"
    2. Confirm the Theme submenu shows: White, Amber, Ice Blue, Green, Hello Kitty Pink, then a separator line, then "Custom..."
    Pass: all seven entries visible with separator in the correct position.

    **SC-2: ColorDialog opens in front of the widget**
    1. Click "Custom..." in the Theme submenu
    2. Confirm: the native Windows color picker dialog appears in front of the always-on-top widget — NOT behind it, NOT on another monitor, NOT causing the widget to freeze
    3. Confirm: the dialog opens with the full custom color panel (HSV sliders / RGB input / hex input) already expanded — not just the basic 48-swatch palette
    4. Confirm: the dialog's current color matches the active accent color (pre-seeded correctly)
    Pass: dialog visible, interactive, expanded, showing the current accent color.

    **SC-3: Confirming a color applies it immediately**
    1. In the dialog, pick a visually distinctive color (e.g., bright red or orange — clearly different from white)
    2. Click OK
    3. Confirm: the phrase text recolors to the chosen color immediately (no restart required)
    4. Optionally verify additional elements: dial hands (if in dial mode), stats bars/text (if stats visible)
    Pass: color change visible immediately across accent elements.

    **SC-4: Canceling leaves current color unchanged**
    1. Right-click → Theme → Custom...
    2. In the dialog, move the color selector to a completely different color
    3. Click Cancel (or press Escape)
    4. Confirm: the widget accent color is still the color chosen in SC-3 (no change)
    Pass: current accent color preserved after cancel.

    **SC-5: No preset checkmark when custom color is active**
    1. After SC-3 (a custom color is active, e.g., bright red)
    2. Right-click → Theme submenu
    3. Confirm: none of the five preset entries (White, Amber, Ice Blue, Green, Hello Kitty Pink) has a checkmark
    Pass: all five presets unchecked.

    **SC-6: Custom color persists across restart**
    1. After SC-3 (a non-preset custom color is active)
    2. Close the widget entirely (right-click → Close or close the process)
    3. Relaunch: `dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj`
    4. Confirm: the widget launches with the custom color still applied (phrase text / stats / dial show the custom color, not white)
    Pass: custom color restored exactly as chosen, not rounded to nearest preset.
  </how-to-verify>
  <verify>
    <automated>MISSING — ColorDialog visibility and runtime color behavior require human observation</automated>
    <manual>Run app, test all six steps as described in how-to-verify above</manual>
  </verify>
  <done>All five Phase 21 success criteria confirmed by user: Custom... entry present below separator (SC-1), ColorDialog opens in front of widget (SC-2), confirming applies color immediately (SC-3), canceling leaves color unchanged (SC-4), no preset checkmark when custom active (SC-5), custom color persists across restart (SC-6).</done>
  <resume-signal>Type "approved" if all six checks pass, or describe which step failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all five Phase 21 success criteria (THEME-02):
1. SC-1: "Custom..." entry present in Theme submenu below separator
2. SC-2: ColorDialog opens in front of the always-on-top widget (HWND owner works)
3. SC-3: Confirming a color applies it immediately to all 14 accent elements
4. SC-4: Canceling dialog leaves current accent color unchanged
5. SC-5: No preset checkmark shown when a non-preset custom color is active
6. SC-6: Custom color restored on next widget launch
</verification>

<success_criteria>
Human confirms "approved" after testing all six verification steps. THEME-02 verified from the user's perspective. v2.0 Visual Identity milestone complete.
</success_criteria>

<output>
After human approval, create `.planning/phases/21-custom-color-picker/21-02-SUMMARY.md`

Then update:
- `.planning/ROADMAP.md` — mark Phase 21 complete: 0/? -> 2/2, Status: Complete, date: today; update Progress table
- `.planning/REQUIREMENTS.md` — mark THEME-02 as [x] complete
- `.planning/STATE.md` — Phase 21 complete; v2.0 Visual Identity milestone complete; Progress: 100%
</output>

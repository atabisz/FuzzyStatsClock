---
phase: 64-blinking-colon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/Controls/LcdClockView.xaml.cs
autonomous: true
requirements:
  - LCD-06

must_haves:
  truths:
    - "Colon1 alternates between lit and unlit every second when LCD is active"
    - "Colon2 (seconds separator) remains always lit — it never blinks"
    - "No new DispatcherTimer exists in the codebase"
  artifacts:
    - path: "FuzzyClock.App/Controls/LcdClockView.xaml.cs"
      provides: "_colonVisible bool field; Colon1.Character toggled each UpdateTime() call"
      contains: "_colonVisible"
  key_links:
    - from: "LcdClockView._timer (1s)"
      to: "LcdClockView.UpdateTime()"
      via: "existing Tick handler"
      pattern: "_colonVisible\\s*=\\s*!_colonVisible"
---

<objective>
Add a blinking colon to the LCD clock face by toggling a `_colonVisible` boolean field on each `UpdateTime()` call. Colon1 (HH:MM separator) blinks; Colon2 (MM:SS separator) never blinks.

Purpose: Satisfies LCD-06 — the canonical LCD clock aesthetic where the colon pulses at 1 Hz.
Output: Two-line change to LcdClockView.xaml.cs; build and tests pass with no regressions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle _colonVisible in LcdClockView.UpdateTime()</name>
  <files>FuzzyClock.App/Controls/LcdClockView.xaml.cs</files>
  <action>
    Make exactly two edits to LcdClockView.xaml.cs:

    1. Add a private field after the existing private fields (around line 131):
       ```csharp
       private bool _colonVisible = true;
       ```

    2. In UpdateTime(), replace the single line:
       ```csharp
       Colon1.Character = ':';
       ```
       with:
       ```csharp
       _colonVisible    = !_colonVisible;
       Colon1.Character = _colonVisible ? ':' : ' ';
       ```

    Do NOT touch Colon2 — it keeps `Colon2.Character = ':'` (always lit, no blink).
    Do NOT add any new DispatcherTimer, field, or method beyond the one field above.
    `SevenSegmentEncoder.Encode(' ')` returns 0x00 (all segments off), so space renders as fully unlit — this is the correct off-state for the colon segment.
  </action>
  <verify>
    <automated>dotnet build C:/src/FuzzyStatsClock/FuzzyClock.sln -v quiet 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Build produces 0 errors and 0 new warnings. _colonVisible field exists. Colon1.Character assignment uses the ternary; Colon2.Character is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Full test gate</name>
  <files></files>
  <action>
    Run the full test suite to confirm no regressions. No code changes in this task.
  </action>
  <verify>
    <automated>dotnet test C:/src/FuzzyStatsClock/FuzzyClock.sln --no-build -v quiet 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All tests pass (>=351), 0 failures.</done>
</task>

</tasks>

<verification>
- `_colonVisible` field declared in LcdClockView.xaml.cs
- `_colonVisible = !_colonVisible` toggles on every UpdateTime() call
- `Colon1.Character` assigned via ternary (`:` or ` `)
- `Colon2.Character = ':'` unchanged
- No new DispatcherTimer anywhere in the diff
- `dotnet build` 0 errors, 0 new warnings
- `dotnet test` >=351 passing, 0 failures
</verification>

<success_criteria>
When LCD clock style is active, Colon1 visibly alternates lit/unlit each second. Colon2 remains always lit when the seconds row is visible. The codebase contains no new timer. Build and all tests pass clean.
</success_criteria>

<output>
After completion, create `.planning/phases/64-blinking-colon/64-blinking-colon-01-SUMMARY.md`
</output>

---
phase: 51-readme-docs-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-04

must_haves:
  truths:
    - "README describes Settings window (how to open, three tabs, named themes)"
    - "README describes English phrase styles and language selection"
    - "README describes installer (FuzzyClockSetup.exe) with SmartScreen workaround"
    - "README describes edge snapping, single-instance behavior, and dark-mode Settings"
    - "README describes phrase wrapping with two split styles and configuration"
    - "README tray menu table matches the actual pruned tray menu (8 items)"
    - "README test count matches actual dotnet test output"
  artifacts:
    - path: "README.md"
      provides: "Complete user-facing documentation for v3.5"
      contains: "Settings Window"
  key_links:
    - from: "README.md"
      to: "FuzzyClock.App/TrayMenuBuilder.cs"
      via: "tray menu table matches actual menu items"
      pattern: "Open Settings"
---

<objective>
Update README.md to document all features shipped in v3.2 through v3.5: Settings window (3 tabs), named themes, English phrase style personalities, multilingual language selection, installer with SmartScreen guidance, edge snapping, single-instance behavior, dark-mode Settings styling, and phrase wrapping.

Purpose: DOCS-04 requires the README to accurately reflect the current feature set. The README currently documents through ~v2.8 and is missing nine feature areas.
Output: Updated README.md with all v3.2-v3.5 features documented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-readme-docs-pass/51-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README features, tray table, installation, and settings documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state -- 136 lines, documents through ~v2.8)
    - FuzzyClock.App/TrayMenuBuilder.cs (exact tray menu items and labels)
    - FuzzyClock.App/SettingsWindow.xaml (tab names, controls, theme cards)
    - FuzzyClock.App/AppSettings.cs (PhraseStyle, PhraseLocale, PhraseWrapEnabled, PhraseWrapStyle, Theme fields)
    - FuzzyClock.Core/PhraseWrapService.cs (wrap trigger, split styles, allowNatural)
    - FuzzyClock.App/App.xaml.cs (single-instance Mutex name, named pipe name)
    - FuzzyClock.App/MainWindow.xaml.cs (EdgeSnapThresholdPx constant)
    - FuzzyClock.iss (installer output name, install path)
  </read_first>
  <action>
Run `dotnet test FuzzyClock.slnx --nologo --no-restore -v q` first to get the exact current test count.

Then update README.md with all changes below. Preserve the existing document structure. All additions are specified by exact location and content.

**1. Features section -- ADD these new bullets** after the existing "Persistence" bullet (last current feature):

```markdown
- **Settings window** — open from the system tray ("Open Settings..."); three tabs organize all preferences:
  - *Appearance* — named themes, accent color, opacity, font size, clock style, phrase style, phrase wrap
  - *Stats* — stats panel visibility, individual row toggles, update interval, process threshold, date display
  - *Behavior* — phrase language, ghost mode, auto-contrast, auto-launch, battery alert threshold
- **Named themes** — five one-click theme presets in the Settings Appearance tab: Midnight, Neon, Ghost, Warm, Terminal; each sets a coordinated accent color
- **Phrase styles** — four English phrase personalities: Classic (default natural), Terse (compact), Poetic (literary), Rude (irreverent); selectable in Settings Appearance tab (English locales only)
- **Multilingual phrases** — time phrases available in English, French, Spanish, German, Japanese, and Polish; auto-detects from Windows display language or override in Settings Behavior tab
- **Phrase wrapping** — when a phrase is more than 10% wider than the stats panel, it automatically splits across two lines; two split styles: Nearest Midpoint (word break closest to string center, default) and Natural Pause (split after first grammatical beat, English only); configurable in Settings Appearance tab
- **Edge snapping** — dragging the widget to within 8 pixels of any screen edge snaps it flush to that edge; respects the taskbar working area
- **Single instance** — launching the app when it is already running brings the existing window to the front instead of opening a second instance
- **Battery low alert** — battery stat row background turns red when charge drops below a configurable threshold (10%/15%/20%) and the device is unplugged; configurable in Settings Behavior tab
- **Dark-mode Settings** — the Settings window uses a dark background with light text matching the widget's minimal aesthetic
```

**2. Features section -- MODIFY existing bullets:**

- Change the "Accent colors" bullet to: `- **Accent colors** — five quick presets (White, Amber, Ice Blue, Green, Hello Kitty Pink) via tray or Settings, or pick any custom color; applies to all text, hands, bars, and decorations`
- Change the "Window opacity" bullet to: `- **Window opacity** — 25% / 50% / 75% / 100% via Settings or scroll wheel in 10% steps`
- Change the "Font size" bullet to: `- **Font size** — Small (16pt) / Medium (24pt) / Large (32pt) / Extra Large (40pt) via Settings (phrase mode only)`

**3. Add new Installation section** -- insert AFTER the "Requirements" section and BEFORE the "Build" section:

```markdown
## Installation

Download `FuzzyClockSetup-X.Y.Z.exe` from the latest [GitHub Release](../../releases/latest) and run it. The installer places the app in `%LOCALAPPDATA%\Programs\FuzzyClock\` with no admin elevation required.

A standalone portable `FuzzyClock-X.Y.Z.exe` is also available from the same release page.

> **SmartScreen warning:** The binary is not code-signed. Windows SmartScreen may show a warning on first run. Click **More info** then **Run anyway** to proceed.

Upgrading: run the new installer over the existing installation. Settings are preserved. If the app is running, the installer will prompt you to close it first.

Uninstalling: use **Settings > Apps > Installed Apps** or the Start Menu uninstaller. App files are removed; `settings.json` is preserved by default (an optional checkbox during uninstall lets you remove it).
```

**4. Update Test section** -- replace the line starting with "122 unit tests:" with the actual count from the `dotnet test` output. Use the format: `{N} unit tests: phrase engine, dial geometry, uptime formatter, date formatter, phrase wrap service, settings validation and migration, and app integration tests.` (Add "phrase wrap service" to the list.)

**5. Update tray menu table** -- replace the entire table in the "Right-click context menu" section with the actual pruned menu:

```markdown
| Item | Description |
|------|-------------|
| **Open Settings...** | Open the Settings window (Appearance / Stats / Behavior tabs) |
| **Ghost Mode** | Enable/disable hover-to-hide (checkmark = active) |
| **Show Stats** | Show/hide the stats panel (checkmark = active) |
| **Auto-Contrast** | Enable/disable automatic text contrast adjustment (checkmark = active) |
| **Auto-Launch at Login** | Enable/disable start at Windows login (checkmark = active) |
| **Reset to Defaults** | Restore factory settings (accent, opacity, font size, phrase style, locale, all toggles) |
| **About** | Show version information |
| **Quit** | Exit the application |
```

**6. Update the "System tray" subsection** -- replace the paragraph and toggle list at lines 96-101 with:

```markdown
The tray icon is the primary UI surface. Right-click it to access quick toggles (Ghost Mode, Show Stats, Auto-Contrast, Auto-Launch) and open the full Settings window. All detailed configuration (themes, phrase style, language, stats rows, date format, battery alert, phrase wrap) lives in the Settings window.
```

**7. Remove the stale toggle items list** (lines 97-101 starting with "Toggle items available...") since it is now covered by the updated paragraph above and the tray table.

**8. Update Project Structure tree** -- add these entries:
- Under `FuzzyClock.Core/`: add `PhraseWrapService` to the comment: `# Pure logic (PhraseEngine, DialGeometry, UptimeFormatter, DateFormatter, PhraseWrapService, ContrastService)`
- Under `FuzzyClock.App/`: add a line: `│   ├── SettingsWindow.xaml(.cs)     # Three-tab settings UI (Appearance / Stats / Behavior)`

**9. Add a "Settings Window" subsection** in the Usage section, AFTER the "System tray" subsection:

```markdown
### Settings Window

Open from the tray menu ("Open Settings..."). The window has three tabs:

**Appearance** — Named theme presets (Midnight / Neon / Ghost / Warm / Terminal), accent color swatches and custom picker, opacity slider, font size, clock style (Phrase / Dial), phrase style (Classic / Terse / Poetic / Rude), and phrase wrap controls (enable/disable, split style).

**Stats** — Stats panel toggle, individual row visibility (CPU / GPU / Memory / Paging / Battery / Uptime), update interval, process count threshold, date visibility, and date format.

**Behavior** — Phrase language (Auto / English / French / Spanish / German / Japanese / Polish), ghost mode, auto-contrast, auto-launch at login, and battery alert threshold (10% / 15% / 20%).
```
  </action>
  <verify>
    <automated>grep -c "Settings window" README.md && grep -c "Named themes" README.md && grep -c "Phrase styles" README.md && grep -c "Phrase wrapping" README.md && grep -c "Edge snapping" README.md && grep -c "Single instance" README.md && grep -c "SmartScreen" README.md && grep -c "Open Settings" README.md && grep -c "Natural Pause" README.md && grep -c "Multilingual" README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "Settings window" README.md returns at least 2 matches (feature bullet + section)
    - grep "Named themes" README.md returns at least 1 match
    - grep "Phrase styles" README.md returns at least 1 match
    - grep "Multilingual phrases" README.md returns at least 1 match
    - grep "Phrase wrapping" README.md returns at least 1 match
    - grep "Edge snapping" README.md returns at least 1 match
    - grep "Single instance" README.md returns at least 1 match
    - grep "SmartScreen" README.md returns at least 1 match
    - grep "Open Settings" README.md returns at least 2 matches (tray table + settings section)
    - grep "Natural Pause" README.md returns at least 1 match
    - grep "Battery low alert" README.md returns at least 1 match
    - grep "FuzzyClockSetup" README.md returns at least 1 match
    - grep "PhraseWrapService" README.md returns at least 1 match (project structure)
    - grep "SettingsWindow" README.md returns at least 1 match (project structure)
    - The tray menu table does NOT contain "Font Size", "Dial Face", "Date Format", "Theme", "Opacity" rows (these moved to Settings window)
    - grep "122 unit tests" README.md returns 0 matches (stale count replaced)
    - The Installation section appears before the Build section
  </acceptance_criteria>
  <done>README.md documents all v3.2-v3.5 features per DOCS-04 success criteria: Settings window with 3 tabs, named themes, phrase styles, multilingual, installer with SmartScreen workaround, edge snapping, single-instance, dark-mode Settings, phrase wrapping with both split styles, and accurate tray menu table and test count.</done>
</task>

</tasks>

<verification>
1. All DOCS-04 success criteria met:
   - SC1: README describes Settings window (how to open, three tabs) and named themes
   - SC2: README describes English phrase style personalities and language selection
   - SC3: README Installation section references FuzzyClockSetup.exe and documents SmartScreen workaround
   - SC4: README describes edge snapping, single-instance behavior, and dark-mode Settings
   - SC5: README describes phrase wrapping (trigger, two split styles, configuration)
2. Tray menu table matches actual TrayMenuBuilder.cs (8 items: Open Settings, Ghost Mode, Show Stats, Auto-Contrast, Auto-Launch, Reset to Defaults, About, Quit)
3. Test count updated from dotnet test output
4. No stale references to old tray submenus (Theme/Font Size/Dial Face/Date Format/Opacity removed from tray table)
</verification>

<success_criteria>
- README.md contains documentation for all 10 missing feature areas identified in research
- Tray menu table has exactly 8 items matching TrayMenuBuilder.cs
- Installation section exists with FuzzyClockSetup.exe reference and SmartScreen guidance
- Settings Window subsection exists with all three tabs described
- Test count is current (not 122)
- No factual errors (all details verified from source files via read_first)
</success_criteria>

<output>
After completion, create `.planning/phases/51-readme-docs-pass/51-01-SUMMARY.md`
</output>

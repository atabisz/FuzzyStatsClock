---
phase: 50-installer-ci
plan: 02
subsystem: infra
tags: [github-actions, inno-setup, ci-cd, release-workflow, checksums, dotnet-publish]

# Dependency graph
requires:
  - phase: 50-installer-ci
    plan: 01
    provides: FuzzyClock.iss with SourceDir preprocessor variable for ISCC compilation
provides:
  - Complete CI release pipeline in .github/workflows/release.yml
  - Version extraction from git tag (v* → X.Y.Z) via GITHUB_OUTPUT
  - Version-stamped dotnet publish (Version/AssemblyVersion/FileVersion)
  - Inno Setup installer compilation via ISCC.exe in CI
  - SHA256 checksums.txt generated by PowerShell Get-FileHash
  - Three-artifact draft GitHub Release on v* tag push
affects:
  - release process
  - milestone completion workflow

# Tech tracking
tech-stack:
  added: []
  patterns:
    - GITHUB_REF tag stripping and X.Y.Z padding in bash
    - ISCC.exe invocation via shell:cmd with full path and preprocessor defines
    - SHA256 checksum generation via PowerShell Get-FileHash
    - softprops/action-gh-release@v2 draft release with multiple file artifacts

key-files:
  created: []
  modified:
    - .github/workflows/release.yml

key-decisions:
  - "Compile installer BEFORE renaming EXE so ISCC can find publish/FuzzyClock.exe (plain name)"
  - "Job name kept as 'build' (not 'release') to match existing workflow"
  - "AssemblyVersion passed as X.Y.Z.0 (4-part) per .NET assembly version requirement"
  - "Checksums written with friendly filenames (not full paths) for readability"

patterns-established:
  - "Step order: extract version -> restore -> test -> publish -> compile installer -> rename EXE -> checksums -> draft release"
  - "ISCC invocation: shell:cmd with quoted full path C:\\Program Files (x86)\\Inno Setup 6\\ISCC.exe"

requirements-completed: [INST-07]

# Metrics
duration: 5min
completed: 2026-03-18
---

# Phase 50 Plan 02: Release Workflow Summary

**GitHub Actions release.yml updated to extract git tag version, stamp EXE, compile Inno Setup installer via ISCC, generate SHA256 checksums, and upload all three artifacts as a draft GitHub Release**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-18T02:07:00Z
- **Completed:** 2026-03-18T02:12:37Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Version extracted from git tag (strips `v`, pads to X.Y.Z) and set as step output via `$GITHUB_OUTPUT`
- `dotnet publish` stamped with `-p:Version`, `-p:AssemblyVersion` (4-part), and `-p:FileVersion` from tag
- Inno Setup installer compiled via `ISCC.exe` (full path, `shell: cmd`) with `/DAppVersion` and `/DSourceDir=publish`
- EXE renamed to `FuzzyClock-X.Y.Z.exe` AFTER installer compilation (ISCC reads unversioned name)
- `checksums.txt` generated with PowerShell `Get-FileHash` (SHA256) for both EXE and installer
- Draft GitHub Release created via `softprops/action-gh-release@v2` with `draft: true`, `generate_release_notes: true`, three file artifacts

## Task Commits

Each task was committed atomically:

1. **Task 1: Update release.yml with version injection, installer compilation, checksums, and draft release** - `056c76f` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `.github/workflows/release.yml` - Complete release pipeline with 8 steps: version extraction, restore, test, publish, compile installer, rename EXE, checksums, draft release

## Decisions Made
- Compile installer step placed BEFORE rename EXE step — ISCC reads `publish/FuzzyClock.exe` (unversioned), the rename produces `FuzzyClock-X.Y.Z.exe` only after ISCC is done
- Job name kept as `build` to avoid breaking any existing references (research example used `release` but plan specified preserving existing structure)
- AssemblyVersion passed as `${{ steps.version.outputs.version }}.0` (appends `.0`) to satisfy .NET 4-part version requirement

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. ISCC.exe is pre-installed on `windows-latest` GitHub Actions runners.

## Next Phase Readiness
- Phase 50 (Installer + CI) is complete: FuzzyClock.iss (plan 01) + release.yml (plan 02)
- Ready for Phase 51: README Docs Pass
- To trigger a release: push a `v*` tag (e.g. `git tag v3.5 && git push origin v3.5`)

---
*Phase: 50-installer-ci*
*Completed: 2026-03-18*

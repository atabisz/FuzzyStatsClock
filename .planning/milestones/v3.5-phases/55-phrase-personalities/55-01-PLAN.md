---
phase: 55-phrase-personalities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.Core/RudePhraseProvider.cs
  - FuzzyClock.Core/PiratePhraseProvider.cs
  - FuzzyClock.Core/DwarfPhraseProvider.cs
  - FuzzyClock.Core/JivePhraseProvider.cs
  - FuzzyClock.Core/ValleyGirlPhraseProvider.cs
  - FuzzyClock.Core/YodaPhraseProvider.cs
  - FuzzyClock.Core/ShakespearePhraseProvider.cs
autonomous: true
requirements:
  - PHRASE-01
  - PHRASE-02
  - PHRASE-03
  - PHRASE-04
  - PHRASE-05
  - PHRASE-06
  - PHRASE-07

must_haves:
  truths:
    - "RudePhraseProvider bucket table uses internet-slang vocabulary (WTF, bruh, dafaq, smh) — no trace of old British passive-aggressive strings"
    - "Six new provider classes exist in FuzzyClock.Core/, each implementing IPhraseProvider with a 12-bucket table, HourWords array, special noon/midnight, and GetStructuredPhrase returning ('', GetPhrase(dt))"
    - "ShakespearePhraseProvider has a private OrdinalHourWords array and resolves {ho} tokens alongside {h} and {h1}"
  artifacts:
    - path: "FuzzyClock.Core/RudePhraseProvider.cs"
      provides: "Rude 2.0 bucket table"
      contains: "bruh"
    - path: "FuzzyClock.Core/PiratePhraseProvider.cs"
      provides: "Pirate personality provider"
      exports: ["PiratePhraseProvider"]
    - path: "FuzzyClock.Core/DwarfPhraseProvider.cs"
      provides: "Dwarf personality provider"
      exports: ["DwarfPhraseProvider"]
    - path: "FuzzyClock.Core/JivePhraseProvider.cs"
      provides: "Jive personality provider"
      exports: ["JivePhraseProvider"]
    - path: "FuzzyClock.Core/ValleyGirlPhraseProvider.cs"
      provides: "Valley Girl personality provider"
      exports: ["ValleyGirlPhraseProvider"]
    - path: "FuzzyClock.Core/YodaPhraseProvider.cs"
      provides: "Yoda personality provider"
      exports: ["YodaPhraseProvider"]
    - path: "FuzzyClock.Core/ShakespearePhraseProvider.cs"
      provides: "Shakespearean personality provider"
      exports: ["ShakespearePhraseProvider"]
  key_links:
    - from: "Each new provider class"
      to: "IPhraseProvider"
      via: "implements IPhraseProvider"
      pattern: "class.*PhraseProvider : IPhraseProvider"
---

<objective>
Rewrite RudePhraseProvider with internet-slang vocabulary and create six new IPhraseProvider implementations for the Pirate, Dwarf, Jive, Valley Girl, Yoda, and Shakespearean personalities.

Purpose: Provider classes must exist before they can be registered. This plan creates all seven concrete provider files in FuzzyClock.Core/; wiring happens in Plan 02.
Output: 1 rewritten file + 6 new .cs files, all compilable, all implementing IPhraseProvider.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-phrase-personalities/55-RESEARCH.md
</context>

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From FuzzyClock.Core/IPhraseProvider.cs:
```csharp
public interface IPhraseProvider
{
    string GetPhrase(DateTime dt);
    (string Qualifier, string Emphasis) GetStructuredPhrase(DateTime dt);
}
```

From FuzzyClock.Core/RudePhraseProvider.cs (current — will be rewritten):
```csharp
// Bucket table pattern: (int UpperBound, string Template)[]
// GetPhrase: checks totalMinutes for noon (720) / midnight (0) specials first,
//            then walks Buckets returning first where minute <= upperBound,
//            substituting {h} and {h1} via HourWords array.
// GetStructuredPhrase: returns ("", GetPhrase(dt))
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite RudePhraseProvider with Rude 2.0 vocabulary</name>
  <files>FuzzyClock.Core/RudePhraseProvider.cs</files>
  <action>
Replace the entire Buckets array in RudePhraseProvider.cs with the Rude 2.0 vocabulary from the research doc. Keep the class skeleton (HourWords, GetPhrase logic, GetStructuredPhrase) unchanged — only the bucket strings and the two specials change.

New Buckets array (exact strings, do not paraphrase):
```
( 2, "{h} o'clock, bruh")
( 7, "just after {h}, tf")
(12, "ten past {h}, smh")
(17, "quarter past {h}, ngl")
(22, "WTF, still quarter past {h}")
(27, "almost half past {h}, lmao")
(32, "half past {h}, bruh")
(37, "just past half {h}, dafaq")
(42, "almost quarter to {h1}, rn")
(47, "quarter to {h1}, literally")
(52, "nearly {h1}, smh")
(59, "almost {h1}, WTF")
```

New specials (replace noon → "noon, bruh" and midnight → "midnight, wtf are you doing").

Update the XML doc comment from "Blunt callout phrase provider" to "Internet-slang phrase provider (Rude 2.0)".
  </action>
  <verify>
    <automated>dotnet build FuzzyClock.Core/FuzzyClock.Core.csproj --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>RudePhraseProvider.cs builds cleanly; Buckets array contains "bruh" and "WTF"; old strings "move it" and "get on with it" are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Create six new personality provider classes</name>
  <files>
    FuzzyClock.Core/PiratePhraseProvider.cs,
    FuzzyClock.Core/DwarfPhraseProvider.cs,
    FuzzyClock.Core/JivePhraseProvider.cs,
    FuzzyClock.Core/ValleyGirlPhraseProvider.cs,
    FuzzyClock.Core/YodaPhraseProvider.cs,
    FuzzyClock.Core/ShakespearePhraseProvider.cs
  </files>
  <action>
Create six new provider classes in FuzzyClock.Core/ using the exact skeleton pattern from RudePhraseProvider. All are in the `FuzzyClock.Core` namespace (no subdirectory). Each class:
- Has `private static readonly string[] HourWords = ["", "one", ... "twelve"]`
- Has `private static readonly (int UpperBound, string Template)[] Buckets = [...]`
- Implements `GetPhrase(DateTime dt)` with totalMinutes noon/midnight check, 12-hour conversion, bucket walk, {h}/{h1} substitution
- Implements `GetStructuredPhrase(DateTime dt) => ("", GetPhrase(dt))`
- Throws `InvalidOperationException` if no bucket matches

Use the exact vocabulary tables from the research doc (55-RESEARCH.md). Do not paraphrase or invent alternatives.

**PiratePhraseProvider** (locale key "en-pirate"):
Buckets as specified in research. Specials: noon → "high noon at sea, arr", midnight → "the dead of night, yarr".

**DwarfPhraseProvider** (locale key "en-dwarf"):
Buckets as specified. Specials: noon → "midday. eat.", midnight → "deep into the night, bah".

**JivePhraseProvider** (locale key "en-jive"):
Buckets as specified. Specials: noon → "high noon, daddy-o", midnight → "the witching hour, cat".

**ValleyGirlPhraseProvider** (locale key "en-valleygirl"):
Buckets as specified. Specials: noon → "like, it's literally noon", midnight → "omg it's literally midnight".

**YodaPhraseProvider** (locale key "en-yoda"):
Buckets as specified. Specials: noon → "noon it is, hmm", midnight → "midnight, the dark hour, yes".

**ShakespearePhraseProvider** (locale key "en-shakespeare"):
In addition to HourWords, add:
```csharp
private static readonly string[] OrdinalHourWords =
    ["", "first", "second", "third", "fourth", "fifth", "sixth",
         "seventh", "eighth", "ninth", "tenth", "eleventh", "twelfth"];
```
In GetPhrase, after computing hour12, resolve {ho} token before {h} and {h1}:
```csharp
return template
    .Replace("{ho}", OrdinalHourWords[hour12])
    .Replace("{h}",  HourWords[hour12])
    .Replace("{h1}", HourWords[nextHour12]);
```
Specials: noon → "Hark! 'Tis the noontide hour", midnight → "The witching hour doth toll".
Buckets as specified in research (uses {ho} token in several entries).
  </action>
  <verify>
    <automated>dotnet build FuzzyClock.Core/FuzzyClock.Core.csproj --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <done>All six new .cs files exist in FuzzyClock.Core/; project builds with 0 errors; each class is public and implements IPhraseProvider.</done>
</task>

</tasks>

<verification>
After both tasks:
- `dotnet build FuzzyClock.Core/FuzzyClock.Core.csproj` exits 0
- `ls FuzzyClock.Core/*PhraseProvider.cs` shows 10 provider files (4 existing + 6 new)
- `grep -l "bruh" FuzzyClock.Core/RudePhraseProvider.cs` finds the file (Rude 2.0 is live)
- `grep -rn "move it\|get on with it" FuzzyClock.Core/RudePhraseProvider.cs` returns empty (old strings gone)
</verification>

<success_criteria>
- RudePhraseProvider uses internet-slang vocabulary; old British passive-aggressive strings absent
- Six new provider files exist with correct class names, correct namespace, correct bucket tables
- ShakespearePhraseProvider has OrdinalHourWords array and resolves {ho} token
- FuzzyClock.Core.csproj builds cleanly (0 errors)
</success_criteria>

<output>
After completion, create `.planning/phases/55-phrase-personalities/55-01-SUMMARY.md`
</output>

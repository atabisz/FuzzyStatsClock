---
phase: 48-settings-window-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/SettingsWindow.xaml
autonomous: false
requirements:
  - SETR-01
  - SETR-02
  - SETR-03
  - SETR-04

must_haves:
  truths:
    - "Opening Settings shows a dark background with light foreground text — no light-gray system-default chrome"
    - "CheckBox, RadioButton, ComboBox, Button, and Slider controls render with consistent dark-mode appearance"
    - "Section groups have visible breathing room — nothing feels cramped"
    - "Closing Settings and right-clicking the widget or tray shows no visual change to MainWindow"
  artifacts:
    - path: "FuzzyClock.App/SettingsWindow.xaml"
      provides: "Dark-styled Settings window via ThemeMode"
      contains: "ThemeMode=\"Dark\""
    - path: "FuzzyClock.App/App.xaml"
      provides: "Unchanged application-level resources (no leakage)"
      contains: "<Application.Resources />"
  key_links:
    - from: "SettingsWindow.xaml Window element"
      to: "Fluent dark theme for all standard controls"
      via: "ThemeMode=\"Dark\" attribute"
      pattern: "ThemeMode=\"Dark\""
    - from: "SegmentButtonStyle triggers"
      to: "Dark-mode segment button colors"
      via: "Replaced hardcoded #FFFFFFFF/#FFBDBDBD/#FFD0D0D0 to dark equivalents"
      pattern: "#FF3C3C3C|#FF666666|#FF555555"
    - from: "Font Size / Clock Style rail Borders"
      to: "Dark rail background"
      via: "Replaced #FFE8E8E8 to #FF3A3A3A"
      pattern: "#FF3A3A3A"
    - from: "Theme swatch inner Borders"
      to: "Dark swatch card background"
      via: "Replaced #FFF0F0F5 to #FF2D2D2D, labels #FF333333 to #FFD0D0D0"
      pattern: "#FF2D2D2D"
    - from: "Behavior tab description TextBlocks"
      to: "Legible muted text on dark background"
      via: "Replaced #FF666666 to #FF999999"
      pattern: "#FF999999"
---

<objective>
Apply dark-mode styling to SettingsWindow.xaml so it matches the widget's minimal aesthetic. Add ThemeMode="Dark" to the Window element (handles all standard controls automatically) and replace all hardcoded light hex colors that would look wrong on a dark background.

Purpose: The Settings window currently uses default light-mode system styling, which clashes with the dark widget aesthetic. SETR-01 through SETR-04 require a dark-mode Settings window with no style leakage to MainWindow.

Output: Updated SettingsWindow.xaml with ThemeMode="Dark", dark-appropriate hardcoded colors, verified spacing, and a human visual sign-off confirming the result.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-settings-window-visual-redesign/48-RESEARCH.md
@FuzzyClock.App/SettingsWindow.xaml
@FuzzyClock.App/App.xaml
</context>

<interfaces>
<!-- Key decisions locked in STATE.md — executor must honor these exactly -->

Locked decisions:
- ThemeMode="Dark" XAML attribute on SettingsWindow only — App.xaml stays empty to prevent MainWindow style leakage
- Post-research confirmation: ThemeMode on Window is window-local, App.xaml has empty Application.Resources (no leakage risk)

Hardcoded light colors to replace (from RESEARCH.md Pattern 2 audit):

| Location | Current Value | Dark Replacement |
|----------|--------------|-----------------|
| SegmentButtonStyle selected Background | #FFFFFFFF | #FF3C3C3C |
| SegmentButtonStyle selected BorderBrush | #FFBDBDBD | #FF666666 |
| SegmentButtonStyle hover Background | #FFD0D0D0 | #FF555555 |
| Font Size rail Border Background | #FFE8E8E8 | #FF3A3A3A |
| Clock Style rail Border Background | #FFE8E8E8 | #FF3A3A3A |
| Theme swatch inner Border Background (x5) | #FFF0F0F5 | #FF2D2D2D |
| Theme swatch label Foreground (x5) | #FF333333 | #FFD0D0D0 |
| Behavior tab description TextBlocks Foreground (x2) | #FF666666 | #FF999999 |

Current Window element (lines 1-8 of SettingsWindow.xaml) — add ThemeMode="Dark":
```xaml
<Window x:Class="FuzzyClock.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FuzzyClock Settings"
        Width="480" Height="600"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen">
```
Add `ThemeMode="Dark"` as final attribute on the Window element.

App.xaml current state (must remain unchanged):
```xaml
<Application.Resources />
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ThemeMode and replace hardcoded light colors in SettingsWindow.xaml</name>
  <files>FuzzyClock.App/SettingsWindow.xaml</files>
  <action>
Make the following surgical edits to SettingsWindow.xaml:

**1. Window element — add ThemeMode="Dark":**
Add `ThemeMode="Dark"` as the last attribute on the opening `<Window` element (after `WindowStartupLocation="CenterScreen"`).

**2. SegmentButtonStyle triggers — replace 3 light colors:**
- Selected state Background: `#FFFFFFFF` -> `#FF3C3C3C`
- Selected state BorderBrush: `#FFBDBDBD` -> `#FF666666`
- Hover state Background: `#FFD0D0D0` -> `#FF555555`
Update the inline comments: "Selected state: white pill with subtle border" -> "Selected state: dark pill" and "Hover on unselected: light gray" -> "Hover on unselected: dark".

**3. Font Size rail Border Background: `#FFE8E8E8` -> `#FF3A3A3A`** (the Border wrapping BtnFontS/BtnFontM/BtnFontL/BtnFontXL in the Appearance tab Grid Row 1)

**4. Clock Style rail Border Background: `#FFE8E8E8` -> `#FF3A3A3A`** (the Border wrapping BtnPhrase/BtnDial in the Appearance tab Grid Row 2)

**5. All 5 theme swatch inner Borders — replace Background and label Foreground:**
Each named theme swatch (Midnight, Neon, Ghost, Warm, Terminal) has an inner `Border Width="60" Height="64" Background="#FFF0F0F5"`. Change all 5 to `Background="#FF2D2D2D"`.
Each swatch has a `TextBlock` label with `Foreground="#FF333333"`. Change all 5 to `Foreground="#FFD0D0D0"`.

**6. Behavior tab description TextBlocks — replace Foreground (2 occurrences):**
- `Foreground="#FF666666"` on the "Auto-detects from Windows display language..." TextBlock -> `#FF999999`
- `Foreground="#FF666666"` on the "Alert when unplugged and battery is at or below:" TextBlock -> `#FF999999`

**Do NOT modify:**
- App.xaml (must remain `<Application.Resources />`)
- MainWindow.xaml (must remain completely unchanged)
- SettingsWindow.xaml.cs (no C# changes needed)
- The `FocusVisualStyle="{x:Null}"` setter in SegmentButtonStyle — preserve as-is
- The accent color swatches (SwatchWhite/Amber/Ice/Green/Pink) — their colors are intentional accent palette values, not UI chrome
- The ring-selection Borders' BorderThickness/CornerRadius/Padding — only Background colors listed above change
- Spacing and Margin values — SETR-03 is already satisfied per research (existing margins are adequate); do not alter layout
  </action>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock &amp;&amp; dotnet build FuzzyClock.App/FuzzyClock.App.csproj --no-restore -v quiet 2>&amp;1 | tail -5</automated>
  </verify>
  <done>SettingsWindow.xaml contains ThemeMode="Dark" on Window element; all 13 hardcoded light color occurrences replaced with dark equivalents; project builds with 0 errors; App.xaml and MainWindow.xaml are byte-for-byte unchanged</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dark-mode Settings window</name>
  <action>Human verifies the Settings window renders correctly in dark mode. All automated XAML edits completed in Task 1.</action>
  <what-built>
    Dark-mode SettingsWindow: ThemeMode="Dark" applied to SettingsWindow only. Standard controls (CheckBox, RadioButton, ComboBox, Button, Slider, TabControl) restyled via Fluent dark theme. Hardcoded light colors in SegmentButtonStyle, rail borders, theme swatches, and description text replaced with dark equivalents.
  </what-built>
  <how-to-verify>
    1. Build and run: `cd C:/src/FuzzyStatsClock &amp;&amp; dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj`
    2. Right-click the tray icon -> "Open Settings..." — the Settings window should open with a dark background and light text
    3. Check the Appearance tab: theme swatches have dark card backgrounds (#2D2D2D) with light labels; Font Size and Clock Style rails are dark (#3A3A3A); the selected segment button shows a dark pill (#3C3C3C) not a white one
    4. Check the Stats tab: all CheckBoxes render with Fluent dark styling (dark checkboxes); RadioButtons are dark-ringed; ComboBoxes have dark backgrounds
    5. Check the Behavior tab: description text below "Phrase Language" and "Battery Alert" headers is readable muted gray (#999999); CheckBoxes and RadioButtons match Stats tab
    6. Hover over an unselected Font Size or Clock Style button — should show dark gray hover (#555555), not light gray
    7. Close Settings. Right-click the widget — the tray menu should look identical to before. The transparent widget overlay must be unaffected
    8. Open a ComboBox dropdown (e.g., Phrase Language) — ideally the dropdown list has a dark background. If it shows Aero2 light styling, that is acceptable (known .NET 10 Popup limitation) — note it but do not block approval
  </how-to-verify>
  <verify>
    <automated>cd C:/src/FuzzyStatsClock &amp;&amp; dotnet test --no-build -v quiet 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Settings window visually confirmed dark with consistent styling across all three tabs; widget overlay appearance unchanged; all 224 tests still pass</done>
  <resume-signal>Type "approved" if the Settings window appears dark with consistent styling, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
Build must pass: `dotnet build FuzzyClock.App/FuzzyClock.App.csproj` with 0 errors.
Test suite must still pass: `dotnet test` — all 224 tests green (no test changes in this phase).
App.xaml diff: no changes.
MainWindow.xaml diff: no changes.
SettingsWindow.xaml diff: ThemeMode="Dark" present; 13 color value replacements present.
</verification>

<success_criteria>
1. Settings window opens with dark background and light foreground text (SETR-01)
2. All five control types (CheckBox, RadioButton, ComboBox, Button, Slider) have consistent dark-mode appearance (SETR-02)
3. Section groups have adequate whitespace — existing margins were already sufficient, confirmed by visual check (SETR-03)
4. App.xaml and MainWindow.xaml are byte-for-byte unchanged; widget overlay appearance is unaffected (SETR-04)
5. All 224 tests still pass (no regressions)
6. Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/48-settings-window-visual-redesign/48-01-SUMMARY.md`
</output>

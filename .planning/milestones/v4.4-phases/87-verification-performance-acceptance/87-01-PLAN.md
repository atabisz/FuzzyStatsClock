---
phase: 87-verification-performance-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App.Tests/LerpRatioTests.cs
  - FuzzyClock.App.Tests/OnSampleTickTests.cs
  - FuzzyClock.App/GhostModeController.cs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - "Pure-static GhostModeController.LerpRatio terminal-state-snap invariant is reachable from MSTest without WPF/timer setup (D-LERP-01)"
    - "GhostModeController.LerpRatio with target=1.0 returns 1.0 exactly across multiple current values; with target=0.0 returns 0.0 exactly across multiple current values (D-LERP-01)"
    - "GhostModeController.LerpRatio with mid-range target (e.g. 0.5) does NOT return target — proves the snap is target-driven, not current-driven (D-LERP-01)"
    - "Pure-logic core of the sampling loop is reachable from MSTest without spinning real timers/threads via internal OnSampleTick seam (D-SEAM-01)"
    - "OnSampleTick returns GhostTransition.None when cursor far away and !_isGhostMode (D-SEAM-01)"
    - "OnSampleTick returns GhostTransition.Activate when cursor inside rect and !_isGhostMode (D-SEAM-01)"
    - "OnSampleTick returns GhostTransition.RestoreNoEvent when cursor at mid-range and _isGhostMode == true (D-SEAM-01)"
    - "OnSampleTick returns GhostTransition.RestoreWithEvent when cursor far away and _isGhostMode == true (D-SEAM-01)"
    - "Test code can directly write controller._isGhostMode = true to set up RestoreNoEvent / RestoreWithEvent rows after the visibility relaxation (D-SEAM-02)"
    - "Existing 578-test baseline (449 Core + 129 App) still passes unchanged after this plan, plus new LerpRatioTests + OnSampleTickTests rows (TEST-01, TEST-04 baseline preservation)"
  artifacts:
    - path: "FuzzyClock.App.Tests/LerpRatioTests.cs"
      provides: "MSTest unit coverage for GhostModeController.LerpRatio terminal-state snap"
      contains: "[TestClass]"
    - path: "FuzzyClock.App.Tests/OnSampleTickTests.cs"
      provides: "MSTest unit coverage for the four GhostTransition values returned by GhostModeController.OnSampleTick"
      contains: "[TestClass]"
    - path: "FuzzyClock.App/GhostModeController.cs"
      provides: "_isGhostMode field with internal visibility (was private) so OnSampleTickTests can directly drive ghost-mode state for setup"
      contains: "internal volatile bool _isGhostMode"
  key_links:
    - from: "FuzzyClock.App.Tests/LerpRatioTests.cs"
      to: "FuzzyClock.App/GhostModeController.cs::LerpRatio"
      via: "static method call across InternalsVisibleTo boundary"
      pattern: "GhostModeController\\.LerpRatio\\("
    - from: "FuzzyClock.App.Tests/OnSampleTickTests.cs"
      to: "FuzzyClock.App/GhostModeController.cs::OnSampleTick"
      via: "instance method call + direct internal field write across InternalsVisibleTo boundary"
      pattern: "controller\\._isGhostMode\\s*="
    - from: "FuzzyClock.App.Tests/OnSampleTickTests.cs"
      to: "FuzzyClock.App/GhostModeController.cs::GhostTransition"
      via: "enum value assertion"
      pattern: "GhostTransition\\.(None|Activate|RestoreNoEvent|RestoreWithEvent)"
---

<objective>
Add automated MSTest coverage for the two pure-static seams introduced by Phases 85 and 86 — `GhostModeController.LerpRatio` (terminal-state snap, D-LERP-01) and `GhostModeController.OnSampleTick` (four `GhostTransition` values, D-SEAM-01) — and relax `_isGhostMode` from `private` to `internal` so the OnSampleTick seam tests can directly drive the ghost-mode pre-state for `RestoreNoEvent` / `RestoreWithEvent` rows (D-SEAM-02b).

Purpose: Lock in TEST-01 (existing pure-static tests still pass), TEST-02 (`LerpRatio` covered), TEST-03 (`OnSampleTick` reachable without timers/threads), and TEST-04 (full suite green). Two new test classes plus a one-keyword field-visibility relaxation — no other production code changes.

Output:
- New test class `FuzzyClock.App.Tests/LerpRatioTests.cs` with `[TestClass]` and `[DataRow]` table covering target=1.0 snap, target=0.0 snap, and a non-snap mid-range assertion
- New test class `FuzzyClock.App.Tests/OnSampleTickTests.cs` with `[TestClass]` and four-row coverage of `GhostTransition.None` / `Activate` / `RestoreNoEvent` / `RestoreWithEvent`
- One-keyword edit at `FuzzyClock.App/GhostModeController.cs:70` changing `private volatile bool _isGhostMode;` to `internal volatile bool _isGhostMode;`
- `dotnet test FuzzyClock.sln --nologo --verbosity quiet` exits 0 with `Passed: NNN, Failed: 0, Skipped: 0` where NNN >= 578 + new test rows added in this plan
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-verification-performance-acceptance/87-CONTEXT.md
@.planning/phases/87-verification-performance-acceptance/87-PATTERNS.md

<interfaces>
<!-- Key types and signatures the executor needs. Extracted from production code. -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From FuzzyClock.App/GhostModeController.cs (relevant declarations):
- `public enum GhostTransition { None, Activate, RestoreNoEvent, RestoreWithEvent }`
- `public readonly record struct SampleResult(double Ratio, bool RatioChanged, GhostTransition Transition);`
- `private volatile bool _isGhostMode;` at line 70 — to be relaxed to `internal` in this plan (D-SEAM-02b)
- `internal SampleResult OnSampleTick(int cursorX, int cursorY, int rectLeft, int rectTop, int rectRight, int rectBottom, bool modifiersHeld)` — exact signature must be confirmed by reading the file before authoring tests
- `internal static double LerpRatio(double current, double target, double alpha, double deltaSeconds)` at lines 475-487
- `public bool IsActive => _isGhostMode;` getter — preserved unchanged
- Established widget rect convention used by `GhostModeControllerProximityTests`: Left=100, Top=100, Right=200, Bottom=200 (100x100 widget)

From FuzzyClock.App/FuzzyClock.App.csproj (lines 7-11):
`InternalsVisibleTo("FuzzyClock.App.Tests")` — already in place; new test classes inherit access to all `internal` members of `FuzzyClock.App` for free. No csproj edit required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax _isGhostMode visibility from private to internal at GhostModeController.cs:70</name>
  <files>FuzzyClock.App/GhostModeController.cs</files>
  <read_first>
    - FuzzyClock.App/GhostModeController.cs (read lines 55-95 to see the field cluster, the existing comment style for D-XX traceability, and the sibling fields at lines 72-81 already declared `internal`/`private`/`volatile` — confirms the in-file convention; also read lines 360-435 to confirm the sole production-side `_isGhostMode = false;` write inside `OnSampleTick` at ~line 423 is unaffected, and confirm the `Activate()` write site)
    - .planning/phases/87-verification-performance-acceptance/87-CONTEXT.md (D-SEAM-02b decision text and Phase 85 D-06 ownership rule preservation note)
    - .planning/phases/87-verification-performance-acceptance/87-PATTERNS.md (§ "GhostModeController.cs:70 (model, visibility relaxation)" — exact required edit shape and invariant preservation note)
  </read_first>
  <action>
    Change exactly one keyword on the `_isGhostMode` field declaration at GhostModeController.cs line 70: `private volatile bool _isGhostMode;` becomes `internal volatile bool _isGhostMode;` per D-SEAM-02b. Preserve the existing trailing comment that references D-06 cross-thread reader semantics; you may optionally append a `Phase 87 D-SEAM-02b: relaxed to internal for OnSampleTickTests setup` clause to the trailing comment but this is not mandatory. Do NOT change the field type, do NOT change the `volatile` modifier, do NOT remove or rewrite the comment, do NOT touch any other line in the file. The `public bool IsActive => _isGhostMode;` getter and the production-side writes inside `OnSampleTick` (writes `false`) and `Activate()` (writes `true`) MUST remain byte-for-byte unchanged — this edit is visibility-only, semantic-neutral.
  </action>
  <verify>
    <automated>grep -n "internal volatile bool _isGhostMode" FuzzyClock.App/GhostModeController.cs && ! grep -n "private volatile bool _isGhostMode" FuzzyClock.App/GhostModeController.cs && dotnet build FuzzyClock.sln --nologo --verbosity quiet</automated>
  </verify>
  <acceptance_criteria>
    - Source: `GhostModeController.cs` line 70 contains exactly `internal volatile bool _isGhostMode;` (modulo the trailing comment)
    - Source: `GhostModeController.cs` no longer contains the literal string `private volatile bool _isGhostMode`
    - Source: `GhostModeController.cs` still contains `public bool IsActive => _isGhostMode;` (getter preserved)
    - Source: count of production-side `_isGhostMode = ` writes in `GhostModeController.cs` is exactly 2 (one `= false` inside `OnSampleTick`, one `= true` inside `Activate()`) — Phase 85 D-06 ownership rule preserved
    - Build: `dotnet build FuzzyClock.sln --nologo --verbosity quiet` exits 0 with 0 Warnings, 0 Errors
    - Test command: `dotnet test FuzzyClock.sln --nologo --verbosity quiet` exits 0 with `Passed: 578, Failed: 0, Skipped: 0` (existing baseline preserved before any new test files are added)
  </acceptance_criteria>
  <done>Field-visibility relaxation lands as a single-keyword edit; build is green; existing 578-test baseline still passes; production-side write ownership rule preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LerpRatioTests covering D-LERP-01 terminal-state snap (target=1.0 snap, target=0.0 snap, mid-range non-snap)</name>
  <files>FuzzyClock.App.Tests/LerpRatioTests.cs</files>
  <behavior>
    - `GhostModeController.LerpRatio(current, target=1.0, alpha=15.0, deltaSeconds=0.016)` returns exactly `1.0` for current ∈ {0.0, 0.5, 0.999, 1.0} (target=1.0 snap, D-LERP-01)
    - `GhostModeController.LerpRatio(current, target=0.0, alpha=15.0, deltaSeconds=0.016)` returns exactly `0.0` for current ∈ {0.0, 0.5, 0.999, 1.0} (target=0.0 snap, D-LERP-01)
    - `GhostModeController.LerpRatio(current=0.0, target=0.5, alpha=15.0, deltaSeconds=0.016)` does NOT return `0.5` — proves the snap is target-driven, not current-driven (D-LERP-01 negative case). Precise expected value follows the formula `current + (target - current) * (1 - exp(-alpha * deltaSeconds))` ≈ `0.0 + 0.5 * 0.21372...` ≈ `0.10686`; assert with epsilon 0.0001
  </behavior>
  <read_first>
    - FuzzyClock.App.Tests/GhostModeControllerProximityTests.cs (lines 1-42 — analog file for imports, namespace, [TestClass]/[TestMethod]/[DataRow] convention, `Assert.AreEqual(expected, result, 0.0001)` epsilon, and the optional non-parametric helper-method shape at lines 30-42)
    - FuzzyClock.App/GhostModeController.cs (lines 475-487 — the `internal static double LerpRatio(double current, double target, double alpha, double deltaSeconds)` body so you understand the snap branches and the `alpha * deltaSeconds` clamp behavior; also confirms the exact method signature)
    - .planning/phases/87-verification-performance-acceptance/87-CONTEXT.md (D-LERP-01 scope, D-LERP-02 row layout — the `<specifics>` block has the exact 6-row `[DataRow]` table for terminal snap)
    - .planning/phases/87-verification-performance-acceptance/87-PATTERNS.md (§ "LerpRatioTests.cs (test, pure-function table-driven)" — exact mirror instructions)
  </read_first>
  <action>
    Create `FuzzyClock.App.Tests/LerpRatioTests.cs` mirroring the structure of `GhostModeControllerProximityTests.cs` exactly: file-scoped `using FuzzyClock.App;` then `namespace FuzzyClock.App.Tests;`, then `[TestClass] public class LerpRatioTests { ... }`. Inside the class, add one `[TestMethod]` named `LerpRatio_TerminalStateSnap` carrying the 6 `[DataRow]` rows from CONTEXT.md `<specifics>` D-LERP-02 verbatim — `[DataRow(1.0, 1.0, 15.0, 0.016, 1.0, DisplayName = "target=1.0, current=1.0 -> 1.0 (snap)")]`, `[DataRow(1.0, 1.0, 15.0, 0.016, 0.5, ...)]`, `[DataRow(1.0, 1.0, 15.0, 0.016, 0.0, ...)]`, `[DataRow(0.0, 0.0, 15.0, 0.016, 0.0, ...)]`, `[DataRow(0.0, 0.0, 15.0, 0.016, 0.5, ...)]`, `[DataRow(0.0, 0.0, 15.0, 0.016, 1.0, ...)]`. The DataRow column order is `(target, expected, alpha, deltaSeconds, current)` per the literal layout in CONTEXT.md `<specifics>`; the method signature MUST match that column order — `public void LerpRatio_TerminalStateSnap(double target, double expected, double alpha, double deltaSeconds, double current)`. Method body: `double result = GhostModeController.LerpRatio(current, target, alpha, deltaSeconds); Assert.AreEqual(expected, result, 0.0001);`. Add a separate `[TestMethod] public void LerpRatio_MidRangeTarget_DoesNotSnap()` asserting `GhostModeController.LerpRatio(0.0, 0.5, 15.0, 0.016)` does NOT return 0.5 — assert the precise computed value `0.0 + 0.5 * (1 - Math.Exp(-15.0 * 0.016))` with epsilon 0.0001 (use `Math.Exp` to compute expected inline rather than a hardcoded literal so the assertion stays robust to formula re-derivation). Also assert `Assert.AreNotEqual(0.5, result, "Mid-range target must not snap; snap is target-driven")` for explicit negative coverage. Do NOT add convergence-shape tests, step-size-bound tests, NaN/Infinity tests, or alpha=0 / negative-deltaSeconds tests — D-LERP-01 explicitly excludes these as over-spec.
  </action>
  <verify>
    <automated>dotnet test FuzzyClock.sln --filter "FullyQualifiedName~LerpRatioTests" --nologo --verbosity quiet</automated>
  </verify>
  <acceptance_criteria>
    - Source: `FuzzyClock.App.Tests/LerpRatioTests.cs` exists and contains `[TestClass]`, `public class LerpRatioTests`, `using FuzzyClock.App;`, `namespace FuzzyClock.App.Tests;`
    - Source: `LerpRatioTests.cs` contains `[TestMethod]` `LerpRatio_TerminalStateSnap` with at least 6 `[DataRow]` decorators matching the D-LERP-02 layout (3 target=1.0 rows + 3 target=0.0 rows)
    - Source: `LerpRatioTests.cs` contains a separate `[TestMethod]` whose name contains `MidRange` and which asserts the lerp does NOT return the target for a mid-range target
    - Source: `LerpRatioTests.cs` contains exactly one `GhostModeController.LerpRatio(` call inside `LerpRatio_TerminalStateSnap` and exactly one `GhostModeController.LerpRatio(` call inside the mid-range method
    - Source: `LerpRatioTests.cs` contains no `[DataRow]` covering NaN, Infinity, alpha=0, or negative deltaSeconds (D-LERP-01 over-spec exclusions)
    - Test command: `dotnet test FuzzyClock.sln --filter "FullyQualifiedName~LerpRatioTests" --nologo --verbosity quiet` exits 0 with `Passed: N, Failed: 0` where N >= 7 (6 DataRow rows + 1 mid-range method)
    - Build: `dotnet build FuzzyClock.sln --nologo --verbosity quiet` exits 0 with 0 Warnings, 0 Errors
  </acceptance_criteria>
  <done>`LerpRatioTests.cs` lands; all rows pass; D-LERP-01 terminal-state-snap invariant is now reachable from MSTest without WPF/timer setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add OnSampleTickTests covering D-SEAM-01 four GhostTransition classes via direct internal _isGhostMode setup</name>
  <files>FuzzyClock.App.Tests/OnSampleTickTests.cs</files>
  <behavior>
    - `OnSampleTick(cursorX=50, cursorY=150, rect=(100,100,200,200), modifiersHeld=false)` with `_isGhostMode=false` returns `Transition == GhostTransition.None` — far away, not ghost
    - `OnSampleTick(cursorX=150, cursorY=150, rect=(100,100,200,200), modifiersHeld=false)` with `_isGhostMode=false` returns `Transition == GhostTransition.Activate` — inside rect, not ghost
    - `OnSampleTick(cursorX=75, cursorY=150, rect=(100,100,200,200), modifiersHeld=false)` with `_isGhostMode=true` returns `Transition == GhostTransition.RestoreNoEvent` — mid-range, ghost (ratio in (0.0, 1.0))
    - `OnSampleTick(cursorX=50, cursorY=150, rect=(100,100,200,200), modifiersHeld=false)` with `_isGhostMode=true` returns `Transition == GhostTransition.RestoreWithEvent` — far away, ghost (ratio == 0.0)
  </behavior>
  <read_first>
    - FuzzyClock.App.Tests/GhostModeControllerTests.cs (lines 1-39 — analog file for the top-of-file `// NOTE:` comment block convention, imports/namespace/[TestClass] shape, `[DataRow]` parametric pattern with stacked rows + `Assert.AreEqual(expected, result)` enum assertion, and the per-row `var controller = new GhostModeController(); controller.UpdateModifierConfig(...);` arrange-act-assert shape)
    - FuzzyClock.App.Tests/GhostModeControllerProximityTests.cs (lines 12-28 — confirms the established widget rect convention `100, 100, 200, 200` used across the test corpus; the OnSampleTick rows MUST use the same rect)
    - FuzzyClock.App/GhostModeController.cs (lines 60-95 to read the `GhostTransition` enum + `SampleResult` record-struct + `_isGhostMode` field declaration in context; lines 360-435 to read the full `OnSampleTick` body so you understand the exact parameter list, the ratio→transition decision tree, the `_isGhostMode = false` write inside the method, and which input shape produces which transition)
    - .planning/phases/87-verification-performance-acceptance/87-CONTEXT.md (D-SEAM-01 transition coverage scope, D-SEAM-02 setup pattern, D-SEAM-02b visibility relaxation — the `<specifics>` block has the exact four `(cursorX, cursorY, isGhostModePre, expectedTransition)` row layout)
    - .planning/phases/87-verification-performance-acceptance/87-PATTERNS.md (§ "OnSampleTickTests.cs (test, parametric seam)" — exact mirror instructions for class header, NOTE block, and direct-internal-write setup pattern)
  </read_first>
  <action>
    Create `FuzzyClock.App.Tests/OnSampleTickTests.cs` mirroring `GhostModeControllerTests.cs`. Top of file: a `// NOTE:` comment block (mirroring lines 1-4 of `GhostModeControllerTests.cs`) explaining that direct-write `controller._isGhostMode = true;` setup is for seam-state arrangement only and does not violate the Phase 85 D-06 ownership rule (production-side writes still owned by `OnSampleTick` writing `false` and `Activate()` writing `true`; tests are the only new writer, and only for setup). Then `using FuzzyClock.App;` + `namespace FuzzyClock.App.Tests;` + `[TestClass] public class OnSampleTickTests`. Add one `[TestMethod] public void OnSampleTick_TransitionClasses_ReturnsExpected(int cursorX, int cursorY, bool isGhostModePre, GhostTransition expectedTransition)` carrying four `[DataRow]` rows verbatim from CONTEXT.md `<specifics>`: `[DataRow(50, 150, false, GhostTransition.None, DisplayName = "far+!ghost -> None")]`, `[DataRow(150, 150, false, GhostTransition.Activate, DisplayName = "inside+!ghost -> Activate")]`, `[DataRow(75, 150, true, GhostTransition.RestoreNoEvent, DisplayName = "mid+ghost -> RestoreNoEvent")]`, `[DataRow(50, 150, true, GhostTransition.RestoreWithEvent, DisplayName = "far+ghost -> RestoreWithEvent")]`. Method body (Arrange-Act-Assert shape mirroring the analog): `var controller = new GhostModeController(); controller._isGhostMode = isGhostModePre; var result = controller.OnSampleTick(cursorX, cursorY, 100, 100, 200, 200, modifiersHeld: false); Assert.AreEqual(expectedTransition, result.Transition);`. Confirm the exact `OnSampleTick` parameter list against the read of `GhostModeController.cs` lines 360-410 — if the parameter order in the production seam differs from `(cursorX, cursorY, rectLeft, rectTop, rectRight, rectBottom, modifiersHeld)`, adjust the call site only (do not modify the production seam). Each row is one independent `OnSampleTick` call with explicit setup — do NOT chain Activate-then-Restore call sequences across rows; D-SEAM-02 forbids row-to-row coupling. Do NOT add coverage for SEM-03 modifier-force-zero, SEM-05 !IsEnabled disable-gate, or the `RatioChanged` flag — D-SEAM-01 explicitly excludes these. The widget rect MUST be `100, 100, 200, 200` to stay consistent with the corpus convention from `GhostModeControllerProximityTests`.
  </action>
  <verify>
    <automated>dotnet test FuzzyClock.sln --filter "FullyQualifiedName~OnSampleTickTests" --nologo --verbosity quiet</automated>
  </verify>
  <acceptance_criteria>
    - Source: `FuzzyClock.App.Tests/OnSampleTickTests.cs` exists and contains `[TestClass]`, `public class OnSampleTickTests`, `using FuzzyClock.App;`, `namespace FuzzyClock.App.Tests;`
    - Source: `OnSampleTickTests.cs` contains a top-of-file `// NOTE:` comment block (mirroring the `GhostModeControllerTests.cs` convention)
    - Source: `OnSampleTickTests.cs` contains exactly four `[DataRow]` decorators with the literal cursor coordinates `(50, 150)`, `(150, 150)`, `(75, 150)`, `(50, 150)` and the literal transition values `GhostTransition.None`, `GhostTransition.Activate`, `GhostTransition.RestoreNoEvent`, `GhostTransition.RestoreWithEvent`
    - Source: `OnSampleTickTests.cs` contains the literal substring `controller._isGhostMode = isGhostModePre;` (proves the direct-internal-write setup pattern is in place)
    - Source: `OnSampleTickTests.cs` contains the literal widget-rect arguments `100, 100, 200, 200` inside the `OnSampleTick(...)` call
    - Source: `OnSampleTickTests.cs` does NOT reference SEM-03 modifier coverage (`useCtrl`/`useAlt`/`useShift` or `UpdateModifierConfig`), `IsEnabled`, or `RatioChanged` — the four-class invariant is the only scope (D-SEAM-01 exclusions enforced)
    - Test command: `dotnet test FuzzyClock.sln --filter "FullyQualifiedName~OnSampleTickTests" --nologo --verbosity quiet` exits 0 with `Passed: 4, Failed: 0`
    - Build: `dotnet build FuzzyClock.sln --nologo --verbosity quiet` exits 0 with 0 Warnings, 0 Errors
    - Behavior: `OnSampleTick` invoked with `_isGhostMode=false` and cursor far away returns `Transition == GhostTransition.None`
    - Behavior: `OnSampleTick` invoked with `_isGhostMode=true` and cursor far away returns `Transition == GhostTransition.RestoreWithEvent`
  </acceptance_criteria>
  <done>`OnSampleTickTests.cs` lands; all four rows pass; D-SEAM-01 four-class invariant is reachable from MSTest without timers/threads; full MSTest suite (`dotnet test FuzzyClock.sln --nologo --verbosity quiet`) reports `Passed >= 578 + new rows added across Tasks 2 and 3, Failed: 0, Skipped: 0` — TEST-01 and TEST-04 baseline preserved.</done>
</task>

</tasks>

<verification>
After all three tasks land:
1. `dotnet build FuzzyClock.sln --nologo --verbosity quiet` exits 0 with 0 Warnings, 0 Errors.
2. `dotnet test FuzzyClock.sln --nologo --verbosity quiet` exits 0 with `Passed: NNN, Failed: 0, Skipped: 0` where `NNN >= 578 + (LerpRatioTests rows) + (OnSampleTickTests rows)` — expect ~586–590 total.
3. `grep -n "internal volatile bool _isGhostMode" FuzzyClock.App/GhostModeController.cs` returns exactly one match at line 70.
4. `grep -n "private volatile bool _isGhostMode" FuzzyClock.App/GhostModeController.cs` returns zero matches.
5. Both new test files exist under `FuzzyClock.App.Tests/` with `[TestClass]` declarations.
</verification>

<success_criteria>
- TEST-01: existing 449 Core + 129 App = 578 baseline tests still pass unchanged after this plan.
- TEST-02: `LerpRatioTests` covers D-LERP-01 terminal-state snap (target=1.0 across multiple currents, target=0.0 across multiple currents, mid-range non-snap).
- TEST-03: `OnSampleTickTests` covers D-SEAM-01 four `GhostTransition` classes — seam reachable without timers/threads.
- TEST-04: `dotnet test FuzzyClock.sln --nologo --verbosity quiet` reports `Passed >= 578 + new rows, Failed: 0, Skipped: 0`.
- D-SEAM-02b: `_isGhostMode` field is `internal volatile bool` at `GhostModeController.cs:70`; production-side write ownership rule preserved.
</success_criteria>

<output>
Create `.planning/phases/87-verification-performance-acceptance/87-01-SUMMARY.md` when done — capture: new test row counts (LerpRatio + OnSampleTick), final `dotnet test` Passed/Failed/Skipped numbers, the one-line `_isGhostMode` field-visibility diff, and any deviations from the D-LERP-02 / D-SEAM-01 row layouts in CONTEXT.md.
</output>

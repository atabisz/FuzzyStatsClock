---
phase: 17-context-aware-font-size-menu
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified: []
autonomous: false
requirements:
  - MENU-01
must_haves:
  truths:
    - "In phrase mode, Font Size submenu is present and checkmark reflects active size"
    - "In dial mode, Font Size submenu is absent from the context menu"
    - "Switching back to phrase mode restores Font Size submenu"
    - "Font size preference is preserved across dial mode round-trip"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "Verified MENU-01 behavior via human acceptance"
      contains: "MenuFontSize.Visibility"
  key_links: []
---

<objective>
Human verification of all four Phase 17 success criteria for MENU-01: Font Size submenu hidden in dial mode, restored in phrase mode, font size preference preserved.

Purpose: Confirms the context-aware menu behavior works end-to-end in the running application.

Output: Human acceptance recorded; Phase 17 closed; v1.9 Context-Aware Menus milestone complete.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-context-aware-font-size-menu/17-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verify all four MENU-01 success criteria</name>
  <files>none — observation only</files>
  <action>Human verification of all four Phase 17 behavioral requirements implemented in Plan 17-01.</action>
  <what-built>
    Font Size submenu (MenuFontSize) is now context-aware: hidden in dial mode, present in phrase mode.
    Implemented by adding x:Name="MenuFontSize" to XAML and wiring MenuFontSize.Visibility in
    ContextMenu_Opened and SetDialMode — identical pattern to Phase 16's DIAL-09 (MenuDialFace).
  </what-built>
  <how-to-verify>
    Build and launch the app:
    ```
    cd C:/src/gsd1 && dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj
    ```

    **Step 1 — Phrase mode baseline (Criterion 1):**
    Right-click the widget. Confirm "Font Size" submenu IS present. Hover over it and confirm
    Small/Medium/Large items appear with a checkmark on the currently active size.
    If Dial Mode is currently active, click it first to return to phrase mode.

    **Step 2 — Dial mode hides Font Size (Criterion 2):**
    Right-click and click "Dial Mode" to switch to dial mode.
    Right-click again. Confirm "Font Size" submenu is ABSENT from the menu.
    Open the menu two more times and confirm it remains absent on every open.

    **Step 3 — Return to phrase mode restores Font Size (Criterion 3):**
    With dial mode active, right-click and click "Dial Mode" again to return to phrase mode.
    Right-click once more. Confirm "Font Size" submenu IS present again.

    **Step 4 — Font size preference preserved across round-trip (Criterion 4):**
    Hover over "Font Size". Confirm the checkmark is on the same size as in Step 1.
    The mode switch must not have changed _currentFontSize.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual behavior requires human observation</automated>
    <manual>Run app, confirm all 4 steps as described in how-to-verify above</manual>
  </verify>
  <done>All four Phase 17 success criteria confirmed by user: Font Size submenu present in phrase mode, absent in dial mode, restored on return, font size preference unchanged.</done>
  <resume-signal>Type "approved" if all four criteria pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All four MENU-01 success criteria confirmed by human observation:
1. Phrase mode: Font Size submenu present with correct checkmark
2. Dial mode: Font Size submenu absent
3. Return to phrase: Font Size submenu restored
4. Font size unchanged by mode switches
</verification>

<success_criteria>
Human approves all four MENU-01 criteria. Phase 17 complete. v1.9 milestone closed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-context-aware-font-size-menu/17-02-SUMMARY.md` using the summary template.
</output>

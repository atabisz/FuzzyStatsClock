---
milestone: v2.6
audited: 2026-03-03T03:11:58Z
status: passed
scores:
  requirements: 6/6
  phases: 2/2
  integration: 6/6
  flows: 6/6
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 31-auto-launch-at-login
    items:
      - "AutoLaunchService.IsEnabled() defined but never called — by design (settings.json is source of truth; registry synced from AutoLaunchEnabled on startup, not read back)"
  - phase: 32-per-monitor-position-memory
    items:
      - "Stale XML doc comment MainWindow.xaml.cs L225: references removed Left=-1 sentinel; actual code uses TryGetValue(LastActiveMonitor). Zero functional impact."
      - "AppSettingsTests.cs STEST-01 round-trip test does not explicitly assert AutoLaunchEnabled=true after JSON round-trip. Minor coverage gap."
  - phase: cross-milestone
    items:
      - "SUMMARY.md frontmatter for all 4 plans lacks requirements-completed field. Documentation gap only — VERIFICATION.md and REQUIREMENTS.md both confirm full coverage."
human_verification_needed:
  - phase: 31-auto-launch-at-login
    items:
      - "Tray 'Auto-Launch at Login' checkable item visible and unchecked on first launch"
      - "HKCU Run registry entry written when enabled; removed when disabled"
      - "AutoLaunchEnabled round-trips through restart"
      - "Reset to Defaults disables auto-launch and removes registry entry"
  - phase: 32-per-monitor-position-memory
    items:
      - "QueryDisplayConfig friendly name resolution on real multi-monitor hardware"
      - "Cross-monitor drag: source monitor entry absent from settings.json after drag"
      - "Disconnected monitor: widget centers on primary screen"
---

# Milestone v2.6 Polish — Audit Report

**Milestone Goal:** Widget is effortless to set up — launches automatically at Windows login and always returns to the right position on the right monitor.

**Audited:** 2026-03-03T03:11:58Z
**Status:** PASSED
**Phases:** 2/2 complete | **Requirements:** 6/6 satisfied
**Integration flows:** 6/6 complete
**Build:** 0 errors | **Tests:** 78 passed, 0 failed

---

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Description | Phase | VERIFICATION.md | REQUIREMENTS.md | Final Status |
|--------|-------------|-------|-----------------|-----------------|--------------|
| STRT-01 | Tray toggle with checkmark for auto-launch | 31 | passed (5/5) | [x] Complete | **satisfied** |
| STRT-02 | Auto-launch setting persists to settings.json + restores on launch | 31 | passed (5/5) | [x] Complete | **satisfied** |
| STRT-03 | HKCU Run entry written when enabled; removed when disabled | 31 | passed (5/5) | [x] Complete | **satisfied** |
| MON-01 | Widget tracks last-used position per connected monitor | 32 | passed (10/10) | [x] Complete | **satisfied** |
| MON-02 | On startup, widget restores to position on last-used monitor | 32 | passed (10/10) | [x] Complete | **satisfied** |
| MON-03 | Widget centers on primary if saved monitor is absent | 32 | passed (10/10) | [x] Complete | **satisfied** |

Note: SUMMARY.md frontmatter lacks `requirements-completed` field for all plans. Both VERIFICATION.md (passed) and REQUIREMENTS.md ([x]) confirm full coverage — documentation gap only, not a satisfaction blocker.

No orphaned requirements — all 6 REQ-IDs appear in both phase VERIFICATION.md files with full evidence.

---

## Phase Verification Summary

| Phase | Status | Score | Anti-Patterns | Key Tech Debt |
|-------|--------|-------|---------------|---------------|
| 31: Auto-Launch at Login | ✓ passed | 5/5 must-haves | None | IsEnabled() unused (by design) |
| 32: Per-Monitor Position Memory | ✓ passed | 10/10 must-haves | None | Stale comment L225; STEST-01 gap |

---

## Cross-Phase Integration

### Schema Coexistence (Primary Concern)

Phase 31 added `AutoLaunchEnabled` to `AppSettings`. Phase 32 rewrote `AppSettings` (removed `Left`/`Top`, added `MonitorPositions`/`LastActiveMonitor`). **Verified: both changes coexist correctly.**

- `AppSettings` record: Phase 31 fields (`AutoLaunchEnabled`) preserved alongside Phase 32 fields (`MonitorPositions`, `LastActiveMonitor`).
- `SettingsService.Load()` migration: `with` expression replaces only `MonitorPositions`/`LastActiveMonitor` — `AutoLaunchEnabled` carried over unchanged from deserialized JSON.
- `SettingsService.Defaults()` (Phase 32 rewrite): explicitly enumerates `AutoLaunchEnabled = false` — closes prior tech debt from partial audit (when only Phase 31 was complete).

### E2E Flows

| Flow | Requirements | Status |
|------|-------------|--------|
| User enables auto-launch from tray toggle | STRT-01, STRT-03 | ✓ Complete |
| Auto-launch preference survives restart | STRT-02 | ✓ Complete |
| Reset to Defaults disables auto-launch | STRT-01, STRT-03 | ✓ Complete |
| Widget restores to last-used monitor on startup | MON-02 | ✓ Complete |
| Widget saves position per monitor after drag | MON-01 | ✓ Complete |
| Widget centers on primary when saved monitor disconnected | MON-03 | ✓ Complete |

**Broken flows:** 0 | **Orphaned exports:** 1 (`AutoLaunchService.IsEnabled()` — by design) | **Missing connections:** 0

---

## Tech Debt

### Phase 31: Auto-Launch at Login
- **`AutoLaunchService.IsEnabled()` dead code** (Low) — Defined and correct but never called. Design uses settings.json as source of truth; registry is written unconditionally from `AutoLaunchEnabled` on startup. Could be removed or kept as a testable surface.

### Phase 32: Per-Monitor Position Memory
- **Stale comment** (Cosmetic) — `MainWindow.xaml.cs` L225: `"If a saved position exists (Left != -1)"` references the removed `Left` sentinel. Actual code correctly uses `TryGetValue(LastActiveMonitor)`. Zero functional impact.
- **Missing test assertion** (Low) — `AppSettingsTests.cs` STEST-01 does not assert `AutoLaunchEnabled = true` round-trip. Mechanism identical to `GhostModeEnabled` (tested). Low risk.

### Cross-Milestone
- **SUMMARY.md frontmatter** (Low) — `requirements-completed` field absent from all 4 plan SUMMARY files. Does not affect functionality or verification.

**Total tech debt items:** 4 (all non-blocking, cosmetic or documentation)

---

## Human Verification Required

All code paths verified by static analysis. The following require manual testing:

### Phase 31 (Windows Registry / Tray UI)
1. Tray "Auto-Launch at Login" item visible and unchecked on first launch
2. Registry entry written at `HKCU\SOFTWARE\Microsoft\Windows\CurrentVersion\Run` when enabled; absent when disabled
3. Setting persists across restart (checkmark + registry re-synced by `ApplySettings`)
4. "Reset to Defaults" removes registry entry and unchecks tray item

### Phase 32 (Multi-Monitor Hardware)
1. `LastActiveMonitor` in settings.json shows friendly name (e.g. `"dell u2720q"`) not GDI fallback
2. Cross-monitor drag: source monitor entry absent from `MonitorPositions` after drag
3. Disconnected monitor: widget appears on primary screen (not off-screen at saved coordinates)

---

## Milestone Goal Assessment

**Goal:** Widget is effortless to set up — launches automatically at Windows login and always returns to the right position on the right monitor.

✓ Auto-launch at login — tray toggle writes/removes HKCU Run entry; persisted to settings.json; restored on next launch
✓ Per-monitor position memory — widget saves and restores position per monitor identity; centers on primary when monitor absent

**Milestone goal: ACHIEVED (pending human runtime verification)**

---

_Audited: 2026-03-03T03:11:58Z_
_Auditor: Claude (gsd-integration-checker + orchestrator)_
_Supersedes partial audit from 2026-03-03 (Phase 31 only)_

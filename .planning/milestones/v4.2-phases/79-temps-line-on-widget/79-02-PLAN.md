---
phase: 79-temps-line-on-widget
plan: 02
type: execute
wave: 2
depends_on:
  - 79-01
files_modified:
  - .planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - TEMP-LINE-01
  - TEMP-LINE-02
  - TEMP-LINE-03
  - TEMP-LINE-04
  - TEMP-LINE-05
  - TEMP-LINE-06

must_haves:
  truths:
    - "On live dev hardware, TempsText renders in StatsPanel directly below UptimeText when master toggle is ON"
    - "The rendered line uses compact inline format: 2-space separator, integer Celsius, ° only; dev-box case likely shows `GPU 51°` single segment"
    - "Only friendly labels (CPU/GPU/Mobo/NVMe) appear — never raw LHM sensor names like Tctl/Tdie or Core #0"
    - "N/A sensors (-1f sentinel) are silently omitted — no `NVMe (N/A)` placeholder; remaining segments reflow"
    - "Toggling master OFF in Settings collapses TempsText IMMEDIATELY (no widget restart)"
    - "Toggling an individual sensor checkbox adds/removes that segment within one dispatcher frame"
    - "TempsText color matches UptimeText color exactly (accent-color + auto-contrast parity)"
    - "Moving the widget from bright to dark desktop background adjusts both UptimeText and TempsText in lockstep"
    - "User signs off checkpoint with `approved`"
  artifacts:
    - path: ".planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md"
      provides: "Human-verify sign-off record with per-item checklist results"
      contains: "approved"
  key_links:
    - from: "Human verifier"
      to: "live widget rendering"
      via: "`dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj -c Release`"
      pattern: "widget launches and shows temps line on supported hardware"
---

<objective>
Human-verify the Phase 79 rendering contract on live dev hardware. Exercise all 6 TEMP-LINE requirements + the Phase 78 event-reflow bridge (TEMP-TAB-05 SC5) + the Phase 33 auto-contrast participation. User runs through a 22-item / 7-category checklist against `dotnet run` and signs off with `approved`. This plan contains exactly ONE task — a blocking human-verify checkpoint — per D-18 and Phase 78-02 precedent.

Purpose: No production code can be automated-verified to render pixels correctly in WPF. Manual verification is the terminal gate for visible-to-user correctness.

Output: `.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md` documenting the checklist results + user approval, plus STATE.md / ROADMAP.md / REQUIREMENTS.md updates marking Phase 79 complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-temps-line-on-widget/79-CONTEXT.md
@.planning/phases/79-temps-line-on-widget/79-UI-SPEC.md
@.planning/phases/79-temps-line-on-widget/79-01-PLAN.md
@.planning/phases/78-temps-tab-in-settings/78-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify TempsText rendering + reflow + accent + auto-contrast</name>
  <files>.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - c:/src/FuzzyStatsClock/.planning/phases/79-temps-line-on-widget/79-CONTEXT.md (D-18 + D-19 dev-box expectations)
    - c:/src/FuzzyStatsClock/.planning/phases/79-temps-line-on-widget/79-UI-SPEC.md (Verification Hooks section — the 10 dimensions the auditor tracks)
    - c:/src/FuzzyStatsClock/.planning/phases/78-temps-tab-in-settings/78-02-SUMMARY.md (Task 3 Human-Verify template — mirror the presentation shape)
  </read_first>

  <what-built>
    Plan 79-01 (automated, already shipped):
    - New `TempsText` TextBlock as last child of StatsPanel in MainWindow.xaml, cloning UptimeText styling (11px Segoe UI Light, Margin="0,2,0,0", design-time Foreground="White").
    - New `UpdateTempsDisplay()` private method in MainWindow.xaml.cs reading `_temperatureService?.{Cpu,Gpu,Mobo,Nvme}TempC ?? -1f` + `_settings.Temp*Visible`, calling `TemperatureFormatter.Format(...)`, then setting `TempsText.Text = formatted;` and `TempsText.Visibility = (_settings.TempsLineVisible && formatted.Length > 0) ? Visible : Collapsed;`.
    - Timer tick at line ~140 of MainWindow.xaml.cs extended: `UpdateTempsDisplay();` appended after `UpdateUptimeDisplay();`.
    - All 5 Phase 78 Settings event handlers (TempsLineVisibleChanged / TempCpuVisibleChanged / TempGpuVisibleChanged / TempMoboVisibleChanged / TempNvmeVisibleChanged) extended with `UpdateTempsDisplay();` immediately after `SaveSettings();` — immediate reflow.
    - `ApplyTheme` (line ~1637) + `ApplyDisplayColor` (line ~1674) each extended with `TempsText.Foreground = brush;` immediately after the existing `UptimeText.Foreground = brush;` line — Phase 33 critical pattern.
    - `FuzzyClock.App.Tests/TempsLineTests.cs` with 6 parametric DataRow rows covering the visibility-predicate truth table + 2 formatter-consumption tests. Suite green at >= 556.
  </what-built>

  <action>
    Present the 22-item / 7-category checklist below to the user. User launches the widget and walks each numbered item in order. Upon full sign-off (`approved`), write `.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md` mirroring the 78-02 SUMMARY structure (YAML frontmatter + Work Completed section documenting the 22-item checklist results + Self-Check block). Then update STATE.md (new frontmatter block, `completed_phases: 5`, `last_activity` cites Phase 79 completion), ROADMAP.md (Phase 79 entry -> `- [x] ... (completed 2026-05-04)`, progress table row `| 79. Temps Line on Widget | 2/2 | Complete | 2026-05-04 |`), and REQUIREMENTS.md (TEMP-LINE-01..06 each flipped from `- [ ]` to `- [x]` with "Complete (2026-05-04)" evidence references to Plan 79-01 commit hashes and this human-verify checkpoint). Commit all four files in a single metadata commit with message `docs(79-02): capture human-verify sign-off`. Do NOT add Co-Authored-By trailer (project CLAUDE.md rule).

    If user reports any failure (e.g., `fail 17: temps stayed cyan when accent changed to magenta`), DO NOT write the SUMMARY or mark Phase 79 complete. Instead, report the failure back to the orchestrator — gap closure planning (`/gsd:plan-phase 79 --gaps`) will spawn a follow-up plan 79-03 to address the specific failing items.

    The 22-item checklist (present verbatim to user; mirror the Phase 78-02 Task 3 presentation shape):

    From a PowerShell prompt at `c:\src\FuzzyStatsClock\`:

        dotnet run --project FuzzyClock.App/FuzzyClock.App.csproj -c Release

    Widget should launch within ~5 seconds. Stats panel must be visible to see the temps line — if hidden, right-click the widget and enable "Stats".

    Category 1 — Baseline render (TEMP-LINE-01, TEMP-LINE-03):
      1. Right-click widget > Settings > Temps tab > check "Show Temps Line" (master ON). Close Settings.
      2. Within one stats-tick interval (<= 5s), a new text line appears in StatsPanel directly BELOW the uptime line. SAME font, SAME size, SAME color as uptime line above.
      3. Rendered text uses only friendly labels `CPU`/`GPU`/`Mobo`/`NVMe` — no raw names like `Tctl/Tdie`, `Core #0`, `GPU Core`, `Hot Spot`. On dev box per D-19, expected line is `GPU 51°` (GPU reading varies with load).
      4. NO placeholder `(N/A)` suffix, no stray separator, no trailing whitespace.

    Category 2 — Format shape (TEMP-LINE-02, TEMP-LINE-04):
      5. Visible segments joined by EXACTLY two spaces. (Trivially true if only one segment.)
      6. Each segment ends with degree sign `°` (U+00B0). NO `C` or `F` suffix. Not the ordinal `º`, not an apostrophe.
      7. Each temperature is INTEGER (no decimal). Reading tracks reality — load GPU briefly (play YouTube 10s); integer ticks upward within a few timer intervals.
      8. Dev-box expectation per D-19 + Phase 75 spike: CPU ABSENT, Mobo ABSENT, NVMe ABSENT; line is JUST `GPU 51°`.

    Category 3 — Master-toggle reflow (TEMP-TAB-05 SC5):
      9. With temps line visible, right-click > Settings > Temps tab > UNCHECK "Show Temps Line".
     10. Temps line disappears IMMEDIATELY (single dispatcher frame). Uptime shifts up to fill gap. No empty stripe.
     11. Re-check "Show Temps Line". Line reappears IMMEDIATELY with same content.
     12. Close Settings. Quit widget (right-click > Quit). Relaunch. Temps line visible at launch — master toggle persisted.

    Category 4 — Per-sensor reflow (TEMP-LINE-04, TEMP-TAB-05 SC5):
     13. With master ON and line visible, Settings > Temps > UNCHECK GPU (if currently checked).
     14. Line re-renders immediately. Dev box: if GPU was only segment, line COLLAPSES. Full hardware: GPU segment disappears, other segments reflow with 2-space separator preserved.
     15. Re-check GPU. Segment reappears.
     16. Toggle any OTHER per-sensor checkbox (CPU/Mobo/NVMe — whichever is enabled). Confirm same immediate reflow.

    Category 5 — Accent color parity (TEMP-LINE-06):
     17. Right-click > Accent Color > pick distinct color (Magenta or Amber). Uptime AND temps lines change in SAME TICK — no mismatch, no one-tick lag.
     18. Right-click > Accent Color > Custom > pick custom RGB. Both lines change together.
     19. Set accent back to default (Cyan).

    Category 6 — Auto-contrast participation (TEMP-LINE-06):
     20. Enable Auto-Contrast via right-click. Move widget over BRIGHT desktop background (white browser). Both lines shift to darker/more saturated accent in lockstep.
     21. Move over BLACK/dark desktop. Both lines shift back in lockstep. NEVER a frame where the two lines have visibly different colors.

    Category 7 — Timer piggyback + regression sweep (TEMP-LINE-05 + stability):
     22. Hover over widget (fast-refresh 500ms). Temps integer updates every ~500ms under hover. Move mouse away; widget returns to normal 5s cadence. No new timer-related error, no double-update, no CPU spike attributable to Phase 79.

    Regression sweep (no item number — confirm unchanged):
      - Phrase / Dial / LCD / Nixie clock modes all still work.
      - Right-click widget menu (Phase 77) still opens and mirrors tray menu.
      - Ghost mode + proximity fade (Phase 66-69) still work.
      - All other Settings tabs still work.
      - `%LOCALAPPDATA%\FuzzyClock\settings.json` contains valid JSON after quitting.

    User reply format:
      - `approved` (all 22 passed)
      - `fail <item numbers>: <brief description>` (blocks completion; gap closure required)
  </action>

  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path '.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md')) { Write-Error 'SUMMARY missing — awaiting approval'; exit 1 }; if ((Select-String -LiteralPath '.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md' -Pattern 'approved' -SimpleMatch).Count -lt 1) { Write-Error 'approval token missing'; exit 1 }; Write-Output 'checkpoint closed'"</automated>
  </verify>

  <done>
    User replied `approved` after walking all 22 items; 79-02-SUMMARY.md captures results; STATE.md / ROADMAP.md / REQUIREMENTS.md all reflect Phase 79 complete; a single metadata commit `docs(79-02): capture human-verify sign-off` landed with no Co-Authored-By trailer.
  </done>

  <resume-signal>Reply `approved` (or `fail <item numbers>: <brief description>`).</resume-signal>
</task>

</tasks>

<verification>
```bash
cd c:/src/FuzzyStatsClock
test -f .planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md
grep -c "approved" .planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md
grep -c "TEMP-LINE-06.*Complete" .planning/REQUIREMENTS.md
git log --format="%h %s" -1
# expect: <hash>  docs(79-02): capture human-verify sign-off
```
</verification>

<success_criteria>
- User signs off with `approved` after walking the 22-item checklist.
- 79-02-SUMMARY.md captures the approval + per-category results.
- STATE.md, ROADMAP.md, REQUIREMENTS.md all reflect Phase 79 complete.
- Single metadata commit `docs(79-02): capture human-verify sign-off` (no Co-Authored-By).
- Phase 79 is done; Phase 80 (Release) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/79-temps-line-on-widget/79-02-SUMMARY.md` capturing human-verify results and mirroring the 78-02 SUMMARY structure.
</output>

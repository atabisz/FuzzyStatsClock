---
phase: 34-uptime-process-count-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/MainWindow.xaml.cs
autonomous: true
requirements:
  - PROC-01

must_haves:
  truths:
    - "Uptime line displays process count as {N}p appended at the end of the line"
    - "Process count updates on every stats tick (same DispatcherTimer as uptime/cpu averages)"
    - "88 tests pass with zero failures"
  artifacts:
    - path: "FuzzyClock.App/MainWindow.xaml.cs"
      provides: "UpdateUptimeDisplay() with process count in {N}p format"
      contains: "Process.GetProcesses()"
  key_links:
    - from: "FuzzyClock.App/MainWindow.xaml.cs"
      to: "System.Diagnostics.Process.GetProcesses()"
      via: "UpdateUptimeDisplay() called by _statsTimer.Tick"
      pattern: "Process\\.GetProcesses\\(\\)"
    - from: "FuzzyClock.App/MainWindow.xaml.cs"
      to: "UptimeText.Text"
      via: "string interpolation {procCount}p"
      pattern: "\\{procCount\\}p"
---

<objective>
Verify and confirm that the process count feature (PROC-01) is fully wired in the app. Inspect UpdateUptimeDisplay() to confirm: Process.GetProcesses() is called, result is formatted as {N}p at the end of the uptime line, all Process objects are disposed, and the update fires on each stats tick. If any part is incomplete or incorrectly formatted, fix it now.

Purpose: PROC-01 must be provably satisfied before v2.8 ships. The format spec is `{N}p` appended after the load averages (e.g. `up 5h 3m   0.52  0.47  0.43  142p`).
Output: Confirmed implementation with all 88 tests passing.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@FuzzyClock.App/MainWindow.xaml.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and confirm PROC-01 process count implementation</name>
  <files>FuzzyClock.App/MainWindow.xaml.cs</files>
  <action>
Read FuzzyClock.App/MainWindow.xaml.cs and locate UpdateUptimeDisplay(). Verify the following are ALL true:

1. `System.Diagnostics.Process.GetProcesses()` is called inside UpdateUptimeDisplay()
2. All returned Process objects are disposed (foreach p in procs, p.Dispose())
3. The count is formatted as `{procCount}p` (no space between count and 'p') appended AFTER the three load averages
4. The full line format is: `{uptimeStr}   {avg1m / 100f:F2}  {avg5m / 100f:F2}  {avg15m / 100f:F2}  {procCount}p`
5. UpdateUptimeDisplay() is called inside _statsTimer.Tick (same tick as UpdateStatsDisplay)
6. The loop that builds procCount uses a `pct >= 5.0` threshold — only active processes (≥5% CPU) are counted, NOT all processes unconditionally

If all six conditions are confirmed correct — no code changes are needed. Document the finding.

If ANY condition is wrong or missing, fix it in-place:
- Wrong format: correct the string interpolation
- Missing dispose: add dispose loop
- Missing call site: add UpdateUptimeDisplay() to _statsTimer.Tick after UpdateStatsDisplay()
- Missing GetProcesses: add the three-line block (GetProcesses → procCount → dispose loop)

Do NOT refactor this into a separate method or Core class — keep the inline implementation exactly as-is if it is correct.
  </action>
  <verify>
    <automated>dotnet test FuzzyClock.slnx --no-build 2>&1 | tail -6 && grep -c "pct >= 5" C:/src/FuzzyStatsClock/FuzzyClock.App/MainWindow.xaml.cs</automated>
    <manual>Confirm UpdateUptimeDisplay() contains `Process.GetProcesses()`, `pct >= 5` threshold, and `{procCount}p` by reading the file after any edits</manual>
  </verify>
  <done>
UpdateUptimeDisplay() calls Process.GetProcesses(), counts only processes with pct >= 5.0 (active processes ≥5% CPU — NOT unconditional total count), disposes all Process objects, and formats the result as {N}p at the end of the uptime line. All 88 tests pass.
  </done>
</task>

</tasks>

<verification>
Run: dotnet test FuzzyClock.slnx --no-build

Expected: Passed! — Failed: 0, Passed: 88, Skipped: 0

Also confirm by reading the implementation:
- UpdateUptimeDisplay() contains `Process.GetProcesses()`
- The count loop uses `pct >= 5.0` (or equivalent) — active processes only, not unconditional total
- String interpolation ends with `{procCount}p`
- _statsTimer.Tick calls UpdateUptimeDisplay()
</verification>

<success_criteria>
1. Process count appears in the uptime line as `{N}p` (e.g. 142p) with no space between number and p
2. Process objects are disposed after count is read
3. UpdateUptimeDisplay() fires on every _statsTimer.Tick
4. All 88 tests pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/34-uptime-process-count-readme/34-01-SUMMARY.md`
</output>

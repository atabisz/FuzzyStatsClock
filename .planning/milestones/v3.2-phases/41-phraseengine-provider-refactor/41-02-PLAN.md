---
phase: 41-phraseengine-provider-refactor
plan: 02
type: tdd
wave: 2
depends_on: ["41-01"]
files_modified:
  - FuzzyClock.Core.Tests/PhraseEngineCoordinatorTests.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "PhraseEngine.CurrentLocale returns 'en-classic' on startup"
    - "SetLocale with a known locale returns true and updates CurrentLocale"
    - "SetLocale with an unknown locale returns false and leaves CurrentLocale unchanged"
    - "GetPhrase delegates correctly through the facade after a SetLocale round-trip"
    - "All 126 tests pass (122 pre-existing + 4 new coordinator tests)"
  artifacts:
    - path: "FuzzyClock.Core.Tests/PhraseEngineCoordinatorTests.cs"
      provides: "Coordinator contract tests for SetLocale/CurrentLocale"
      contains: "PhraseEngineCoordinatorTests"
  key_links:
    - from: "FuzzyClock.Core.Tests/PhraseEngineCoordinatorTests.cs"
      to: "FuzzyClock.Core/PhraseEngine.cs"
      via: "static facade calls"
      pattern: "PhraseEngine\\.SetLocale|PhraseEngine\\.CurrentLocale"
---

<objective>
TDD coverage for the new PhraseEngine coordinator API: SetLocale(), CurrentLocale, and delegation behavior. Four tests validate the contract introduced in Plan 01 using RED→GREEN→REFACTOR.

Purpose: The coordinator contract (SetLocale/CurrentLocale) is new behavior with no prior tests. TDD ensures the contract is specified before trusting the implementation is correct.
Output: PhraseEngineCoordinatorTests.cs with 4 tests; total suite grows from 122 to 126 passing.
</objective>

<execution_context>
@C:/Users/altab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/altab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-phraseengine-provider-refactor/41-01-SUMMARY.md

<interfaces>
<!-- API under test — established in Plan 01 -->
From FuzzyClock.Core/PhraseEngine.cs (after Plan 01 refactor):
```csharp
public static class PhraseEngine
{
    public static string CurrentLocale { get; private set; }   // "en-classic" on startup

    // Returns true if locale known and provider swapped; false if unknown (no change).
    public static bool SetLocale(string locale);

    public static string GetPhrase(DateTime dt);
    public static (string Qualifier, string Emphasis) GetStructuredPhrase(DateTime dt);
}
```

From FuzzyClock.Core.Tests/MSTestSettings.cs (existing pattern — test parallelization):
```csharp
[assembly: Parallelize(Workers = 0, Scope = ExecutionScope.MethodLevel)]
```
IMPORTANT: PhraseEngine is a static class. Its state persists across test methods in the process.
Each test that calls SetLocale MUST reset to "en-classic" in [TestCleanup] to prevent state leaks.
</interfaces>
</context>

<feature>
  <name>PhraseEngine coordinator contract</name>
  <files>FuzzyClock.Core.Tests/PhraseEngineCoordinatorTests.cs, FuzzyClock.Core/PhraseEngine.cs</files>
  <behavior>
    Test cases:
    1. DefaultLocale_IsEnClassic → PhraseEngine.CurrentLocale == "en-classic" (no SetLocale called)
    2. SetLocale_KnownLocale_ReturnsTrue → SetLocale("en-classic") returns true; CurrentLocale == "en-classic"
    3. SetLocale_UnknownLocale_ReturnsFalse_LocaleUnchanged → SetLocale("fr") returns false; CurrentLocale still == "en-classic"
    4. GetPhrase_DelegatesCorrectly_AfterSetLocaleRoundTrip → SetLocale("en-classic"); GetPhrase(new DateTime(2024,1,15,3,30,0)) == "half past three"

    Guard: [TestCleanup] method calls PhraseEngine.SetLocale("en-classic") to reset static state after every test method.
    This prevents the state leak pitfall (Pitfall 3 from RESEARCH.md): if SetLocale("xx") is tested, the false-return path
    leaves state unchanged, but the true-path test must still reset.
  </behavior>
  <implementation>
    RED: Write the test file as shown in RESEARCH.md code examples. Run `dotnet test` — tests for SetLocale("fr") returning false will pass (returns false correctly), but confirm test file compiles and the tests for CurrentLocale and delegation run. The test structure validates the contract.

    GREEN: Plan 01 already implements the production code. All four tests should pass immediately — this is a verification TDD cycle, not a build-before-test cycle. If any test fails, diagnose the Plan 01 implementation and fix PhraseEngine.cs.

    REFACTOR: Inspect the test class for clarity. Ensure [TestCleanup] covers all test methods. Add XML doc comment on the class explaining the static-state isolation requirement.
  </implementation>
</feature>

<verification>
`dotnet test C:/src/FuzzyStatsClock/FuzzyClock.sln --no-restore 2>&1 | tail -10`

Must report: 126 passed, 0 failed (122 pre-existing + 4 new coordinator tests).
Must exit with code 0.
</verification>

<success_criteria>
- PhraseEngineCoordinatorTests.cs exists in FuzzyClock.Core.Tests/ with namespace FuzzyClock.Core.Tests
- 4 [TestMethod] methods covering: default locale, known SetLocale, unknown SetLocale, delegation
- [TestCleanup] resets static state to "en-classic" after each method
- All 126 tests pass: `dotnet test` exits 0 with 126 passed, 0 failed
- No existing test file modified
</success_criteria>

<output>
After completion, create `.planning/phases/41-phraseengine-provider-refactor/41-02-SUMMARY.md`
</output>

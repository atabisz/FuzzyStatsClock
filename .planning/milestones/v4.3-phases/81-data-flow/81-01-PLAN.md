---
phase: 81-data-flow
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App.Tests/AppSettingsTests.cs
autonomous: true
requirements:
  - CFG-04
  - TST-01
  - TST-02

must_haves:
  truths:
    - "Deserializing v4.2 settings.json (missing UseCtrl/UseAlt/UseShift) yields UseCtrl=true, UseAlt=true, UseShift=false"
    - "Serializing AppSettings with UseCtrl=false, UseAlt=false, UseShift=true round-trips correctly"
    - "MSTest suite remains green with 3 new absent-field tests + round-trip extension"
  artifacts:
    - path: "FuzzyClock.App.Tests/AppSettingsTests.cs"
      provides: "3 absent-field tests for UseCtrl/UseAlt/UseShift + round-trip extension"
      min_lines: 30
  key_links:
    - from: "AppSettingsTests.cs"
      to: "AppSettings.cs"
      via: "JsonSerializer.Deserialize<AppSettings>"
      pattern: "JsonSerializer\\.Deserialize<AppSettings>"
---

<objective>
Establish test coverage for the three modifier configuration fields (UseCtrl, UseAlt, UseShift) before schema extension.

Purpose: TDD first — tests define the contract (init defaults true/true/false, round-trip behavior) before production code. Prevents silent upgrade bugs where v4.2 users lose Ctrl+Alt default on upgrade.

Output: 3 new absent-field test methods + round-trip test extension in AppSettingsTests.cs, all passing against placeholder AppSettings schema.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/81-data-flow/81-RESEARCH.md

# Existing test patterns (RED-GREEN-REFACTOR reference)
@FuzzyClock.App.Tests/AppSettingsTests.cs

# Schema to test against (will be extended in Plan 81-02)
@FuzzyClock.App/AppSettings.cs
</context>

<interfaces>
<!-- Existing AppSettings pattern from codebase -->
From FuzzyClock.App/AppSettings.cs:
```csharp
public record AppSettings
{
    // 56 existing fields with init-property pattern...
    public bool   UptimeVisible        { get; init; } = true;  // Example: explicit = true
    public bool   TempsLineVisible     { get; init; } = false; // Example: explicit = false
    // Phase 81 will add: UseCtrl, UseAlt, UseShift
}
```

From FuzzyClock.App.Tests/AppSettingsTests.cs:
```csharp
// Absent-field test pattern (lines 90-100)
[TestMethod]
public void Deserialize_MissingUptimeVisible_DefaultsToTrue()
{
    const string json = """{"FontSize":32}""";
    var result = JsonSerializer.Deserialize<AppSettings>(json)!;
    Assert.IsTrue(result.UptimeVisible,
        "UptimeVisible should default to true when absent from JSON (init default), not false (C# bool default)");
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Create absent-field tests for UseCtrl/UseAlt/UseShift</name>
  <files>FuzzyClock.App.Tests/AppSettingsTests.cs</files>
  
  <read_first>
    - FuzzyClock.App.Tests/AppSettingsTests.cs (lines 90-118 show absent-field test pattern)
    - .planning/phases/81-data-flow/81-RESEARCH.md (lines 252-280 show exact test code)
  </read_first>
  
  <behavior>
    - Test 1: Deserialize_MissingUseCtrl_DefaultsToTrue — minimal JSON (e.g., {"FontSize":32}), assert result.UseCtrl == true
    - Test 2: Deserialize_MissingUseAlt_DefaultsToTrue — minimal JSON, assert result.UseAlt == true
    - Test 3: Deserialize_MissingUseShift_DefaultsToFalse — minimal JSON, assert result.UseShift == false
    - All three tests MUST FAIL initially (RED phase) because AppSettings does not yet have UseCtrl/UseAlt/UseShift fields
  </behavior>
  
  <action>
Add three test methods to AppSettingsTests.cs after the existing Deserialize_MissingUptimeVisible_DefaultsToTrue method (around line 103):

1. **Deserialize_MissingUseCtrl_DefaultsToTrue**
   - const string json = """{"FontSize":32}"""; (minimal v4.2 settings.json simulation)
   - var result = JsonSerializer.Deserialize&lt;AppSettings&gt;(json)!;
   - Assert.IsTrue(result.UseCtrl, "UseCtrl should default to true when absent from JSON (init default), not false (C# bool default)");

2. **Deserialize_MissingUseAlt_DefaultsToTrue**
   - const string json = """{"FontSize":32}""";
   - var result = JsonSerializer.Deserialize&lt;AppSettings&gt;(json)!;
   - Assert.IsTrue(result.UseAlt, "UseAlt should default to true when absent from JSON (init default), not false (C# bool default)");

3. **Deserialize_MissingUseShift_DefaultsToFalse**
   - const string json = """{"FontSize":32}""";
   - var result = JsonSerializer.Deserialize&lt;AppSettings&gt;(json)!;
   - Assert.IsFalse(result.UseShift, "UseShift should default to false when absent from JSON (init default = C# bool default)");

**Pattern notes:**
- Clone the UptimeVisible absent-field test pattern exactly (lines 90-102)
- Assertion messages MUST explain WHY the default matters (prevents upgrade bug)
- Minimal JSON uses """...""" raw string literal (C# 11+ syntax)
- Tests will compile but FAIL on `dotnet test` (RED) because AppSettings lacks the three fields

**Critical:** Do NOT add UseCtrl/UseAlt/UseShift to AppSettings.cs in this task. The tests must fail first (RED phase). Plan 81-02 adds the fields (GREEN phase).
  </action>
  
  <acceptance_criteria>
    - FuzzyClock.App.Tests/AppSettingsTests.cs contains 3 new [TestMethod] methods after line 103
    - Each method name matches pattern `Deserialize_MissingUseX_DefaultsToY`
    - Each method has const string json = """{"FontSize":32}"""; line
    - Each method has Assert message explaining "init default" vs "C# bool default"
    - `dotnet test --filter "FullyQualifiedName~AppSettingsTests"` produces 3 compilation errors: 'AppSettings' does not contain a definition for 'UseCtrl'/'UseAlt'/'UseShift'
    - Test project compiles after commenting out the three new tests (verification that only the new tests break compilation)
  </acceptance_criteria>
  
  <verify>
    <automated>dotnet build FuzzyClock.App.Tests/FuzzyClock.App.Tests.csproj 2>&1 | grep -E "error CS1061.*UseCtrl|UseAlt|UseShift"</automated>
  </verify>
  
  <done>Three absent-field test methods exist, correctly asserting init defaults (true/true/false), compilation fails with 3 CS1061 errors proving RED phase complete</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend round-trip test for UseCtrl/UseAlt/UseShift</name>
  <files>FuzzyClock.App.Tests/AppSettingsTests.cs</files>
  
  <read_first>
    - FuzzyClock.App.Tests/AppSettingsTests.cs (lines 19-85 show full round-trip test structure)
    - .planning/phases/81-data-flow/81-RESEARCH.md (lines 232-249 show extension pattern)
  </read_first>
  
  <behavior>
    - Extend RoundTrip_FullyPopulated_AllFieldsMatch method to include UseCtrl/UseAlt/UseShift
    - Original instance sets UseCtrl=false, UseAlt=false, UseShift=true (flipped from defaults to prove serialization)
    - After deserialization, assert all three fields match original
    - Test MUST FAIL initially (RED phase) because AppSettings does not yet have the three fields
  </behavior>
  
  <action>
Extend the existing RoundTrip_FullyPopulated_AllFieldsMatch test method (lines 19-85):

1. **Add to `original` instance initialization (around line 52, after GhostFadeRadiusPx):**
   ```csharp
   UseCtrl  = false,  // flipped from default true to prove serialization
   UseAlt   = false,  // flipped from default true
   UseShift = true,   // flipped from default false
   ```

2. **Add assertions after line 84 (after GhostFadeRadiusPx assertion):**
   ```csharp
   Assert.AreEqual(original.UseCtrl,  result.UseCtrl,  "UseCtrl");
   Assert.AreEqual(original.UseAlt,   result.UseAlt,   "UseAlt");
   Assert.AreEqual(original.UseShift, result.UseShift, "UseShift");
   ```

**Pattern notes:**
- Fields are initialized with NON-DEFAULT values (false/false/true instead of true/true/false) to prove round-trip actually serializes them
- Assertion label strings match field names exactly (3rd parameter convention)
- Test will compile but FAIL on `dotnet test` (RED) because AppSettings lacks the fields

**Critical:** Do NOT add UseCtrl/UseAlt/UseShift to AppSettings.cs in this task. Plan 81-02 adds the fields (GREEN phase).
  </action>
  
  <acceptance_criteria>
    - RoundTrip_FullyPopulated_AllFieldsMatch contains 3 new fields in `original` initialization block
    - Values are UseCtrl=false, UseAlt=false, UseShift=true (flipped from defaults)
    - 3 new Assert.AreEqual lines exist after GhostFadeRadiusPx assertion
    - `dotnet build FuzzyClock.App.Tests` produces 6 compilation errors (3 for original init, 3 for assertions): 'AppSettings' does not contain a definition for 'UseCtrl'/'UseAlt'/'UseShift'
  </acceptance_criteria>
  
  <verify>
    <automated>dotnet build FuzzyClock.App.Tests/FuzzyClock.App.Tests.csproj 2>&1 | grep -c "error CS1061" | grep -E "^(6|9)$"</automated>
  </verify>
  
  <done>Round-trip test extended with 3 fields + 3 assertions, compilation fails with 6-9 CS1061 errors (3 from Task 1 + 6 from Task 2), proving RED phase complete for both test additions</done>
</task>

</tasks>

<verification>
**RED phase gate:**
1. `dotnet build FuzzyClock.App.Tests` exits non-zero (compilation fails)
2. All compilation errors are CS1061: 'AppSettings' does not contain a definition for 'UseCtrl'/'UseAlt'/'UseShift'
3. Error count is 9 (3 from absent-field tests + 6 from round-trip test)
4. No other compilation errors exist (pre-existing code remains clean)

**GREEN phase waits for Plan 81-02** which adds the three fields to AppSettings.cs.
</verification>

<success_criteria>
- [ ] 3 absent-field test methods added to AppSettingsTests.cs
- [ ] Round-trip test extended with 3 fields and 3 assertions
- [ ] All 4 test additions follow exact patterns from RESEARCH.md
- [ ] `dotnet build FuzzyClock.App.Tests` fails with 9 CS1061 errors referencing UseCtrl/UseAlt/UseShift
- [ ] Assertion messages document "init default" vs "C# bool default" rationale
- [ ] Test project compiles cleanly when new test lines are commented out (proves isolation)
</success_criteria>

<output>
After completion, create `.planning/phases/81-data-flow/81-01-SUMMARY.md` documenting:
- 3 absent-field tests added (UseCtrl/UseAlt/UseShift)
- Round-trip test extended (3 fields + 3 assertions)
- Compilation error count (9 CS1061 as expected)
- RED phase complete, ready for Plan 81-02 GREEN phase
</output>

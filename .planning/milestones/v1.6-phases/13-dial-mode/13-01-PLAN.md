---
phase: 13-dial-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FuzzyClock.App/AppSettings.cs
  - FuzzyClock.App/SettingsService.cs
  - FuzzyClock.App/MainWindow.xaml
  - FuzzyClock.App/MainWindow.xaml.cs
autonomous: true
requirements:
  - DIAL-01
  - DIAL-02
  - DIAL-04
  - DIAL-05

must_haves:
  truths:
    - "AppSettings record has a DialMode bool field with default false"
    - "SettingsService.Defaults() includes DialMode = false"
    - "MainWindow.xaml has a DialCanvas (80x80) in row 0 with Visibility=Collapsed"
    - "DialCanvas contains HourHand and MinuteHand Line elements (white, 2px, round caps)"
    - "MenuDialMode IsCheckable MenuItem exists in the ContextMenu with Click handler stub"
    - "Project builds with 0 errors"
  artifacts:
    - path: "FuzzyClock.App/AppSettings.cs"
      provides: "DialMode init-property bool field"
      contains: "public bool   DialMode"
    - path: "FuzzyClock.App/SettingsService.cs"
      provides: "Defaults() with DialMode = false"
      contains: "DialMode = false"
    - path: "FuzzyClock.App/MainWindow.xaml"
      provides: "DialCanvas with HourHand/MinuteHand; MenuDialMode in context menu"
      contains: "DialCanvas"
  key_links:
    - from: "FuzzyClock.App/AppSettings.cs"
      to: "FuzzyClock.App/SettingsService.cs"
      via: "DialMode field referenced in Defaults()"
      pattern: "DialMode\\s*="
    - from: "FuzzyClock.App/MainWindow.xaml"
      to: "FuzzyClock.App/MainWindow.xaml.cs"
      via: "MenuDialMode_Click event attribute on MenuItem"
      pattern: "MenuDialMode_Click"
---

<objective>
Add the AppSettings.DialMode persistence field, update SettingsService.Defaults(), add the DialCanvas with hand Line elements to the XAML, and add the MenuDialMode context menu item with a click-handler stub.

Purpose: Establishes the data model and visual scaffold for dial mode — no wiring yet, but every artifact that the code-behind needs in plan 02 will exist and the build will be clean.
Output: Updated AppSettings.cs, SettingsService.cs, MainWindow.xaml — all compiling cleanly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@FuzzyClock.App/AppSettings.cs
@FuzzyClock.App/SettingsService.cs
@FuzzyClock.App/MainWindow.xaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AppSettings.DialMode field and update SettingsService.Defaults()</name>
  <files>
    FuzzyClock.App/AppSettings.cs
    FuzzyClock.App/SettingsService.cs
  </files>
  <action>
    In AppSettings.cs, add the following line after the `PagVisible` field (line 14), before the closing brace:

    ```csharp
    public bool   DialMode            { get; init; } = false;
    ```

    The complete record should then read:
    ```csharp
    public record AppSettings
    {
        public double Left                 { get; init; } = -1;
        public double Top                  { get; init; } = 20;
        public int    FontSize             { get; init; } = 32;
        public bool   StatsVisible         { get; init; } = false;
        public int    StatsIntervalSeconds { get; init; } = 3;
        public bool   CpuVisible           { get; init; } = true;
        public bool   GpuVisible           { get; init; } = true;
        public bool   MemVisible           { get; init; } = true;
        public bool   PagVisible           { get; init; } = true;
        public bool   DialMode             { get; init; } = false;
    }
    ```

    In SettingsService.cs, add `DialMode = false` to the Defaults() initializer. The updated Defaults() method must read:
    ```csharp
    public static AppSettings Defaults() => new()
    {
        Left = -1, Top = 20, FontSize = 32,
        StatsVisible = false, StatsIntervalSeconds = 3,
        CpuVisible = true, GpuVisible = true, MemVisible = true,
        PagVisible = true, DialMode = false
    };
    ```

    Do NOT add a zero-guard for DialMode in Load() — it is a bool, not an int, so there is no dangerous zero-equivalent. The existing StatsIntervalSeconds zero-guard is sufficient and must not be changed.
  </action>
  <verify>
    <automated>cd C:/src/gsd1 && dotnet build FuzzyClock.App/FuzzyClock.App.csproj -c Debug --no-restore 2>&1 | tail -5</automated>
    <manual>Confirm AppSettings.cs has DialMode field and SettingsService.Defaults() has DialMode = false</manual>
  </verify>
  <done>AppSettings record has public bool DialMode { get; init; } = false; and SettingsService.Defaults() includes DialMode = false; build 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Add DialCanvas with hand Lines and MenuDialMode to MainWindow.xaml</name>
  <files>
    FuzzyClock.App/MainWindow.xaml
  </files>
  <action>
    Two additions to MainWindow.xaml:

    **Addition 1 — DialCanvas inside the inner Grid in row 0:**

    The inner Grid (child of the Border in Grid.Row="0") currently has two TextBlock children: ShadowText and PhraseText. Add DialCanvas as a third child of that same inner Grid, after PhraseText. This Canvas is hidden by default (Visibility=Collapsed) and will overlay the phrase area when dial mode is active.

    Add the following block immediately after the closing `</TextBlock>` tag of PhraseText (the one with x:Name="PhraseText") and before the closing `</Grid>` of the inner Grid:

    ```xml
    <!-- DialCanvas: 80x80 analog clock hands — toggled visible in dial mode.
         Sits in the same inner Grid as PhraseText; both cannot be visible simultaneously.
         Center = (40,40). Hand X1/Y1 are set from center; X2/Y2 computed via trig in code-behind.
         Initial X2/Y2 = 0,0 — code-behind UpdateDialDisplay() sets correct values before Show(). -->
    <Canvas x:Name="DialCanvas"
            Width="80" Height="80"
            Visibility="Collapsed">
        <Line x:Name="HourHand"
              X1="40" Y1="40" X2="40" Y2="40"
              Stroke="White" StrokeThickness="2"
              StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
        <Line x:Name="MinuteHand"
              X1="40" Y1="40" X2="40" Y2="40"
              Stroke="White" StrokeThickness="2"
              StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
    </Canvas>
    ```

    **Addition 2 — MenuDialMode MenuItem in the ContextMenu:**

    The ContextMenu currently has: Font Size submenu, Stats submenu, Close item. Add MenuDialMode as a new top-level MenuItem between the Stats submenu `</MenuItem>` closing tag and the `<MenuItem Header="Close" ...>` item:

    ```xml
    <MenuItem x:Name="MenuDialMode"
              Header="Dial Mode"
              IsCheckable="True"
              Click="MenuDialMode_Click" />
    ```

    The final ContextMenu item order must be:
    1. Font Size (submenu)
    2. Stats (submenu)
    3. Dial Mode (checkable, new)
    4. Close

    Do NOT add a code-behind handler body in this plan — plan 02 wires the handler. The Click="MenuDialMode_Click" attribute creates a partial-class stub that will be implemented in plan 02. The build will fail if a handler is declared in XAML but has no matching C# method, so the stub must be added as part of verifying this task. Add the following empty stub to MainWindow.xaml.cs at the bottom of the class (before the closing brace), so the build passes:

    ```csharp
    private void MenuDialMode_Click(object sender, RoutedEventArgs e) { }
    ```

    Add this stub to FuzzyClock.App/MainWindow.xaml.cs immediately before the closing brace of the class.
  </action>
  <verify>
    <automated>cd C:/src/gsd1 && dotnet build FuzzyClock.App/FuzzyClock.App.csproj -c Debug 2>&1 | tail -5</automated>
    <manual>Open MainWindow.xaml and confirm DialCanvas with HourHand/MinuteHand exists inside the inner Grid in row 0. Confirm MenuDialMode appears in the ContextMenu between Stats and Close.</manual>
  </verify>
  <done>Build 0 errors; DialCanvas (80x80, Visibility=Collapsed) with HourHand and MinuteHand Line elements (Stroke=White, StrokeThickness=2, round caps) is present in row 0 inner Grid; MenuDialMode IsCheckable MenuItem is present between Stats and Close in the ContextMenu</done>
</task>

</tasks>

<verification>
After both tasks: `dotnet build FuzzyClock.App/FuzzyClock.App.csproj -c Debug` reports 0 errors and 0 warnings related to new code.

Confirm in source:
- `AppSettings.cs` line count increased by 1 (DialMode field)
- `SettingsService.cs` Defaults() includes `DialMode = false`
- `MainWindow.xaml` contains `x:Name="DialCanvas"`, `x:Name="HourHand"`, `x:Name="MinuteHand"`, `x:Name="MenuDialMode"`
- `MainWindow.xaml.cs` contains empty `MenuDialMode_Click` stub
</verification>

<success_criteria>
- AppSettings.DialMode init-property field exists with default false
- SettingsService.Defaults() includes DialMode = false
- DialCanvas (Width=80, Height=80, Visibility=Collapsed) lives inside the inner Grid in row 0
- HourHand and MinuteHand are Line elements inside DialCanvas with Stroke=White, StrokeThickness=2, round caps
- MenuDialMode IsCheckable MenuItem is in the ContextMenu after Stats, before Close
- Empty MenuDialMode_Click stub in MainWindow.xaml.cs makes the build pass
- dotnet build reports 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-dial-mode/13-01-SUMMARY.md` using the summary template.
</output>

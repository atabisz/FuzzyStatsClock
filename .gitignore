# Build output
bin/
obj/
installer/
publish/

# User-specific files
*.user
*.suo
.vs/

# NuGet
*.nupkg
packages/

# Test results
TestResults/
*.trx
*.coverage

# Claude tooling (GSD workflow, agents, commands)
.claude/
.superpowers/

# Scratch / throwaway experiments (spike enumerators, one-shot probes)
scratch/

# Electron port (v5.0). electron/bun.lock IS committed — it is the reproducible-install
# record, and the Electron version is load-bearing for every platform claim.
node_modules/
electron/dist/
electron/release/
